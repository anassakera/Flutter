
---

# 📅 **المرحلة الأولى: Back-End (PHP, MySQL, XAMPP)**

| الشهر | الأسبوع | اليوم | الجزء الأول (ساعة ونصف) Structured Learning | الجزء الثاني (ساعة ونصف) Open Learning & Problem Solving |
|-------|---------|------|---------------------------------------------|------------------------------------------------------------|
| 1 | 1 | الاثنين | مقدمة PHP، تثبيت XAMPP، أول كود PHP | تدريب عملي على التثبيت، حل مشاكل، عصف ذهني |
| 1 | 1 | الخميس | المتغيرات، أنواع البيانات، العمليات الحسابية والمنطقية | تمارين عملية، حل تحديات بسيطة |
| 1 | 2 | الاثنين | الجمل الشرطية (if, else, switch)، الحلقات (for, while) | تمارين عملية، تحديات برمجية |
| 1 | 2 | الخميس | التعامل مع النماذج Forms، استقبال البيانات ($_GET, $_POST) | تطبيق عملي: نموذج تسجيل دخول |
| 1 | 3 | الاثنين | مقدمة MySQL، إنشاء قواعد البيانات والجداول | تدريب عملي: إنشاء قاعدة بيانات وجدول |
| 1 | 3 | الخميس | ربط PHP مع MySQL (CRUD Operations) | تطبيق عملي: نظام CRUD بسيط |
| 1 | 4 | الاثنين | بناء API بسيط (JSON Response) | تدريب عملي: بناء API لعرض بيانات |
| 1 | 4 | الخميس | تأمين API (Authentication & Authorization) | تطبيق عملي: تأمين API |
| 2 | 5 | الاثنين | العلاقات بين الجداول (Relationships) | تدريب عملي: إنشاء علاقات بين الجداول |
| 2 | 5 | الخميس | رفع الملفات والصور (File Upload) | تطبيق عملي: رفع ملفات وصور |
| 2 | 6 | الاثنين | معالجة الأخطاء (Error Handling) | تدريب عملي: معالجة الأخطاء |
| 2 | 6 | الخميس | تحسين أداء API (Performance Optimization) | تطبيق عملي: تحسين أداء API |
| 2 | 7 | الاثنين | مشروع عملي صغير (Mini Project API) | تدريب عملي مفتوح على المشروع |
| 2 | 7 | الخميس | استكمال المشروع العملي الصغير | مناقشة مفتوحة، حل مشاكل المشروع |
| 2 | 8 | الاثنين | مراجعة شاملة لما سبق | حل تحديات ومشاكل مفتوحة |
| 2 | 8 | الخميس | تقييم مرحلي، مناقشة مفتوحة | عصف ذهني حول أفكار مشاريع |
| 3 | 9 | الاثنين | بدء المشروع النهائي (Full API Project)، التخطيط والتصميم | تدريب عملي مفتوح على المشروع |
| 3 | 9 | الخميس | بناء قواعد البيانات والجداول للمشروع النهائي | حل مشاكل قواعد البيانات |
| 3 | 10 | الاثنين | تطوير API للمشروع النهائي | تدريب عملي مفتوح على API |
| 3 | 10 | الخميس | استكمال تطوير API | حل مشاكل API، مناقشة مفتوحة |
| 3 | 11 | الاثنين | اختبار API، معالجة الأخطاء | تدريب عملي مفتوح على الاختبار |
| 3 | 11 | الخميس | تحسين API، مراجعة نهائية | حل مشاكل، مناقشة مفتوحة |
| 3 | 12 | الاثنين | تقديم المشروع النهائي، تقييم | مناقشة مفتوحة، عصف ذهني |
| 3 | 12 | الخميس | مراجعة شاملة، نصائح وتوجيهات | توزيع شهادات، مناقشة مفتوحة |

---

# 📅 **المرحلة الثانية: Front-End (Flutter, Dart, Figma)**

| الشهر | الأسبوع | اليوم | الجزء الأول (ساعة ونصف) Structured Learning | الجزء الثاني (ساعة ونصف) Open Learning & Problem Solving |
|-------|---------|------|---------------------------------------------|------------------------------------------------------------|
| 4 | 1 | الاثنين | مقدمة Dart، تثبيت البيئة، المتغيرات والعمليات | تمارين عملية، حل تحديات بسيطة |
| 4 | 1 | الخميس | الدوال Functions، الجمل الشرطية والتكرارات | تطبيق عملي، تحديات برمجية |
| 4 | 2 | الاثنين | البرمجة الكائنية OOP (Classes, Objects) | تدريب عملي على OOP |
| 4 | 2 | الخميس | مقدمة Flutter، تثبيت البيئة، أول تطبيق | تدريب عملي: تصميم واجهة بسيطة |
| 4 | 3 | الاثنين | تصميم الواجهات باستخدام Figma | تدريب عملي: تحويل تصميم Figma إلى Flutter |
| 4 | 3 | الخميس | Widgets الأساسية (Text, Container, Row, Column) | تطبيق عملي: تصميم واجهات |
| 4 | 4 | الاثنين | إدارة الحالة الأساسية (setState, StatefulWidget) | تدريب عملي على إدارة الحالة |
| 4 | 4 | الخميس | التنقل بين الشاشات (Navigation & Routing) | تطبيق عملي: إنشاء شاشات متعددة |
| 5 | 5 | الاثنين | الاتصال بالـ API (HTTP Package)، التعامل مع JSON | تدريب عملي: ربط التطبيق مع API |
| 5 | 5 | الخميس | التخزين المحلي (SharedPreferences) | تطبيق عملي: حفظ بيانات محليًا |
| 5 | 6 | الاثنين | قواعد البيانات المحلية (SQLite) | تدريب عملي: إنشاء قاعدة بيانات محلية |
| 5 | 6 | الخميس | إدارة الحالة المتقدمة (Provider) | تطبيق عملي: استخدام Provider |
| 5 | 7 | الاثنين | تصميم متجاوب Responsive UI | تدريب عملي: تصميم واجهات متجاوبة |
| 5 | 7 | الخميس | التعامل مع الصور والوسائط (Images, Assets) | تطبيق عملي: إضافة صور ووسائط |
| 5 | 8 | الاثنين | مشروع عملي صغير (Mini Flutter App) | تدريب عملي مفتوح على المشروع |
| 5 | 8 | الخميس | استكمال المشروع العملي الصغير | مناقشة مفتوحة، حل مشاكل المشروع |
| 6 | 9 | الاثنين | بدء المشروع النهائي (Full Flutter App)، التخطيط والتصميم | تدريب عملي مفتوح على المشروع |
| 6 | 9 | الخميس | بناء واجهات المشروع النهائي | حل مشاكل التصميم |
| 6 | 10 | الاثنين | ربط المشروع النهائي مع API | تدريب عملي مفتوح على الربط |
| 6 | 10 | الخميس | استكمال ربط API، معالجة الأخطاء | حل مشاكل API، مناقشة مفتوحة |
| 6 | 11 | الاثنين | اختبار التطبيق، تحسين الأداء | تدريب عملي مفتوح على الاختبار |
| 6 | 11 | الخميس | نشر التطبيق (Deployment) | تدريب عملي: نشر التطبيق |
| 6 | 12 | الاثنين | تقديم المشروع النهائي، تقييم | مناقشة مفتوحة، عصف ذهني |
| 6 | 12 | الخميس | مراجعة شاملة، نصائح وتوجيهات | توزيع شهادات، مناقشة مفتوحة |

---


## **📅 المرحلة الأولى: Back-End (PHP, MySQL, XAMPP)**

---

### **الأسبوع الأول**

---

#### **الحصة 1 (الاثنين): مقدمة PHP، تثبيت XAMPP، أول كود PHP**

**🎯 أهداف الحصة:**

1.  التعرف على لغة PHP واستخداماتها.
2.  فهم الحاجة للخادم المحلي وتثبيت XAMPP بنجاح.
3.  كتابة وتشغيل أول برنامج PHP بسيط ("Hello World").
4.  التعرف على بنية ملفات PHP الأساسية.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً وسهلاً بكم جميعًا في أولى خطواتنا في عالم تطوير الواجهات الخلفية (Back-End)! أنا [اسمك] وسأكون معكم في هذه الرحلة الممتعة. اليوم، سنبدأ مع لغة قوية ومشهورة جدًا اسمها PHP."

1.  **مقدمة عن PHP والخادم المحلي (20 دقيقة)**
    *   "طيب، ما هي PHP؟ تخيلوا أن موقع الويب مثل مطعم. الواجهة الأمامية (Front-End) اللي يصممها مستخدم التطبيق (مثل Flutter اللي راح نتعلمه لاحقًا) هي واجهة المطعم وقائمة الطعام اللي تشوفوها. لكن مين يجهز الأكل في المطبخ ويجلب الطلبات الخاصة؟ هذا هو دور الواجهة الخلفية (Back-End)، ولغة PHP هي واحدة من أشهر الطباخين في هذا المطبخ!"
    *   "PHP هي لغة برمجة تشتغل على **الخادم (Server)**. يعني الكود ما يشتغل في متصفح الزائر مباشرة، لا، هو يروح للخادم، الخادم ينفذه، ويرجع النتيجة (غالبًا كـ HTML) للمتصفح."
    *   "ليش PHP مشهورة؟ لأنها: مجانية ومفتوحة المصدر، سهلة التعلم نسبيًا مقارنة بلغات أخرى، مدعومة بشكل كبير جدًا (ملايين المبرمجين والمصادر)، وتشتغل كويس جدًا مع قواعد البيانات زي MySQL اللي راح نتعلمها." (ممكن ذكر أمثلة مثل WordPress).
    *   "طيب، عشان نقدر نطور ونختبر كود PHP على أجهزتنا، ما نقدر نشغله مباشرة بالمتصفح زي HTML و CSS. نحتاج نحاكي بيئة الخادم الحقيقي. هنا يجي دور **الخادم المحلي (Local Server)**."

2.  **تثبيت XAMPP (خطوة بخطوة) (40 دقيقة)**
    *   "أشهر وأسهل طريقة لإعداد خادم محلي هي باستخدام حزمة اسمها XAMPP. هي حزمة مجانية تجمع لنا كل اللي نحتاجه: Apache (هذا هو برنامج خادم الويب نفسه)، MySQL (برنامج قاعدة البيانات)، و PHP نفسها."
    *   "يلا كلنا نروح على موقع `apachefriends.org` ونحمل النسخة المناسبة لنظام تشغيلنا (Windows, Mac, Linux)."
    *   **(توجيه الطلاب أثناء التحميل والتثبيت):** "أثناء التثبيت، ممكن تظهر لكم بعض التحذيرات، مثلاً في ويندوز عن UAC أو عن مضاد الفيروسات، غالبًا نضغط OK أو نسمح له. الأفضل نثبته في مكان غير الـ C:\Program Files إذا أمكن، مثلاً C:\xampp لتجنب مشاكل الأذونات."
    *   "بعد التثبيت، ابحثوا عن **XAMPP Control Panel** وشغلوه."
    *   "في لوحة التحكم، راح نشوف خدمات كثيرة. اللي يهمنا الآن هما **Apache** و **MySQL**. اضغطوا على زر **Start** بجانب كل واحد منهم. لازم يتغير لونهم للأخضر ويظهر لهم أرقام منافذ (Ports)."
    *   **"كيف نتأكد إنه اشتغل؟"**
        *   "افتحوا المتصفح واكتبوا في العنوان: `http://localhost`. المفروض تظهر لكم صفحة ترحيب XAMPP." (انتظر للتأكد من أن الجميع وصل لهذه النقطة).
        *   "ممتاز! الآن للتأكد من أن PHP شغالة، اكتبوا: `http://localhost/dashboard/phpinfo.php`. لازم تظهر صفحة مليئة بمعلومات عن نسخة PHP وإعداداتها." (اشرح قليلاً محتوى الصفحة).

3.  **كتابة أول كود PHP (Hello World) (20 دقيقة)**
    *   "وين نكتب كود PHP تبعنا؟ لازم نحطه في مجلد خاص داخل XAMPP اسمه `htdocs`. هذا المجلد هو اللي يشوفه خادم Apache."
    *   "روحوا لمسار تثبيت XAMPP (مثلاً `C:\xampp`) وادخلوا مجلد `htdocs`."
    *   "داخل `htdocs`، خلينا نسوي مجلد جديد لمشاريعنا في الدورة، مثلاً نسميه `php_course`."
    *   "الآن، افتحوا أي محرر أكواد (مثل VS Code) وأنشئوا ملف جديد داخل مجلد `php_course`. **مهم جداً**: احفظوا الملف باسم ينتهي بـ `.php`. خلينا نسميه `hello.php`."
    *   "داخل الملف، نكتب التالي:"
        ```php
        <?php
            // This is a PHP comment, the server ignores it

            echo "Hello, World from PHP!"; // echo is used to output text

        ?>
        ```
    *   **شرح الكود:**
        *   "`<?php` و `?>` هما الوسوم اللي تخبر الخادم أن الكود اللي بينهم هو PHP."
        *   "`echo` هو أمر طباعة. يطبع النص اللي بعده على الصفحة."
        *   "`//` هذا تعليق، المترجم يتجاهله، نستخدمه لشرح الكود."
    *   **"كيف نشغل هذا الكود؟"**
        *   "نرجع للمتصفح، ونكتب العنوان كالتالي: `http://localhost/php_course/hello.php` (اسم المجلد اللي سويناه / اسم الملف)."
        *   "المفروض تشوفوا جملة "Hello, World from PHP!" مطبوعة على الصفحة." (تأكد من أن الجميع تمكن من تشغيله).

4.  **مراجعة سريعة وختام الجزء الأول (10 دقائق)**
    *   "إذًا اليوم تعلمنا: ايش هي PHP، ليش نحتاج خادم محلي، ثبتنا XAMPP وشغلنا Apache و MySQL، وعرفنا وين نحفظ ملفاتنا (`htdocs`)، وكتبنا وشغلنا أول كود PHP باستخدام `echo`."
    *   "أي أسئلة على هذا الجزء قبل ما ننتقل للتطبيق العملي وحل المشاكل؟"

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"تمام يا جماعة، الآن وقت نوسخ إيدينا بالكود ونحل أي مشاكل واجهتكم. الهدف من هذا الجزء هو نتأكد إن الكل قادر يثبت البيئة ويكتب ويشغل كود بسيط."

1.  **التأكد من عمل البيئة للجميع (30 دقيقة)**
    *   "خليني ألف عليكم واحد واحد، أو اللي عنده مشكلة يرفع إيده. هل الكل قدر يثبت XAMPP؟ هل Apache و MySQL شغالين باللون الأخضر؟ هل قدرتوا توصلوا لـ `localhost` و `phpinfo.php`؟"
    *   **(حل مشاكل شائعة بشكل جماعي أو فردي):**
        *   **Apache لا يعمل:** "أحد أشهر الأسباب هو تعارض المنافذ (Port conflict)، خصوصًا المنفذ 80. ممكن برنامج ثاني زي Skype أو برنامج آخر يستخدمه. ممكن نجرب نغير منفذ Apache من إعدادات XAMPP (Config -> httpd.conf) ونبحث عن `Listen 80` ونغيرها مثلاً إلى `Listen 8080` ونجرب نشغل Apache مرة ثانية. لو غيرناه، لازم نكتب العنوان في المتصفح كـ `http://localhost:8080`."
        *   **MySQL لا يعمل:** "أحيانًا يكون في نسخة MySQL ثانية مثبتة وتسبب تعارض. نتأكد ونوقف أي خدمة MySQL ثانية."
        *   **أخطاء أذونات (Permissions):** "لو ثبتنا XAMPP في Program Files، ممكن نحتاج نشغل لوحة التحكم كمسؤول (Run as Administrator)."
        *   **كود PHP يظهر كما هو في المتصفح:** "هذا غالبًا يعني إن Apache ما قاعد يعالج ملف PHP بشكل صحيح. نتأكد إن اسم الملف ينتهي بـ `.php` وإنه موجود داخل `htdocs` أو مجلد فرعي منه، وإننا نطلبه عبر `http://localhost/` وليس بفتحه كملف عادي (File -> Open)."

2.  **تدريب عملي على إنشاء ملفات وتشغيلها (30 دقيقة)**
    *   "ممتاز، الآن بعد ما تأكدنا البيئة شغالة عند الأغلب، خلينا نتدرب أكثر."
    *   **"مهمة 1:"** "كل واحد ينشئ ملف جديد في مجلد `php_course` يسميه `info.php`. داخل الملف، استخدموا دالة `phpinfo();` فقط بين وسوم PHP. شغلوا الملف من المتصفح وتأكدوا إنه يعرض نفس المعلومات اللي شفناها قبل شوي."
        ```php
        <?php
            phpinfo();
        ?>
        ```
    *   **"مهمة 2:"** "أنشئوا ملف ثالث اسمه `my_details.php`. داخل الملف، استخدموا `echo` لطباعة اسمكم وعمركم ومدينتكم، كل معلومة في سطر جديد. **تلميح:** كيف ننزل سطر جديد في HTML؟ باستخدام وسم `<br>`."
        ```php
        <?php
            echo "My Name: [Your Name]<br>";
            echo "My Age: [Your Age]<br>";
            echo "My City: [Your City]";
        ?>
        ```
    *   **(متابعة الطلاب وتقديم المساعدة عند الحاجة)**

3.  **عصف ذهني ومناقشة مفتوحة (20 دقيقة)**
    *   "طيب يا شباب، بعد ما شفنا كيف PHP تشتغل، وبشكل بسيط جدًا، ايش تتخيلوا ممكن نسوي فيها؟ إيش هي أنواع تطبيقات الويب اللي ممكن تحتاج برمجة من جهة الخادم؟"
    *   (تشجيع الأفكار: تسجيل دخول، عرض بيانات متغيرة، حفظ تعليقات المستخدمين، سلة تسوق، إلخ).
    *   "ايش الفرق بين صفحة HTML ثابتة وصفحة PHP ديناميكية؟"
    *   "هل واجهتكم أي صعوبات غير مشاكل التثبيت؟ أي شيء ما كان واضح في الشرح؟"

4.  **تمارين إضافية للمنزل (اختياري) + ختام (10 دقائق)**
    *   "كممارسة إضافية في البيت، جربوا التالي:"
        *   **تمرين 1:** أنشئ ملف `experiment.php`. جرب تطبع نصوص مختلفة باستخدام `echo`. جرب تطبع نص يحتوي على علامات اقتباس مفردة (`'`) و مزدوجة (`"`). هل تلاحظ أي فرق؟
        *   **تمرين 2:** حاول تدمج كود HTML بسيط داخل ملف PHP. مثلاً، اطبع عنوان `<h1>` باستخدام `echo`.
            ```php
            <?php
                echo "<h1>Welcome to my PHP Page!</h1>";
                echo "<p>This paragraph is also generated by PHP.</p>";
            ?>
            ```
    *   "ممتاز جدًا لليوم الأول! أهم شيء إن بيئة العمل جاهزة عندكم، وعرفنا كيف نكتب ونشغل أبسط أشكال كود PHP. في الحصة القادمة يوم الخميس بإذن الله، راح نبدأ نتعامل مع البيانات بشكل فعلي باستخدام المتغيرات وأنواع البيانات المختلفة. أي أسئلة أخيرة؟"
    *   "يعطيكم العافية ونشوفكم الخميس!"

---

#### **الحصة 2 (الخميس): المتغيرات، أنواع البيانات، العمليات الحسابية والمنطقية**

**🎯 أهداف الحصة:**

1.  فهم مفهوم المتغيرات وكيفية تعريفها واستخدامها في PHP.
2.  التعرف على أنواع البيانات الأساسية في PHP (String, Integer, Float, Boolean).
3.  استخدام العمليات الحسابية لإجراء عمليات رياضية.
4.  استخدام عمليات المقارنة والعمليات المنطقية لاتخاذ قرارات بسيطة.
5.  التعرف على أدوات بسيطة لفحص المتغيرات مثل `var_dump()`.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم مرة أخرى! في حصة اليوم، راح نبدأ نتعامل مع أساس البرمجة: **البيانات (Data)**. كيف نخزنها؟ كيف نغيرها؟ وكيف نجري عليها عمليات؟ هذا كله يتم باستخدام **المتغيرات (Variables)** و**العمليات (Operators)**."

1.  **مراجعة سريعة (5 دقائق)**
    *   "مين يذكرنا بسرعة، إيش سوينا المرة الماضية؟ (تثبيت XAMPP، `htdocs`, `.php`, `echo`). ممتاز!"

2.  **المتغيرات (Variables) (25 دقيقة)**
    *   "إيش هو المتغير؟ ببساطة، هو مثل **صندوق** أو **حاوية** نعطيها اسم ونخزن فيها قيمة معينة. هذه القيمة ممكن تتغير أثناء تشغيل البرنامج، عشان كذا اسمها متغيرات."
    *   **"كيف نعرف متغير في PHP؟"**
        *   "سهل جدًا! لازم يبدأ بعلامة الدولار `$`."
        *   "بعدها يجي اسم المتغير. قواعد التسمية: يبدأ بحرف أو شرطة سفلية (`_`)، وممكن يحتوي حروف، أرقام، وشرطة سفلية. **مهم:** PHP حساسة لحالة الأحرف (Case-Sensitive)، يعني `$name` غير `$Name`."
        *   "لغة PHP ذكية (Dynamic Typing)، ما نحتاج نحدد نوع البيانات اللي راح نخزنها في المتغير مسبقًا زي بعض اللغات الثانية."
    *   **"كيف نحط قيمة داخل المتغير (الإسناد Assignment)؟"**
        *   "نستخدم علامة يساوي `=`."
    *   **"يلا نشوف أمثلة:"** (كتابة الكود وتجربته مباشرة)
        ```php
        <?php
            // Let's define some variables
            $studentName = "Ali"; // Storing a name (text)
            $studentAge = 22;      // Storing an age (whole number)
            $coursePrice = 499.99; // Storing a price (decimal number)
            $isRegistered = true;  // Storing a status (yes/no, true/false)

            // How to use them? We can print them using echo
            echo "Student Name: ";
            echo $studentName;
            echo "<br>"; // New line in HTML

            echo "Age: " . $studentAge . " years old<br>"; // Combining text and variable using (.)

            echo "Course Price: $" . $coursePrice . "<br>";

            echo "Is Registered? ";
            echo $isRegistered; // Note: true prints as '1', false prints as empty string or '0' depending on context
            echo "<br>";
        ?>
        ```
    *   **"ملاحظة مهمة:** شفنا كيف ندمج نص مع متغير باستخدام النقطة `.` (Concatenation)."

3.  **أنواع البيانات (Data Types) (25 دقيقة)**
    *   "زي ما شفنا في الأمثلة، المتغيرات ممكن تخزن أنواع مختلفة من البيانات. خلينا نتعرف على أهم الأنواع الأساسية:"
        *   **String (نص):** سلسلة من الحروف، لازم تكون بين علامات اقتباس مزدوجة `"` أو مفردة `'`. مثال: `"Welcome"`, `'PHP is fun'`.
        *   **Integer (عدد صحيح):** رقم بدون فاصلة عشرية. مثال: `10`, `-50`, `0`.
        *   **Float / Double (عدد عشري):** رقم يحتوي على فاصلة عشرية. مثال: `3.14`, `-0.05`, `10.0`.
        *   **Boolean (قيمة منطقية):** لها قيمتين فقط: `true` (صح) أو `false` (خطأ). مفيدة جدًا في الشروط اللي راح نشوفها بعدين.
        *   **Array (مصفوفة):** تسمح بتخزين مجموعة من القيم في متغير واحد. راح ناخذها بالتفصيل لاحقًا. مثال سريع: `$colors = ["Red", "Green", "Blue"];`
        *   **NULL:** يمثل متغير بدون قيمة. ` $car = null; `
    *   **"كيف نعرف نوع البيانات في متغير؟"**
        *   "PHP فيها دالة مفيدة جدًا اسمها `var_dump()`. هذه الدالة تطبع معلومات تفصيلية عن المتغير: نوعه، حجمه (إذا نص أو مصفوفة)، وقيمته."
        *   **"نجربها:"**
            ```php
            <?php
                $message = "Learning PHP";
                $year = 2024;
                $pi = 3.14159;
                $isLearningFun = true;

                var_dump($message); echo "<br>";
                var_dump($year);    echo "<br>";
                var_dump($pi);      echo "<br>";
                var_dump($isLearningFun); echo "<br>";

                $nothing = null;
                var_dump($nothing); echo "<br>";
            ?>
            ```
        *   "لاحظوا كيف `var_dump()` تعطينا تفاصيل أكثر من `echo`، وهذا مفيد جدًا وقت تصحيح الأخطاء (Debugging)."

4.  **العمليات (Operators) (30 دقيقة)**
    *   "المتغيرات ما تكون مفيدة لو ما قدرنا نسوي عليها عمليات. PHP توفر لنا أنواع مختلفة من العمليات:"
    *   **أ. العمليات الحسابية (Arithmetic):**
        *   `+` (جمع), `-` (طرح), `*` (ضرب), `/` (قسمة).
        *   `%` (باقي القسمة - Modulo): مفيدة لمعرفة إذا كان رقم زوجي أو فردي مثلاً (`$num % 2`).
        *   **مثال:**
            ```php
            <?php
                $num1 = 15;
                $num2 = 4;

                $sum = $num1 + $num2; // 19
                $difference = $num1 - $num2; // 11
                $product = $num1 * $num2; // 60
                $quotient = $num1 / $num2; // 3.75
                $remainder = $num1 % $num2; // 3 ( لأن 15 = 3*4 + 3 )

                echo "Sum: $sum <br>";
                echo "Quotient: $quotient <br>";
                echo "Remainder: $remainder <br>";
            ?>
            ```
    *   **ب. عمليات الإسناد (Assignment):**
        *   `=` (إسناد بسيط، شفناه قبل).
        *   `+=`, `-=`, `*=`, `/=`, `%=` (إسناد مركب، اختصار لعملية ثم إسناد).
        *   **مثال:** `$counter = 5; $counter += 3; // الآن counter = 8 (نفس counter = counter + 3)`
    *   **ج. عمليات المقارنة (Comparison):**
        *   هذه العمليات تقارن بين قيمتين وترجع `true` أو `false`.
        *   `==` (يساوي في القيمة فقط). **مثال:** `5 == "5"` (هذه `true`!).
        *   `===` (يساوي في القيمة **والنوع** - Identical). **مثال:** `5 === "5"` (هذه `false` لأن الأنواع مختلفة: integer و string). **نصيحة: استخدم `===` غالبًا لتجنب مشاكل غير متوقعة.**
        *   `!=` أو `<>` (لا يساوي في القيمة).
        *   `!==` (لا يساوي في القيمة أو النوع).
        *   `<` (أصغر من), `>` (أكبر من), `<=` (أصغر أو يساوي), `>=` (أكبر أو يساوي).
        *   **مثال:** `var_dump(10 > 5);` // Output: bool(true)
                 `var_dump(10 === 10.0);` // Output: bool(false) because integer vs float
    *   **د. العمليات المنطقية (Logical):**
        *   تُستخدم لدمج أكثر من شرط (أكثر من تعبير يرجع `true` أو `false`).
        *   `&&` أو `and` (و - AND): النتيجة تكون `true` **فقط إذا كان كلا الطرفين** `true`.
        *   `||` أو `or` (أو - OR): النتيجة تكون `true` **إذا كان أحد الطرفين (أو كلاهما)** `true`.
        *   `!` (ليس - NOT): تعكس الحالة المنطقية (`!true` تصير `false`, و `!false` تصير `true`).
        *   **مثال:**
            ```php
            <?php
                $age = 25;
                $hasLicense = true;
                $isCitizen = true;

                // Can vote? (Must be citizen AND age >= 18)
                $canVote = $isCitizen && ($age >= 18);
                var_dump($canVote); echo "<br>"; // bool(true)

                // Can rent a specific car? (Maybe age >= 25 OR has special permit)
                $hasSpecialPermit = false;
                $canRent = ($age >= 25) || $hasSpecialPermit;
                var_dump($canRent); echo "<br>"; // bool(true)

                // Is NOT a minor?
                $isMinor = ($age < 18);
                var_dump(!$isMinor); echo "<br>"; // bool(true)
            ?>
            ```

5.  **مراجعة سريعة وختام الجزء الأول (5 دقائق)**
    *   "إذًا، غطينا اليوم المتغيرات (`$`), أنواع البيانات الأساسية (string, int, float, bool), العمليات الحسابية (`+ - * / %`), المقارنة (`== === != > <`), والمنطقية (`&& || !`). هذه هي الأدوات الأساسية اللي راح نستخدمها كثير جدًا."
    *   "أي أسئلة على هذا الجزء؟"

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"يلا نطبق اللي تعلمناه! الجزء هذا راح نكتب فيه أكواد بسيطة نستخدم فيها المتغيرات والعمليات بأنواعها."

1.  **تدريب على المتغيرات وأنواع البيانات (25 دقيقة)**
    *   **"مهمة 1:"** "كل واحد يفتح ملف PHP جديد (مثلاً `variables_practice.php`)."
    *   "عرفوا متغيرات لتخزين المعلومات التالية عن كتاب معين: عنوان الكتاب (نص)، اسم المؤلف (نص)، سنة النشر (رقم صحيح)، سعر الكتاب (رقم عشري)، هل هو متوفر (منطقي `true`/`false`)."
    *   "استخدموا `echo` لطباعة كل معلومة في سطر منفصل، مع وصف مناسب (مثال: `echo 'Book Title: ' . $bookTitle . '<br>';`)."
    *   "بعدها، استخدموا `var_dump()` لكل متغير عشان تشوفوا نوع البيانات اللي PHP استنتجته."
    *   **(متابعة ومساعدة الطلاب)**

2.  **تدريب على العمليات الحسابية (25 دقيقة)**
    *   **"مهمة 2:"** "في نفس الملف أو ملف جديد (مثلاً `calculations.php`)."
    *   "عرفوا متغيرين يمثلون سعر منتج معين والضريبة المضافة (مثلاً كنسبة مئوية، 15%)."
        ```php
        $price = 150; // SAR
        $taxRate = 0.15; // 15%
        ```
    *   "احسبوا قيمة الضريبة (السعر * نسبة الضريبة)."
    *   "احسبوا السعر الإجمالي (السعر الأصلي + قيمة الضريبة)."
    *   "اطبعوا السعر الأصلي، قيمة الضريبة، والسعر الإجمالي بشكل واضح."
    *   **"تحدي بسيط:"** "إذا كان عندكم متغير يمثل عدد الدقائق (مثلاً `135` دقيقة)، كيف تحسبون كم ساعة وكم دقيقة متبقية باستخدام العمليات الحسابية (القسمة `/` وباقي القسمة `%`)؟"
        *   (الحل: الساعات = قسمة صحيحة للدقائق على 60، الدقائق المتبقية = باقي قسمة الدقائق على 60).
        ```php
        $totalMinutes = 135;
        $hours = floor($totalMinutes / 60); // floor() لتقريب الرقم للأسفل
        $remainingMinutes = $totalMinutes % 60;
        echo "$totalMinutes minutes is equal to $hours hours and $remainingMinutes minutes.<br>";
        ```

3.  **تدريب على عمليات المقارنة والمنطق (30 دقيقة)**
    *   **"مهمة 3:"** "ملف جديد (مثلاً `comparisons.php`)."
    *   "عرفوا متغيرين، `$a = 10` و `$b = '10'`."
    *   "استخدموا `var_dump()` لطباعة نتيجة المقارنات التالية وشوفوا الفرق:"
        *   `$a == $b`
        *   `$a === $b`
        *   `$a != $b`
        *   `$a !== $b`
    *   **"مهمة 4:"** "تخيلوا عندكم درجة طالب في اختبارين (`$score1`, `$score2`). الطالب ينجح في المادة إذا كانت **كلتا** الدرجتين 60 أو أعلى. اكتبوا تعبير منطقي باستخدام `&&` يخزن نتيجة النجاح (true/false) في متغير `$isPass` واطبعوها باستخدام `var_dump()`."
    *   **"مهمة 5:"** "تخيلوا سيناريو ثاني: الطالب ينجح إذا كانت **إحدى** درجاته على الأقل 90 أو أعلى. اكتبوا تعبير منطقي باستخدام `||` يخزن النتيجة في متغير `$isExcellent` واطبعوها."
    *   **"مهمة 6:"** "اطبعوا عكس قيمة `$isPass` باستخدام `!`."

4.  **حل مشاكل ومراجعة (10 دقائق)**
    *   "هل واجهتكم أي أخطاء؟ أخطاء شائعة ممكن تكون: نسيان علامة `$` قبل اسم المتغير، أخطاء إملائية في أسماء المتغيرات، نسيان الفاصلة المنقوطة `;` في نهاية الأمر، لخبطة بين `==` و `===`."
    *   "أي مفهوم ما كان واضح؟ الفرق بين أنواع البيانات؟ العمليات المنطقية؟"
    *   "تأكدوا من حفظ ملفاتكم وتجربتها."

5.  **تمارين إضافية للمنزل + ختام (5 دقائق)**
    *   "للتدريب الإضافي:"
        *   **تمرين 1:** اكتب سكربت يحسب مساحة ومحيط دائرة. عرف متغير لنصف القطر (`$radius`) واستخدم قيمة `pi` (ممكن استخدام `3.14159` أو الدالة `pi()`). المعادلات: المساحة = π * r² ، المحيط = 2 * π * r. (تلميح: للتربيع ممكن تستخدم `*` مرتين أو دالة `pow($radius, 2)`).
        *   **تمرين 2:** عرف متغير للعمر (`$userAge`). استخدم `var_dump()` لطباعة نتيجة التحقق مما إذا كان العمر يقع ضمن فئة الشباب (مثلاً بين 18 و 30 شاملة). (تلميح: ستحتاج `&&`).
    *   "ممتاز! اليوم تعمقنا أكثر في أساسيات PHP. المرة القادمة، راح نتعلم كيف نخلي برامجنا تتخذ قرارات وتكرر الأوامر باستخدام الجمل الشرطية والحلقات. يعطيكم العافية!"

---

### **الأسبوع الثاني**

---

#### **الحصة 3 (الاثنين): الجمل الشرطية (if, else, switch)، الحلقات (for, while)**

**🎯 أهداف الحصة:**

1.  فهم الحاجة للجمل الشرطية وكيفية استخدام `if`, `else`, `elseif`.
2.  التعرف على جملة `switch` كبديل للشروط المتعددة.
3.  فهم الحاجة للحلقات التكرارية وكيفية استخدام حلقة `for`.
4.  كيفية استخدام حلقة `while` للتكرار بناءً على شرط.
5.  التعرف على مخاطر الحلقات اللانهائية.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"مساء/صباح الخير جميعًا! في الحصص الماضية تعلمنا كيف نخزن البيانات (المتغيرات) ونجري عليها عمليات. اليوم، راح نتعلم كيف نخلي برامجنا **أذكى**! كيف نجعلها تتخذ قرارات مختلفة بناءً على شروط معينة، وكيف نجعلها تكرر تنفيذ الأوامر بدون ما نكتبها مئة مرة. هذا يتم باستخدام **الجمل الشرطية (Conditional Statements)** و **الحلقات التكرارية (Loops)**."

1.  **مراجعة سريعة (5 دقائق)**
    *   "مين يذكرنا بأنواع البيانات اللي أخذناها؟ والعمليات المنطقية (`&&`, `||`, `!`)؟ متى ترجع `true` أو `false`؟ ممتاز، لأننا راح نستخدمها كثير اليوم."

2.  **الجمل الشرطية (Conditional Statements) (35 دقيقة)**
    *   "ليش نحتاجها؟ تخيل برنامج يحسب تقدير طالب. التقدير يعتمد **على** درجة الطالب، صح؟ هنا نحتاج شرط. أو برنامج تسجيل دخول، ما نسمح للمستخدم بالدخول **إلا إذا** كانت كلمة المرور صحيحة. هذه هي فايدة الشروط."
    *   **أ. جملة `if` (إذا):**
        *   "أبسط شكل. تنفذ الكود اللي بداخلها **فقط إذا** كان الشرط `true`."
        *   **البنية:**
            ```php
            if (condition) {
                // Code to execute if condition is true
            }
            // Code here runs regardless
            ```
        *   **مثال:**
            ```php
            <?php
                $temperature = 35;
                if ($temperature > 30) {
                    echo "It's a hot day!";
                }
            ?>
            ```
    *   **ب. جملة `if...else` (إذا... وإلا):**
        *   "هنا عندنا خيارين. إذا الشرط `true` نفذ الكود الأول، **وإلا** (إذا الشرط `false`) نفذ الكود الثاني."
        *   **البنية:**
            ```php
            if (condition) {
                // Code if condition is true
            } else {
                // Code if condition is false
            }
            ```
        *   **مثال:** التحقق من نجاح الطالب.
            ```php
            <?php
                $grade = 55;
                if ($grade >= 60) {
                    echo "Result: Pass";
                } else {
                    echo "Result: Fail";
                }
            ?>
            ```
    *   **ج. جملة `if...elseif...else` (إذا... وإلا إذا... وإلا):**
        *   "لما يكون عندنا أكثر من شرطين. يتحقق من الشرط الأول، إذا `true` ينفذه ويتجاهل الباقي. إذا `false`، يروح للشرط الثاني (`elseif`)، إذا `true` ينفذه ويتجاهل الباقي... وهكذا. إذا كل الشروط `false`، ينفذ الكود اللي في `else` الأخيرة."
        *   **البنية:**
            ```php
            if (condition1) {
                // Code for condition 1
            } elseif (condition2) {
                // Code for condition 2
            } elseif (condition3) {
                // Code for condition 3
            } else {
                // Code if all above are false
            }
            ```
        *   **مثال:** حساب تقدير الطالب (A, B, C, D, F).
            ```php
            <?php
                $score = 78;
                if ($score >= 90) {
                    echo "Grade: A";
                } elseif ($score >= 80) {
                    echo "Grade: B";
                } elseif ($score >= 70) {
                    echo "Grade: C";
                } elseif ($score >= 60) {
                    echo "Grade: D";
                } else {
                    echo "Grade: F";
                }
            ?>
            ```
    *   **د. جملة `switch`:**
        *   "هي طريقة أخرى لكتابة الشروط المتعددة، خصوصًا لما نكون نقارن **متغير واحد** بقيم مختلفة محددة."
        *   **البنية:**
            ```php
            switch (variable_to_check) {
                case value1:
                    // Code if variable equals value1
                    break; // IMPORTANT! Stops execution here
                case value2:
                    // Code if variable equals value2
                    break;
                // ... more cases
                default:
                    // Code if none of the cases match
            }
            ```
        *   **"مهم جدًا:** كلمة `break;` بعد كل `case`. لو نسيناها، راح يكمل تنفيذ الكود اللي في الـ `case` التالية حتى لو ما تطابقت! وهذا غالبًا ما نبغاه."
        *   **`default`:** اختيارية، تنفذ إذا لم يتطابق المتغير مع أي من الحالات `case`.
        *   **مثال:** طباعة رسالة بناءً على يوم الأسبوع (رقم).
            ```php
            <?php
                $dayOfWeek = 3; // 1=Sunday, 2=Monday, ...

                switch ($dayOfWeek) {
                    case 1:
                        echo "It's Sunday!";
                        break;
                    case 2:
                        echo "It's Monday, start of the week!";
                        break;
                    case 3:
                        echo "It's Tuesday.";
                        break;
                    case 4:
                        echo "It's Wednesday.";
                        break;
                    case 5:
                        echo "It's Thursday, almost weekend!";
                        break;
                    case 6:
                        echo "It's Friday!";
                        break;
                    case 7:
                        echo "It's Saturday, weekend!";
                        break;
                    default:
                        echo "Invalid day number.";
                }
            ?>
            ```
        *   "متى نستخدم `switch` ومتى `if/elseif`؟ `switch` أنظف لما نقارن متغير واحد بقيم ثابتة. `if/elseif` أكثر مرونة وتسمح بشروط معقدة (مثل مقارنة نطاقات `score >= 90`)."

3.  **الحلقات التكرارية (Loops) (40 دقيقة)**
    *   "تخيلوا نبغى نطبع الأرقام من 1 إلى 100. هل راح نكتب `echo 1; echo 2; ... echo 100;`؟ طبعًا لا! هنا تجي فايدة الحلقات. هي تخلينا نكرر تنفيذ مجموعة من الأوامر عدة مرات."
    *   **أ. حلقة `for`:**
        *   "نستخدمها غالبًا لما نعرف **مسبقًا** كم مرة نبغى نكرر الكود."
        *   **البنية:**
            ```php
            for (initialization; condition; increment/decrement) {
                // Code to be repeated
            }
            ```
        *   **شرح الأجزاء الثلاثة:**
            *   `initialization`: يتنفذ **مرة واحدة فقط** في البداية. غالبًا نستخدمه لتعريف متغير عداد (Counter)، مثلاً `$i = 1;`.
            *   `condition`: يتم التحقق منه **قبل كل مرة تكرار**. إذا كان `true`، يتم تنفيذ الكود داخل الحلقة. إذا صار `false`، تتوقف الحلقة. مثلاً `$i <= 10;`.
            *   `increment/decrement`: يتنفذ **في نهاية كل مرة تكرار**. غالبًا نستخدمه لزيادة أو إنقاص العداد. مثلاً `$i++` (يعني زيادة `i` بمقدار 1).
        *   **مثال:** طباعة الأرقام من 1 إلى 5.
            ```php
            <?php
                echo "Numbers from 1 to 5:<br>";
                for ($i = 1; $i <= 5; $i++) {
                    echo "Number: " . $i . "<br>";
                }
                echo "Loop finished!";
            ?>
            ```
        *   (تتبع تنفيذ الحلقة خطوة بخطوة على السبورة أو شفهيًا).
    *   **ب. حلقة `while`:**
        *   "نستخدمها لما نبغى نكرر الكود **طالما** أن شرط معين متحقق. ممكن ما نكون عارفين بالضبط كم مرة راح يتكرر."
        *   **البنية:**
            ```php
            while (condition) {
                // Code to be repeated
                // !!! IMPORTANT: Make sure something inside the loop
                // eventually makes the condition false !!!
            }
            ```
        *   **"تحذير خطير: الحلقة اللانهائية (Infinite Loop)!"**
            *   "إذا الشرط في `while` ظل دائمًا `true`، الحلقة ما راح توقف أبدًا! هذا ممكن يعلق البرنامج أو الخادم. لازم نتأكد إن الكود داخل الحلقة يغير شيء يخلي الشرط يصير `false` في النهاية."
        *   **مثال:** طباعة الأرقام من 1 إلى 5 باستخدام `while`.
            ```php
            <?php
                echo "Numbers from 1 to 5 (using while):<br>";
                $counter = 1; // Initialization before the loop
                while ($counter <= 5) { // Condition check
                    echo "Counter: " . $counter . "<br>";
                    $counter++; // Increment inside the loop (VERY IMPORTANT!)
                }
                echo "While loop finished!";
            ?>
            ```
        *   "لو نسينا `counter++;` داخل الحلقة، إيش راح يصير؟ (الإجابة: حلقة لانهائية تطبع Counter: 1 للأبد)."
    *   **(مقدمة سريعة لـ `foreach`، سيتم شرحها بالتفصيل مع المصفوفات):**
        *   "في حلقة ثالثة اسمها `foreach`، وهي مخصصة للتكرار على عناصر المصفوفات (Arrays)، راح نشوفها بالتفصيل لما ناخذ المصفوفات."

4.  **مراجعة سريعة وختام الجزء الأول (10 دقائق)**
    *   "إذًا، تعلمنا اليوم كيف نتحكم بمسار تنفيذ البرنامج باستخدام الشروط (`if`, `elseif`, `else`, `switch`) وكيف نكرر الأوامر باستخدام الحلقات (`for`, `while`)."
    *   "متى نستخدم `if` ومتى `switch`؟ متى نستخدم `for` ومتى `while`؟ إيش هو أخطر شيء ممكن يصير مع `while`؟ (الحلقة اللانهائية)."
    *   "أي أسئلة؟"

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن جاء وقت البرمجة! راح نطبق الشروط والحلقات في تمارين عملية."

1.  **تدريب على الجمل الشرطية (30 دقيقة)**
    *   **"مهمة 1 (if/else):"** "اكتبوا سكربت PHP يعرف متغير للعمر (`$age`). استخدموا `if/else` لطباعة رسالة تقول "Eligible to vote" إذا كان العمر 18 أو أكبر، و "Not eligible to vote" إذا كان أصغر."
    *   **"مهمة 2 (if/elseif/else):"** "اكتبوا سكربت يحدد رسالة ترحيب بناءً على الوقت الحالي من اليوم. ممكن نستخدم دالة `date('H')` اللي ترجع الساعة الحالية بصيغة 24 ساعة (كنص). مثلاً:"
        *   إذا الساعة أقل من 12، اطبع "Good Morning!".
        *   إذا الساعة بين 12 و 18 (غير شاملة)، اطبع "Good Afternoon!".
        *   غير ذلك (بعد 6 مساءً)، اطبع "Good Evening!".
            ```php
            <?php
                $currentHour = date('H'); // Get current hour as string (00-23)
                // Note: Comparing strings might work here, but converting to int is safer
                $currentHour = (int)$currentHour;

                if ($currentHour < 12) {
                    echo "Good Morning!";
                } elseif ($currentHour >= 12 && $currentHour < 18) {
                    echo "Good Afternoon!";
                } else {
                    echo "Good Evening!";
                }
            ?>
            ```
    *   **"مهمة 3 (switch):"** "اكتبوا سكربت يعرف متغير `$userRole` ممكن يكون قيمته 'admin', 'editor', أو 'guest'. استخدموا `switch` لطباعة رسالة مختلفة لكل دور. مثلاً: 'Welcome Admin!', 'Editor Panel', 'Guest Access'. وإذا كان الدور غير معروف، اطبعوا 'Unknown Role' باستخدام `default`."

2.  **تدريب على الحلقات التكرارية (40 دقيقة)**
    *   **"مهمة 4 (for):"** "استخدموا حلقة `for` لطباعة الأرقام الزوجية فقط من 2 إلى 20." (تلميح: ممكن تبدأ العداد من 2 وتزيده بمقدار 2 في كل مرة `$i += 2`).
    *   **"مهمة 5 (for):"** "استخدموا حلقة `for` لطباعة جدول ضرب الرقم 7 (من 7x1 إلى 7x10)." (الشكل المطلوب: `7 x 1 = 7`, `7 x 2 = 14`, ...).
    *   **"مهمة 6 (while):"** "تخيلوا عندكم رصيد (`$balance = 100`) وتكلفة سحب (`$withdrawCost = 15`). استخدموا حلقة `while` لمحاكاة عملية السحب وطباعة الرصيد المتبقي بعد كل سحب، طالما أن الرصيد كافي لعملية السحب (أكبر من أو يساوي تكلفة السحب)."
    *   **"تحدي (while):"** "استخدموا حلقة `while` ودالة `rand(1, 10)` (تولد رقم عشوائي بين 1 و 10) لطباعة الأرقام العشوائية حتى يظهر الرقم 5. اطبعوا كم محاولة أخذت العملية."
        ```php
        <?php
            $attempts = 0;
            $randomNumber = 0;
            while ($randomNumber != 5) {
                $randomNumber = rand(1, 10);
                $attempts++;
                echo "Attempt #$attempts: Got $randomNumber <br>";
            }
            echo "Success! Found 5 after $attempts attempts.";
        ?>
        ```

3.  **تصحيح الأخطاء الشائعة ومناقشة (15 دقائق)**
    *   "ايش الأخطاء اللي واجهتكم؟"
    *   **أخطاء شائعة في الشروط:** نسيان الأقواس `()`, نسيان الأقواس المعقوفة `{}` (خصوصًا لو الكود داخل الشرط أكثر من سطر)، استخدام `=` بدل `==` للمقارنة.
    *   **أخطاء شائعة في الحلقات:** خطأ في شرط التوقف (يسبب حلقة لانهائية أو حلقة ما تشتغل أبدًا)، نسيان تحديث العداد داخل `while`, خطأ في قيمة البداية أو النهاية في `for`.
    *   "خلينا نشوف كود زميلكم اللي ما اشتغل ونحاول نصلحه سوا..." (اختار مثال من طالب يواجه مشكلة).

4.  **تمارين إضافية للمنزل + ختام (5 دقائق)**
    *   "للتدريب الإضافي:"
        *   **تمرين 1:** (FizzBuzz) اكتب سكربت يطبع الأرقام من 1 إلى 100. لكن:
            *   بدل الأرقام اللي تقبل القسمة على 3، اطبع كلمة "Fizz".
            *   بدل الأرقام اللي تقبل القسمة على 5، اطبع كلمة "Buzz".
            *   بدل الأرقام اللي تقبل القسمة على 3 و 5 معًا، اطبع كلمة "FizzBuzz".
            *   (تلميح: استخدم باقي القسمة `%` وجمل `if/elseif/else`).
        *   **تمرين 2:** استخدم حلقة `for` لطباعة نمط مثلث بسيط من النجوم `*`:
            ```
            *
            **
            ***
            ****
            *****
            ```
            (تلميح: قد تحتاج حلقة `for` داخل حلقة `for` أخرى، أو استخدام دالة مثل `str_repeat()`).
    *   "شغل ممتاز اليوم! الآن صارت برامجنا أكثر ديناميكية وتقدر تتخذ قرارات وتكرر المهام. في الحصة القادمة، راح نتعلم كيف نستقبل مدخلات من المستخدم باستخدام نماذج HTML ونعالجها في PHP. يعطيكم العافية!"

---

#### **الحصة 4 (الخميس): التعامل مع النماذج Forms، استقبال البيانات ($_GET, $_POST)**

**🎯 أهداف الحصة:**

1.  فهم أساسيات نماذج HTML (`<form>`, `<input>`, `<label>`, etc.).
2.  التعرف على طريقتي إرسال البيانات `GET` و `POST` والفرق بينهما.
3.  كيفية استقبال البيانات المُرسلة في PHP باستخدام `$_GET` و `$_POST`.
4.  أهمية التحقق من وجود البيانات (`isset()`) قبل استخدامها.
5.  مقدمة بسيطة لأهمية تنظيف المدخلات (`htmlspecialchars()`).

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم مجددًا! حتى الآن، برامجنا تتعامل مع بيانات عرفناها داخل الكود نفسه. لكن تطبيقات الويب الحقيقية تحتاج تتفاعل مع **المستخدم**! كيف نسمح للمستخدم بإدخال بيانات (مثل اسمه، إيميله، تعليق) ونستقبلها في PHP؟ هذا يتم عن طريق **نماذج HTML (HTML Forms)**."

1.  **مراجعة سريعة (5 دقائق)**
    *   "مين يذكرنا بالجمل الشرطية؟ والحلقات؟ ممتاز! اليوم راح نربط هذا مع واجهة المستخدم."

2.  **مقدمة لنماذج HTML (HTML Forms) (25 دقيقة)**
    *   "إيش هو نموذج HTML؟ هو مجموعة من حقول الإدخال (input fields)، أزرار الاختيار (radio buttons)، مربعات الاختيار (checkboxes)، قوائم منسدلة (dropdowns)، وزر إرسال (submit button)، كلها داخل وسم `<form>`."
    *   "الهدف منها هو جمع معلومات من المستخدم وإرسالها لمكان آخر (غالبًا ملف PHP على الخادم) للمعالجة."
    *   **العناصر الأساسية:**
        *   **`<form>`:** الوسم الرئيسي اللي يحيط بكل عناصر النموذج.
            *   `action="process.php"`: تحدد **اسم الملف (أو الرابط) اللي راح يستقبل ويعالج البيانات** لما المستخدم يضغط إرسال.
            *   `method="post"` (أو `"get"`): تحدد **طريقة إرسال البيانات**. راح نشرح الفرق بعد شوي.
        *   **`<input>`:** أهم عنصر والأكثر استخدامًا. له أنواع كثيرة تحددها السمة `type`:
            *   `type="text"`: حقل نصي عادي.
            *   `type="password"`: حقل كلمة مرور (يظهر نجوم أو نقاط).
            *   `type="email"`: حقل إيميل (المتصفح ممكن يتحقق من صيغة الإيميل).
            *   `type="number"`: حقل أرقام.
            *   `type="submit"`: زر الإرسال اللي يشغل الـ `action`.
            *   `type="radio"`: زر اختيار واحد من عدة خيارات (لازم يكون لهم نفس الـ `name`).
            *   `type="checkbox"`: مربع اختيار (يمكن اختيار أكثر من واحد).
            *   **أهم سمتين في `<input>`:**
                *   `name="username"`: **مهم جدًا جدًا!** هذا هو الاسم اللي راح نستخدمه في PHP للوصول لقيمة هذا الحقل. لازم يكون فريد لكل حقل (ما عدا الـ radio buttons).
                *   `id="user"`: معرف فريد للعنصر في صفحة HTML، يُستخدم غالبًا مع `<label>` أو CSS/JavaScript.
        *   **`<label for="user">Username:</label>`:** نص وصفي مرتبط بحقل إدخال معين (يرتبط بالـ `id`). لما تضغط على الـ label، مؤشر الكتابة ينتقل للحقل المرتبط به (مفيد لتجربة المستخدم وإمكانية الوصول).
        *   **`<textarea name="message" rows="4" cols="50"></textarea>`:** حقل لإدخال نصوص طويلة متعددة الأسطر.
        *   **`<select name="city">...</select>` و `<option value="riyadh">Riyadh</option>`:** لإنشاء قائمة منسدلة.
        *   **`<button type="submit">Send</button>`:** طريقة أخرى لإنشاء زر إرسال.
    *   **مثال لنموذج تسجيل دخول بسيط (HTML فقط):** (نكتبه سوا في ملف جديد، مثلاً `login_form.html`)
        ```html
        <!DOCTYPE html>
        <html lang="en">
        <head>
            <meta charset="UTF-8">
            <title>Login Form</title>
        </head>
        <body>
            <h1>Login</h1>
            <form action="handle_login.php" method="post">
                <div>
                    <label for="uname">Username:</label><br>
                    <input type="text" id="uname" name="username" required placeholder="Enter your username">
                </div>
                <br>
                <div>
                    <label for="pword">Password:</label><br>
                    <input type="password" id="pword" name="password" required>
                </div>
                <br>
                <div>
                    <button type="submit">Login</button>
                </div>
            </form>
        </body>
        </html>
        ```
        *   "لاحظوا `action` يشير لملف `handle_login.php` (هذا اللي راح نكتبه بعدين)، و `method` هو `post`. كل حقل `input` له `name` فريد (`username`, `password`)."

3.  **طرق إرسال البيانات: GET و POST (20 دقيقة)**
    *   "شفنا في وسم `<form>` السمة `method`. ممكن تكون `get` أو `post`. ايش الفرق بينهم؟"
    *   **`GET`:**
        *   "لما تستخدم `GET`، بيانات النموذج **تُلحق بعنوان URL** في المتصفح بعد علامة استفهام `?`."
        *   "الشكل يكون: `process.php?name=Ali&age=30` (كل حقل وقيمته `name=value` وبينهم علامة `&`)."
        *   **متى نستخدمها؟** مناسبة لعمليات البحث (زي بحث جوجل، تشوف كلمات البحث في الرابط)، أو لما نطلب بيانات غير حساسة، أو لما نبغى المستخدم يقدر يحفظ الرابط (Bookmark) مع البيانات (مثل صفحة منتج معين برقم ID).
        *   **عيوبها:** البيانات مكشوفة في الرابط، في حد لطول الـ URL وبالتالي حجم البيانات اللي نقدر نرسلها.
    *   **`POST`:**
        *   "لما تستخدم `POST`، بيانات النموذج تُرسل **بشكل مخفي** في جسم طلب HTTP (HTTP Request Body). ما تظهر في الرابط."
        *   **متى نستخدمها؟** هي الطريقة المفضلة لإرسال بيانات حساسة (مثل كلمات المرور)، أو بيانات كبيرة، أو لما نكون نسوي عملية تغير حالة على الخادم (مثل إضافة مستخدم جديد، تحديث بيانات).
        *   **عيوبها:** ما نقدر نسوي Bookmark للطلب مع بياناته بسهولة. لو المستخدم سوى Refresh للصفحة بعد الإرسال، المتصفح غالبًا يسأل إذا تبغى تعيد إرسال البيانات (لأنها عملية تغيير).
    *   **"القاعدة العامة:** استخدم `POST` لمعظم النماذج اللي تغير بيانات أو ترسل معلومات حساسة. استخدم `GET` لعمليات البحث أو جلب البيانات اللي ممكن مشاركة رابطها."

4.  **استقبال ومعالجة البيانات في PHP (35 دقيقة)**
    *   "تمام، المستخدم عبى النموذج وضغط إرسال. البيانات وصلت للملف اللي حددناه في `action` (مثلاً `handle_login.php`). كيف نستقبلها في PHP؟"
    *   "PHP توفر لنا مصفوفتين خاصتين (Superglobals) لهذا الغرض:"
        *   **`$_GET`:** مصفوفة تحتوي على كل البيانات اللي أُرسلت باستخدام طريقة `GET`.
        *   **`$_POST`:** مصفوفة تحتوي على كل البيانات اللي أُرسلت باستخدام طريقة `POST`.
    *   "كيف نوصل لقيمة حقل معين؟ نستخدم اسم الحقل (اللي حددناه في السمة `name` في HTML) كمفتاح (key) للمصفوفة."
        *   إذا الطريقة `GET`: `$username = $_GET['username'];`
        *   إذا الطريقة `POST`: `$username = $_POST['username'];`
    *   **"خطوة مهمة جدًا: التحقق قبل الاستخدام!"**
        *   "ما نقدر نفترض إن المستخدم عبى كل الحقول أو إن البيانات وصلت سليمة. لو حاولنا نستخدم مفتاح مش موجود في `$_POST` أو `$_GET` (مثلاً المستخدم ما عبى الحقل أو الاسم خطأ)، راح يصير عندنا خطأ (Notice: Undefined index)."
        *   "الحل؟ نستخدم دالة `isset()` قبل ما نستخدم المتغير. `isset()` تتحقق إذا كان المتغير موجود وله قيمة (غير `NULL`)."
        *   **مثال:** `if (isset($_POST['username'])) { $username = $_POST['username']; } else { // Handle error, maybe username is missing }`
    *   **"نكتب ملف `handle_login.php` اللي يستقبل بيانات النموذج السابق:"**
        ```php
        <?php
            echo "<h1>Processing Login...</h1>";

            // 1. Check if the form was submitted using POST
            if ($_SERVER['REQUEST_METHOD'] == 'POST') {

                // 2. Check if username and password were sent and are not empty
                if ( isset($_POST['username']) && !empty($_POST['username']) &&
                     isset($_POST['password']) && !empty($_POST['password']) )
                {
                    // 3. Get the data (Sanitize it later!)
                    $submitted_username = $_POST['username'];
                    $submitted_password = $_POST['password']; // NEVER use password directly in real apps!

                    echo "Welcome, " . htmlspecialchars($submitted_username) . "!<br>";
                    echo "Your submitted password (for demo): " . htmlspecialchars($submitted_password) . "<br>";

                    // In a real application, you would:
                    // 1. Sanitize inputs thoroughly.
                    // 2. Hash the submitted password.
                    // 3. Compare the hashed password with the one stored in the database.
                    // 4. Redirect to a dashboard or show an error message.

                } else {
                    echo "<p style='color:red;'>Error: Username and password are required.</p>";
                    // You might redirect back to the form here
                    // header('Location: login_form.html');
                }

            } else {
                // If accessed directly without POST method
                echo "<p style='color:orange;'>Error: Invalid request method.</p>";
                // Maybe redirect to the form
                // header('Location: login_form.html');
            }

            echo "<br><a href='login_form.html'>Back to Login Form</a>";
        ?>
        ```
    *   **شرح الكود:**
        *   `$_SERVER['REQUEST_METHOD']`: مصفوفة سوبر جلوبال ثانية تعطينا معلومات عن الخادم والطلب، منها طريقة الطلب (GET/POST).
        *   `isset()`: للتحقق من وجود المتغير.
        *   `empty()`: للتحقق من أن المتغير ليس فارغًا (ليس `""`, `0`, `false`, `null`, etc.).
        *   **`htmlspecialchars()`:** "هذه دالة مهمة جدًا للأمان! تخيل لو المستخدم كتب كود HTML أو JavaScript في حقل الاسم. لو طبعناه مباشرة باستخدام `echo`، المتصفح راح ينفذ الكود! هذا ممكن يسبب مشاكل أمنية (XSS - Cross-site scripting). دالة `htmlspecialchars()` تحول الرموز الخاصة زي `<` و `>` إلى أكواد HTML آمنة (`&lt;` و `&gt;`) بحيث تظهر كـ نص عادي وما تتنفذ. **دائمًا استخدمها لما تطبع أي بيانات جاية من المستخدم.**"

5.  **مراجعة سريعة وختام الجزء الأول (5 دقائق)**
    *   "إذًا، تعلمنا كيف نبني نموذج HTML، الفرق بين GET و POST، كيف نستقبل البيانات في PHP باستخدام `$_GET`/`$_POST`، أهمية التحقق بـ `isset()` و `empty()`، وأهمية `htmlspecialchars()` للحماية."
    *   "أي أسئلة؟"

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن، وقت ننشئ نماذجنا الخاصة ونخلي PHP تستقبل البيانات منها."

1.  **بناء نموذج بسيط وتجربة الإرسال (30 دقيقة)**
    *   **"مهمة 1:"** "كل واحد ينشئ ملفين:"
        *   `contact_form.html`: يحتوي على نموذج بسيط لـ "تواصل معنا" فيه حقول للاسم (text)، الإيميل (email)، والرسالة (textarea). استخدموا `method="post"` و `action="handle_contact.php"`. لا تنسوا `<label>` و السمة `name` لكل حقل.
        *   `handle_contact.php`: ملف PHP يستقبل البيانات المُرسلة.
    *   **"في ملف `handle_contact.php`:"**
        *   تحققوا أن الطلب هو `POST`.
        *   تحققوا أن كل الحقول (الاسم، الايميل، الرسالة) موجودة (`isset`) وليست فارغة (`!empty`).
        *   إذا كل شيء تمام، اطبعوا رسالة ترحيب بالاسم، واطبعوا الإيميل والرسالة باستخدام `htmlspecialchars()`.
        *   إذا في حقل ناقص، اطبعوا رسالة خطأ مناسبة.
    *   **(مساعدة الطلاب في بناء النموذج والـ PHP script)**

2.  **تجربة طريقة GET (20 دقيقة)**
    *   **"مهمة 2:"** "عدلوا ملف `contact_form.html` ليستخدم `method="get"`."
    *   "عدلوا ملف `handle_contact.php` ليقرأ البيانات من `$_GET` بدل `$_POST`."
    *   "جربوا إرسال النموذج الآن. لاحظوا كيف ظهرت البيانات في الـ URL. ناقشوا متى يكون هذا مناسب ومتى لا يكون مناسب لهذا النوع من النماذج."

3.  **إضافة حقول أخرى للنموذج (25 دقيقة)**
    *   **"مهمة 3:"** "طوروا نموذج التواصل (`contact_form.html`) وملف المعالجة (`handle_contact.php`) ليحتوي على:"
        *   **أزرار اختيار (Radio Buttons)** لسؤال "كيف تفضل التواصل؟" (الخيارات: Email, Phone). أعطوهم نفس الـ `name` (مثلاً `contact_preference`) وقيم `value` مختلفة (`email`, `phone`).
        *   **مربع اختيار (Checkbox)** لسؤال "هل ترغب بالاشتراك في النشرة البريدية؟". أعطوه `name` (مثلاً `subscribe`) وقيمة `value="yes"`.
    *   **"في `handle_contact.php`:"**
        *   استقبلوا قيمة زر الراديو المختار (إذا تم اختياره).
        *   استقبلوا قيمة مربع الاختيار. **ملاحظة:** إذا لم يتم تحديد مربع الاختيار، فإنه **لن يُرسل** مع بيانات النموذج. لذا، التحقق بـ `isset($_POST['subscribe'])` يكفي لمعرفة إذا تم تحديده أم لا.
        *   اطبعوا القيم المستلمة لهذه الحقول الجديدة.

4.  **مناقشة مشاكل وأخطاء شائعة (10 دقائق)**
    *   "ما هي الأخطاء اللي ظهرت لكم؟"
    *   **أخطاء شائعة:**
        *   `Notice: Undefined index ...`: غالبًا بسبب نسيان `isset()` أو خطأ إملائي في الـ `name` في HTML أو مفتاح المصفوفة في PHP.
        *   البيانات لا تصل: تأكد من أن `method` في HTML مطابق للمصفوفة المستخدمة في PHP (`GET` مع `$_GET`, `POST` مع `$_POST`). تأكد أن `action` يشير للملف الصحيح. تأكد أن زر الإرسال من نوع `type="submit"`.
        *   كود HTML يظهر عند طباعة المدخلات: نسيان `htmlspecialchars()`.
        *   الـ Radio button أو Checkbox لا تعمل كما هو متوقع: تأكد من `name` و `value`.

5.  **تمارين إضافية للمنزل + ختام (5 دقائق)**
    *   "للتدريب الإضافي:"
        *   **تمرين 1:** أنشئ نموذج آلة حاسبة بسيطة. يحتوي على حقلين لإدخال رقمين (`number1`, `number2`) وقائمة منسدلة (`<select name="operation">`) فيها العمليات (`add`, `subtract`, `multiply`, `divide`). ملف PHP يستقبل الأرقام والعملية المختارة، يجري العملية الحسابية، ويطبع النتيجة. (تأكد من التحقق أن المدخلات أرقام قبل إجراء العملية باستخدام `is_numeric()`).
        *   **تمرين 2:** ابحث عن السمة `required` في حقول HTML. جرب إضافتها لبعض الحقول في نموذج التواصل ولاحظ كيف أن المتصفح نفسه يمنع الإرسال إذا كانت فارغة (هذا يسمى Client-Side Validation، وهو جيد لكن لا يغني عن التحقق في PHP Server-Side Validation).
    *   "عمل رائع اليوم! الآن أصبح بإمكاننا بناء صفحات تتفاعل مع المستخدم بشكل حقيقي. في الأسبوع القادم، راح نبدأ بتخزين هذه البيانات بشكل دائم باستخدام قواعد بيانات MySQL ونربطها مع PHP. يعطيكم العافية!"

---
بالتأكيد، لنستكمل تفصيل البرنامج التدريبي بنفس الأسلوب.

---

### **الأسبوع الثالث**

---

#### **الحصة 5 (الاثنين): مقدمة MySQL، إنشاء قواعد البيانات والجداول**

**🎯 أهداف الحصة:**

1.  فهم مفهوم قواعد البيانات وأهميتها.
2.  التعرف على MySQL كنظام إدارة قواعد بيانات علائقية.
3.  استخدام أداة phpMyAdmin لإدارة قواعد البيانات.
4.  إنشاء قاعدة بيانات جديدة.
5.  إنشاء جدول وتحديد أعمدته وأنواع البيانات والقيود الأساسية (PK, AI, Unique, Null).

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم من جديد! في الحصة الماضية، تعلمنا كيف نستقبل بيانات من المستخدم عن طريق نماذج HTML. ممتاز! لكن وين تروح هذه البيانات بعد ما نستقبلها؟ لو أغلقنا المتصفح أو السكربت انتهى، البيانات تضيع! نحتاج طريقة لتخزينها بشكل **دائم** ومنظم عشان نقدر نرجع لها ونستخدمها بعدين. هنا يأتي دور **قواعد البيانات (Databases)**."

1.  **مراجعة سريعة (5 دقائق)**
    *   "تذكير سريع: كيف كنا نستقبل بيانات النموذج في PHP؟ (باستخدام `$_GET` و `$_POST`). وأيش الدالة المهمة اللي تحمينا من عرض كود خطير أرسله المستخدم؟ (`htmlspecialchars()`). ممتاز!"

2.  **مقدمة لقواعد البيانات و MySQL (20 دقيقة)**
    *   **"ما هي قاعدة البيانات؟"** تخيلوها مثل خزانة ملفات كبيرة ومنظمة جدًا، لكنها إلكترونية. نحط فيها معلوماتنا (بيانات الطلاب، المنتجات، المقالات، أي شيء) بشكل مرتب عشان نقدر نوصل لها بسهولة ونعدل عليها ونبحث فيها."
    *   **"ليش نستخدمها بدل تخزين كل شيء في ملفات نصية عادية مثلاً؟"**
        *   **التنظيم:** البيانات تكون منظمة في جداول (Tables) تشبه جداول Excel، فيها صفوف (Rows/Records) وأعمدة (Columns/Fields).
        *   **الوصول السهل والفعال:** نقدر نبحث عن معلومة معينة بسرعة كبيرة حتى لو عندنا ملايين السجلات.
        *   **تعدد المستخدمين:** أكثر من مستخدم أو برنامج يقدر يوصل للبيانات ويتعامل معها بنفس الوقت.
        *   **الأمان والصلاحيات:** نقدر نحدد مين يقدر يشوف أو يعدل بيانات معينة.
        *   **العلاقات (Relationships):** نقدر نربط الجداول ببعضها (مثلاً جدول الطلاب وجدول الدورات المسجلين فيها). هذا هو أساس **قواعد البيانات العلائقية (Relational Databases)**.
    *   **"نظام إدارة قواعد البيانات (DBMS):"** هو البرنامج اللي يسمح لنا نتعامل مع قاعدة البيانات (ننشئها، نضيف بيانات، نحذف، نعدل...). في أنواع كثيرة، أشهرها قواعد البيانات العلائقية.
    *   **"SQL (Structured Query Language):"** هذه هي اللغة الموحدة اللي نستخدمها عشان نتكلم مع قواعد البيانات العلائقية ونعطيها أوامر (زي: "أنشئ جدول"، "أضف هذا الطالب"، "أعطني كل الطلاب اللي أعمارهم فوق 20").
    *   **"MySQL:"** هو واحد من أشهر أنظمة إدارة قواعد البيانات العلائقية (RDBMS) في العالم، وهو مفتوح المصدر ومجاني، وسريع وموثوق، ويستخدم بكثافة في تطبيقات الويب، وهو اللي يجي مع XAMPP اللي ثبتناه.

3.  **أداة phpMyAdmin (15 دقيقة)**
    *   "كيف نتعامل مع MySQL؟ ممكن عن طريق كتابة أوامر SQL مباشرة في شاشة سوداء (Command Line)، لكن الأسهل في البداية هو استخدام أدوات بواجهة رسومية. XAMPP يوفر لنا أداة ويب ممتازة اسمها **phpMyAdmin**."
    *   **"كيف نوصل لها؟"** "شغلوا Apache و MySQL من لوحة تحكم XAMPP، وبعدين افتحوا المتصفح واكتبوا: `http://localhost/phpmyadmin`."
    *   **(جولة سريعة في واجهة phpMyAdmin):** "لاحظوا على اليسار قائمة بقواعد البيانات الموجودة حاليًا (مثل `mysql`, `information_schema`... هذه لا تلمسوها!). في الأعلى في تبويبات رئيسية زي Databases (لإنشاء قاعدة بيانات جديدة), SQL (لتنفيذ أوامر SQL مباشرة), Status, Users, Export, Import..."

4.  **إنشاء قاعدة بيانات (Database) (15 دقيقة)**
    *   "يلا ننشئ أول قاعدة بيانات خاصة بمشروعنا."
    *   "اضغطوا على تبويب 'Databases' في الأعلى، أو على 'New' في القائمة اليسرى."
    *   "في خانة 'Database name'، اكتبوا اسم لقاعدة البيانات، يفضل بالإنجليزية وبحروف صغيرة وممكن نستخدم الشرطة السفلية `_`. خلينا نسميها مثلاً `university_db`."
    *   "بجانب اسم قاعدة البيانات، في قائمة منسدلة اسمها 'Collation'. هذه تحدد كيفية ترتيب ومقارنة النصوص، وهي مهمة لدعم اللغات المختلفة زي اللغة العربية. اختاروا منها `utf8mb4_general_ci` أو `utf8mb4_unicode_ci`. هذا الخيار يدعم الأحرف العربية والرموز التعبيرية بشكل جيد."
    *   "اضغطوا زر 'Create'."
    *   "المفروض الآن تشوفوا اسم قاعدة البيانات الجديدة ظهر في القائمة اليسرى."

5.  **إنشاء جدول (Table) (30 دقيقة)**
    *   "قاعدة البيانات مثل الخزانة، والجداول هي الملفات أو الأدراج داخل الخزانة. الآن نحتاج ننشئ جدول داخل `university_db` عشان نخزن فيه بيانات الطلاب."
    *   "بعد ما أنشأنا قاعدة البيانات، phpMyAdmin غالبًا يعرض لنا شاشة لإنشاء جدول جديد مباشرة. أو ممكن نضغط على اسم قاعدة البيانات (`university_db`) في القائمة اليسرى، وبعدين نلاقي قسم 'Create table'."
    *   "في خانة 'Name'، نكتب اسم الجدول، مثلاً `students`."
    *   "في خانة 'Number of columns'، نحدد مبدئيًا عدد الأعمدة اللي نحتاجها. خلينا نقول 4 أعمدة حاليًا (ID, الاسم, الايميل, تاريخ التسجيل). اضغط 'Go' أو 'Create'."
    *   "الآن تظهر لنا شاشة تفصيلية لتعريف كل عمود:"
        *   **العمود الأول (ID الطالب):**
            *   `Name`: `id` (اسم العمود)
            *   `Type`: `INT` (نوع البيانات: عدد صحيح Integer)
            *   `Length/Values`: (ممكن نتركه فارغ أو نحط 11)
            *   `Index`: اختار `PRIMARY`. **"إيش يعني Primary Key (PK) - المفتاح الأساسي؟"** هو معرف فريد **لا يتكرر أبدًا** لكل صف (طالب) في الجدول. مثل رقم الهوية للطالب، مستحيل اثنين يكون لهم نفس الرقم. هذا أساسي لربط الجداول والبحث السريع.
            *   `A_I` (Auto Increment): **ضع علامة صح هنا**. هذه الخاصية الرهيبة تخلي MySQL هو اللي يعطي رقم ID تلقائي وفريد لكل طالب جديد نضيفه (يبدأ من 1 ويزيد تلقائيًا 2, 3, 4...). ما نحتاج ندخل الـ ID يدويًا.
        *   **العمود الثاني (اسم الطالب):**
            *   `Name`: `name`
            *   `Type`: `VARCHAR`. **"إيش يعني VARCHAR؟"** يعني نص متغير الطول (Variable Character). مناسب للأسماء والعناوين والنصوص القصيرة إلى المتوسطة.
            *   `Length/Values`: `255`. هذا يحدد **أقصى** طول مسموح للنص في هذا الحقل. 255 هو طول شائع ومناسب للأسماء.
            *   `Null`: **شيل علامة الصح من هنا**. معناها أن هذا الحقل **لا يمكن** أن يكون فارغًا (Not Null). لازم ندخل اسم لكل طالب.
        *   **العمود الثالث (ايميل الطالب):**
            *   `Name`: `email`
            *   `Type`: `VARCHAR`
            *   `Length/Values`: `255`
            *   `Index`: اختار `UNIQUE`. **"إيش يعني Unique Key؟"** يعني القيمة في هذا الحقل **يجب أن تكون فريدة** على مستوى الجدول كله، ما تتكرر. هذا مناسب للإيميل أو اسم المستخدم، عشان نضمن مافي طالبين بنفس الإيميل. (يختلف عن Primary Key، ممكن يكون عندنا أكثر من حقل Unique في الجدول، وممكن حقل Unique يقبل قيمة NULL - بعكس PK).
            *   `Null`: **شيل علامة الصح** (إجباري).
        *   **العمود الرابع (تاريخ التسجيل):**
            *   `Name`: `registration_date`
            *   `Type`: `TIMESTAMP` أو `DATETIME`. كلاهما يخزن تاريخ ووقت. `TIMESTAMP` له نطاق زمني محدود (تقريبًا 1970-2038) لكنه يستخدم غالبًا لتتبع وقت إنشاء أو آخر تحديث للصف. `DATETIME` نطاقه أوسع. خلينا نختار `TIMESTAMP` الآن.
            *   `Default`: اختار `CURRENT_TIMESTAMP`. هذا معناه إنه إذا أضفنا طالب جديد **بدون** ما نحدد قيمة لهذا الحقل، MySQL راح يحط فيه التاريخ والوقت الحالي تلقائيًا. مفيد جدًا!
    *   "بعد ما عرفنا الأعمدة الأربعة، تأكدوا من الـ Storage Engine تحت (غالبًا InnoDB وهو المفضل)، واضغطوا زر 'Save'."
    *   "المفروض الآن تشوفوا جدول `students` ظهر تحت قاعدة بيانات `university_db` على اليسار."

6.  **استعراض بنية الجدول (5 دقائق)**
    *   "اضغطوا على اسم جدول `students` على اليسار. راح تفتح لنا صفحة الجدول. لاحظوا التبويبات في الأعلى:"
        *   `Browse`: لعرض البيانات الموجودة في الجدول (حالياً فارغ).
        *   `Structure`: يعرض لنا تصميم الجدول اللي سويناه (أسماء الأعمدة، أنواعها، القيود مثل PK, Unique...). هذه شاشة مهمة للمراجعة.
        *   `SQL`: لتنفيذ أوامر SQL مباشرة على هذا الجدول.
        *   `Insert`: لإضافة بيانات جديدة يدويًا عن طريق نموذج.
        *   `Operations`: لعمليات على الجدول نفسه (تغيير اسمه، إفراغه...).
    *   "تصفحوا تبويب `Structure` وتأكدوا إن الأعمدة والخصائص اللي حددناها موجودة صح."

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"تمام، الآن صار عندكم فكرة عن قواعد البيانات والجداول وكيف ننشئها باستخدام phpMyAdmin. وقت التجربة العملية بأنفسكم!"

1.  **استكشاف phpMyAdmin (15 دقيقة)**
    *   **"نشاط استكشافي:"** "خذوا لكم كم دقيقة تتجولون في phpMyAdmin. اضغطوا على أسماء قواعد البيانات الموجودة على اليسار (مثل `mysql`, `performance_schema`, `phpmyadmin`). شوفوا الجداول اللي داخلها، واضغطوا على تبويب `Structure` لبعض الجداول. **تحذير:** هذه قواعد بيانات يستخدمها النظام، لا تغيروا فيها شيء أو تحذفوها! الهدف فقط تشوفوا أمثلة لجداول حقيقية."

2.  **إنشاء قاعدة بيانات وجداول خاصة بكم (45 دقيقة)**
    *   **"مهمة 1: قاعدة بيانات شخصية"** "كل واحد منكم ينشئ قاعدة بيانات جديدة خاصة فيه. ممكن تسموها `[اسمكم]_db` (مثلاً `ali_db`). لا تنسوا تختاروا الـ Collation `utf8mb4_general_ci`."
    *   **"مهمة 2: جدول الدورات (Courses)"** "داخل قاعدة البيانات اللي أنشأتوها، أنشئوا جدول جديد اسمه `courses`. هذا الجدول راح يحتوي على الأعمدة التالية:"
        *   `course_id`: النوع `INT`, اجعلوه `PRIMARY KEY` و `AUTO_INCREMENT`.
        *   `course_name`: النوع `VARCHAR`, الطول مثلاً `100`, واجعلوه `NOT NULL` (إجباري).
        *   `credits`: النوع `INT`, يمثل عدد الساعات المعتمدة للدورة. اجعلوه `NOT NULL`.
    *   **"مهمة 3: جدول المدرسين (Instructors)"** "في نفس قاعدة البيانات، أنشئوا جدول ثاني اسمه `instructors`. يحتوي على:"
        *   `instructor_id`: النوع `INT`, اجعلوه `PRIMARY KEY` و `AUTO_INCREMENT`.
        *   `instructor_name`: النوع `VARCHAR`, الطول `100`, `NOT NULL`.
        *   `department`: النوع `VARCHAR`, الطول `50`. (هذا ممكن نسمح له يكون `NULL`، يعني ممكن مدرس ما يكون تابع لقسم معين).
    *   **(متابعة الطلاب أثناء عملهم وتقديم المساعدة)**

3.  **حل المشكلات ومناقشة (20 دقيقة)**
    *   "ايش المشاكل اللي واجهتكم أثناء إنشاء قاعدة البيانات أو الجداول؟"
    *   **مشاكل شائعة:**
        *   **خطأ في الاتصال بـ phpMyAdmin:** تأكد أن Apache و MySQL شغالين في XAMPP Control Panel.
        *   **أخطاء عند الإنشاء:**
            *   "Syntax error": غالبًا خطأ في اسم العمود (ممكن كلمة محجوزة) أو اختيار نوع بيانات غير مناسب. phpMyAdmin يساعد في تجنبها لكن لو بتكتب SQL يدويًا ممكن تصير.
            *   "Duplicate entry ... for key 'PRIMARY'": محاولة إدخال قيمة مكررة في حقل مفتاح أساسي (ما راح تصير لو مستخدمين Auto Increment صح).
            *   "Incorrect table definition; there can be only one auto column and it must be defined as a key": محاولة جعل عمود Auto Increment وهو ليس Primary Key أو جزء من مفتاح.
    *   **"نقاش:"**
        *   "ليش اخترنا `VARCHAR(100)` لاسم الدورة وما اخترنا مثلاً `TEXT`؟" (الجواب: `VARCHAR` أسرع للبحث والفهرسة لما نحدد طول أقصى معقول. `TEXT` مناسب للنصوص الطويلة جدًا مثل محتوى مقال كامل).
        *   "متى نستخدم `INT` ومتى ممكن نحتاج `FLOAT` أو `DECIMAL`؟" (`INT` للأعداد الصحيحة زي الـ ID أو عدد الساعات. `FLOAT`/`DECIMAL` للأرقام العشرية زي الأسعار أو المعدلات. `DECIMAL` أدق للقيم المالية).
        *   "إيش الفايدة الحقيقية من `AUTO_INCREMENT`؟" (يضمن IDs فريدة بدون ما نضطر نفكر فيها أو نتعامل مع احتمالية التكرار لو أكثر من مستخدم أضاف بيانات بنفس اللحظة).
        *   "ما الفرق الجوهري بين `PRIMARY KEY` و `UNIQUE`؟" (الجدول يمكن أن يحتوي على PK واحد فقط، وهو لا يقبل NULL أبدًا. يمكن أن يحتوي على عدة UNIQUE keys، والـ UNIQUE key يمكن أن يقبل قيمة NULL واحدة).

4.  **تمرين منزلي (تصميم) + ختام (10 دقائق)**
    *   **"تمرين للمنزل (للتفكير والتصميم):"** "فكروا في تطبيق مدونة (Blog) بسيط. إيش الجداول اللي ممكن نحتاجها؟ (مثلاً: جدول للمستخدمين `users`, جدول للمقالات `posts`, جدول للتعليقات `comments`). لكل جدول، فكروا في الأعمدة اللي ممكن تحتاجوها (زي: `user_id`, `username`, `password_hash`, `post_id`, `title`, `content`, `post_date`, `comment_id`, `comment_text`, `user_id_fk`, `post_id_fk`...). حاولوا ترسموا تصميم بسيط على ورقة أو باستخدام أي أداة بسيطة، وحددوا أنواع البيانات لكل عمود، والمفتاح الأساسي (PK) لكل جدول." (هذا يمهد لمفهوم العلاقات والمفاتيح الخارجية Foreign Keys).
    *   "ممتاز جدًا! اليوم وضعنا حجر الأساس لتخزين بياناتنا بشكل منظم باستخدام MySQL و phpMyAdmin. في الحصة القادمة، راح نربط اللي تعلمناه اليوم مع اللي تعلمناه في PHP، ونبدأ نضيف ونقرأ بيانات من قاعدة البيانات باستخدام كود PHP. يعطيكم العافية!"

---

#### **الحصة 6 (الخميس): ربط PHP مع MySQL (CRUD Operations)**

**🎯 أهداف الحصة:**

1.  التعرف على طرق الاتصال بـ MySQL من PHP (MySQLi و PDO).
2.  الاتصال بقاعدة البيانات باستخدام MySQLi (الأسلوب الإجرائي).
3.  ضبط ترميز الاتصال لدعم اللغة العربية.
4.  تنفيذ عمليات CRUD الأساسية (Create, Read, Update, Delete) باستخدام PHP و SQL.
5.  التعرف على دوال MySQLi الرئيسية (`mysqli_connect`, `mysqli_query`, `mysqli_fetch_assoc`, `mysqli_num_rows`, `mysqli_affected_rows`, `mysqli_error`, `mysqli_close`, `mysqli_set_charset`).

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً وسهلاً! الأسبوع الماضي أنشأنا قاعدة بياناتنا وجداولنا باستخدام phpMyAdmin. اليوم، راح نسوي الخطوة الأهم: نخلي كود PHP تبعنا **يتكلم** مع قاعدة بيانات MySQL هذه عشان نقدر نخزن فيها بيانات ونسترجعها ونعدلها ونحذفها. هذه العمليات الأربعة الأساسية تسمى اختصارًا **CRUD**."

1.  **مراجعة سريعة (5 دقائق)**
    *   "مين يذكرنا باسم قاعدة البيانات اللي أنشأناها؟ واسم جدول الطلاب؟ وايش الأعمدة اللي حطيناها فيه؟ إيش هو المفتاح الأساسي؟ وإيش يعني Auto Increment؟ ممتاز!"

2.  **طرق الاتصال بـ MySQL من PHP (10 دقائق)**
    *   "PHP توفر لنا طريقتين أساسيتين للتعامل مع MySQL:"
        *   **MySQLi (MySQL Improved Extension):**
            *   هذه مكتبة محسنة ومخصصة للتعامل مع MySQL **فقط**.
            *   تدعم أسلوبين في الكتابة: إجرائي (Procedural - مجموعة دوال عادية زي `mysqli_connect()`) وكائني (Object-Oriented - إنشاء كائن من كلاس `mysqli`).
            *   تعتبر أسهل للمبتدئين خصوصًا بالأسلوب الإجرائي. **وهي اللي راح نركز عليها في البداية.**
        *   **PDO (PHP Data Objects):**
            *   هذه تعتبر طبقة تجريدية (Abstraction Layer) تسمح لك بالاتصال **بأنواع مختلفة** من قواعد البيانات (MySQL, PostgreSQL, SQLite, SQL Server...) باستخدام نفس مجموعة الدوال والأوامر تقريبًا.
            *   تعتمد على الأسلوب الكائني فقط.
            *   تعتبر أكثر مرونة واحترافية لو كنت تخطط إن تطبيقك ممكن يشتغل على قواعد بيانات مختلفة في المستقبل.
    *   "الاثنين كويسين، لكن راح نبدأ بـ MySQLi (الأسلوب الإجرائي) لسهولته."

3.  **الاتصال بقاعدة البيانات (Connecting) (20 دقيقة)**
    *   "عشان PHP يقدر يتصل بـ MySQL، يحتاج يعرف أربع معلومات أساسية:"
        *   **اسم المضيف (Server Name/Hostname):** عنوان الجهاز اللي عليه قاعدة البيانات. بما إننا شغالين على جهازنا المحلي باستخدام XAMPP، العنوان هو `localhost` (أو أحيانًا `127.0.0.1`).
        *   **اسم المستخدم (Username):** اسم المستخدم الخاص بقاعدة البيانات اللي له صلاحية الوصول. في XAMPP، المستخدم الافتراضي هو `root`.
        *   **كلمة المرور (Password):** كلمة مرور مستخدم قاعدة البيانات. في XAMPP، كلمة مرور المستخدم `root` الافتراضية هي **فارغة** (`""`).
        *   **اسم قاعدة البيانات (Database Name):** اسم قاعدة البيانات اللي نبغى نتصل بها، اللي هي `university_db`.
    *   **"كيف نكتب كود الاتصال؟"** (نفتح ملف PHP جديد، مثلاً `db_connect.php`)
        ```php
        <?php
            $servername = "localhost";
            $username = "root";
            $password = "";
            $dbname = "university_db";

            // Create connection using mysqli procedural style
            $conn = mysqli_connect($servername, $username, $password, $dbname);

            // Check connection
            if (!$conn) {
                // If connection fails, stop the script and show error
                die("Connection failed: " . mysqli_connect_error());
            }

            echo "Connected successfully!<br>";

            // **** IMPORTANT FOR ARABIC SUPPORT ****
            // Set the character set to utf8mb4 AFTER connecting
            if (!mysqli_set_charset($conn, "utf8mb4")) {
                printf("Error loading character set utf8mb4: %s\n", mysqli_error($conn));
                exit();
            } else {
                // Optional: echo confirmation
                // printf("Current character set: %s\n", mysqli_character_set_name($conn));
            }


            // We will add CRUD operations here later...


            // Good practice to close the connection at the end of the script
            // mysqli_close($conn);
            // Note: PHP often closes it automatically when script finishes,
            // but explicitly closing is clearer.
        ?>
        ```
    *   **شرح الكود:**
        *   `mysqli_connect()`: هذه الدالة اللي تسوي الاتصال. ترجع لنا كائن الاتصال (أو `false` إذا فشل).
        *   `if (!$conn)`: نتحقق إذا الاتصال فشل.
        *   `die()`: توقف تنفيذ السكربت فورًا وتطبع الرسالة.
        *   `mysqli_connect_error()`: ترجع نص رسالة الخطأ اللي حصلت أثناء محاولة الاتصال.
        *   **`mysqli_set_charset($conn, "utf8mb4")`:** ***هذه خطوة ضرورية جدًا جدًا إذا كنا بنتعامل مع بيانات باللغة العربية!*** لازم ننفذها مباشرة بعد الاتصال الناجح عشان نضمن أن البيانات العربية تُقرأ وتُكتب بشكل صحيح من وإلى قاعدة البيانات.
        *   `mysqli_close()`: تغلق الاتصال.

4.  **عمليات CRUD (Create, Read, Update, Delete) (50 دقيقة)**
    *   "الآن بعد ما اتصلنا، نقدر نبدأ ننفذ أوامر SQL على قاعدة البيانات باستخدام دالة `mysqli_query()`."

    *   **أ. Create (إضافة بيانات - INSERT):**
        *   **أمر SQL:** `INSERT INTO table_name (column1, column2) VALUES ('value1', 'value2');`
        *   **مثال PHP:** إضافة طالب جديد لجدول `students`.
            ```php
            <?php
            // ... (Include or repeat connection code from db_connect.php) ...
            // Make sure $conn is available and charset is set

            $newName = "خالد"; // Example Arabic name
            $newEmail = "khalid@example.com";

            // Construct the SQL query (WARNING: Vulnerable to SQL Injection!)
            $sql = "INSERT INTO students (name, email) VALUES ('$newName', '$newEmail')";

            echo "Executing query: " . $sql . "<br>";

            // Execute the query
            if (mysqli_query($conn, $sql)) {
                // Get the ID of the newly inserted record
                $last_id = mysqli_insert_id($conn);
                echo "New record created successfully. Last inserted ID is: " . $last_id;
            } else {
                echo "Error: " . $sql . "<br>" . mysqli_error($conn);
            }

            mysqli_close($conn);
            ?>
            ```
        *   **شرح:**
            *   `mysqli_query($conn, $sql)`: ننفذ أمر الـ SQL. ترجع `true` للـ INSERT الناجح، و `false` للفاشل.
            *   `mysqli_error($conn)`: ترجع نص الخطأ إذا فشل الـ query. مفيدة جدًا لمعرفة سبب المشكلة.
            *   `mysqli_insert_id($conn)`: بعد عملية INSERT ناجحة، هذه الدالة ترجع قيمة الـ ID اللي أُنشئ تلقائيًا (إذا كان العمود Auto Increment).
            *   **"تحذير أمني مهم:"** "الطريقة اللي كتبنا فيها الـ SQL (`VALUES ('$newName', '$newEmail')`) خطيرة جدًا وعرضة لهجوم اسمه **SQL Injection**. لو المستخدم قدر يتحكم بقيم `$newName` أو `$newEmail` (مثلاً من فورم)، ممكن يكتب كود SQL ضار داخلها ويتلاعب بقاعدة البيانات. راح نتعلم الطريقة الآمنة (Prepared Statements) لاحقًا، لكن الآن ركزوا على فهم عملية الـ INSERT نفسها."

    *   **ب. Read (قراءة بيانات - SELECT):**
        *   **أمر SQL:** `SELECT column1, column2 FROM table_name WHERE condition ORDER BY column;`
        *   **مثال PHP:** قراءة كل الطلاب من جدول `students` وعرضهم.
            ```php
            <?php
            // ... (Connection code) ...

            $sql = "SELECT id, name, email, registration_date FROM students ORDER BY name ASC"; // Get all students, order by name alphabetically
            $result = mysqli_query($conn, $sql); // For SELECT, $result is a result object or false

            if ($result) {
                // Check if there are any rows returned
                if (mysqli_num_rows($result) > 0) {
                    echo "<h2>Student List:</h2>";
                    echo "<table border='1'>
                            <tr>
                                <th>ID</th>
                                <th>Name</th>
                                <th>Email</th>
                                <th>Registered On</th>
                            </tr>";
                    // Fetch associative array for each row
                    while($row = mysqli_fetch_assoc($result)) {
                        echo "<tr>";
                        echo "<td>" . $row["id"] . "</td>";
                        echo "<td>" . htmlspecialchars($row["name"]) . "</td>"; // Use htmlspecialchars!
                        echo "<td>" . htmlspecialchars($row["email"]) . "</td>";
                        echo "<td>" . $row["registration_date"] . "</td>";
                        echo "</tr>";
                    }
                    echo "</table>";
                } else {
                    echo "0 results found in the students table.";
                }
                // Free the result set (optional but good practice)
                mysqli_free_result($result);
            } else {
                echo "Error executing query: " . mysqli_error($conn);
            }

            mysqli_close($conn);
            ?>
            ```
        *   **شرح:**
            *   `mysqli_query()` مع `SELECT` ترجع كائن نتيجة (result object) إذا نجحت، أو `false` إذا فشلت.
            *   `mysqli_num_rows($result)`: تعطينا عدد الصفوف (السجلات) اللي رجعت من الاستعلام.
            *   `mysqli_fetch_assoc($result)`: تجلب صف واحد من النتيجة على شكل مصفوفة ترابطية (associative array)، حيث تكون أسماء الأعمدة هي المفاتيح (keys). كل مرة نستدعيها تجيب الصف التالي، ولما تخلص الصفوف ترجع `null`.
            *   الحلقة `while($row = mysqli_fetch_assoc($result))`: طريقة شائعة جدًا للمرور على كل الصفوف اللي رجعت من الاستعلام.
            *   `htmlspecialchars()`: استخدمناها عند طباعة الاسم والايميل كإجراء أمني احتياطي.

    *   **ج. Update (تحديث بيانات - UPDATE):**
        *   **أمر SQL:** `UPDATE table_name SET column1 = 'new_value1', column2 = 'new_value2' WHERE condition;`
        *   **مثال PHP:** تحديث إيميل طالب معين نعرف الـ ID تبعه.
            ```php
            <?php
            // ... (Connection code) ...

            $student_id_to_update = 1; // Example: Update student with ID = 1
            $new_email_address = "khalid.updated@newdomain.com";

            // Construct the SQL query (Still vulnerable!)
            $sql = "UPDATE students SET email='$new_email_address' WHERE id=$student_id_to_update";

            echo "Executing query: " . $sql . "<br>";

            if (mysqli_query($conn, $sql)) {
                // Check how many rows were affected
                $affected_rows = mysqli_affected_rows($conn);
                if ($affected_rows > 0) {
                     echo "Record updated successfully. Rows affected: " . $affected_rows;
                } else {
                     echo "Record not found with ID $student_id_to_update, or the email was already the same.";
                }
            } else {
                echo "Error updating record: " . mysqli_error($conn);
            }

            mysqli_close($conn);
            ?>
            ```
        *   **شرح:**
            *   `mysqli_affected_rows($conn)`: بعد تنفيذ `UPDATE` (أو `INSERT` أو `DELETE`)، هذه الدالة ترجع عدد الصفوف اللي تأثرت بالعملية. مفيدة للتأكد إذا التحديث تم فعلاً.

    *   **د. Delete (حذف بيانات - DELETE):**
        *   **أمر SQL:** `DELETE FROM table_name WHERE condition;`
        *   **مثال PHP:** حذف طالب معين بناءً على الـ ID.
            ```php
            <?php
            // ... (Connection code) ...

            $student_id_to_delete = 2; // Example: Delete student with ID = 2

            // Construct the SQL query (Still vulnerable!)
            $sql = "DELETE FROM students WHERE id=$student_id_to_delete";

            echo "Executing query: " . $sql . "<br>";

            if (mysqli_query($conn, $sql)) {
                 $affected_rows = mysqli_affected_rows($conn);
                 if ($affected_rows > 0) {
                    echo "Record deleted successfully. Rows affected: " . $affected_rows;
                 } else {
                    echo "Record not found with ID $student_id_to_delete.";
                 }
            } else {
                echo "Error deleting record: " . mysqli_error($conn);
            }

            mysqli_close($conn);
            ?>
            ```
        *   **"تحذير شديد اللهجة:"** "كونوا حذرين جدًا جدًا جدًا مع أمر `DELETE`! لو نفذتوه بدون جملة `WHERE` (مثلاً `DELETE FROM students`)، راح **يحذف كل البيانات** في الجدول بلا رجعة! دائمًا تأكدوا من شرط `WHERE` عند الحذف أو التحديث."

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن وقت نطبق هذه العمليات الأربعة (CRUD) بأنفسنا باستخدام PHP."

1.  **الإعداد والتأكد من البيئة (10 دقائق)**
    *   "تأكدوا إن قاعدة بيانات `university_db` موجودة عندكم وفيها جدول `students` (ولو فارغ عادي). تأكدوا إن XAMPP شغال (Apache و MySQL)."
    *   "ممكن تضيفوا كم طالب يدويًا باستخدام تبويب `Insert` في phpMyAdmin عشان يكون عندنا بيانات نشتغل عليها للقراءة والحذف."

2.  **التمرين 1: الاتصال وقراءة وعرض البيانات (30 دقيقة)**
    *   **"المهمة:"** "أنشئوا ملف PHP جديد اسمه `view_students.php`."
    *   "داخل الملف:"
        1.  اكتبوا كود الاتصال بقاعدة بيانات `university_db` (مثل اللي شفناه في `db_connect.php`). لا تنسوا التحقق من الاتصال وضبط الـ charset `utf8mb4`.
        2.  اكتبوا استعلام `SELECT` لجلب كل الأعمدة (`*` أو حددوا الأعمدة `id, name, email`) من جدول `students`. ممكن ترتبوهم بالـ ID باستخدام `ORDER BY id ASC`.
        3.  نفذوا الاستعلام باستخدام `mysqli_query()`.
        4.  تحققوا إذا الاستعلام نجح وإذا كان فيه نتائج (`mysqli_num_rows() > 0`).
        5.  إذا فيه نتائج، استخدموا حلقة `while` مع `mysqli_fetch_assoc()` لقراءة كل صف وعرضه داخل جدول HTML بسيط (أعمدة الجدول: ID, Name, Email). استخدموا `htmlspecialchars()` عند عرض الاسم والايميل.
        6.  إذا لا يوجد طلاب، اطبعوا رسالة "لا يوجد طلاب حاليًا".
        7.  إذا فشل الاستعلام، اطبعوا رسالة خطأ باستخدام `mysqli_error()`.
        8.  أغلقوا الاتصال في النهاية `mysqli_close()`.
    *   "جربوا تشغيل الملف `view_students.php` من المتصفح وشوفوا النتيجة."
    *   **(مساعدة الطلاب في كتابة الكود وتصحيح الأخطاء)**

3.  **التمرين 2: إضافة طالب جديد عبر نموذج (35 دقيقة)**
    *   **"المهمة:"** "نحتاج ملفين لهذا التمرين:"
        *   **`add_student_form.html`:** ملف HTML يحتوي على نموذج بسيط فيه حقلين:
            *   حقل للاسم: `<input type="text" name="student_name" required>`
            *   حقل للإيميل: `<input type="email" name="student_email" required>`
            *   زر إرسال: `<button type="submit">Add Student</button>`
            *   النموذج لازم يرسل البيانات باستخدام `method="post"` إلى الملف الثاني `action="add_student.php"`.
        *   **`add_student.php`:** ملف PHP يستقبل البيانات.
            1.  ابدأوا بكود الاتصال بالداتابيز وضبط الـ charset.
            2.  تحققوا أن الطلب هو `POST` (`$_SERVER['REQUEST_METHOD']`).
            3.  تحققوا أن `student_name` و `student_email` موجودين (`isset`) وليسوا فارغين (`!empty`).
            4.  إذا الشروط تحققت، خذوا القيم من `$_POST`.
            5.  **مهم:** قبل استخدام القيم في الـ SQL، ممكن ننظفها بشكل بسيط مبدئيًا (لاحقًا بنستخدم طرق أفضل):
                *   `$name = mysqli_real_escape_string($conn, $_POST['student_name']);`
                *   `$email = mysqli_real_escape_string($conn, $_POST['student_email']);`
                *   **شرح `mysqli_real_escape_string`:** هذه الدالة تحاول تهريب (escape) الحروف الخاصة اللي ممكن تسبب مشاكل في جملة SQL (مثل علامة الاقتباس المفردة `'`). هي خطوة أولى نحو الأمان، لكنها **ليست كافية** لوحدها ضد كل أنواع SQL Injection.
            6.  اكتبوا جملة `INSERT INTO students (name, email) VALUES ('$name', '$email');`.
            7.  نفذوا الاستعلام `mysqli_query()`.
            8.  تحققوا إذا الإضافة نجحت أو فشلت واطبعوا رسالة مناسبة للمستخدم (مثلاً "تمت إضافة الطالب بنجاح" أو "خطأ: " مع `mysqli_error()`).
            9.  ممكن تضيفوا رابط للرجوع لصفحة `view_students.php` عشان يشوفوا الطالب الجديد.
            10. إذا الشروط في خطوة 3 ما تحققت (بيانات ناقصة)، اطبعوا رسالة خطأ.
            11. أغلقوا الاتصال.
    *   "جربوا النموذج: افتحوا `add_student_form.html`، عبوا البيانات، واضغطوا إضافة. بعدين روحوا لـ `view_students.php` وتأكدوا إن الطالب انضاف."

4.  **التمرين 3 (متقدم قليلاً): رابط حذف بسيط (10 دقائق - أو يكمل في البيت)**
    *   **"المهمة:"** "نرجع نعدل ملف `view_students.php`."
    *   "في جدول عرض الطلاب، بجانب كل طالب، أضيفوا عمود جديد فيه رابط 'حذف'."
    *   "هذا الرابط لازم يودي لملف جديد اسمه `delete_student.php` ولازم يمرر الـ ID حق الطالب اللي نبغى نحذفه مع الرابط. كيف نمرر بيانات مع الرابط؟ باستخدام طريقة **GET**!"
    *   "شكل الرابط داخل الحلقة `while` راح يكون كالتالي (مثلاً):"
        ```html
        echo "<td><a href='delete_student.php?id=" . $row["id"] . "'>Delete</a></td>";
        ```
    *   **"الآن، أنشئوا ملف `delete_student.php`:"**
        1.  اتصلوا بالداتابيز.
        2.  تحققوا إذا كان فيه `id` جاي مع الرابط باستخدام `isset($_GET['id'])`.
        3.  خذوا قيمة الـ ID من `$_GET['id']`. **مهم:** تأكدوا إنها رقم صحيح! ممكن تستخدموا `(int)$_GET['id']` أو `intval($_GET['id'])`.
        4.  اكتبوا جملة `DELETE FROM students WHERE id = $student_id;` (هنا `$student_id` هو الرقم اللي أخذناه من GET).
        5.  نفذوا الاستعلام.
        6.  اطبعوا رسالة نجاح أو فشل.
        7.  الأفضل بعد الحذف الناجح، نسوي إعادة توجيه (redirect) للمستخدم يرجعه لصفحة `view_students.php` عشان يشوف القائمة المحدثة. ممكن نستخدم: `header("Location: view_students.php"); exit();` (ملاحظة: `header()` لازم تتنفذ قبل أي طباعة HTML).
        8.  أغلقوا الاتصال.
    *   **"نقاش سريع:"** "هل استخدام GET للحذف آمن؟ لا، لأنه ممكن أي شخص يكتب الرابط يدويًا ويحذف، وممكن محركات البحث تأرشف الرابط بالخطأ وتحذف بيانات! الحذف والتعديل لازم يتموا غالبًا باستخدام POST أو طرق أكثر أمانًا راح نشوفها."

5.  **حل المشاكل والختام (5 دقائق)**
    *   "ايش المشاكل اللي واجهتكم؟ أخطاء اتصال؟ أخطاء SQL؟" (استخدام `mysqli_error($conn)` هو صديقكم المفضل هنا!).
    *   "تأكدوا إن أسماء الجداول والأعمدة في كود PHP مطابقة تمامًا للي في قاعدة البيانات (حساسة لحالة الأحرف أحيانًا حسب النظام)."
    *   **"تمرين للمنزل:"** "حاولوا تسووا نموذج وملف PHP لتعديل بيانات طالب موجود. النموذج ممكن يكون فيه حقل مخفي (`<input type="hidden">`) يحمل ID الطالب اللي نبغى نعدله، وحقول للاسم والايميل الجديدين. ملف PHP يستقبل الـ ID والبيانات الجديدة وينفذ جملة `UPDATE`."
    *   "رائع جدًا! اليوم قدرنا نخلي PHP يتفاعل بشكل كامل مع قاعدة البيانات (CRUD). في الحصة القادمة، راح ننقل تركيزنا شوي ونبدأ نبني واجهة برمجية (API) بسيطة ترجع لنا هذه البيانات بصيغة JSON عشان نقدر نستخدمها في تطبيقات ثانية زي تطبيقات الموبايل اللي راح نبنيها بـ Flutter. يعطيكم العافية!"

---

### **الأسبوع الرابع**

---

#### **الحصة 7 (الاثنين): بناء API بسيط (JSON Response)**

**🎯 أهداف الحصة:**

1.  فهم مفهوم الواجهة البرمجية للتطبيقات (API) وأهميتها.
2.  التعرف على تنسيق JSON واستخدامه الشائع في APIs.
3.  بناء API بسيط باستخدام PHP لجلب بيانات من MySQL وإرجاعها كـ JSON.
4.  استخدام دالة `json_encode()` لتحويل مصفوفة PHP إلى JSON.
5.  أهمية ضبط ترويسة الاستجابة `Content-Type` إلى `application/json`.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"مرحبًا مجددًا! حتى الآن، تعلمنا كيف نستخدم PHP لقراءة بيانات من قاعدة البيانات وعرضها كصفحة HTML مباشرة في المتصفح. هذا ممتاز لمواقع الويب التقليدية. لكن ماذا لو أردنا أن تطبيق موبايل (مثل تطبيق Flutter اللي راح نتعلمه لاحقًا) أو تطبيق ويب آخر (مبني بتقنيات مختلفة زي React أو Angular) هو اللي يطلب هذه البيانات ويعرضها بطريقته الخاصة؟ هنا يأتي دور **الواجهات البرمجية للتطبيقات (APIs)**."

1.  **مراجعة سريعة (5 دقائق)**
    *   "إيش العمليات الأربعة الأساسية اللي نقدر نسويها على قاعدة البيانات باستخدام PHP؟ (CRUD: Create, Read, Update, Delete). وايش الدالة اللي نستخدمها لتنفيذ أوامر SQL؟ (`mysqli_query`). والدالة اللي تجيب صف واحد من النتائج كمصفوفة؟ (`mysqli_fetch_assoc`). ممتاز!"

2.  **ما هو API؟ (15 دقيقة)**
    *   "API اختصار لـ **Application Programming Interface**. فكروا فيها كـ **عقد** أو **وسيط** بين برنامجين مختلفين عشان يقدروا يكلموا بعض ويتبادلوا معلومات أو وظائف."
    *   **"أشهر مثال: النادل في المطعم!"**
        *   أنت (العميل/التطبيق الأمامي Front-end) تريد طلب طبق معين.
        *   النادل (الـ API) هو الواجهة اللي تتعامل معها. أنت تعطيه طلبك (Request) بصيغة معينة يفهمها (من قائمة الطعام).
        *   النادل يأخذ طلبك للمطبخ (الخادم/النظام الخلفي Back-end) اللي ما تشوفه مباشرة.
        *   المطبخ يجهز الطبق (يعالج الطلب وينفذ المنطق ويجيب البيانات).
        *   النادل يرجع لك الطبق الجاهز (الاستجابة Response) بالشكل المطلوب.
    *   **"ليش نحتاج API في تطوير الويب والموبايل؟"**
        *   **فصل الواجهات (Decoupling):** نفصل تمامًا بين تصميم الواجهة الأمامية (Flutter, React, HTML/CSS/JS) وبين منطق العمل وقاعدة البيانات في الخلفية (PHP, Python, Node.js...). هذا يخلي كل جزء يتطور بشكل مستقل.
        *   **إعادة الاستخدام (Reusability):** نقدر نبني منطق العمل وقاعدة البيانات مرة واحدة كـ API، وبعدين نستخدمه من تطبيق ويب، تطبيق أندرويد، تطبيق iOS، أو حتى من برامج أخرى تحتاج بياناتنا.
        *   **التنظيم والصيانة:** الكود يصير أكثر تنظيمًا وسهل الصيانة والتطوير.
    *   **"API الويب (Web API):"** هو النوع اللي يهمنا، وهو يستخدم بروتوكول HTTP (نفس اللي يستخدمه المتصفح) عشان يرسل الطلبات ويستقبل الاستجابات. الطلب يكون موجه لـ URL معين (يسمى Endpoint).

3.  **ما هو JSON؟ (15 دقيقة)**
    *   "طيب، لما الـ API (النادل) يرجع لنا بالبيانات (الطبق الجاهز)، بأي شكل أو لغة يسلمها لنا عشان التطبيق اللي طلبها (أنت) يقدر يفهمها؟"
    *   "هنا يجي دور **JSON** (تُنطق جيسون). هي اختصار لـ **JavaScript Object Notation**."
    *   "JSON هو تنسيق **نصي خفيف جدًا** لتبادل البيانات. هو الآن **المعيار الأكثر استخدامًا** في الـ Web APIs."
    *   **"ليش هو مشهور؟"**
        *   **سهل القراءة للبشر:** شكله مرتب وواضح.
        *   **سهل للآلات (البرامج):** سهل جدًا على لغات البرمجة المختلفة (PHP, JavaScript, Dart, Python...) إنها تحلله (Parse) وتنشئه (Generate).
    *   **"كيف شكل JSON؟"**
        *   يعتمد على مفهومين أساسيين من JavaScript (لكنه مستقل عن JavaScript ويُستخدم في كل اللغات):
            *   **الكائنات (Objects):** مجموعة من أزواج **مفتاح/قيمة (Key/Value Pairs)**، تكون داخل أقواس معقوفة `{}`. المفتاح يكون نص (String) داخل علامات اقتباس مزدوجة `"`، والقيمة ممكن تكون أي نوع بيانات JSON. مثال: `{"name": "Ali", "age": 30}`.
            *   **المصفوفات (Arrays):** قائمة مرتبة من القيم، تكون داخل أقواس مربعة `[]`. القيم ممكن تكون من أي نوع بيانات JSON. مثال: `["PHP", "Flutter", "MySQL"]`.
        *   **أنواع البيانات المسموحة كـ قيم (Values) في JSON:**
            *   نص (String): لازم يكون داخل علامات اقتباس مزدوجة `"like this"`.
            *   رقم (Number): صحيح أو عشري، بدون علامات اقتباس `123`, `3.14`.
            *   قيمة منطقية (Boolean): `true` أو `false` (بدون علامات اقتباس).
            *   مصفوفة (Array): `[ ... ]`.
            *   كائن (Object): `{ ... }`.
            *   القيمة الفارغة `null` (بدون علامات اقتباس).
    *   **مثال JSON لبيانات طالب:**
        ```json
        {
          "id": 15,
          "student_name": "Fatima",
          "email": "fatima@example.com",
          "is_active": true,
          "enrolled_courses": ["PHP101", "DB205"]
        }
        ```
    *   **مثال JSON لمصفوفة من الطلاب:**
        ```json
        [
          { "id": 15, "name": "Fatima" },
          { "id": 16, "name": "Omar" },
          { "id": 17, "name": "Aisha" }
        ]
        ```

4.  **بناء API بسيط لجلب الطلاب (PHP + JSON) (45 دقيقة)**
    *   **"الهدف:"** "ننشئ ملف PHP، لما نطلبه من المتصفح أو من تطبيق ثاني، ما يرجع لنا صفحة HTML، لا، يرجع لنا قائمة الطلاب من قاعدة البيانات بصيغة JSON."
    *   **"الخطوات:"** (ننشئ ملف جديد، مثلاً `api_get_students.php`)
        1.  **الاتصال بقاعدة البيانات:** نفس كود الاتصال اللي استخدمناه من قبل (`mysqli_connect`, check connection, set charset `utf8mb4`).
        2.  **كتابة استعلام SQL:** `SELECT id, name, email FROM students ORDER BY id DESC;` (نجيب آخر الطلاب المضافين أولاً).
        3.  **تنفيذ الاستعلام:** `$result = mysqli_query($conn, $sql);`
        4.  **تحويل النتائج إلى مصفوفة PHP:**
            *   نتحقق إذا الاستعلام نجح وإذا فيه نتائج (`$result && mysqli_num_rows($result) > 0`).
            *   ننشئ مصفوفة PHP فارغة عشان نخزن فيها الطلاب: `$students_array = [];` (الاختصار `[]` هو نفس `array()`).
            *   نستخدم حلقة `while` مع `mysqli_fetch_assoc($result)` عشان نجيب كل صف.
            *   داخل الحلقة، نضيف كل صف (اللي هو عبارة عن مصفوفة PHP ترابطية) إلى مصفوفة الطلاب الرئيسية: `$students_array[] = $row;`
        5.  **تحويل مصفوفة PHP إلى نص JSON:**
            *   "PHP توفر لنا دالة سحرية لهذا الغرض: `json_encode()`."
            *   `$json_output = json_encode($students_array, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE);`
            *   **شرح المعاملات الثانية (Flags):**
                *   `JSON_PRETTY_PRINT`: تخلي شكل الـ JSON الناتج مقروء ومنسق بمسافات وفواصل أسطر (مفيد للتصحيح والقراءة).
                *   `JSON_UNESCAPED_UNICODE`: **ضروري جدًا** عشان الحروف العربية تظهر بشكل صحيح في الـ JSON الناتج، بدل ما تظهر كـ `\uXXXX`.
        6.  **ضبط ترويسة الاستجابة (Response Header):**
            *   "هذه خطوة **حاسمة ومهمة جدًا!** لازم نخبر المتصفح أو التطبيق اللي طلب البيانات إن اللي راجع له الآن هو بيانات بصيغة JSON وليس HTML أو نص عادي."
            *   "نستخدم دالة `header()` في PHP **قبل** طباعة أي شيء آخر (قبل `echo`)."
            *   `header('Content-Type: application/json; charset=utf-8');`
            *   (نضيف `charset=utf-8` للتأكيد على ترميز المخرجات).
        7.  **طباعة الـ JSON:**
            *   `echo $json_output;`
        8.  **إغلاق الاتصال:** `mysqli_close($conn);`

    *   **"الكود الكامل لـ `api_get_students.php`:"**
        ```php
        <?php
            // --- Database Connection ---
            $servername = "localhost";
            $username = "root";
            $password = "";
            $dbname = "university_db";

            $conn = mysqli_connect($servername, $username, $password, $dbname);

            // Check connection
            if (!$conn) {
                // In a real API, return a JSON error
                header('Content-Type: application/json; charset=utf-8');
                http_response_code(500); // Internal Server Error
                echo json_encode(['error' => 'Database connection failed: ' . mysqli_connect_error()]);
                exit();
            }

            // Set charset
            mysqli_set_charset($conn, "utf8mb4");

            // --- Fetch Data ---
            $sql = "SELECT id, name, email, registration_date FROM students ORDER BY id DESC";
            $result = mysqli_query($conn, $sql);

            $students_array = []; // Initialize empty array

            if ($result && mysqli_num_rows($result) > 0) {
                // Fetch associative array
                while($row = mysqli_fetch_assoc($result)) {
                    $students_array[] = $row; // Add each row (which is an assoc array) to the main array
                }
                mysqli_free_result($result); // Free result set
            } elseif (!$result) {
                // Handle query error
                 header('Content-Type: application/json; charset=utf-8');
                 http_response_code(500); // Internal Server Error
                 echo json_encode(['error' => 'Error executing query: ' . mysqli_error($conn)]);
                 mysqli_close($conn);
                 exit();
            }
            // If query succeeded but returned 0 rows, $students_array will just be empty, which is fine.

            // --- Close Connection ---
            mysqli_close($conn);

            // --- Set Header and Output JSON ---
            // THIS MUST BE CALLED BEFORE ANY OUTPUT (echo, HTML, even whitespace)
            header('Content-Type: application/json; charset=utf-8');

            echo json_encode($students_array, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE);

        ?>
        ```
    *   **"كيف نختبر الـ API هذا؟"**
        *   "افتحوا المتصفح وروحوا للرابط: `http://localhost/php_course/api_get_students.php` (أو حسب مسار مجلدكم)."
        *   "المفروض ما تشوفوا جدول HTML، بل تشوفوا نص يشبه اللي شفناه في أمثلة JSON، يحتوي على بيانات الطلاب."
        *   "ممكن تستخدموا أدوات المطور في المتصفح (F12 -> Network tab) عشان تشوفوا الـ Response Header وتتأكدوا إن الـ `Content-Type` هو `application/json`."
        *   "في أدوات متخصصة لاختبار الـ APIs مثل Postman أو Insomnia، وهي أفضل من المتصفح لأنها تعطينا تحكم أكبر، راح نتعرف عليها لاحقًا."

5.  **مراجعة وختام الجزء الأول (5 دقائق)**
    *   "إذًا، عرفنا اليوم ايش هو API وليش مهم، وإيش هو JSON وليش هو لغة تبادل البيانات المفضلة للـ APIs. وتعلمنا خطوة بخطوة كيف نبني API بسيط جدًا باستخدام PHP و MySQLi يجيب بيانات الطلاب ويرجعها كـ JSON، مع التركيز على `json_encode()` و `header('Content-Type: ...')`."
    *   "أي أسئلة؟"

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن دوركم تجربوا بأنفسكم بناء أول API لكم!"

1.  **التمرين 1: بناء API جلب الطلاب (30 دقيقة)**
    *   **"المهمة:"** "كل واحد يطبق الخطوات اللي شرحناها وينشئ ملف `api_get_students.php` الخاص فيه."
    *   "انسخوا كود الاتصال أو اكتبوه من جديد."
    *   "اكتبوا الـ SQL."
    *   "نفذوا الاستعلام."
    *   "اجمعوا النتائج في مصفوفة PHP."
    *   "حولوا المصفوفة إلى JSON باستخدام `json_encode()` مع الخيارات المناسبة (`PRETTY_PRINT`, `UNESCAPED_UNICODE`)."
    *   "ضعوا `header('Content-Type: application/json; charset=utf-8');` **في المكان الصحيح** (قبل الـ echo)."
    *   "اطبعوا الـ JSON."
    *   "أغلقوا الاتصال."
    *   "جربوا تشغيل الملف من المتصفح وتأكدوا إن النتيجة هي JSON وتحتوي بيانات الطلاب اللي عندكم في الجدول."
    *   **(مساعدة الطلاب وتصحيح الأخطاء)**

2.  **التمرين 2: بناء API جلب الدورات (25 دقيقة)**
    *   **"المهمة:"** "بنفس الطريقة، أنشئوا ملف API جديد اسمه `api_get_courses.php`."
    *   "هذا الـ API لازم يتصل بنفس قاعدة البيانات (`university_db` أو قاعدة بياناتكم الشخصية إذا أنشأتم جدول `courses` فيها)."
    *   "يجلب كل البيانات من جدول `courses` (اللي فيه `course_id`, `course_name`, `credits`)."
    *   "يرجع النتائج كـ JSON."
    *   "لا تنسوا الـ `header()` والـ flags في `json_encode()`."
    *   "اختبروه في المتصفح."

3.  **التمرين 3 (متقدم): API لجلب تفاصيل طالب واحد (30 دقيقة)**
    *   **"المهمة:"** "نحتاج API ثاني يجيب لنا تفاصيل طالب **واحد فقط** بناءً على الـ ID تبعه."
    *   "أنشئوا ملف جديد اسمه `api_get_student_details.php`."
    *   "هذا الـ API راح يستقبل الـ ID عن طريق الـ URL (طريقة GET). يعني الرابط راح يكون شكله: `api_get_student_details.php?id=5`."
    *   **"داخل الملف:"**
        1.  اتصلوا بالداتابيز.
        2.  تحققوا إذا كان فيه `id` مرسل في الـ URL باستخدام `isset($_GET['id'])`.
        3.  إذا الـ `id` موجود، خذوا قيمته وخزنوها في متغير `$student_id`. **مهم:** حولوا القيمة إلى رقم صحيح `(int)$_GET['id']` للأمان ولمنع أخطاء في الـ SQL.
        4.  اكتبوا جملة `SELECT` تجيب كل بيانات الطالب اللي الـ `id` تبعه يساوي `$student_id`. (مثلاً: `SELECT * FROM students WHERE id = $student_id LIMIT 1;`). الـ `LIMIT 1` هنا جيد للأداء وللتأكيد إننا نتوقع نتيجة واحدة.
        5.  نفذوا الاستعلام.
        6.  بما إننا نتوقع طالب واحد فقط (أو لا شيء)، ما نحتاج حلقة `while`. نستخدم `mysqli_fetch_assoc()` مرة واحدة مباشرة.
        7.  `$student_data = mysqli_fetch_assoc($result);`
        8.  **التعامل مع حالة عدم وجود الطالب:** إذا `$student_data` كانت `null` أو `false` (يعني الـ ID غير موجود)، لازم نرجع استجابة JSON مناسبة تدل على الخطأ. ممكن نرجع كائن JSON فارغ `{}` أو الأفضل رسالة خطأ واضحة ونغير كود الحالة HTTP.
            ```php
            if ($student_data) {
                // Found the student
                header('Content-Type: application/json; charset=utf-8');
                echo json_encode($student_data, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE);
            } else {
                // Student not found
                header('Content-Type: application/json; charset=utf-8');
                http_response_code(404); // Not Found HTTP status code
                echo json_encode(['error' => 'Student not found with ID: ' . $student_id]);
            }
            ```
        9.  إذا الـ `id` لم يتم إرساله في الـ URL أصلًا (خطوة 2 فشلت)، رجعوا خطأ مختلف (مثل 400 Bad Request).
        10. أغلقوا الاتصال.
    *   "اختبروا هذا الـ API بتجربة ID موجود و ID غير موجود في المتصفح."

4.  **مناقشة مشاكل وأدوات (5 دقائق)**
    *   "ايش المشاكل اللي ظهرت؟"
    *   **"Headers already sent..."**: هذا أشهر خطأ! يحصل لو طبعتوا أي شيء (حتى مسافة بيضاء قبل `<?php`) قبل استدعاء دالة `header()` أو `http_response_code()`. تأكدوا إنها أول شيء يتم إرساله.
    *   **"الـ JSON الناتج شكله غريب أو الحروف العربية تالفة:"** تأكدوا من استخدام `JSON_UNESCAPED_UNICODE` ومن ضبط الـ charset في الاتصال (`mysqli_set_charset`) وفي الـ header (`charset=utf-8`).
    *   **"أخطاء SQL في الاستعلام اللي فيه `WHERE id = ...`:"** تأكدوا إن الـ ID اللي جاي من `$_GET` هو رقم صحيح وإن جملة الـ SQL مكتوبة صح.
    *   **"مقدمة لأدوات اختبار API:"** "المتصفح ممتاز للعرض، لكن لو نبغى نرسل بيانات مع الطلب (زي POST) أو نتحكم بالـ Headers بشكل دقيق، نحتاج أدوات أفضل مثل **Postman** أو **Insomnia**. هذه برامج مجانية تسمح لنا نصمم ونختبر الـ APIs بشكل احترافي. (عرض سريع لواجهة Postman وكيفية عمل طلب GET للـ API اللي أنشأوه)."

5.  **تمرين منزلي + ختام (5 دقائق)**
    *   **"تمرين للمنزل:"** "أنشئوا ملف API جديد اسمه `api_get_instructors.php` يجلب قائمة المدرسين من جدول `instructors` (اللي يفترض أنشأتموه في الحصة السابقة) ويرجعهم كـ JSON."
    *   "رائع! اليوم قفزنا قفزة كبيرة وبدأنا نفكر بطريقة الـ API. الآن بياناتنا صارت جاهزة عشان أي تطبيق يقدر يطلبها ويستخدمها. في الحصة القادمة، راح نتكلم عن كيف نحمي هذا الـ API وما نخليه متاح لأي شخص، راح ندخل في موضوع الـ Authentication. يعطيكم العافية!"

---

#### **الحصة 8 (الخميس): تأمين API (Authentication & Authorization - مقدمة)**

**🎯 أهداف الحصة:**

1.  فهم الحاجة لتأمين الـ APIs.
2.  التمييز بين المصادقة (Authentication) والتفويض (Authorization).
3.  التعرف على طرق المصادقة الشائعة (Basic Auth, API Keys, Token-based/JWT).
4.  تطبيق طريقة حماية بسيطة باستخدام مفتاح API ثابت (Static API Key).
5.  فهم أهمية التحقق من الصلاحيات (Authorization) كمفهوم.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"مساء الخير! في المرة الماضية بنينا أول API لنا يرجع بيانات الطلاب بصيغة JSON. ممتاز! لكن حاليًا، أي شخص يعرف رابط الـ API يقدر يوصل لهذه البيانات. ماذا لو كانت هذه البيانات حساسة؟ أو لو كان الـ API يسمح بإضافة أو حذف بيانات؟ لا يمكن أن نتركه مفتوحًا هكذا! اليوم سنتعلم كيف **نؤمّن (Secure)** الـ API الخاص بنا."

1.  **مراجعة سريعة (5 دقائق)**
    *   "إيش هو الـ API؟ (وسيط/عقد). وإيش هو الـ JSON؟ (تنسيق تبادل بيانات). وكيف خلينا PHP يرجع JSON؟ (باستخدام `json_encode` و `header('Content-Type')`). تمام."

2.  **لماذا نحتاج لتأمين الـ API؟ (10 دقائق)**
    *   **حماية البيانات الحساسة:** معلومات شخصية للمستخدمين، بيانات مالية، أسرار تجارية... لا يجب أن تكون متاحة للجميع.
    *   **التحكم بالوصول:** ليس كل مستخدم يجب أن يكون قادرًا على فعل كل شيء. قد نريد السماح للبعض بالقراءة فقط، وللبعض الآخر بالإضافة والتعديل.
    *   **منع الإساءة والاستخدام المفرط (Rate Limiting):** قد نريد تحديد عدد الطلبات المسموح بها لكل مستخدم أو تطبيق خلال فترة معينة لمنع استهلاك موارد الخادم بشكل مفرط.
    *   **تتبع الاستخدام والتحليل:** معرفة من يستخدم الـ API وكيف يستخدمه.
    *   **تحقيق الدخل (Monetization):** بعض الـ APIs تكون مدفوعة، والتأمين ضروري للتحكم بالوصول المدفوع.

3.  **الفرق بين Authentication و Authorization (15 دقيقة)**
    *   "هذول مصطلحين مهمين جدًا وكثير يلخبطوا بينهم:"
    *   **Authentication (AuthN - المصادقة أو التحقق من الهوية):**
        *   السؤال هو: **"من أنت؟"** (Who are you?)
        *   الهدف هو التأكد من أن المستخدم أو التطبيق هو **من يدعي أنه هو**.
        *   **مثال:** إدخال اسم مستخدم وكلمة مرور صحيحة، استخدام بصمة الإصبع، إدخال كود SMS. لما تدخل كلمة مرورك صح، النظام **صادق** على هويتك.
    *   **Authorization (AuthZ - التفويض أو التحقق من الصلاحية):**
        *   السؤال هو: **"ماذا يُسمح لك بفعله؟"** (What are you allowed to do?)
        *   الهدف هو التأكد من أن المستخدم (اللي تمت المصادقة عليه وعرفنا مين هو) لديه **الصلاحية** للوصول إلى مورد معين أو تنفيذ إجراء معين.
        *   **مثال:** أنت موظف في شركة (تمت المصادقة عليك ببطاقة الدخول - AuthN). هل مسموح لك تدخل غرفة الاجتماعات؟ (هذا تفويض - AuthZ). هل مسموح لك تدخل مكتب المدير؟ (قد لا يكون لديك هذا التفويض).
    *   **"العلاقة بينهم؟"** المصادقة (AuthN) تأتي **أولاً**. لازم نعرف مين الشخص قبل ما نقرر ايش مسموح له يسوي.

4.  **طرق شائعة لمصادقة API (Authentication) (30 دقيقة)**
    *   "كيف الـ API يتأكد مين اللي قاعد يكلمه؟ في طرق كثيرة، منها:"
    *   **1. HTTP Basic Authentication:**
        *   طريقة قديمة وبسيطة جدًا. التطبيق يرسل اسم المستخدم وكلمة المرور مشفرين بطريقة سهلة الفك (Base64) في ترويسة (Header) اسمها `Authorization`.
        *   **عيوبها:** تعتبر **غير آمنة** لأن كلمة المرور تُرسل مع كل طلب، وحتى لو كانت Base64 فهي سهلة الفك جدًا. يجب استخدامها **فقط** مع اتصال مشفر HTTPS.
    *   **2. مفاتيح API (API Keys):**
        *   فكرة شائعة وبسيطة نسبيًا. الخادم يولد **مفتاح سري فريد** (سلسلة نصية طويلة وعشوائية) لكل تطبيق أو مستخدم مصرح له باستخدام الـ API.
        *   التطبيق اللي يبغى يستخدم الـ API لازم يرسل هذا المفتاح مع كل طلب. غالبًا يتم إرساله في ترويسة خاصة (Custom Header) مثل `X-API-Key: [المفتاح_السري_هنا]` أو أحيانًا كجزء من الـ URL (أقل أمانًا).
        *   الخادم يستقبل الطلب، يشوف المفتاح المرسل، ويتأكد إذا كان هذا المفتاح صالح وموجود عنده في قاعدة بيانات المفاتيح المسموح بها.
        *   **مميزاتها:** أسهل في التطبيق من الطرق المعقدة. مناسبة لحماية الـ APIs اللي ما تحتاج تعرف المستخدم *الشخصي* بالضبط، أو للتواصل بين الخوادم (Server-to-Server).
        *   **عيوبها:** لو تسرب المفتاح، أي شخص يقدر يستخدمه. لا تميز المستخدمين الأفراد بشكل جيد.
    *   **3. OAuth (1.0a, 2.0):**
        *   هذا **بروتوكول معقد وقوي جدًا**، وهو المعيار المستخدم في سيناريوهات مثل "تسجيل الدخول باستخدام Google" أو "السماح لتطبيق X بالوصول لصورك على Facebook".
        *   الفكرة الأساسية هي **التفويض المحدود**: المستخدم يمنح تطبيق معين صلاحية محدودة للوصول لموارده الموجودة على خدمة أخرى، **بدون** ما يعطي كلمة مروره الأساسية للتطبيق.
        *   **قوي وآمن جدًا لكن تطبيقه أعقد بكثير.** (نذكره للعلم، لكن ما راح نطبقه الآن).
    *   **4. المصادقة المعتمدة على التوكن (Token-Based Authentication) - وأشهرها JWT:**
        *   هذه الطريقة **شائعة جدًا جدًا** في الـ Web APIs الحديثة وتطبيقات الموبايل والـ SPAs.
        *   **"كيف تشتغل؟ (الفكرة العامة):"**
            1.  **تسجيل الدخول:** المستخدم يرسل اسمه/إيميله وكلمة المرور للـ API (مثلاً لنقطة نهاية `/login`).
            2.  **التحقق والإصدار:** الخادم يتحقق من صحة البيانات. إذا كانت صحيحة، الخادم **يُنشئ ويُصدر "توكن" (Token)**. التوكن هذا عبارة عن سلسلة نصية فريدة ومميزة، وغالبًا تكون **موقعة رقميًا** من الخادم (عشان يتأكد إنها ما تعدلت). التوكن ممكن يحتوي على معلومات عن المستخدم (مثل الـ ID تبعه، دوره Role) وتاريخ انتهاء صلاحية.
            3.  **الإرسال للعميل:** الخادم يرجع هذا التوكن للعميل كجزء من استجابة تسجيل الدخول الناجح.
            4.  **التخزين:** العميل (تطبيق الويب أو الموبايل) يستقبل التوكن **ويخزنه** في مكان آمن عنده (مثل Local Storage في الويب، أو SharedPreferences/SecureStorage في Flutter).
            5.  **الإرسال مع كل طلب:** لكل طلب لاحق لنقاط النهاية المحمية في الـ API، العميل لازم يرفق هذا التوكن مع الطلب، غالبًا في ترويسة `Authorization` بالشكل التالي: `Authorization: Bearer [التوكن_هنا]`.
            6.  **التحقق من الخادم:** الخادم يستقبل الطلب، يأخذ التوكن من الترويسة، **ويتحقق من توقيعه** (هل هو صادر مني ولم يتم التلاعب به؟) **وصلاحيته** (هل انتهت مدته؟). إذا كان التوكن سليم وصالح، الخادم يثق في هوية المستخدم المذكور في التوكن وينفذ له الطلب.
        *   **مميزات هذه الطريقة:**
            *   **لا نرسل كلمة المرور مع كل طلب،** فقط نرسل التوكن.
            *   **Stateless (عديمة الحالة):** الخادم ما يحتاج يحفظ معلومات جلسة (Session) لكل مستخدم متصل. كل المعلومات اللي يحتاجها للتحقق موجودة في التوكن نفسه. هذا يخلي الـ API قابل للتوسع (Scalable) بشكل أفضل.
        *   **JWT (JSON Web Token):** هو **معيار (Standard)** مشهور جدًا لتطبيق Token-Based Authentication. التوكن يكون عبارة عن JSON مقسم لثلاثة أجزاء (Header, Payload, Signature) وموقع رقميًا. (ما راح نتعمق في تفاصيله الآن، لكن مهم تعرفوا الاسم).

5.  **مثال عملي بسيط: استخدام مفتاح API ثابت (Static API Key) (25 دقيقة)**
    *   **"الهدف:"** "خلينا نطبق أبسط طريقة حماية على الـ API اللي سويناه (`api_get_students.php`). راح نخليه يطلب مفتاح سري بسيط (نفس المفتاح لكل الطلبات في هذا المثال) عشان يشتغل."
    *   **"الخطوات:"**
        1.  **تحديد مفتاح سري:** في بداية ملف PHP، نعرّف ثابت (Constant) يحمل المفتاح السري تبعنا. **تنويه:** في التطبيق الحقيقي، هذا المفتاح ما يكون مكتوب مباشرة في الكود، بل يكون فريد لكل مستخدم ومخزن بشكل آمن في قاعدة البيانات أو ملف إعدادات. لكن للتجربة الآن:
            ```php
            define('SECRET_API_KEY', 'MyVerySecretKey12345'); // Choose a strong random key
            ```
        2.  **قراءة الترويسات الواردة:** نحتاج نقرأ الترويسة اللي نتوقع المفتاح يكون فيها. خلينا نتفق إن العميل لازم يرسل ترويسة اسمها `X-API-Key`. كيف نقرأها في PHP؟
            *   نستخدم دالة `getallheaders()`: ترجع مصفوفة بكل الترويسات. (قد لا تعمل في كل بيئات PHP).
            *   أو الطريقة الأضمن غالبًا: نستخدم مصفوفة `$_SERVER`. الترويسات المخصصة تظهر فيها مسبوقة بـ `HTTP_` وكل الحروف تكون كبيرة، والشرطات `-` تتحول لشرطات سفلية `_`. فترويسة `X-API-Key` نقرأها من `$_SERVER['HTTP_X_API_KEY']`.
            *   نستخدم معامل الدمج الفارغ (Null Coalescing Operator `??` - متاح في PHP 7+) عشان نتعامل مع حالة عدم وجود الترويسة بأمان:
                ```php
                $receivedApiKey = $_SERVER['HTTP_X_API_KEY'] ?? null;
                ```
        3.  **التحقق من المفتاح:** نقارن المفتاح اللي وصل (`$receivedApiKey`) بالمفتاح السري اللي عرفناه (`SECRET_API_KEY`).
        4.  **في حالة الفشل (المفتاح غلط أو مش موجود):**
            *   **لا نكمل تنفيذ الكود!**
            *   نرجع استجابة خطأ للمستخدم. لازم نحدد كود الحالة HTTP المناسب، مثل `401 Unauthorized` (غير مصرح له) أو `403 Forbidden` (ممنوع).
            *   نضبط ترويسة `Content-Type` إلى `application/json`.
            *   نطبع رسالة خطأ بصيغة JSON.
            *   نوقف تنفيذ السكربت باستخدام `exit();`.
        5.  **في حالة النجاح:** نسمح للسكربت يكمل شغله عادي (يتصل بالداتابيز ويجيب البيانات...).

    *   **"تعديل ملف `api_get_students.php` ليصبح محميًا:"**
        ```php
        <?php
            // Define the secret API key
            define('SECRET_API_KEY', 'MyVerySecretKey12345');

            // --- Authentication Check ---
            // Get the API key from the request header
            $receivedApiKey = $_SERVER['HTTP_X_API_KEY'] ?? null;

            // Validate the API key
            if ($receivedApiKey === null || $receivedApiKey !== SECRET_API_KEY) {
                // If key is missing or invalid, send error response and exit
                header('Content-Type: application/json; charset=utf-8');
                http_response_code(401); // Unauthorized
                echo json_encode([
                    'status' => 'error',
                    'message' => 'Unauthorized: Missing or invalid API Key.'
                ]);
                exit(); // Stop script execution immediately
            }
            // --- End Authentication Check ---

            // If we reach here, the API key was valid. Proceed with the rest of the script...

            // --- Database Connection ---
            $servername = "localhost";
            // ... (rest of the connection and query code as before) ...

            // --- Set Header and Output JSON ---
            header('Content-Type: application/json; charset=utf-8');
            echo json_encode($students_array, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE);
        ?>
        ```
    *   **"كيف نختبره؟"**
        *   "جربوا تفتحوا الرابط في المتصفح مباشرة. المفروض الآن ما تشوفوا بيانات الطلاب، بل تشوفوا رسالة الخطأ JSON اللي عرفناها (Unauthorized)."
        *   "الآن، نستخدم أداة زي **Postman**:"
            *   أنشئوا طلب جديد (Request) من نوع `GET` لنفس رابط الـ API.
            *   روحوا لقسم الـ Headers في Postman.
            *   أضيفوا ترويسة جديدة: المفتاح (Key) هو `X-API-Key` والقيمة (Value) هي المفتاح السري اللي عرفناه (`MyVerySecretKey12345`).
            *   اضغطوا 'Send'.
            *   المفروض الآن تشوفوا بيانات الطلاب تظهر بنجاح في الـ Response Body!
            *   "جربوا تغيروا قيمة المفتاح في Postman أو تحذفوا الترويسة وتضغطوا Send مرة ثانية، راح ترجع رسالة الخطأ."

6.  **مقدمة بسيطة للتفويض (Authorization) (5 دقائق)**
    *   "تمام، الآن الـ API تبعنا محمي بمفتاح. عرفنا مين اللي يقدر يوصل له (اللي معاه المفتاح الصحيح - هذا AuthN). لكن ماذا لو أردنا أن نعطي صلاحيات مختلفة؟"
    *   "مثلاً، تخيلوا عندنا API ثاني لحذف طالب (`api_delete_student.php?id=...`). ممكن نبغى نسمح فقط للمستخدمين اللي عندهم دور 'admin' إنهم يستخدموا هذا الـ API، بينما المستخدمين اللي دورهم 'viewer' يقدروا يستخدموا `api_get_students.php` فقط."
    *   "هذا هو **التفويض (Authorization)**. بعد ما نتأكد من هوية المستخدم (عن طريق API Key أو الأفضل Token)، نحتاج نتحقق من **دوره (Role)** أو **صلاحياته (Permissions)** (اللي تكون مخزنة في قاعدة البيانات مع معلومات المستخدم) قبل ما ننفذ له الإجراء المطلوب."
    *   "هذا يتطلب تصميم أكثر تعقيدًا لقاعدة البيانات ونظام المستخدمين، راح نتطرق له بشكل أعمق لما نبدأ نبني المشروع المتكامل."

7.  **مراجعة وختام الجزء الأول (5 دقائق)**
    *   "إذًا، عرفنا ليش نحمي الـ API، والفرق بين Authentication و Authorization. شفنا طرق المصادقة المختلفة (API Key, Token/JWT...). وطبقنا حماية بسيطة باستخدام Static API Key وعرفنا كيف نختبرها بـ Postman. وأخذنا فكرة عن أهمية التحقق من الصلاحيات (Authorization) كخطوة تالية."
    *   "أي أسئلة؟"

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن دوركم تطبقوا الحماية على الـ APIs اللي أنشأتموها وتتعودوا على استخدام Postman."

1.  **التمرين 1: تطبيق الحماية بـ Static API Key (35 دقيقة)**
    *   **"المهمة:"** "طبقوا نفس الخطوات اللي شرحناها لتأمين ملف `api_get_students.php` الخاص بكم باستخدام مفتاح API ثابت (Static API Key)."
    *   "عرفوا `SECRET_API_KEY` في بداية الملف."
    *   "أضيفوا كود التحقق من الترويسة `X-API-Key`."
    *   "تأكدوا من إرجاع خطأ `401 Unauthorized` مع رسالة JSON مناسبة في حالة فشل التحقق، واستخدام `exit()`."
    *   **"الاختبار (مهم):"**
        *   اختبروا من المتصفح (المفروض يفشل).
        *   استخدموا Postman:
            *   جربوا بدون إرسال الترويسة (المفروض يفشل).
            *   جربوا بإرسال الترويسة `X-API-Key` مع قيمة **خاطئة** (المفروض يفشل).
            *   جربوا بإرسال الترويسة `X-API-Key` مع القيمة **الصحيحة** (المفروض ينجح وتظهر البيانات).
    *   **(مساعدة الطلاب في تطبيق الكود واستخدام Postman بشكل صحيح)**

2.  **التمرين 2: تأمين API الدورات (25 دقيقة)**
    *   **"المهمة:"** "بنفس الطريقة تمامًا، طبقوا الحماية باستخدام نفس الـ `SECRET_API_KEY` على ملف `api_get_courses.php` اللي أنشأتموه سابقًا."
    *   "اختبروه بـ Postman بنفس الخطوات (بدون مفتاح، بمفتاح خطأ، بمفتاح صح)."

3.  **التمرين 3 (تطبيق على API مع باراميتر): (15 دقيقة)**
    *   **"المهمة:"** "عدلوا ملف `api_get_student_details.php?id=...` بحيث يتطلب أيضًا نفس الـ API Key للوصول إليه."
    *   "ضعوا كود التحقق من الـ API Key في بداية الملف."
    *   "اختبروه بـ Postman، جربوا طلب ID موجود مرة مع المفتاح الصحيح، ومرة بدونه."

4.  **مناقشة وتوسيع الفهم (10 دقائق)**
    *   "لماذا الطريقة اللي طبقناها (مفتاح واحد ثابت لكل الـ APIs) تعتبر غير عملية وغير آمنة في التطبيقات الحقيقية؟"
        *   (الجواب: لو تسرب المفتاح، كل الـ APIs تصير مكشوفة. صعب نلغي الوصول لتطبيق معين بدون تغيير المفتاح للكل. ما نقدر نعرف مين بالضبط اللي سوى الطلب).
    *   "كيف ممكن نحسنها؟"
        *   (الجواب: ممكن يكون لكل مستخدم أو تطبيق مفتاح API خاص فيه، والمفاتيح تكون مخزنة في قاعدة بيانات. الخادم يتحقق من المفتاح المرسل مقابل قاعدة البيانات).
    *   "كيف طريقة التوكن (JWT) اللي تكلمنا عنها نظريًا تحل مشكلة معرفة المستخدم بشكل أفضل من مجرد API Key؟"
        *   (الجواب: التوكن نفسه يحمل معلومات عن المستخدم (مثل ID أو Role)، والخادم يتحقق من التوقيع. فكل طلب يحمل هوية المستخدم الموثوقة).

5.  **حل المشكلات والختام (5 دقائق)**
    *   "هل واجهتكم مشاكل في قراءة الهيدر؟ أو في إرجاع كود الخطأ الصحيح؟ أو في استخدام Postman؟"
    *   **"تمرين منزلي (بحث):"** "ابحثوا عن مصطلح JWT (JSON Web Token). حاولوا تفهموا بشكل عام ايش الأجزاء الثلاثة الرئيسية اللي يتكون منها التوكن (Header, Payload, Signature). ما هو دور كل جزء بشكل مبسط؟ (لا تحتاجوا تفهموا كيفية التوقيع بالتفصيل، فقط الفكرة العامة)."
    *   "ممتاز! اليوم دخلنا عالم تأمين الـ APIs. الخطوة القادمة في الشهر الثاني راح تكون تطبيق عملي أكثر وبناء API متكامل أكثر، وراح نبدأ نشوف كيف نربط الجداول ببعضها (العلاقات) ونتعامل مع رفع الملفات وغيرها. يعطيكم العافية!"

---

بالتأكيد، لنواصل تفصيل البرنامج التدريبي للشهر الثاني، والذي يركز على مواضيع متقدمة في تطوير الواجهات الخلفية ومشروع عملي صغير.

---

## **📅 المرحلة الأولى: Back-End (PHP, MySQL, XAMPP)**

---

### **الشهر الثاني: تطوير API متقدم**

---

#### **الأسبوع الخامس**

---

#### **الحصة 9 (الاثنين): العلاقات بين الجداول (Relationships)**

**🎯 أهداف الحصة:**

1.  فهم أهمية العلاقات بين الجداول في قواعد البيانات العلائقية.
2.  التعرف على أنواع العلاقات الثلاثة الرئيسية: واحد لواحد (One-to-One)، واحد لكثير (One-to-Many)، وكثير لكثير (Many-to-Many).
3.  فهم مفهوم المفتاح الخارجي (Foreign Key) ودوره في ربط الجداول.
4.  تطبيق علاقة واحد لكثير (One-to-Many) عمليًا باستخدام phpMyAdmin والمفاتيح الخارجية.
5.  كتابة استعلامات SQL تستخدم `JOIN` لجلب بيانات من جداول مرتبطة.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم في الشهر الثاني من رحلتنا! في الشهر الأول وضعنا أساسًا قويًا في PHP و MySQL و APIs. اليوم، راح نتعمق أكثر في تصميم قواعد البيانات ونعرف كيف نخلي جداولنا **تتكلم مع بعضها** بشكل فعال ومنطقي باستخدام **العلاقات (Relationships)**."

1.  **مراجعة سريعة (5 دقائق)**
    *   "مين يذكرنا كيف أمّنا الـ API في المرة الماضية؟ (باستخدام API Key). وإيش الفرق بين Authentication و Authorization؟ ممتاز!"
    *   "تذكروا لما صممنا جدول `students` وجدول `courses` وجدول `instructors`؟ حاليًا، هذه الجداول منفصلة تمامًا. ما نعرف مين المدرس اللي يدرّس أي دورة، أو أي طالب مسجل في أي دورة. العلاقات هي اللي راح تحل لنا هذه المشكلة."

2.  **لماذا العلاقات مهمة؟ (10 دقائق)**
    *   **تقليل التكرار (Reducing Redundancy):** تخيلوا لو في جدول الطلاب، كنا نضيف اسم الدورة واسم المدرس لكل طالب مسجل فيها. لو تغير اسم المدرس أو اسم الدورة، لازم نعدله في كل سجلات الطلاب! هذا سيء جدًا. العلاقات تسمح لنا نخزن معلومة الدورة أو المدرس مرة واحدة في جدولها الخاص، ونربطها بالطلاب.
    *   **ضمان تكامل البيانات (Ensuring Data Integrity):** العلاقات (باستخدام المفاتيح الخارجية) تضمن إنه ما نقدر نضيف طالب لدورة غير موجودة أصلًا، أو نحذف مدرس ولسه في دورات مربوطة فيه (إلا إذا حددنا كيف نتعامل مع هذه الحالة).
    *   **تنظيم أفضل:** قاعدة البيانات تصير أكثر تنظيمًا ومنطقية وتعكس الواقع بشكل أفضل.
    *   **استعلامات أقوى:** نقدر نجيب معلومات مركبة بسهولة (مثل: "أعطني أسماء كل الطلاب المسجلين في دورة PHP مع أسماء مدرسيهم").

3.  **أنواع العلاقات (Types of Relationships) (25 دقيقة)**
    *   "في ثلاث أنواع رئيسية للعلاقات بين جدولين (Table A و Table B):"
    *   **أ. واحد لواحد (One-to-One / 1:1):**
        *   "كل سجل في Table A يرتبط بسجل **واحد فقط** في Table B، وكل سجل في Table B يرتبط بسجل **واحد فقط** في Table A."
        *   **مثال:** علاقة بين جدول `users` وجدول `user_profiles`. كل مستخدم (`user`) له ملف شخصي (`profile`) واحد فقط، وكل ملف شخصي يخص مستخدم واحد فقط.
        *   **كيف نطبقها؟** غالبًا نخلي المفتاح الأساسي (PK) لجدول `user_profiles` هو نفسه المفتاح الأساسي لجدول `users` ويكون أيضًا مفتاح خارجي (Foreign Key) يشير لـ `users`.
        *   (هذه العلاقة أقل شيوعًا من الأنواع الأخرى).
    *   **ب. واحد لكثير (One-to-Many / 1:N):**
        *   "كل سجل في Table A (الجدول 'الواحد') يمكن أن يرتبط **بأكثر من سجل** في Table B (الجدول 'الكثير'). لكن كل سجل في Table B يرتبط بسجل **واحد فقط** في Table A."
        *   **هذه العلاقة شائعة جدًا جدًا!**
        *   **أمثلة:**
            *   قسم واحد (`departments` - جدول الواحد) فيه **عدة** موظفين (`employees` - جدول الكثير). الموظف الواحد يتبع لقسم واحد فقط.
            *   مؤلف واحد (`authors` - جدول الواحد) يكتب **عدة** كتب (`books` - جدول الكثير). الكتاب الواحد له مؤلف واحد فقط (في هذا المثال المبسط).
            *   **مثالنا:** مدرس واحد (`instructors` - الواحد) يدرّس **عدة** دورات (`courses` - الكثير). الدورة الواحدة (في تصميمنا الحالي) يدرسها مدرس واحد فقط.
        *   **كيف نطبقها؟** في جدول "الكثير" (مثلاً `courses`)، نضيف عمود جديد ليكون **مفتاح خارجي (Foreign Key - FK)**. هذا العمود يخزن قيمة المفتاح الأساسي (PK) للجدول "الواحد" المرتبط به (مثلاً نخزن `instructor_id` في جدول `courses`).
    *   **ج. كثير لكثير (Many-to-Many / N:M):**
        *   "كل سجل في Table A يمكن أن يرتبط **بأكثر من سجل** في Table B، وكل سجل في Table B يمكن أن يرتبط **بأكثر من سجل** في Table A."
        *   **هذه العلاقة شائعة جدًا أيضًا.**
        *   **أمثلة:**
            *   طالب واحد (`students`) يمكن أن يسجل في **عدة** دورات (`courses`). والدورة الواحدة (`courses`) يمكن أن يسجل فيها **عدة** طلاب (`students`).
            *   منتج واحد (`products`) يمكن أن يكون ضمن **عدة** طلبات (`orders`). والطلب الواحد (`orders`) يمكن أن يحتوي على **عدة** منتجات (`products`).
        *   **كيف نطبقها؟** ما نقدر نسويها مباشرة بين الجدولين. نحتاج **جدول وسيط (Junction Table / Pivot Table)**.
            *   هذا الجدول الوسيط (مثلاً نسميه `student_courses`) يحتوي على عمودين (على الأقل) هما مفاتيح خارجية (FKs): واحد يشير للمفتاح الأساسي لـ Table A (مثلاً `student_id`)، والثاني يشير للمفتاح الأساسي لـ Table B (مثلاً `course_id`).
            *   المفتاح الأساسي للجدول الوسيط غالبًا يكون مركب من العمودين (Composite Primary Key)، لضمان عدم تكرار نفس العلاقة (نفس الطالب ما يسجل بنفس الدورة مرتين).

4.  **المفتاح الخارجي (Foreign Key - FK) (20 دقيقة)**
    *   "المفتاح الخارجي هو **الأساس** لتطبيق العلاقات في قاعدة البيانات."
    *   "هو عمود (أو مجموعة أعمدة) في جدول، قيمته يجب أن تطابق قيمة المفتاح الأساسي (PK) لجدول آخر."
    *   **وظيفته:**
        *   يربط السجلات بين الجدولين.
        *   يفرض قيود التكامل المرجعي (Referential Integrity). يعني MySQL ما راح يسمح لك:
            *   تدخل قيمة في عمود الـ FK مش موجودة كـ PK في الجدول الأصلي (ما تقدر تضيف دورة لمدرس غير موجود).
            *   تحذف سجل من الجدول الأصلي (جدول المدرسين) ولسه في سجلات في الجدول الثاني (الدورات) تشير إليه (إلا إذا حددنا ماذا نفعل في هذه الحالة).
    *   **خيارات التعامل مع الحذف/التحديث (ON DELETE / ON UPDATE):**
        *   لما نعرف الـ FK في phpMyAdmin أو SQL، نقدر نحدد ايش يصير في الجدول اللي فيه الـ FK لو السجل المرتبط في الجدول الأصلي (اللي فيه الـ PK) انحذف أو تعدل الـ PK تبعه:
            *   `RESTRICT` (الافتراضي غالبًا): يمنع الحذف/التحديث في الجدول الأصلي طالما في سجلات مرتبطة. (آمن).
            *   `CASCADE`: لو حذفت مدرس، كل الدورات المرتبطة فيه تنحذف تلقائيًا! ولو عدلت ID المدرس، يتعدل تلقائيًا في كل الدورات المرتبطة. (قوي لكن خطير أحيانًا!).
            *   `SET NULL`: لو حذفت مدرس، عمود `instructor_id` في الدورات المرتبطة يصير `NULL` (طبعًا هذا يتطلب إن عمود الـ FK يسمح بقيم `NULL`).
            *   `NO ACTION`: مشابه لـ `RESTRICT`.

5.  **تطبيق علاقة (1:N) عمليًا (phpMyAdmin) (25 دقيقة)**
    *   **"الهدف:"** نربط جدول `courses` بجدول `instructors` (المدرس يدرس عدة دورات، الدورة يدرسها مدرس واحد).
    *   **"الخطوات في phpMyAdmin:"**
        1.  **التأكد من وجود PK:** تأكدوا إن جدول `instructors` فيه عمود مفتاح أساسي (PK) واضح (اللي هو `instructor_id`).
        2.  **إضافة عمود FK في جدول 'الكثير':**
            *   اختاروا قاعدة البيانات `university_db`.
            *   اختاروا جدول `courses`.
            *   روحوا لتبويب `Structure`.
            *   تحت قائمة الأعمدة، اضغطوا على "Add 1 column(s) After [آخر عمود موجود]" واضغطوا Go.
            *   **تعريف العمود الجديد:**
                *   `Name`: `instructor_id` (أو اسم مشابه، يفضل يكون نفس اسم الـ PK في الجدول الأصلي).
                *   `Type`: **لازم يكون نفس نوع بيانات الـ PK في جدول `instructors`** (اللي هو `INT`).
                *   `Null`: **هل ممكن دورة ما يكون لها مدرس؟** إذا نعم، اسمحوا بالـ `NULL`. إذا لا (كل دورة لازم يكون لها مدرس)، شيلوا الصح. خلينا نسمح بالـ `NULL` مبدئيًا.
                *   اضغطوا `Save`.
        3.  **إنشاء الفهرس (Index) للـ FK:**
            *   لسه وإحنا في تبويب `Structure` لجدول `courses`, انزلوا تحت شوية لجزئية "Indexes".
            *   اضغطوا على "Create an index" أو زر مشابه.
            *   في النافذة اللي تظهر:
                *   اسم الفهرس (Index Name): ممكن تسموه `fk_course_instructor` أو MySQL ممكن يقترحه.
                *   حددوا العمود اللي أضفناه (`instructor_id`).
                *   نوع الفهرس (Index Type): خلوه `INDEX`. (إنشاء فهرس على عمود الـ FK مهم جدًا لأداء الاستعلامات اللي تربط الجداول).
                *   اضغطوا `Go` أو `Save`.
        4.  **إنشاء العلاقة (Foreign Key Constraint):**
            *   بعد إنشاء الجدول وإضافة عمود الـ FK والفهرس، نروح نحدد العلاقة نفسها.
            *   اضغطوا على اسم جدول `courses` على اليسار.
            *   روحوا لتبويب `Structure`.
            *   فوق قائمة الأعمدة، في روابط نصية صغيرة، اضغطوا على `Relation view`.
            *   في شاشة 'Relation view':
                *   اضغطوا على "Add constraint" أو ما شابه.
                *   اسم القيد (Constraint name): MySQL يقترح اسم غالبًا (مثل `courses_ibfk_1`).
                *   **`ON DELETE`**: اختاروا السلوك المطلوب عند حذف مدرس (مثلاً `RESTRICT` أو `SET NULL` إذا سمحتم بالـ Null).
                *   **`ON UPDATE`**: اختاروا السلوك المطلوب عند تحديث ID مدرس (غالبًا `RESTRICT` أو `CASCADE`).
                *   **`Column`**: اختاروا عمود المفتاح الخارجي في الجدول الحالي (`courses`) اللي هو `instructor_id`.
                *   **`Database`**: اختاروا قاعدة البيانات الحالية (`university_db`).
                *   **`Table`**: اختاروا الجدول الأصلي (الجدول 'الواحد') اللي نبغى نربط معه (`instructors`).
                *   **`Column`**: اختاروا عمود المفتاح الأساسي (PK) في الجدول الأصلي (`instructor_id`).
                *   اضغطوا `Save`.
            *   **التحقق:** ارجعوا لـ `Structure` -> `Relation view`، المفروض تشوفوا العلاقة معرفة. وممكن تشوفوا خطوط تربط الجداول في تبويب 'Designer' في phpMyAdmin (إذا كان مفعل).

6.  **استخدام `JOIN` لجلب بيانات من جداول مرتبطة (10 دقائق - نظري)**
    *   "تمام، الآن الجداول مربوطة في تصميم قاعدة البيانات. كيف نستفيد من هذا الربط في استعلامات `SELECT` عشان نجيب بيانات من الجدولين سوا؟ باستخدام **`JOIN`**."
    *   "`JOIN` تسمح لنا ندمج صفوف من جدولين أو أكثر بناءً على عمود مرتبط بينهم (اللي هو غالبًا PK مع FK)."
    *   **`INNER JOIN` (الأشهر):**
        *   "تجيب فقط الصفوف اللي **لها مقابل** في كلا الجدولين بناءً على شرط الربط."
        *   **مثال:** لو نبغى نجيب اسم الدورة واسم المدرس اللي يدرسها.
            ```sql
            SELECT
                courses.course_name, -- Select course name from courses table
                instructors.instructor_name -- Select instructor name from instructors table
            FROM
                courses -- Start with the courses table
            INNER JOIN
                instructors -- Join it with the instructors table
            ON
                courses.instructor_id = instructors.instructor_id; -- The condition for joining (FK = PK)
            ```
        *   "هذا الاستعلام راح يجيب فقط الدورات اللي **لها** مدرس مرتبط بها في جدول المدرسين (لو فيه دورة الـ `instructor_id` تبعها `NULL` أو يشير لـ ID غير موجود، ما راح تظهر في نتيجة `INNER JOIN`)."
    *   **أنواع `JOIN` أخرى (للإشارة السريعة):**
        *   `LEFT JOIN`: يجيب **كل** الصفوف من الجدول الأيسر (اللي قبل `LEFT JOIN`)، والصفوف المطابقة من الجدول الأيمن. لو مافي تطابق في الجدول الأيمن، تكون أعمدة الجدول الأيمن `NULL`. (مثال: جيب لي كل الدورات، وحتى لو مالها مدرس، اظهرها بس خل اسم المدرس NULL).
        *   `RIGHT JOIN`: عكس `LEFT JOIN` (يجيب كل اللي في الجدول الأيمن).
        *   `FULL OUTER JOIN`: يجيب كل الصفوف من كلا الجدولين (غير مدعوم مباشرة في MySQL بنفس الاسم، له طرق محاكاة).

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن نطبق اللي تعلمناه عن العلاقات والـ JOINs."

1.  **التمرين 1: تطبيق علاقة One-to-Many (40 دقيقة)**
    *   **"المهمة:"** "كل واحد يطبق الخطوات اللي شرحناها لربط جدول `courses` بجدول `instructors` في قاعدة بياناته (`university_db` أو `[اسمكم]_db`) باستخدام phpMyAdmin."
    *   "تأكدوا إن جدول `instructors` موجود وفيه PK."
    *   "أضيفوا عمود `instructor_id` (نوعه `INT`) لجدول `courses`."
    *   "اجعلوا هذا العمود يسمح بـ `NULL` (عشان لو عندكم دورات قديمة ما تعرفوا مدرسها)."
    *   "أنشئوا فهرس (Index) على عمود `instructor_id`."
    *   "روحوا لـ `Relation view` في جدول `courses` وعرفوا الـ Foreign Key constraint:"
        *   العمود: `instructor_id` في `courses`.
        *   يشير إلى: جدول `instructors`، عمود `instructor_id`.
        *   `ON DELETE`: اختاروا `SET NULL` (إذا مدرس انحذف، الدورات اللي كان يدرسها يصير الـ instructor_id فيها NULL).
        *   `ON UPDATE`: اختاروا `CASCADE` (لو ID المدرس تغير، يتغير تلقائيًا في جدول courses).
    *   **"تعبئة بيانات للتجربة:"**
        *   تأكدوا إن عندكم مدرسين في جدول `instructors` (أضيفوا كم واحد يدويًا إذا مافيه).
        *   روحوا لجدول `courses` (تبويب `Browse`). الآن لما تعدلوا أو تضيفوا دورة، المفروض تشوفوا قائمة منسدلة عند حقل `instructor_id` فيها أسماء المدرسين الموجودين (phpMyAdmin يسويها تلقائيًا بسبب الـ FK)!
        *   اربطوا بعض الدورات بمدرسين موجودين، وخلوا دورة أو اثنتين بدون مدرس (`instructor_id` يكون `NULL`).
    *   **(مساعدة الطلاب خطوة بخطوة و حل مشاكل تعريف الـ FK)**. مشاكل شائعة: عدم تطابق نوع البيانات بين PK و FK، محاولة ربط بعمود ليس PK أو Unique، أخطاء في اختيار الجداول والأعمدة في Relation view.

2.  **التمرين 2: كتابة استعلامات JOIN (30 دقيقة)**
    *   **"المهمة:"** "نروح لتبويب `SQL` في phpMyAdmin (بعد اختيار قاعدة البيانات `university_db`). جربوا تكتبوا وتنفذوا الاستعلامات التالية:"
    *   **استعلام 1 (INNER JOIN):** "اكتبوا استعلام يجلب اسم الدورة (`course_name`) واسم المدرس (`instructor_name`) **فقط للدورات اللي لها مدرس**."
        ```sql
        SELECT courses.course_name, instructors.instructor_name
        FROM courses
        INNER JOIN instructors ON courses.instructor_id = instructors.instructor_id;
        ```
    *   **استعلام 2 (LEFT JOIN):** "اكتبوا استعلام يجلب اسم **كل** الدورات، واسم المدرس المرتبط بها (إذا وجد). لو الدورة مالها مدرس، يظهر `NULL` في خانة اسم المدرس."
        ```sql
        SELECT courses.course_name, instructors.instructor_name
        FROM courses -- Table on the left
        LEFT JOIN instructors ON courses.instructor_id = instructors.instructor_id;
        ```
        *   "قارنوا نتيجة هذا الاستعلام بنتيجة الاستعلام الأول. لاحظوا كيف `LEFT JOIN` أظهرت الدورات اللي مالها مدرس."
    *   **استعلام 3 (مع WHERE):** "اكتبوا استعلام يجلب اسم الدورة واسم المدرس، فقط للدورات اللي يدرسها مدرس معين (مثلاً المدرس اللي الـ `instructor_id` تبعه هو 1)."
        ```sql
        SELECT courses.course_name, instructors.instructor_name
        FROM courses
        INNER JOIN instructors ON courses.instructor_id = instructors.instructor_id
        WHERE instructors.instructor_id = 1;
        -- Or alternatively: WHERE courses.instructor_id = 1; (Gives same result here)
        ```
    *   **(مساعدة الطلاب في كتابة الـ SQL وفهم النتائج)**

3.  **مناقشة تصميم علاقة Many-to-Many (15 دقيقة)**
    *   **"سيناريو:"** "تكلمنا عن علاقة الطلاب (`students`) والدورات (`courses`). قلنا إنها Many-to-Many (الطالب يسجل في عدة دورات، والدورة يسجل فيها عدة طلاب)."
    *   **"سؤال للنقاش:"** "كيف نطبق هذه العلاقة؟ إيش الجدول الإضافي اللي نحتاجه؟ وما هي الأعمدة اللي لازم تكون فيه؟ وما هو المفتاح الأساسي لهذا الجدول الوسيط؟"
    *   **(توجيه النقاش للوصول إلى تصميم الجدول الوسيط):**
        *   نحتاج جدول وسيط، نسميه مثلاً `student_enrollments`.
        *   الأعمدة الأساسية فيه:
            *   `student_id`: نوعه `INT`, وهو FK يشير لـ `students.id`.
            *   `course_id`: نوعه `INT`, وهو FK يشير لـ `courses.course_id`.
        *   المفتاح الأساسي (PK) لجدول `student_enrollments` يكون **مركب** من العمودين (`student_id`, `course_id`) معًا. هذا يضمن إن نفس الطالب ما يقدر يسجل في نفس الدورة أكثر من مرة.
        *   ممكن نضيف أعمدة إضافية لهذا الجدول إذا احتجنا، مثل `enrollment_date` (تاريخ التسجيل) أو `grade` (درجة الطالب في هذه الدورة).
    *   **(يمكن تطبيق إنشاء هذا الجدول الوسيط والعلاقات كجزء من التمرين أو تركه كتمرين منزلي).**

4.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1 (تطبيق Many-to-Many):** أنشئ الجدول الوسيط `student_enrollments` كما ناقشناه، مع الأعمدة `student_id` و `course_id` و `enrollment_date` (نوع `TIMESTAMP`, القيمة الافتراضية `CURRENT_TIMESTAMP`). اجعل (`student_id`, `course_id`) مفتاح أساسي مركب. عرّف المفاتيح الخارجية (FKs) التي تربط `student_id` بجدول `students` و `course_id` بجدول `courses`. (اختر `CASCADE` لـ `ON DELETE` لكلا العلاقتين، عشان لو انحذف طالب أو دورة، تسجيلاته المرتبطة تنحذف تلقائيًا).
    *   **تمرين 2 (JOIN مع 3 جداول):** اكتب استعلام SQL يجلب اسم الطالب (`students.name`), اسم الدورة (`courses.course_name`), واسم المدرس (`instructors.instructor_name`) لكل عملية تسجيل موجودة في جدول `student_enrollments`. (تلميح: ستحتاج لاستخدام `JOIN` مرتين لربط الجداول الثلاثة: `student_enrollments` مع `students`, و `student_enrollments` مع `courses`, و `courses` مع `instructors`).
    *   "اليوم تعلمنا مفهوم حيوي جدًا في قواعد البيانات وهو العلاقات وكيف نربط الجداول باستخدام المفاتيح الخارجية ونستعلم عنها باستخدام JOIN. هذا راح يخلي تطبيقاتنا أكثر قوة ومنطقية. في الحصة القادمة، راح ننتقل لموضوع مهم ثاني وهو كيفية التعامل مع رفع الملفات والصور من المستخدم وتخزينها. يعطيكم العافية!"

---

#### **الحصة 10 (الخميس): رفع الملفات والصور (File Upload)**

**🎯 أهداف الحصة:**

1.  فهم آلية عمل رفع الملفات في تطبيقات الويب (HTML Form, PHP Script).
2.  التعرف على السمات اللازمة في نموذج HTML لرفع الملفات (`enctype="multipart/form-data"`).
3.  استخدام المصفوفة العامة `$_FILES` في PHP للوصول إلى معلومات الملف المرفوع.
4.  فهم خطوات معالجة الملف المرفوع: التحقق من الأخطاء، الحجم، النوع، ونقل الملف لمجلد دائم.
5.  التعامل مع تحديات الأمان المتعلقة برفع الملفات (التحقق من النوع، تسمية الملفات).
6.  تخزين مسار الملف في قاعدة البيانات.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم! في كثير من التطبيقات، نحتاج نسمح للمستخدمين برفع ملفات، مثل صورة شخصية للبروفايل، أو ملفات مرفقة في رسالة، أو مستندات. اليوم راح نتعلم كيف نتعامل مع عملية **رفع الملفات (File Upload)** من نموذج HTML إلى خادم PHP، وكيف نخزنها بشكل آمن ومنظم."

1.  **مراجعة سريعة (5 دقائق)**
    *   "إيش هي أنواع العلاقات بين الجداول؟ (1:1, 1:N, N:M). وإيش هو المفتاح الخارجي؟ وكيف نجيب بيانات من جدولين مربوطين؟ (باستخدام JOIN). ممتاز!"

2.  **آلية رفع الملفات: نظرة عامة (10 دقائق)**
    *   "عملية رفع الملفات تتكون من جزئين رئيسيين:"
        *   **جزء العميل (Client-Side):** نموذج HTML خاص يسمح للمستخدم يختار ملف من جهازه.
        *   **جزء الخادم (Server-Side):** سكربت PHP يستقبل هذا الملف، يتحقق منه، وينقله من مكان مؤقت إلى مكان دائم على الخادم.
    *   "البيانات الثنائية للملف (File binary data) تُرسل في طلب HTTP (غالبًا POST) بطريقة خاصة."

3.  **تعديل نموذج HTML لرفع الملفات (15 دقيقة)**
    *   "عشان نخلي نموذج HTML يقدر يرفع ملفات، لازم نسوي تعديلين مهمين على وسم `<form>`:"
        1.  **`method="post"`:** رفع الملفات لازم يتم باستخدام طريقة `POST` (ما ينفع `GET`).
        2.  **`enctype="multipart/form-data"`:** ***هذه أهم خطوة!*** السمة `enctype` تحدد كيفية ترميز بيانات النموذج قبل إرسالها. القيمة الافتراضية هي `application/x-www-form-urlencoded` وهي مناسبة للنصوص العادية. لكن لرفع الملفات، لازم نغيرها إلى `multipart/form-data`. هذه الطريقة تقسم بيانات النموذج إلى أجزاء متعددة، جزء لكل حقل نصي وجزء خاص للملف المرفوع. بدون هذه السمة، الملف ما راح يوصل للخادم بشكل صحيح.
    *   "نحتاج أيضًا عنصر إدخال خاص لاختيار الملف:"
        *   **`<input type="file" name="profile_picture">`**: هذا العنصر راح يظهر للمستخدم كزر "Choose File" أو "Browse" يسمح له يختار ملف من جهازه. الـ `name` هنا مهم جدًا عشان نوصل للملف في PHP.
    *   **مثال لنموذج بسيط لرفع صورة مستخدم:** (ملف `upload_form.html`)
        ```html
        <!DOCTYPE html>
        <html lang="en">
        <head>
            <meta charset="UTF-8">
            <title>File Upload Form</title>
        </head>
        <body>
            <h1>Upload Your Profile Picture</h1>
            <form action="handle_upload.php" method="post" enctype="multipart/form-data">
                <div>
                    <label for="userId">User ID:</label>
                    <input type="text" id="userId" name="user_id" required>
                </div>
                <br>
                <div>
                    <label for="picture">Select image to upload:</label><br>
                    <input type="file" id="picture" name="profile_pic" accept="image/png, image/jpeg">
                    <!-- The 'accept' attribute suggests allowed file types to the browser,
                         but it's NOT a security measure. PHP MUST validate the type. -->
                </div>
                <br>
                <div>
                    <button type="submit">Upload Image</button>
                </div>
            </form>
        </body>
        </html>
        ```
        *   "ملاحظة: السمة `accept` في `<input type="file">` هي مجرد **تلميح للمتصفح** ليظهر للمستخدم أنواع الملفات المقترحة. **لا تعتمدوا عليها للأمان!** لازم نتحقق من نوع الملف بدقة في PHP."

4.  **استقبال الملف في PHP: المصفوفة `$_FILES` (20 دقيقة)**
    *   "لما المستخدم يرسل النموذج اللي فيه `enctype="multipart/form-data"`، PHP ما تحط معلومات الملف المرفوع في `$_POST`. بدلاً من ذلك، تستخدم مصفوفة سوبر جلوبال ثانية اسمها **`$_FILES`**."
    *   `$_FILES` هي مصفوفة ثنائية الأبعاد. المفتاح الأول هو **الاسم اللي حددناه في السمة `name`** لعنصر `<input type="file">` (في مثالنا هو `profile_pic`).
    *   لكل ملف مرفوع، `$_FILES` تخزن مصفوفة ترابطية فيها المعلومات التالية:"
        *   **`$_FILES['profile_pic']['name']`:** اسم الملف الأصلي كما هو على جهاز المستخدم (مثلاً `my_photo.jpg`). **(لا تثق بهذا الاسم للأمان!)**
        *   **`$_FILES['profile_pic']['type']`:** نوع MIME للملف كما أرسله المتصفح (مثلاً `image/jpeg`). **(لا تثق بهذا النوع للأمان! ممكن التلاعب به).**
        *   **`$_FILES['profile_pic']['size']`:** حجم الملف بالبايت (Bytes). (مفيد لتحديد حد أقصى للحجم).
        *   **`$_FILES['profile_pic']['tmp_name']`:** ***هذا أهم واحد!*** هو المسار **المؤقت** للملف بعد ما تم رفعه بنجاح إلى الخادم. الملف موجود في هذا المكان المؤقت لفترة قصيرة جدًا، ولازم ننقله لمكان دائم قبل ما السكربت ينتهي، وإلا راح ينحذف تلقائيًا.
        *   **`$_FILES['profile_pic']['error']`:** كود خطأ يوضح حالة الرفع. القيمة `0` (أو الثابت `UPLOAD_ERR_OK`) تعني أن الرفع تم بنجاح وبدون أخطاء. أي قيمة أخرى تدل على وجود مشكلة.

5.  **معالجة الملف المرفوع في PHP (35 دقيقة)**
    *   "الآن، في ملف `handle_upload.php`، لازم نسوي سلسلة من الخطوات لمعالجة الملف بشكل آمن:"
        1.  **التحقق من وجود الملف ومن عدم وجود أخطاء:**
            *   `isset($_FILES['profile_pic'])`: هل تم إرسال ملف بهذا الاسم؟
            *   `$_FILES['profile_pic']['error'] == UPLOAD_ERR_OK`: هل كود الخطأ هو 0 (نجاح)؟ (لازم نتحقق من الأكواد الأخرى مثل `UPLOAD_ERR_INI_SIZE`, `UPLOAD_ERR_FORM_SIZE` لمعرفة سبب الفشل إذا حصل).
        2.  **تحديد مجلد الوجهة (Destination Folder):**
            *   لازم نحدد مجلد على الخادم نبغى نحفظ فيه الملفات المرفوعة بشكل دائم.
            *   **مهم:** هذا المجلد لازم يكون **خارج** المجلد الجذر للويب (`htdocs` أو `www`) إذا أمكن، أو على الأقل نمنع التنفيذ المباشر للملفات فيه (باستخدام `.htaccess` مثلاً) لأسباب أمنية. لو كان لازم يكون داخل مجلد الويب، فليكن في مجلد فرعي (مثلاً `uploads/images/`).
            *   لازم نتأكد إن PHP عندها **صلاحية الكتابة (Write Permission)** على هذا المجلد.
            *   `$uploadDirectory = "uploads/";` (كمثال بسيط، يفترض وجود مجلد `uploads` بجانب ملف PHP).
        3.  **التحقق من حجم الملف (File Size Check):**
            *   نحدد حد أقصى للحجم المسموح به (بالبايت). مثلاً 2 ميجابايت = 2 * 1024 * 1024 بايت.
            *   `$maxFileSize = 2 * 1024 * 1024; // 2MB`
            *   `if ($_FILES['profile_pic']['size'] > $maxFileSize) { // Error: File too large }`
        4.  **التحقق من نوع الملف (File Type Check) - الطريقة الآمنة:**
            *   **لا تعتمد على `$_FILES['profile_pic']['type']`** اللي جاية من المتصفح.
            *   **لا تعتمد فقط على امتداد الملف** (`.jpg`, `.png`) اللي جاي من `$_FILES['profile_pic']['name']`. ممكن شخص يغير اسم ملف فيروس لـ `virus.jpg`!
            *   **الطريقة الأفضل:** نستخدم دوال PHP للتحقق من محتوى الملف الفعلي (MIME type). دالة `mime_content_type($_FILES['profile_pic']['tmp_name'])` أو مكتبة Fileinfo (`finfo_open`, `finfo_file`) هي الطرق الموثوقة.
            *   نحدد قائمة بأنواع MIME المسموح بها (Whitelist).
                ```php
                $allowedMimeTypes = ['image/jpeg', 'image/png', 'image/gif'];
                $fileMimeType = mime_content_type($_FILES['profile_pic']['tmp_name']);
                if (!in_array($fileMimeType, $allowedMimeTypes)) {
                    // Error: Invalid file type
                }
                ```
        5.  **إنشاء اسم ملف فريد وآمن (Secure & Unique Filename):**
            *   **لا تستخدم اسم الملف الأصلي (`$_FILES['...']['name']`) مباشرة!** لأنه ممكن يحتوي على حروف خطيرة (`/`, `..`), ممكن يكون اسم ملف موجود مسبقًا فيتم الكتابة فوقه, وممكن يكشف معلومات عن نظام المستخدم.
            *   **الطريقة الأفضل:** ننشئ اسم جديد وفريد للملف. طرق شائعة:
                *   نستخدم دالة `uniqid()` أو `bin2hex(random_bytes(16))` لتوليد سلسلة عشوائية فريدة.
                *   نضيف عليها امتداد الملف الأصلي (بعد التأكد من أنه آمن ومن ضمن الامتدادات المسموحة). نحصل على الامتداد باستخدام `pathinfo($_FILES['profile_pic']['name'], PATHINFO_EXTENSION)`.
                *   `$originalExtension = strtolower(pathinfo($_FILES['profile_pic']['name'], PATHINFO_EXTENSION));`
                *   `$safeFilename = uniqid('img_', true) . '.' . $originalExtension;`
        6.  **نقل الملف من المسار المؤقت إلى المسار الدائم:**
            *   نستخدم دالة `move_uploaded_file(temporary_path, permanent_path)`.
            *   `$destinationPath = $uploadDirectory . $safeFilename;`
            *   `if (move_uploaded_file($_FILES['profile_pic']['tmp_name'], $destinationPath)) { // Success! } else { // Error moving file }`
            *   `move_uploaded_file` تقوم أيضًا بالتحقق مرة أخرى أن الملف تم رفعه عبر HTTP POST، مما يضيف طبقة أمان.
        7.  **تخزين مسار الملف في قاعدة البيانات:**
            *   بعد التأكد من نجاح كل الخطوات السابقة ونقل الملف بنجاح، الآن نقدر نخزن **مسار الملف** (`$destinationPath` أو فقط `$safeFilename` إذا كنا نعرف المجلد الأساسي) في قاعدة البيانات، مثلاً في جدول `users` في عمود `profile_image_path`. **لا تخزن الملف الثنائي نفسه في قاعدة البيانات مباشرة (إلا في حالات نادرة جدًا)!**

    *   **(الكود سيكون طويلاً نوعًا ما، يمكن كتابة الخطوات الأساسية وترك التفاصيل للتطبيق العملي)**

6.  **مراجعة وختام الجزء الأول (5 دقائق)**
    *   "إذًا، رفع الملفات يتطلب تعديل الفورم (`enctype`), استخدام `$_FILES` في PHP, والقيام بسلسلة من التحققات الهامة (خطأ, حجم, نوع آمن), إنشاء اسم آمن, وأخيرًا نقل الملف باستخدام `move_uploaded_file`. ثم نخزن المسار في الداتابيز."
    *   "ليش التحقق من النوع والاسم مهم جدًا للأمان؟ (لمنع رفع ملفات خبيثة أو الكتابة فوق ملفات النظام)."
    *   "أي أسئلة؟"

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن نطبق خطوات رفع الملفات عمليًا ونواجه التحديات."

1.  **إعداد البيئة (10 دقائق)**
    *   **"المهمة:"**
        *   أنشئوا مجلد جديد داخل مجلد المشروع (بجانب ملفات PHP) اسمه `uploads`.
        *   **تحققوا من صلاحيات الكتابة:** تأكدوا أن خادم الويب (PHP) لديه صلاحية للكتابة داخل مجلد `uploads`. في ويندوز، غالبًا تكون الصلاحيات متاحة افتراضيًا مع XAMPP. في Linux/Mac، قد تحتاجون لتغيير أذونات المجلد باستخدام `chmod 775 uploads` أو `chmod 777 uploads` (للبيئة المحلية فقط، 777 غير آمن للإنتاج) والتأكد من أن مستخدم الويب (مثل `www-data` أو `apache`) هو مالك المجلد أو ضمن المجموعة المالكة.
        *   انسخوا كود نموذج HTML (`upload_form.html`) اللي كتبناه.

2.  **التمرين 1: كتابة سكربت المعالجة الأساسي (40 دقيقة)**
    *   **"المهمة:"** "أنشئوا ملف `handle_upload.php` وطبقوا الخطوات الأساسية:"
        1.  تحققوا إن الطلب `POST` وإن الملف تم إرساله (`isset($_FILES['profile_pic'])`).
        2.  اطبعوا محتويات `$_FILES['profile_pic']` باستخدام `print_r()` أو `var_dump()` عشان تشوفوا المعلومات اللي توصلكم.
        3.  تحققوا من قيمة `$_FILES['profile_pic']['error']`. إذا كانت لا تساوي `UPLOAD_ERR_OK` (أو 0)، اطبعوا رسالة خطأ مناسبة بناءً على كود الخطأ (ابحثوا عن معاني أكواد `UPLOAD_ERR_*` في توثيق PHP).
        4.  إذا الخطأ `0`، حددوا مجلد الرفع `$uploadDirectory = "uploads/";`.
        5.  حددوا اسمًا بسيطًا للملف الوجهة مبدئيًا (لاحقًا بنسويه فريد). مثلاً، نأخذ اسم الملف الأصلي ونحاول تنظيفه قليلًا:
            ```php
            $originalName = basename($_FILES["profile_pic"]["name"]); // basename() removes directory paths
            $destination = $uploadDirectory . $originalName;
            ```
        6.  استخدموا `move_uploaded_file($_FILES['profile_pic']['tmp_name'], $destination)`.
        7.  تحققوا من نتيجة `move_uploaded_file`. إذا `true`، اطبعوا "تم رفع الملف بنجاح باسم: " واسم الملف. إذا `false`، اطبعوا "حدث خطأ أثناء نقل الملف."
    *   **"الاختبار:"**
        *   جربوا رفع ملف صورة صغير من النموذج `upload_form.html`.
        *   شوفوا الرسالة اللي تظهر في `handle_upload.php`.
        *   روحوا لمجلد `uploads` وتأكدوا إن الملف وصل هناك.
        *   جربوا رفع ملف كبير جدًا (أكبر من الحد المسموح في `php.ini` - ابحثوا عن `upload_max_filesize` و `post_max_size` في `phpinfo()` لمعرفة الحدود الحالية) وشوفوا كود الخطأ اللي يظهر.
    *   **(مساعدة الطلاب في التعامل مع الأخطاء ومشاكل الصلاحيات)**

3.  **التمرين 2: إضافة التحققات (حجم ونوع) وإنشاء اسم فريد (30 دقيقة)**
    *   **"المهمة:"** "نعدل على `handle_upload.php` ليصبح أكثر أمانًا:"
        1.  **قبل** `move_uploaded_file`، أضيفوا تحقق من حجم الملف (`$_FILES['profile_pic']['size']`) مقابل حد أقصى (مثلاً 1MB = 1048576 bytes). إذا تجاوز الحد، اطبعوا خطأ و `exit()`.
        2.  أضيفوا تحقق من نوع MIME باستخدام `mime_content_type()` أو `finfo_file()`. عرّفوا مصفوفة بأنواع MIME المسموحة (مثل `['image/jpeg', 'image/png']`). إذا النوع غير مسموح، اطبعوا خطأ و `exit()`.
        3.  بدل استخدام الاسم الأصلي، أنشئوا اسم ملف فريد باستخدام `uniqid()` أو `bin2hex(random_bytes())` + الامتداد الأصلي (بعد التأكد أن الامتداد مسموح به أيضًا `['jpg', 'jpeg', 'png']`).
            ```php
            $extension = strtolower(pathinfo($originalName, PATHINFO_EXTENSION));
            $allowedExtensions = ['jpg', 'jpeg', 'png'];
            if (!in_array($extension, $allowedExtensions)) {
                die("Error: Invalid file extension.");
            }
            $safeFilename = uniqid('user_pic_', true) . '.' . $extension;
            $destination = $uploadDirectory . $safeFilename;
            ```
        4.  عدلوا `move_uploaded_file` ليستخدم `$destination` الجديد.
        5.  إذا نجح الرفع، اطبعوا الاسم الآمن للملف (`$safeFilename`). هذا الاسم هو اللي راح نخزنه في الداتابيز لاحقًا.
    *   **"الاختبار:"** جربوا رفع ملفات بصيغ مسموحة وغير مسموحة، وبأحجام مختلفة. تأكدوا إن الملفات المرفوعة بنجاح تأخذ أسماء فريدة في مجلد `uploads`.

4.  **نقاش تخزين المسار في قاعدة البيانات (5 دقائق)**
    *   "الآن بعد ما صار عندنا اسم الملف الآمن (`$safeFilename`) وتم نقله بنجاح، كيف نربطه بالمستخدم في قاعدة البيانات؟"
    *   (الجواب: نحتاج عمود جديد في جدول `users` أو `students`، مثلاً `profile_image_path` من نوع `VARCHAR`. بعد نجاح الرفع، ننفذ جملة `UPDATE` على جدول المستخدمين لتحديث هذا الحقل بقيمة `$safeFilename` للمستخدم صاحب الـ ID اللي وصل مع الفورم).
    *   "ليش نخزن فقط اسم الملف وليس المسار الكامل (`uploads/xxxx.jpg`)؟" (الجواب: أكثر مرونة لو غيرنا مكان مجلد الـ uploads في المستقبل، فقط نغير المسار الأساسي في مكان واحد في إعدادات التطبيق).

5.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1:** أكملوا التمرين بربط عملية الرفع بقاعدة البيانات. عدلوا نموذج `upload_form.html` ليرسل `user_id`. في `handle_upload.php`، بعد نجاح الرفع، اتصلوا بقاعدة البيانات ونفذوا `UPDATE users SET profile_image_path = '$safeFilename' WHERE id = $user_id;`. (تأكدوا من تأمين `$user_id` و `$safeFilename` قبل استخدامهم في الـ SQL - استخدموا Prepared Statements إذا كنتم تعرفونها، أو على الأقل `(int)` للـ ID و `mysqli_real_escape_string` لاسم الملف).
    *   **تمرين 2 (بحث):** ابحثوا عن كيفية التعامل مع رفع **عدة ملفات** مرة واحدة من نفس النموذج (HTML form multiple file input, PHP handling multiple files `$_FILES`).
    *   "رائع! اليوم غطينا موضوع حيوي ومهم جدًا وهو رفع الملفات. تذكروا دائمًا أن الأمان (التحقق من النوع والحجم وإنشاء اسم فريد) هو الأولوية القصوى في هذه العملية. في الحصة القادمة، راح نتكلم عن كيفية التعامل مع الأخطاء بشكل احترافي في تطبيقاتنا. يعطيكم العافية!"

---
بالتأكيد، سنواصل تفصيل البرنامج التدريبي للأسبوع السادس من الشهر الثاني.

---

### **الشهر الثاني: تطوير API متقدم (تابع)**

---

#### **الأسبوع السادس**

---

#### **الحصة 11 (الاثنين): معالجة الأخطاء (Error Handling)**

**🎯 أهداف الحصة:**

1.  فهم أهمية معالجة الأخطاء بشكل احترافي في تطبيقات الويب و APIs.
2.  التعرف على أنواع الأخطاء المختلفة في PHP (Notices, Warnings, Fatal Errors).
3.  استخدام آليات PHP لمعالجة الأخطاء: `die()` و `exit()`, `@` operator, `error_reporting()`, `set_error_handler()`.
4.  مقدمة لمفهوم الاستثناءات (Exceptions) واستخدام `try...catch` blocks.
5.  تطبيق معالجة الأخطاء في سياق API (إرجاع استجابات JSON مناسبة للأخطاء مع أكواد حالة HTTP صحيحة).

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم مرة أخرى! حتى الآن، بنينا أجزاء مختلفة من التطبيق: اتصال بالداتابيز، CRUD، رفع ملفات، بناء API بسيط. لكن ماذا يحدث لو فشل الاتصال بالداتابيز؟ أو لو المستخدم أدخل بيانات خاطئة؟ أو لو حاولنا القسمة على صفر؟ حتى الآن، كنا نستخدم `echo` لطباعة خطأ بسيط أو `die()` لإيقاف كل شيء. هذا ليس احترافيًا، خصوصًا في API. اليوم، سنتعلم كيف **نعالج الأخطاء (Error Handling)** بشكل منظم وفعال."

1.  **مراجعة سريعة (5 دقائق)**
    *   "كيف كنا نتعامل مع رفع الملفات؟ إيش أهم خطوة في الفورم؟ (`enctype`). وإيش المصفوفة اللي نستخدمها في PHP؟ (`$_FILES`). وإيش أهم التحققات الأمنية؟ (الحجم، النوع الآمن، الاسم الفريد). ممتاز!"

2.  **لماذا معالجة الأخطاء مهمة؟ (10 دقائق)**
    *   **تجربة المستخدم (User Experience):** عرض رسائل خطأ PHP التقنية للمستخدم النهائي سيء ومربك. يجب أن نظهر رسائل واضحة ومفيدة (أو لا نظهر شيئًا ونحاول حل المشكلة خلف الكواليس).
    *   **الأمان (Security):** رسائل الخطأ الافتراضية قد تكشف معلومات حساسة عن بنية النظام، مسارات الملفات، أو استعلامات قاعدة البيانات، مما قد يساعد المخترقين.
    *   **التصحيح والتطوير (Debugging & Development):** أثناء التطوير، نحتاج لرؤية الأخطاء بوضوح لتصحيحها. لكن في بيئة الإنتاج (Live environment)، يجب إخفاء التفاصيل التقنية وتسجيلها في ملفات سجلات (Logs) ليراجعها المطورون لاحقًا.
    *   **موثوقية التطبيق (Reliability):** التطبيق الجيد يجب أن يتوقع الأخطاء ويتعامل معها برشاقة بدلًا من الانهيار الكامل.
    *   **في الـ APIs:** يجب أن يرجع الـ API استجابة خطأ واضحة (غالبًا JSON) مع كود حالة HTTP مناسب (4xx للأخطاء من طرف العميل، 5xx لأخطاء الخادم) حتى يتمكن التطبيق المستدعي (Front-end) من فهم المشكلة والتصرف بناءً عليها.

3.  **أنواع الأخطاء في PHP (15 دقيقة)**
    *   "PHP لديها مستويات مختلفة من الأخطاء، أهمها:"
        *   **Notice (ملاحظة):** أخطاء غير حرجة، غالبًا تشير لشيء محتمل الخطأ لكنه لا يوقف تنفيذ السكربت. مثال: محاولة استخدام متغير غير معرف (`Undefined variable`). في بيئة التطوير، من الجيد رؤيتها وإصلاحها. في الإنتاج، غالبًا تُخفى.
        *   **Warning (تحذير):** أخطاء أكثر جدية من Notices، لكنها أيضًا لا توقف تنفيذ السكربت افتراضيًا. مثال: محاولة تضمين ملف غير موجود (`include 'nonexistent_file.php'`). يجب إصلاحها.
        *   **Fatal Error (خطأ فادح):** هذا خطأ حرج يوقف تنفيذ السكربت فورًا. مثال: استدعاء دالة غير معرفة (`call_undefined_function()`), أو خطأ في بناء الجملة (Parse Error / Syntax Error) يمنع PHP من فهم الكود أصلاً.
        *   **Deprecated (مهمل):** تحذير يشير إلى استخدام ميزة أو دالة أصبحت قديمة وسيتم إزالتها في إصدارات PHP المستقبلية. يجب تحديث الكود لتجنب المشاكل لاحقًا.
    *   "مستوى عرض الأخطاء يمكن التحكم به من خلال إعدادات `php.ini` (خاصة `error_reporting` و `display_errors`). في التطوير، نريد رؤية كل شيء. في الإنتاج، نريد إخفاء الأخطاء عن المستخدم وتسجيلها."

4.  **آليات معالجة الأخطاء التقليدية في PHP (20 دقيقة)**
    *   **`die()` و `exit()`:** كما رأينا، توقفان تنفيذ السكربت فورًا وتطبعان رسالة. بسيطة لكن غير مرنة.
    *   **مُعامل التحكم بالخطأ `@` (Error Control Operator):**
        *   عند وضعه قبل تعبير معين (مثل استدعاء دالة)، فإنه يكبت (Suppress) أي رسائل خطأ (Notice, Warning) قد تنتج عن هذا التعبير.
        *   **مثال:** `$value = @$_GET['maybe_missing'];` (هنا لن يظهر Notice إذا كان `maybe_missing` غير موجود).
        *   **تحذير:** استخدام `@` بكثرة يعتبر **ممارسة سيئة جدًا** لأنه يخفي المشاكل بدلًا من معالجتها! يجب استخدامه بحذر شديد جدًا وفي حالات محددة جدًا (ونادرًا ما تكون مبررة). الأفضل دائمًا استخدام `isset()` أو طرق التحقق الأخرى.
    *   **`error_reporting(level)`:** دالة تسمح لك بتحديد مستوى الأخطاء التي تريد الإبلاغ عنها **أثناء تشغيل السكربت**. مفيدة لضبط مستوى الإبلاغ بشكل مختلف عن إعدادات `php.ini`. (مثلاً `error_reporting(E_ALL);` لعرض كل شيء، `error_reporting(0);` لإخفاء كل شيء).
    *   **`set_error_handler("myErrorHandlerFunction")`:**
        *   هذه دالة قوية تسمح لك بتحديد **دالة خاصة بك** ليتم استدعاؤها تلقائيًا عندما يحدث خطأ (Notice, Warning, Deprecated...).
        *   دالتك المخصصة تتلقى معلومات عن الخطأ (الكود، الرسالة، الملف، السطر) ويمكنك أن تقرر ماذا تفعل بها (تسجيلها في ملف، عرض رسالة مخصصة، أو حتى تحويلها إلى استثناء Exception).
        *   تسمح بتحكم دقيق جدًا في كيفية التعامل مع الأخطاء التقليدية.

5.  **مقدمة للاستثناءات (Exceptions) و `try...catch` (25 دقيقة)**
    *   "الطريقة الحديثة والأكثر شيوعًا لمعالجة الأخطاء المتوقعة في البرمجة الكائنية (OOP) والعديد من مكتبات PHP الحديثة هي **الاستثناءات (Exceptions)**."
    *   **فكرة الاستثناء:** بدلًا من أن الدالة ترجع قيمة خطأ (مثل `false`) أو تسبب Warning/Fatal Error، فإنها **"ترمي" (Throw)** كائن استثناء (Exception object) عندما يحدث خطأ غير متوقع أو حالة استثنائية لا يمكنها التعامل معها بنفسها.
    *   كائن الاستثناء يحمل معلومات عن الخطأ (رسالة، كود، مكان حدوثه...).
    *   **`try...catch` Block:**
        *   هو البنية التي نستخدمها للتعامل مع الكود الذي **قد** يرمي استثناء.
        *   **`try { ... }`:** نضع الكود "المشكوك فيه" (الذي قد يرمي استثناء) داخل بلوك `try`.
        *   **`catch (ExceptionType $e) { ... }`:** إذا رمى أي كود داخل `try` استثناءً من النوع المحدد (`ExceptionType` أو أي كلاس يرث منه)، يتوقف التنفيذ داخل `try` فورًا وينتقل التحكم إلى أول بلوك `catch` مطابق.
        *   داخل `catch`, يمكننا الوصول لكائن الاستثناء (المتغير `$e` في المثال) للحصول على تفاصيل الخطأ (`$e->getMessage()`, `$e->getCode()`, `$e->getFile()`, `$e->getLine()`, `$e->getTraceAsString()`) واتخاذ الإجراء المناسب (تسجيل الخطأ، عرض رسالة للمستخدم...).
        *   يمكن أن يكون لدينا أكثر من بلوك `catch` للتعامل مع أنواع مختلفة من الاستثناءات بشكل مختلف.
        *   **`finally { ... }` (اختياري):** الكود داخل `finally` يتم تنفيذه **دائمًا**، سواء حدث استثناء أم لا، وسواء تم الإمساك به (`catch`) أم لا. مفيد لتنظيف الموارد (مثل إغلاق اتصال قاعدة بيانات أو ملف مفتوح) بغض النظر عن النتيجة.
    *   **مثال بسيط:**
        ```php
        <?php
        function divide($a, $b) {
            if ($b == 0) {
                // Throw an exception instead of causing a warning/error
                throw new Exception("Division by zero is not allowed!");
            }
            return $a / $b;
        }

        try {
            echo "Attempting to divide 10 by 2...<br>";
            $result1 = divide(10, 2);
            echo "Result 1: " . $result1 . "<br>"; // This will execute

            echo "Attempting to divide 10 by 0...<br>";
            $result2 = divide(10, 0); // This will throw an exception
            echo "Result 2: " . $result2 . "<br>"; // This line will NOT be reached

        } catch (Exception $e) {
            // Catch the exception thrown by divide()
            echo "<p style='color:red;'>Caught Exception!</p>";
            echo "Error Message: " . $e->getMessage() . "<br>";
            // Log the detailed error for developers:
            // error_log("Error in " . $e->getFile() . " on line " . $e->getLine() . ": " . $e->getMessage());
        } finally {
            echo "<hr>This finally block always executes.<br>";
        }

        echo "Script continues after try-catch block.<br>";
        ?>
        ```
    *   "العديد من مكتبات PHP الحديثة (مثل PDO للتعامل مع قواعد البيانات) تستخدم Exceptions بشكل افتراضي للإبلاغ عن الأخطاء، مما يجعل `try...catch` ضروريًا عند استخدامها."

6.  **معالجة الأخطاء في سياق API (10 دقائق)**
    *   "عندما يحدث خطأ في سكربت API (سواء كان خطأ PHP تقليدي تم تحويله باستخدام `set_error_handler`, أو Exception تم رميها والإمساك بها بـ `catch`), **لا يجب** أن نطبع رسالة الخطأ التقنية مباشرة!"
    *   **"ماذا نفعل بدلاً من ذلك؟"**
        1.  **تسجيل الخطأ التفصيلي:** نسجل كل تفاصيل الخطأ (الرسالة، الملف، السطر، التتبع Trace) في ملف سجلات (Log file) على الخادم ليراه المطورون لاحقًا. نستخدم دالة `error_log()`.
        2.  **تحديد كود حالة HTTP مناسب:**
            *   `4xx` لأخطاء العميل (مثل `400 Bad Request` إذا كانت البيانات المرسلة ناقصة أو خاطئة، `401 Unauthorized` للمصادقة الفاشلة، `403 Forbidden` لعدم وجود صلاحية، `404 Not Found` للمورد غير الموجود).
            *   `5xx` لأخطاء الخادم (مثل `500 Internal Server Error` لأي خطأ غير متوقع في الكود أو فشل الاتصال بالداتابيز، `503 Service Unavailable`). نستخدم `http_response_code(code);`.
        3.  **إرجاع استجابة JSON بسيطة وواضحة:**
            *   نضبط الـ `Content-Type: application/json`.
            *   نرسل كائن JSON يحتوي على رسالة خطأ عامة ومناسبة للمستخدم (أو للتطبيق الأمامي)، ولا تكشف تفاصيل داخلية.
                ```json
                // Example for 400 Bad Request
                {
                  "status": "error",
                  "message": "Invalid input data provided. Please check required fields."
                }

                // Example for 500 Internal Server Error
                {
                  "status": "error",
                  "message": "An unexpected error occurred on the server. Please try again later."
                }
                ```
        4.  **إنهاء التنفيذ:** `exit();`

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن نطبق مفاهيم معالجة الأخطاء والاستثناءات على الكود اللي كتبناه سابقًا."

1.  **التمرين 1: تجربة مستويات الأخطاء (15 دقيقة)**
    *   **"المهمة:"** "أنشئوا ملف PHP جديد (`error_levels.php`)."
    *   "في بداية الملف، استخدموا `error_reporting(E_ALL);` و `ini_set('display_errors', 1);` للتأكد من رؤية كل الأخطاء (هذا هو الافتراضي غالبًا في XAMPP للتطوير)."
    *   "جربوا كتابة أكواد تسبب أنواع مختلفة من الأخطاء ولاحظوا ما يظهر:"
        *   `echo $undefined_variable;` (Notice)
        *   `include 'non_existent_file.php';` (Warning)
        *   `my_undefined_function();` (Fatal Error)
        *   `$result = 10 / 0;` (Warning: Division by zero)
    *   "الآن، جربوا إضافة `@` قبل `echo $undefined_variable;`. هل اختفت الـ Notice؟"
    *   "جربوا تغيير `error_reporting(0);` في بداية الملف. هل اختفت كل الأخطاء والتحذيرات (ما عدا Fatal)؟"
    *   "هذا التمرين فقط لتتعرفوا على شكل الأخطاء وكيفية التحكم في عرضها مبدئيًا."

2.  **التمرين 2: استخدام `try...catch` لمعالجة خطأ رياضي (25 دقيقة)**
    *   **"المهمة:"** "انسخوا دالة `divide()` اللي استخدمناها في المثال النظري (واللي ترمي Exception عند القسمة على صفر) وضعوها في ملف جديد (`exception_test.php`)."
    *   "استخدموا بلوك `try...catch (Exception $e)` لاستدعاء الدالة:"
        *   مرة بقيم صالحة (مثل `divide(20, 4)`).
        *   ومرة بقيم تسبب الخطأ (مثل `divide(20, 0)`).
    *   "داخل بلوك `catch`:"
        *   اطبعوا رسالة خطأ واضحة للمستخدم (مثلاً: "حدث خطأ: لا يمكن القسمة على صفر.").
        *   اطبعوا رسالة الخطأ التفصيلية من الاستثناء (`$e->getMessage()`).
        *   (اختياري متقدم) استخدموا `error_log("Division Error: " . $e->getMessage());` لتسجيل الخطأ (سيتم تسجيله في ملف سجل أخطاء Apache/PHP المحدد في `php.ini`).
    *   "لاحظوا كيف أن السكربت يكمل تنفيذه بعد بلوك `catch`."

3.  **التمرين 3: تطبيق `try...catch` على اتصال قاعدة البيانات (30 دقيقة)**
    *   **"المهمة:"** "نعدل كود الاتصال بقاعدة البيانات (مثلاً في بداية `api_get_students.php` أو ملف اتصال منفصل) ليستخدم `try...catch` ويكون مناسبًا للـ API."
    *   **"ملاحظة:** مكتبة MySQLi يمكن أن ترمي استثناءات إذا قمنا بتفعيل وضع الإبلاغ عن الأخطاء كاستثناءات. نفعل ذلك بإضافة السطر التالي **قبل** `mysqli_connect()`:"
        ```php
        mysqli_report(MYSQLI_REPORT_ERROR | MYSQLI_REPORT_STRICT);
        ```
    *   **"الآن، نضع كود الاتصال داخل `try`:"**
        ```php
        mysqli_report(MYSQLI_REPORT_ERROR | MYSQLI_REPORT_STRICT); // Enable exceptions for mysqli

        try {
            $conn = mysqli_connect($servername, $username, $password, $dbname);
            mysqli_set_charset($conn, "utf8mb4");
            // If connection succeeds, maybe log success or continue
            // echo "DB Connected Successfully via try-catch!<br>"; // For testing only

        } catch (mysqli_sql_exception $e) {
            // Catch specific mysqli exceptions
            error_log("Database Connection Error: " . $e->getMessage() . " (Code: " . $e->getCode() . ")"); // Log detailed error

            // Prepare JSON error response for the API user
            header('Content-Type: application/json; charset=utf-8');
            http_response_code(500); // Internal Server Error (database is server-side)
            echo json_encode([
                'status' => 'error',
                'message' => 'Database connection failed. Please try again later.'
                // Avoid echoing $e->getMessage() directly to the API user
            ]);
            exit(); // Stop script execution
        }
        // If script reaches here, $conn is valid and connected.
        ```
    *   **"الاختبار:"**
        *   جربوا الكود بمعلومات اتصال صحيحة (المفروض ما يظهر خطأ JSON).
        *   جربوا تغيير اسم قاعدة البيانات (`$dbname`) إلى اسم خاطئ. شغلوا الـ API من Postman. المفروض تشوفوا استجابة JSON للخطأ 500 ورسالة الخطأ العامة، والخطأ التفصيلي تم تسجيله (تحتاجون لمعرفة مكان ملف سجل الأخطاء لمراجعته).

4.  **مناقشة تطبيق معالجة الأخطاء على سيناريوهات أخرى (15 دقيقة)**
    *   **"سيناريو 1: `api_get_student_details.php?id=...`"**
        *   "كيف نعالج خطأ لو الـ `id` لم يتم إرساله في الـ URL؟" (الجواب: نتحقق بـ `isset($_GET['id'])`. إذا `false`، نرجع خطأ 400 Bad Request مع رسالة JSON واضحة).
        *   "كيف نعالج خطأ لو الـ `id` تم إرساله لكن الطالب غير موجود في الداتابيز؟" (الجواب: بعد تنفيذ الاستعلام، نتحقق من النتيجة. إذا لا يوجد صفوف، نرجع خطأ 404 Not Found مع رسالة JSON).
    *   **"سيناريو 2: `handle_upload.php`"**
        *   "كيف نرجع أخطاء رفع الملفات (ملف كبير جدًا، نوع غير صالح) كاستجابة JSON مناسبة للـ API بدلًا من مجرد `die()`؟" (الجواب: بدل `die()`, نضع `header()`, `http_response_code()`, `echo json_encode()`, و `exit()`).
    *   **"القاعدة العامة للـ APIs:"** "أي خطأ يحدث، سواء من المدخلات، أو من العمليات الداخلية، أو من الاتصال بالداتابيز، يجب أن يتم الإمساك به، تسجيله تفصيليًا للمطور، وإرجاع استجابة JSON مناسبة + كود حالة HTTP صحيح للتطبيق المستدعي."

5.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1:** عدلوا ملف `handle_upload.php` ليستخدم معالجة الأخطاء المناسبة للـ API. لكل حالة خطأ (ملف كبير، نوع خطأ، فشل النقل)، أرجعوا استجابة JSON مختلفة مع كود حالة مناسب (مثل 400 أو 413 للحجم، 415 للنوع، 500 لفشل النقل).
    *   **تمرين 2 (بحث):** ابحثوا عن مفهوم "Global Exception Handler" في PHP باستخدام `set_exception_handler()`. كيف يمكن استخدامه للإمساك بأي استثناء لم يتم الإمساك به في `try...catch` وتوفير معالجة أخطاء افتراضية للتطبيق كله؟
    *   "اليوم تعلمنا كيف نجعل تطبيقاتنا و APIs أكثر احترافية وموثوقية من خلال معالجة الأخطاء بشكل صحيح واستخدام الاستثناءات. هذا أساسي لتطوير برمجيات قوية. في الحصة القادمة، سنتحدث عن كيفية تحسين أداء الـ APIs التي نبنيها. يعطيكم العافية!"

---

#### **الحصة 12 (الخميس): تحسين أداء API (Performance Optimization)**

**🎯 أهداف الحصة:**

1.  فهم أهمية أداء الـ API وتأثيره على تجربة المستخدم.
2.  التعرف على عنق الزجاجة (Bottlenecks) الشائعة في أداء الـ API (قاعدة البيانات، الكود، الشبكة).
3.  تقنيات تحسين استعلامات قاعدة البيانات (Indexing, Selecting specific columns, Limiting results).
4.  مقدمة لمفهوم التخزين المؤقت (Caching) وأنواعه (Server-side, Client-side, Database caching).
5.  تقنيات تحسين كود PHP (تجنب العمليات المكلفة داخل الحلقات، استخدام الدوال المدمجة بكفاءة).
6.  أهمية ضغط الاستجابة (Gzip).

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"مساء الخير! لقد قمنا ببناء APIs وتأمينها وتعلمنا معالجة الأخطاء. الآن، لنجعلها ليست فقط تعمل، بل تعمل **بسرعة وكفاءة**! أداء الـ API (سرعة استجابته) مهم جدًا لتجربة المستخدم، خصوصًا في تطبيقات الموبايل. لا أحد يحب الانتظار طويلاً لتحميل البيانات. اليوم، سنتحدث عن **تحسين أداء الـ API (Performance Optimization)**."

1.  **مراجعة سريعة (5 دقائق)**
    *   "كيف كنا نعالج الأخطاء في الـ API بشكل احترافي؟ (تسجيل تفصيلي للمطور، إرجاع JSON + كود HTTP مناسب للمستخدم). وما هي آلية `try...catch`؟ (للإمساك بالاستثناءات). ممتاز!"

2.  **لماذا أداء الـ API مهم؟ (10 دقائق)**
    *   **تجربة المستخدم (UX):** الاستجابات البطيئة تؤدي إلى إحباط المستخدم وقد تجعله يتخلى عن التطبيق.
    *   **استهلاك الموارد:** الـ APIs البطيئة تستهلك موارد الخادم (CPU, Memory) لوقت أطول، مما قد يؤدي لارتفاع التكاليف أو عدم قدرة الخادم على خدمة عدد كبير من المستخدمين.
    *   **قابلية التوسع (Scalability):** الـ API ذو الأداء الجيد يمكنه التعامل مع عدد أكبر من الطلبات المتزامنة بكفاءة.
    *   **تأثير على الـ SEO (لمواقع الويب):** سرعة تحميل الصفحة (التي قد تعتمد على سرعة الـ API) هي عامل مهم في ترتيب محركات البحث.

3.  **تحديد عنق الزجاجة (Bottlenecks) (15 دقيقة)**
    *   "قبل أن نبدأ بالتحسين، يجب أن نعرف **أين** تحدث المشكلة؟ أين يقضي الـ API معظم وقته؟"
    *   **الأماكن الشائعة لعنق الزجاجة:**
        *   **1. قاعدة البيانات (Database):**
            *   **استعلامات بطيئة:** قد يكون استعلام SQL نفسه غير فعال، خصوصًا مع كميات كبيرة من البيانات أو عند استخدام `JOIN` بشكل خاطئ.
            *   **عدم وجود فهارس (Indexes):** هذا هو السبب **الأكثر شيوعًا** لبطء قاعدة البيانات. بدون فهارس مناسبة، MySQL تضطر لمسح الجدول بالكامل للبحث عن البيانات.
            *   **تصميم سيء للجدول:** تصميم غير معياري (Non-normalized) أو استخدام أنواع بيانات غير مناسبة.
        *   **2. كود التطبيق (PHP Script):**
            *   **خوارزميات غير فعالة:** كود يقوم بعمليات حسابية معقدة أو معالجة بيانات كثيرة بطريقة غير مثلى.
            *   **عمليات مكلفة داخل الحلقات:** مثل إجراء استعلام قاعدة بيانات أو عملية I/O (قراءة/كتابة ملف) داخل حلقة `for` أو `while` تتكرر مئات المرات.
            *   **استخدام مفرط للذاكرة:** تحميل كميات كبيرة جدًا من البيانات في الذاكرة مرة واحدة.
        *   **3. الشبكة (Network):**
            *   **زمن الاستجابة (Latency):** الوقت الذي يستغرقه الطلب للوصول للخادم والعودة. يعتمد على المسافة الجغرافية وجودة الشبكة. (تحسينه يتطلب حلول مثل CDN).
            *   **حجم البيانات المنقولة:** إرسال كميات كبيرة جدًا من البيانات (JSON كبير جدًا) يستغرق وقتًا أطول للنقل.
    *   **أدوات التشخيص:**
        *   **قياس وقت تنفيذ الكود:** استخدام `microtime(true)` قبل وبعد أجزاء معينة من الكود لحساب الوقت المستغرق.
        *   **أدوات تحليل الأداء (Profiling Tools):** مثل Xdebug (مع أدوات تحليل مثل Webgrind أو KCacheGrind) أو Blackfire.io لتحليل تفصيلي لأداء كود PHP.
        *   **تحليل استعلامات MySQL:** استخدام `EXPLAIN` قبل جملة `SELECT` لمعرفة كيف يخطط MySQL لتنفيذ الاستعلام وهل يستخدم فهارس. مراقبة Slow Query Log في MySQL.
        *   **أدوات المطور في المتصفح (Network Tab):** لقياس وقت استجابة الـ API وحجم البيانات المنقولة.

4.  **تحسين استعلامات قاعدة البيانات (Database Query Optimization) (25 دقيقة)**
    *   **أ. الفهرسة (Indexing):**
        *   **"هذه أهم نقطة!"** الفهرس مثل فهرس الكتاب، يسمح لـ MySQL بالعثور على الصفوف المطلوبة بسرعة كبيرة دون الحاجة لقراءة الجدول بأكمله.
        *   **متى نستخدم الفهارس؟** يجب إنشاء فهارس على الأعمدة التي تُستخدم **بكثرة** في جمل `WHERE`, `JOIN (ON ...)`، و `ORDER BY`.
        *   **المفتاح الأساسي (PK)** يتم فهرسته تلقائيًا.
        *   **المفتاح الفريد (Unique Key)** يتم فهرسته تلقائيًا.
        *   **المفتاح الخارجي (FK):** **من الضروري جدًا** إنشاء فهرس (Index) يدويًا على أعمدة المفاتيح الخارجية لتحسين أداء عمليات `JOIN` بشكل كبير. (تذكروا، فعلنا هذا في الحصة الماضية عند إنشاء FK لـ `instructor_id`).
        *   **كيف ننشئ فهرسًا (phpMyAdmin):** في تبويب `Structure` للجدول، تحت قسم "Indexes"، يمكن إضافة فهرس جديد على عمود أو مجموعة أعمدة.
        *   **تحذير:** كثرة الفهارس غير الضرورية يمكن أن تبطئ عمليات `INSERT`, `UPDATE`, `DELETE` قليلًا (لأن الفهرس يحتاج للتحديث أيضًا)، لذا يجب الموازنة وإنشاء فهارس فقط على الأعمدة الهامة للاستعلام.
    *   **ب. اختيار الأعمدة المطلوبة فقط (`SELECT specific_columns`):**
        *   تجنب استخدام `SELECT *` إلا إذا كنت تحتاج **فعلاً** لكل الأعمدة.
        *   كلما قل عدد الأعمدة التي تجلبها، قل حجم البيانات المنقولة من قاعدة البيانات إلى PHP، وقل استهلاك الذاكرة.
        *   `SELECT id, name, email FROM students` **أفضل بكثير** من `SELECT * FROM students`.
    *   **ج. تحديد عدد النتائج (`LIMIT`) والتقسيم لصفحات (Pagination):**
        *   إذا كان الـ API يرجع قائمة طويلة (مثل قائمة بكل الطلاب، وقد يكونون بالآلاف)، **لا ترجعهم كلهم مرة واحدة!** هذا يبطئ الاستعلام، يستهلك ذاكرة PHP، ويزيد حجم استجابة JSON بشكل كبير.
        *   **الحل: Pagination.** الـ API يأخذ بارامترات إضافية (مثل `page=2` و `limit=20`) ويستخدم جملة `LIMIT` في SQL لجلب جزء صغير فقط من البيانات في كل مرة (مثلاً 20 طالبًا للصفحة الثانية).
            ```sql
            -- Example: Get 20 students for page 2
            $limit = 20;
            $page = isset($_GET['page']) ? (int)$_GET['page'] : 1;
            $offset = ($page - 1) * $limit; // Calculate the starting point

            $sql = "SELECT id, name FROM students LIMIT $limit OFFSET $offset";
            ```
        *   الـ API يجب أن يرجع أيضًا معلومات عن العدد الإجمالي للصفحات أو السجلات لمساعدة الواجهة الأمامية في عرض عناصر التحكم بالصفحات.
    *   **د. تجنب الاستعلامات داخل الحلقات (N+1 Problem):**
        *   مشكلة شائعة جدًا: تجلب قائمة من العناصر (مثل 10 دورات)، ثم داخل حلقة `foreach` على هذه الدورات، تقوم بعمل استعلام منفصل لجلب اسم المدرس لكل دورة. هذا يعني (N=10 استعلامات للمدرسين + 1 استعلام للدورات) = 11 استعلام!
        *   **الحل:** استخدم `JOIN` في الاستعلام الأصلي لجلب بيانات الدورات والمدرسين معًا في **استعلام واحد فقط**.

5.  **مقدمة للتخزين المؤقت (Caching) (15 دقيقة)**
    *   "Caching هو تخزين نتيجة عملية مكلفة (مثل استعلام قاعدة بيانات أو حساب معقد) مؤقتًا في مكان أسرع (مثل ذاكرة الخادم RAM) بحيث يمكن إرجاع النتيجة المخزنة مباشرة في المرات التالية التي يُطلب فيها نفس الشيء، بدلًا من إعادة تنفيذ العملية المكلفة مرة أخرى."
    *   **متى نستخدم Caching؟** للبيانات التي **لا تتغير كثيرًا** ولكن يتم طلبها بشكل متكرر.
    *   **أنواع Caching:**
        *   **Server-Side Caching:**
            *   **Application Level Cache:** استخدام أدوات مثل Memcached أو Redis (أنظمة تخزين في الذاكرة سريعة جدًا) لتخزين نتائج الـ API أو أجزاء منها. كود PHP يتحقق أولاً إذا كانت النتيجة موجودة في الـ Cache، إذا نعم يرجعها، إذا لا، يجلبها من الداتابيز، يخزنها في الـ Cache للمرة القادمة، ثم يرجعها.
            *   **Opcode Cache:** PHP تقوم بتحويل الكود النصي إلى Opcode (شيء تفهمه آلة Zend). Opcode Cache (مثل OPcache المدمج في PHP) يخزن هذا الـ Opcode المترجم في الذاكرة، مما يسرع تنفيذ كود PHP نفسه بشكل كبير في الطلبات المتكررة. (غالبًا مفعل افتراضيًا في البيئات الحديثة).
        *   **Database Caching:** MySQL نفسها لديها آليات caching داخلية (مثل Query Cache - الذي أصبح أقل أهمية مع InnoDB، و Buffer Pool لتخزين البيانات المستخدمة بكثرة في الذاكرة).
        *   **Client-Side Caching (HTTP Caching):** المتصفح أو التطبيق العميل يمكنه تخزين استجابات الـ API مؤقتًا بناءً على ترويسات HTTP معينة يرسلها الخادم (مثل `Cache-Control`, `ETag`, `Last-Modified`). إذا لم تتغير البيانات، العميل يستخدم النسخة المخزنة لديه بدلًا من طلبها من الخادم مرة أخرى.
    *   **(Caching موضوع كبير، نركز على فهم الفكرة العامة وأهميتها).**

6.  **تحسينات أخرى (كود PHP وضغط الاستجابة) (10 دقائق)**
    *   **كود PHP:**
        *   استخدم الخوارزميات والدوال المدمجة بكفاءة.
        *   تجنب العمليات الحسابية المعقدة أو معالجة النصوص الكثيفة داخل الحلقات إذا أمكن.
        *   حاول تحميل البيانات المطلوبة فقط في الذاكرة.
    *   **ضغط الاستجابة (Gzip Compression):**
        *   يمكن للخادم ضغط استجابة JSON (أو HTML) قبل إرسالها للعميل باستخدام Gzip.
        *   العميل (المتصفح/التطبيق) يفك الضغط تلقائيًا.
        *   هذا يقلل **بشكل كبير** حجم البيانات المنقولة عبر الشبكة، مما يسرع وقت التحميل بشكل ملحوظ، خصوصًا للـ APIs التي ترجع JSON كبير.
        *   غالبًا يتم تفعيل Gzip على مستوى إعدادات خادم الويب (Apache أو Nginx) باستخدام وحدات مثل `mod_deflate`.

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن، لنطبق بعض تقنيات التحسين البسيطة ونرى تأثيرها."

1.  **التمرين 1: تحليل استعلام باستخدام `EXPLAIN` (20 دقيقة)**
    *   **"المهمة:"** "نستخدم `EXPLAIN` لفهم كيف ينفذ MySQL استعلاماتنا."
    *   "روحوا لتبويب `SQL` في phpMyAdmin."
    *   "جربوا كتابة `EXPLAIN` قبل استعلامات مختلفة وراقبوا النتائج:"
        *   `EXPLAIN SELECT * FROM students WHERE id = 5;` (المفروض يستخدم الـ PRIMARY key، ويفحص صف واحد `rows: 1`).
        *   `EXPLAIN SELECT * FROM students WHERE email = 'some_email@example.com';` (بما أن `email` عليه Unique Index، المفروض يكون سريع أيضًا ويستخدم الفهرس).
        *   `EXPLAIN SELECT * FROM students WHERE name = 'Ali';` (**إذا لم يكن هناك فهرس على عمود `name`**، لاحظوا في نتيجة `EXPLAIN` كيف أن MySQL قد يضطر لفحص عدد كبير من الصفوف `rows: [عدد كبير]`, والـ `type` قد يكون `ALL` - يعني Full Table Scan، وهذا بطيء جدًا).
        *   **إضافة فهرس:** "الآن، روحوا لـ `Structure` لجدول `students` وأضيفوا فهرس (Index) جديد على عمود `name`. ارجعوا ونفذوا `EXPLAIN SELECT * FROM students WHERE name = 'Ali';` مرة أخرى. هل تغيرت نتيجة `EXPLAIN`؟ (المفروض الآن يستخدم الفهرس الجديد وعدد الصفوف المفحوصة يقل بشكل كبير)."
    *   **"نقاش:"** "ماذا تعلمنا من `EXPLAIN`؟ (أهمية الفهارس للأعمدة المستخدمة في `WHERE`)."

2.  **التمرين 2: تطبيق `LIMIT` و `OFFSET` (Pagination) (30 دقيقة)**
    *   **"المهمة:"** "نعدل `api_get_students.php` ليدعم Pagination."
    *   "اجعلوا الـ API يستقبل بارامترين اختياريين من الـ URL (GET): `page` (رقم الصفحة، الافتراضي 1) و `limit` (عدد النتائج لكل صفحة، الافتراضي 10)."
        ```php
        // Get pagination parameters (with defaults)
        $page = isset($_GET['page']) ? max(1, (int)$_GET['page']) : 1; // Ensure page is at least 1
        $limit = isset($_GET['limit']) ? max(1, (int)$_GET['limit']) : 10; // Ensure limit is at least 1
        $offset = ($page - 1) * $limit;
        ```
    *   "عدلوا استعلام SQL ليستخدم `LIMIT` و `OFFSET`:"
        ```sql
        $sql = "SELECT id, name, email FROM students ORDER BY id DESC LIMIT $limit OFFSET $offset";
        ```
    *   **(اختياري متقدم):** "احسبوا العدد الإجمالي للطلاب (`SELECT COUNT(*) FROM students`) والعدد الإجمالي للصفحات (`$total_pages = ceil($total_students / $limit);`) عشان نرجعهم في استجابة JSON لمساعدة الواجهة الأمامية."
    *   "عدلوا استجابة JSON لترجع ليس فقط مصفوفة الطلاب، بل كائن يحتوي على معلومات الـ pagination والبيانات:"
        ```json
        {
          "pagination": {
            "current_page": 1,
            "per_page": 10,
            "total_pages": 5, // Example
            "total_records": 48 // Example
          },
          "data": [
            { "id": ..., "name": ..., "email": ... },
            // ... 10 students for page 1
          ]
        }
        ```
    *   **"الاختبار:"** "استخدموا Postman لطلب الـ API:"
        *   بدون بارامترات (المفروض يرجع أول 10).
        *   مع `?limit=5` (يرجع أول 5).
        *   مع `?page=2&limit=5` (يرجع الطلاب من 6 إلى 10).
        *   مع `?page=3` (يرجع الطلاب من 21 إلى 30 إذا الافتراضي 10).

3.  **التمرين 3: تطبيق `JOIN` بدل الاستعلام داخل حلقة (25 دقيقة)**
    *   **"المهمة:"** "تخيلوا نريد بناء API جديد `api_get_courses_with_instructors.php` يرجع قائمة الدورات مع اسم المدرس لكل دورة."
    *   **"الطريقة السيئة (N+1):"** (لا تطبقوها، فقط للمناقشة)
        ```php
        // 1. Get all courses
        $courses_result = mysqli_query($conn, "SELECT course_id, course_name, instructor_id FROM courses");
        $courses_list = [];
        while ($course = mysqli_fetch_assoc($courses_result)) {
            // 2. Inside loop, query for instructor name for EACH course
            $instructor_name = null;
            if ($course['instructor_id']) {
                $inst_result = mysqli_query($conn, "SELECT instructor_name FROM instructors WHERE instructor_id = " . $course['instructor_id']);
                if($inst_row = mysqli_fetch_assoc($inst_result)) {
                    $instructor_name = $inst_row['instructor_name'];
                }
            }
            $course['instructor_name'] = $instructor_name;
            $courses_list[] = $course;
        }
        // Output JSON...
        ```
        *   "كم استعلام يتم هنا لو عندنا 50 دورة؟ (51 استعلام!)."
    *   **"الطريقة الجيدة (JOIN):"** "اكتبوا الكود باستخدام `JOIN` واحد:"
        ```php
        <?php
        // ... (Connection code) ...

        // Use LEFT JOIN to get all courses, even those without instructors
        $sql = "SELECT
                    c.course_id,
                    c.course_name,
                    c.credits,
                    i.instructor_name -- Get instructor name directly
                FROM
                    courses AS c -- Use aliases for shorter names
                LEFT JOIN
                    instructors AS i ON c.instructor_id = i.instructor_id
                ORDER BY c.course_name";

        $result = mysqli_query($conn, $sql);
        $output_array = [];
        if ($result) {
            while($row = mysqli_fetch_assoc($result)) {
                // Maybe adjust the structure if needed before adding to output
                $output_array[] = $row;
            }
            mysqli_free_result($result);
        }
        mysqli_close($conn);

        header('Content-Type: application/json; charset=utf-8');
        echo json_encode($output_array, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE);
        ?>
        ```
    *   "كم استعلام تم هنا؟ (واحد فقط!)."
    *   "نفذوا الكود واختبروه بـ Postman."

4.  **مناقشة Caching و Gzip (5 دقائق)**
    *   "تحدثنا عن Caching نظريًا. هل تتخيلون أي من الـ APIs اللي بنيناها ممكن نستفيد من تخزين نتيجته مؤقتًا؟" (مثلاً قائمة الدورات `api_get_courses` إذا كانت لا تتغير كثيرًا).
    *   "ضغط Gzip غالبًا يتم تفعيله على مستوى الخادم. كيف نتأكد إذا كان مفعل؟" (في أدوات المطور بالمتصفح (Network Tab)، شوفوا الـ Response Headers. ابحثوا عن `Content-Encoding: gzip`. ممكن أيضًا تشوفوا فرق بين Size و Content Size للطلب).

5.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1:** طبقوا Pagination على `api_get_courses_with_instructors.php`.
    *   **تمرين 2 (بحث):** ابحثوا عن "Database Normalization" (تسوية قواعد البيانات). ما هي المستويات الثلاثة الأولى (1NF, 2NF, 3NF) وما الهدف منها؟ (لها علاقة بتقليل التكرار وتحسين التصميم، مما يؤثر إيجابًا على الأداء).
    *   "اليوم تعرفنا على طرق مختلفة لجعل الـ APIs أسرع وأكثر كفاءة، مع التركيز بشكل خاص على تحسينات قاعدة البيانات مثل الفهرسة و Pagination و JOINs. تذكروا، الأداء مهم جدًا! في الأسبوع القادم، سنبدأ بتجميع ما تعلمناه في مشروع عملي صغير. يعطيكم العافية!"

---
بالتأكيد، لنستكمل تفصيل الأسبوعين الأخيرين من الشهر الثاني، واللذين يركزان على المشروع العملي الصغير ومراجعة المرحلة الأولى.

---

### **الشهر الثاني: تطوير API متقدم (تابع)**

---

#### **الأسبوع السابع**

---

#### **الحصة 13 (الاثنين): مشروع عملي صغير (Mini Project API) - الجزء الأول**

**🎯 أهداف الحصة:**

1.  تحديد متطلبات مشروع API صغير (مثال: نظام إدارة مهام بسيط).
2.  تصميم مخطط قاعدة البيانات للمشروع (الجداول، الأعمدة، العلاقات).
3.  إنشاء قاعدة البيانات والجداول باستخدام phpMyAdmin أو SQL.
4.  البدء في بناء نقاط النهاية (Endpoints) الأساسية للـ API (مثل إضافة مهمة، جلب كل المهام).

**الجزء الأول (1.5 ساعة): تخطيط وتصميم المشروع (Structured Learning)**

"أهلاً بكم مجددًا! بعد أن تعلمنا العديد من المفاهيم والتقنيات في الأسابيع الماضية (CRUD, Relationships, File Upload, Error Handling, Performance)، حان الوقت الآن لنجمع كل هذه القطع معًا ونطبقها في **مشروع عملي صغير (Mini Project)**. هذا سيساعدنا على ترسيخ فهمنا ورؤية كيف تتكامل هذه المفاهيم في تطبيق حقيقي."

1.  **مقدمة للمشروع (15 دقيقة)**
    *   **"فكرة المشروع:"** سنقوم ببناء الواجهة الخلفية (Back-End API) لنظام إدارة مهام بسيط (Simple To-Do List API).
    *   **"لماذا هذا المشروع؟"** لأنه بسيط نسبيًا لكنه يغطي معظم المفاهيم الأساسية التي تعلمناها:
        *   تحتاج لعمليات CRUD (إضافة مهمة، قراءة المهام، تحديث حالة المهمة، حذف مهمة).
        *   يمكن أن يتضمن علاقات (إذا أضفنا مستخدمين أو تصنيفات للمهام).
        *   يمكن أن يتضمن رفع ملفات (إذا أردنا إرفاق ملف بالمهمة).
        *   سنطبق فيه معالجة الأخطاء وتحسينات الأداء البسيطة.
    *   **"المنتج النهائي:"** مجموعة من نقاط النهاية (Endpoints) في الـ API يمكن لأي واجهة أمامية (مثل تطبيق Flutter لاحقًا) استخدامها لإدارة المهام.

2.  **تحديد المتطلبات الوظيفية (Functional Requirements) (20 دقيقة)**
    *   "ماذا يجب أن يسمح لنا الـ API بفعله؟ (ما هي الوظائف الأساسية؟)"
    *   **(نقاش وتحديد قائمة المتطلبات مع الطلاب):**
        *   **إضافة مهمة جديدة:** (تحتاج عنوان للمهمة، وربما وصف اختياري).
        *   **جلب كل المهام:** (عرض قائمة بالمهام الموجودة).
        *   **جلب تفاصيل مهمة واحدة:** (بناءً على ID المهمة).
        *   **تحديث مهمة:** (تعديل العنوان، الوصف، أو **تحديث الحالة** - هل هي مكتملة أم لا؟).
        *   **حذف مهمة:** (إزالة مهمة من القائمة).
        *   **(متطلبات إضافية ممكنة - قد نضيفها لاحقًا أو نتركها كتحدي):**
            *   ربط المهمة بمستخدم معين (إذا كان نظامًا متعدد المستخدمين).
            *   إضافة تاريخ استحقاق (Due Date) للمهمة.
            *   تصنيف المهام (Categories).
            *   إرفاق ملف بالمهمة.

3.  **تصميم مخطط قاعدة البيانات (Database Schema Design) (30 دقيقة)**
    *   "بناءً على المتطلبات الأساسية، ما هي الجداول والأعمدة التي نحتاجها؟"
    *   **الجدول الأساسي: `tasks`**
        *   **(نقاش حول الأعمدة المطلوبة وأنواعها وقيودها):**
            *   `id`: `INT`, `PRIMARY KEY`, `AUTO_INCREMENT` (معرف فريد لكل مهمة).
            *   `title`: `VARCHAR(255)`, `NOT NULL` (عنوان المهمة، إجباري).
            *   `description`: `TEXT`, `NULL` (وصف المهمة، اختياري، `TEXT` لأنه قد يكون أطول).
            *   `is_completed`: `BOOLEAN` (أو `TINYINT(1)`), `NOT NULL`, `DEFAULT 0` (لتحديد حالة المهمة، 0 = غير مكتملة، 1 = مكتملة. `BOOLEAN` في MySQL هو مجرد اسم مستعار لـ `TINYINT(1)`).
            *   `created_at`: `TIMESTAMP`, `DEFAULT CURRENT_TIMESTAMP` (تاريخ ووقت إنشاء المهمة).
            *   `updated_at`: `TIMESTAMP`, `DEFAULT CURRENT_TIMESTAMP` **`ON UPDATE CURRENT_TIMESTAMP`** (تاريخ ووقت آخر تحديث للمهمة، يتحدث تلقائيًا عند أي تعديل للصف).
    *   **"هل نحتاج جداول أخرى للمتطلبات الأساسية؟"** (الجواب: حاليًا لا، جدول `tasks` يكفي).
    *   **"ماذا لو أردنا إضافة تصنيفات (Categories)؟"** (نحتاج جدول `categories` (id, name) وعلاقة One-to-Many بين التصنيف والمهام - نضيف `category_id` كـ FK في جدول `tasks`).
    *   **"ماذا لو أردنا نظامًا متعدد المستخدمين؟"** (نحتاج جدول `users` (id, name, email, password) وعلاقة One-to-Many بين المستخدم والمهام - نضيف `user_id` كـ FK في جدول `tasks`).
    *   **"للتبسيط الآن، سنبدأ بجدول `tasks` فقط."**

4.  **إنشاء قاعدة البيانات والجداول (20 دقيقة)**
    *   **"المهمة:"** "لنقم بإنشاء قاعدة البيانات وجدول `tasks` باستخدام phpMyAdmin."
    *   "أنشئوا قاعدة بيانات جديدة (إذا أردتم فصلها عن `university_db`)، مثلاً `todo_api_db`, مع collation `utf8mb4_general_ci`."
    *   "داخلها، أنشئوا جدول `tasks` بالأعمدة والمواصفات التي حددناها للتو:"
        *   `id` (PK, AI)
        *   `title` (VARCHAR 255, NOT NULL)
        *   `description` (TEXT, NULL)
        *   `is_completed` (TINYINT(1), NOT NULL, DEFAULT 0)
        *   `created_at` (TIMESTAMP, DEFAULT CURRENT_TIMESTAMP)
        *   `updated_at` (TIMESTAMP, DEFAULT CURRENT_TIMESTAMP, ON UPDATE CURRENT_TIMESTAMP)
    *   **(مساعدة الطلاب في إنشاء الجدول والتأكد من صحة الإعدادات، خاصةً `ON UPDATE CURRENT_TIMESTAMP`).**

5.  **خطة بناء الـ API Endpoints (5 دقائق)**
    *   "الآن بعد أن أصبح لدينا تصميم قاعدة البيانات، سنقوم ببناء ملفات PHP منفصلة لكل عملية أساسية (Endpoint):"
        *   `api/tasks/create.php`: لإضافة مهمة جديدة (يستقبل بيانات بـ POST).
        *   `api/tasks/read.php`: لجلب كل المهام (يستجيب لـ GET).
        *   `api/tasks/read_one.php`: لجلب مهمة واحدة (يستقبل ID بـ GET).
        *   `api/tasks/update.php`: لتحديث مهمة (يستقبل ID وبيانات بـ PUT أو POST).
        *   `api/tasks/delete.php`: لحذف مهمة (يستقبل ID بـ DELETE أو GET).
    *   "سنقوم بتنظيم الملفات داخل مجلد `api/tasks` لجعل المشروع مرتبًا."
    *   "سنبدأ اليوم ببناء أول endpoint (إضافة مهمة) وجزء من endpoint جلب المهام."

---

**الجزء الثاني (1.5 ساعة): بدء بناء الـ API (Open Learning & Problem Solving)**

"حان وقت كتابة الكود! سنبدأ بإنشاء الهيكل الأساسي ونقطة النهاية الأولى."

1.  **إعداد هيكل المشروع (10 دقائق)**
    *   "داخل مجلد مشروعكم الرئيسي (`php_course` أو ما شابه)، أنشئوا مجلدًا جديدًا للمشروع، مثلاً `todo_api`."
    *   "داخل `todo_api`, أنشئوا مجلدًا اسمه `config` (لإعدادات الاتصال بالداتابيز)."
    *   "داخل `todo_api`, أنشئوا مجلدًا اسمه `api`. وداخل `api`, أنشئوا مجلدًا اسمه `tasks`."
    *   "المسار سيكون مثلاً: `php_course/todo_api/api/tasks/`."

2.  **إنشاء ملف إعدادات الاتصال (15 دقيقة)**
    *   "داخل مجلد `config`, أنشئوا ملفًا اسمه `database.php`."
    *   "هذا الملف سيحتوي على معلومات الاتصال وكود الاتصال بالداتابيز، بحيث يمكننا تضمينه (`include` أو `require`) في كل ملفات الـ API بدلًا من تكرار الكود."
    *   **محتوى `config/database.php` (كمثال بسيط):**
        ```php
        <?php
        // Database Credentials
        $host = 'localhost';
        $db_name = 'todo_api_db'; // Make sure this matches your DB name
        $username = 'root';
        $password = '';
        $conn = null; // Initialize connection variable

        // Set mysqli to throw exceptions on error
        mysqli_report(MYSQLI_REPORT_ERROR | MYSQLI_REPORT_STRICT);

        try {
            $conn = mysqli_connect($host, $username, $password, $db_name);
            mysqli_set_charset($conn, 'utf8mb4');
            // Optional: echo "Database connected." for testing, remove later.
        } catch (mysqli_sql_exception $exception) {
            // Log the detailed error (don't echo sensitive info in production)
            error_log("Database Connection Error: " . $exception->getMessage());

            // Return a generic error for the API user
            // Note: Cannot set headers here if this file is included later.
            // Error handling should be done in the calling API file.
            // We can return null or false, or throw the exception again.
            // For simplicity now, let's just let the exception bubble up
            // or handle it in the API file. Let's return null for now.
             $conn = null; // Indicate connection failure
             // Or rethrow: throw $exception;
        }

        // Function to get the connection (optional, but good practice)
        function getDbConnection() {
            global $conn; // Access the global connection variable
            // You might add logic here to reconnect if $conn is null
            return $conn;
        }

        // Return the connection object (or null if failed)
        // This line is tricky if used with include. Let's rely on the global $conn
        // or the getDbConnection() function.
        ?>
        ```
        *   **(نقاش حول كيفية استخدام هذا الملف: استخدام `require_once '../../config/database.php';` في بداية ملفات الـ API للوصول للمتغير `$conn`).**

3.  **بناء Endpoint: إضافة مهمة (`api/tasks/create.php`) (45 دقيقة)**
    *   **"المهمة:"** "إنشاء ملف يستقبل عنوان المهمة (ووصف اختياري) عبر POST ويضيفها لجدول `tasks`."
    *   **ملف `api/tasks/create.php`:**
        ```php
        <?php
        // Headers for API response
        header('Access-Control-Allow-Origin: *'); // Allow requests from any origin (for testing)
        header('Content-Type: application/json; charset=UTF-8');
        header('Access-Control-Allow-Methods: POST');
        header('Access-Control-Allow-Headers: Access-Control-Allow-Headers,Content-Type,Access-Control-Allow-Methods, Authorization, X-Requested-With');

        // Include database connection
        require_once '../../config/database.php'; // Adjust path as needed

        // Get connection object
        $db_connection = getDbConnection(); // Use the function or global $conn

        if (!$db_connection) {
            // If connection failed in database.php, handle it here
            http_response_code(500);
            echo json_encode(['message' => 'Database connection error']);
            exit();
        }

        // Get raw posted data
        // Use file_get_contents for raw JSON input, or check $_POST for form data
        $data = json_decode(file_get_contents("php://input"));

        // --- Input Validation ---
        // Check if title is provided (assuming title is required)
        if (empty($data->title)) {
            http_response_code(400); // Bad Request
            echo json_encode(['message' => 'Task title is required.']);
            exit();
        }

        // Prepare data for insertion (Sanitize!)
        // Use mysqli_real_escape_string or Prepared Statements
        $title = mysqli_real_escape_string($db_connection, $data->title);
        // Description is optional, handle if it exists
        $description = isset($data->description) ? mysqli_real_escape_string($db_connection, $data->description) : null;

        // --- Create Task ---
        $sql = "INSERT INTO tasks (title, description) VALUES ('$title', ";
        // Handle optional description (if null, insert SQL NULL)
        $sql .= ($description === null) ? "NULL)" : "'$description')";

        try {
            if (mysqli_query($db_connection, $sql)) {
                $last_id = mysqli_insert_id($db_connection);
                http_response_code(201); // 201 Created
                echo json_encode([
                    'message' => 'Task created successfully.',
                    'task_id' => $last_id
                ]);
            } else {
                // This might not be reached if mysqli_report is on, exception is thrown instead
                throw new Exception("Failed to execute query: " . mysqli_error($db_connection));
            }
        } catch (Exception $e) {
            error_log("Error creating task: " . $e->getMessage()); // Log detailed error
            http_response_code(500); // Internal Server Error
            echo json_encode(['message' => 'Error creating task.']);
        } finally {
            if ($db_connection) {
                mysqli_close($db_connection);
            }
        }
        ?>
        ```
    *   **شرح الكود:**
        *   **Headers:** ترويسات مهمة للـ API، خاصة `Access-Control-Allow-Origin: *` للسماح بالطلبات من أي نطاق (مهم للاختبار من أدوات مثل Postman أو من نطاق مختلف للواجهة الأمامية، لكن في الإنتاج يجب تقييده). `Content-Type` للإجابة. `Allow-Methods` و `Allow-Headers` لطلبات OPTIONS (CORS preflight).
        *   **`require_once '../../config/database.php';`:** تضمين ملف الاتصال.
        *   **`file_get_contents("php://input")` و `json_decode()`:** طريقة شائعة لقراءة بيانات JSON الخام التي تُرسل في جسم طلب POST (بدلًا من بيانات form-urlencoded التي تكون في `$_POST`).
        *   **التحقق من المدخلات:** التأكد من وجود الحقول المطلوبة.
        *   **التنظيف (Sanitization):** استخدام `mysqli_real_escape_string` (كحد أدنى) أو الأفضل استخدام **Prepared Statements** (سيتم شرحها لاحقًا أو في المستويات المتقدمة).
        *   **بناء جملة INSERT:** التعامل مع الحقول الاختيارية (مثل `description`).
        *   **استخدام `try...catch`:** للإمساك بأي أخطاء محتملة من `mysqli_query` (إذا كان `mysqli_report` مفعل).
        *   **إرجاع استجابة JSON:** رسالة نجاح مع كود 201 (Created) و ID المهمة الجديدة، أو رسالة خطأ مع كود مناسب (400 أو 500).
        *   **إغلاق الاتصال في `finally`:** لضمان إغلاقه دائمًا.
    *   **"الاختبار:"**
        *   استخدموا Postman.
        *   اختاروا Method: `POST`.
        *   أدخلوا الـ URL: `http://localhost/php_course/todo_api/api/tasks/create.php`
        *   روحوا لقسم `Body`.
        *   اختاروا `raw` ومن القائمة المنسدلة اختاروا `JSON`.
        *   اكتبوا JSON للبيانات:
            ```json
            {
              "title": "My First Task Title",
              "description": "Optional description here"
            }
            ```
        *   اضغطوا `Send`. المفروض تشوفوا رسالة النجاح 201 والـ ID.
        *   جربوا إرسال بدون `title` (المفروض يرجع خطأ 400).
        *   تحققوا من جدول `tasks` في phpMyAdmin للتأكد من إضافة المهمة.

4.  **مناقشة وحل المشكلات (15 دقيقة)**
    *   "هل واجهتكم مشاكل في تضمين ملف الإعدادات؟ أو في قراءة بيانات JSON؟ أو في تنفيذ الـ INSERT؟"
    *   "أهمية Prepared Statements للأمان (بدل `mysqli_real_escape_string`)."
    *   "مناقشة استخدام الأفعال HTTP الصحيحة (POST للإضافة، GET للقراءة، PUT/PATCH للتحديث، DELETE للحذف)."

5.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1:** ابدأوا في بناء Endpoint جلب كل المهام (`api/tasks/read.php`). يجب أن يتصل بالداتابيز، ينفذ `SELECT * FROM tasks ORDER BY created_at DESC`, يجمع النتائج في مصفوفة، ويرجعها كـ JSON.
    *   **تمرين 2 (بحث):** ابحثوا عن "PHP Prepared Statements with MySQLi". حاولوا فهم كيفية استخدام `prepare()`, `bind_param()`, `execute()`, و `get_result()` كبديل آمن لـ `mysqli_query()` مع متغيرات المستخدم.
    *   "بداية ممتازة للمشروع! اليوم صممنا قاعدة البيانات وبنينا أول وأهم endpoint وهو إضافة مهمة. في الحصة القادمة، سنكمل بناء باقي نقاط النهاية (CRUD). يعطيكم العافية!"

---

#### **الحصة 14 (الخميس): استكمال المشروع العملي الصغير - الجزء الثاني**

**🎯 أهداف الحصة:**

1.  استكمال بناء نقاط النهاية (Endpoints) للـ API:
    *   جلب كل المهام (`read.php`).
    *   جلب مهمة واحدة (`read_one.php`).
    *   تحديث مهمة (`update.php`).
    *   حذف مهمة (`delete.php`).
2.  اختبار جميع نقاط النهاية باستخدام Postman.
3.  مناقشة التحسينات الممكنة والتحديات.

**الجزء الأول (1.5 ساعة): بناء باقي Endpoints (Structured Learning & Guided Practice)**

"أهلاً بكم مجددًا! في الحصة الماضية بدأنا مشروع الـ ToDo API وصممنا قاعدة البيانات وبنينا نقطة النهاية الخاصة بإضافة مهمة (`create.php`). اليوم، سنكمل بناء باقي العمليات الأساسية (Read, Update, Delete) لنحصل على API متكامل لإدارة المهام."

1.  **مراجعة سريعة (5 دقائق)**
    *   "ما هي نقطة النهاية التي بنيناها المرة الماضية؟ (`create.php`). وماذا كانت تستخدم لاستقبال البيانات؟ (POST مع JSON Body). وكيف اختبرناها؟ (Postman). تمام!"

2.  **بناء Endpoint: جلب كل المهام (`api/tasks/read.php`) (25 دقيقة)**
    *   **"المهمة:"** إنشاء ملف يرجع قائمة بكل المهام الموجودة في قاعدة البيانات بصيغة JSON.
    *   **ملف `api/tasks/read.php`:**
        ```php
        <?php
        // Headers
        header('Access-Control-Allow-Origin: *');
        header('Content-Type: application/json; charset=UTF-8');

        // Include database
        require_once '../../config/database.php';
        $db_connection = getDbConnection();

        if (!$db_connection) {
            http_response_code(500);
            echo json_encode(['message' => 'Database connection error']);
            exit();
        }

        // --- Read Tasks ---
        $sql = "SELECT id, title, description, is_completed, created_at FROM tasks ORDER BY created_at DESC"; // Get newest first
        $tasks_array = [];

        try {
            $result = mysqli_query($db_connection, $sql);

            if ($result && mysqli_num_rows($result) > 0) {
                while ($row = mysqli_fetch_assoc($result)) {
                    // Maybe cast boolean/int types for consistency in JSON
                    $row['id'] = (int)$row['id'];
                    $row['is_completed'] = (bool)$row['is_completed'];
                    $tasks_array[] = $row;
                }
                 mysqli_free_result($result);
            }
            // If no tasks, $tasks_array remains empty, which is correct (empty list)

        } catch (Exception $e) {
            error_log("Error reading tasks: " . $e->getMessage());
            http_response_code(500);
            echo json_encode(['message' => 'Error reading tasks.']);
            mysqli_close($db_connection); // Close connection on error too
            exit();
        }

        mysqli_close($db_connection);

        // Return JSON response
        http_response_code(200); // OK
        echo json_encode($tasks_array); // Return the array (even if empty)
        ?>
        ```
    *   **شرح:**
        *   Headers بسيطة (فقط GET).
        *   تضمين الاتصال والتحقق منه.
        *   استعلام SELECT لجلب الأعمدة المطلوبة (تجنب `*` إذا لم تكن تحتاج `updated_at` مثلاً). `ORDER BY` مهم لعرض المهام بترتيب منطقي.
        *   حلقة `while` لجمع النتائج في مصفوفة.
        *   **تحويل الأنواع (Type Casting):** `(int)$row['id']`, `(bool)$row['is_completed']`. هذه خطوة جيدة لضمان أن الأرقام تُرجع كأرقام في JSON (وليس كنصوص) والقيم المنطقية تُرجع كـ `true`/`false` (وليس "1" أو "0").
        *   معالجة أخطاء الاستعلام بـ `try...catch`.
        *   إرجاع المصفوفة (`$tasks_array`) كـ JSON. حتى لو لم تكن هناك مهام، يجب أن نرجع مصفوفة فارغة `[]` مع كود 200 OK (وليس خطأ 404).
    *   **"الاختبار (Postman):"** أرسل طلب `GET` إلى `api/tasks/read.php`. تأكد من الحصول على قائمة المهام التي أضفتها.

3.  **بناء Endpoint: جلب مهمة واحدة (`api/tasks/read_one.php`) (20 دقيقة)**
    *   **"المهمة:"** إنشاء ملف يستقبل ID مهمة عبر الـ URL (GET) ويرجع تفاصيل تلك المهمة فقط.
    *   **ملف `api/tasks/read_one.php`:**
        ```php
        <?php
        // Headers
        header('Access-Control-Allow-Origin: *');
        header('Content-Type: application/json; charset=UTF-8');

        require_once '../../config/database.php';
        $db_connection = getDbConnection();

        if (!$db_connection) { /* ... error handling ... */ exit(); }

        // Get ID from URL parameter
        $task_id = isset($_GET['id']) ? (int)$_GET['id'] : 0; // Get ID or default to 0

        // Validate ID
        if ($task_id <= 0) {
            http_response_code(400); // Bad Request
            echo json_encode(['message' => 'Invalid task ID provided.']);
            exit();
        }

        // --- Read Single Task ---
        // Use Prepared Statement (Example with MySQLi Procedural)
        $sql = "SELECT id, title, description, is_completed, created_at FROM tasks WHERE id = ?"; // Placeholder '?'
        $stmt = mysqli_prepare($db_connection, $sql);

        if ($stmt === false) {
             error_log("Error preparing statement: " . mysqli_error($db_connection));
             http_response_code(500); echo json_encode(['message' => 'Error preparing statement.']); exit();
        }

        // Bind the parameter (i for integer)
        mysqli_stmt_bind_param($stmt, "i", $task_id);

        $task_data = null;
        try {
            // Execute the prepared statement
            if (mysqli_stmt_execute($stmt)) {
                $result = mysqli_stmt_get_result($stmt); // Get result set
                if ($row = mysqli_fetch_assoc($result)) {
                    // Task found, cast types
                    $row['id'] = (int)$row['id'];
                    $row['is_completed'] = (bool)$row['is_completed'];
                    $task_data = $row;
                     mysqli_free_result($result);
                }
                // If no row found, $task_data remains null
            } else {
                 throw new Exception("Statement execution failed: " . mysqli_stmt_error($stmt));
            }
        } catch (Exception $e) {
             error_log("Error reading task details: " . $e->getMessage());
             http_response_code(500); echo json_encode(['message' => 'Error reading task details.']);
             mysqli_stmt_close($stmt); mysqli_close($db_connection); exit();
        }

        mysqli_stmt_close($stmt); // Close the statement
        mysqli_close($db_connection); // Close the connection

        // --- Return Response ---
        if ($task_data) {
            http_response_code(200); // OK
            echo json_encode($task_data);
        } else {
            http_response_code(404); // Not Found
            echo json_encode(['message' => 'Task not found with ID: ' . $task_id]);
        }
        ?>
        ```
    *   **شرح التغييرات:**
        *   **استقبال ID:** نقرأ `$_GET['id']` ونتأكد أنه رقم صحيح أكبر من صفر.
        *   **Prepared Statement (مثال):**
            *   `mysqli_prepare()`: نجهز الاستعلام بـ placeholder `?`.
            *   `mysqli_stmt_bind_param()`: نربط قيمة `$task_id` بالـ placeholder (نحدد النوع `i` لـ integer). هذا يحمي من SQL Injection.
            *   `mysqli_stmt_execute()`: ننفذ الاستعلام المجهز.
            *   `mysqli_stmt_get_result()`: نحصل على كائن النتيجة.
            *   `mysqli_fetch_assoc()`: نجلب الصف (إذا وجد).
            *   `mysqli_stmt_close()`: نغلق الـ statement.
        *   **الاستجابة:** إذا وجدنا المهمة (`$task_data` لا يساوي null)، نرجعها مع كود 200. إذا لم نجدها، نرجع خطأ 404 Not Found.
    *   **"الاختبار (Postman):"** أرسل طلب `GET` إلى `api/tasks/read_one.php?id=[ID_موجود]` ومرة أخرى إلى `api/tasks/read_one.php?id=[ID_غير_موجود]`. تأكد من الحصول على النتائج الصحيحة (بيانات المهمة أو خطأ 404).

4.  **بناء Endpoint: تحديث مهمة (`api/tasks/update.php`) (30 دقيقة)**
    *   **"المهمة:"** إنشاء ملف يستقبل ID المهمة والبيانات الجديدة (مثل title, description, is_completed) ويقوم بتحديثها في الداتابيز.
    *   **"تحديد الطريقة (Method):"** عملية التحديث يمكن أن تتم باستخدام `PUT` (إذا كنا نتوقع استبدال كامل للمورد) أو `PATCH` (إذا كنا نريد تحديث جزئي). `POST` أيضًا يمكن استخدامه. للتبسيط الآن، سنستخدم `POST` (أو يمكنكم تجربة `PUT`).
    *   **ملف `api/tasks/update.php` (هيكل أساسي، قد يحتاج تفصيل أكثر):**
        ```php
        <?php
        // Headers (Allow POST or PUT)
        header('Access-Control-Allow-Origin: *');
        header('Content-Type: application/json; charset=UTF-8');
        header('Access-Control-Allow-Methods: POST, PUT'); // Allow both
        header('Access-Control-Allow-Headers: ...');

        require_once '../../config/database.php';
        $db_connection = getDbConnection();
        if (!$db_connection) { /* ... error handling ... */ exit(); }

        // Get raw posted data
        $data = json_decode(file_get_contents("php://input"));

        // --- Validate Input ---
        // Need task ID and at least one field to update
        if (empty($data->id) || (!isset($data->title) && !isset($data->description) && !isset($data->is_completed))) {
            http_response_code(400);
            echo json_encode(['message' => 'Task ID and at least one field to update are required.']);
            exit();
        }
        $task_id = (int)$data->id;

        // --- Build UPDATE Query Dynamically (Careful!) ---
        // Or better: Fetch existing task, update object, then save.
        // For simplicity now, let's build the SET part dynamically.
        $set_parts = [];
        $params = []; // For prepared statement binding
        $types = "";    // For prepared statement types

        if (isset($data->title)) {
            $set_parts[] = "title = ?";
            $params[] = $data->title;
            $types .= "s"; // string
        }
        if (isset($data->description)) {
            $set_parts[] = "description = ?";
            $params[] = $data->description;
            $types .= "s"; // string
        }
        if (isset($data->is_completed)) {
            $set_parts[] = "is_completed = ?";
            $params[] = $data->is_completed ? 1 : 0; // Convert boolean to int
            $types .= "i"; // integer
        }

        if (empty($set_parts)) { // Double check if nothing to update
             http_response_code(400); echo json_encode(['message' => 'No fields provided for update.']); exit();
        }

        // Add the task ID to the end of params for the WHERE clause
        $params[] = $task_id;
        $types .= "i"; // integer for ID

        $sql = "UPDATE tasks SET " . implode(", ", $set_parts) . " WHERE id = ?";
        $stmt = mysqli_prepare($db_connection, $sql);

        if ($stmt === false) { /* ... error handling ... */ exit(); }

        // Bind parameters dynamically using argument unpacking (...) (PHP 5.6+)
        mysqli_stmt_bind_param($stmt, $types, ...$params);

        try {
            if (mysqli_stmt_execute($stmt)) {
                // Check if any row was actually updated
                if (mysqli_stmt_affected_rows($stmt) > 0) {
                    http_response_code(200); // OK
                    echo json_encode(['message' => 'Task updated successfully.']);
                } else {
                    // Maybe task ID didn't exist, or data was the same
                    // Check if task exists first (optional but better)
                    http_response_code(404); // Or 304 Not Modified if data was same
                    echo json_encode(['message' => 'Task not found or no changes made.']);
                }
            } else {
                 throw new Exception("Statement execution failed: " . mysqli_stmt_error($stmt));
            }
        } catch (Exception $e) {
             /* ... error handling ... */
        } finally {
             mysqli_stmt_close($stmt);
             mysqli_close($db_connection);
        }
        ?>
        ```
    *   **شرح:**
        *   استقبال البيانات وفحص وجود ID وحقل واحد على الأقل للتحديث.
        *   **بناء جملة UPDATE ديناميكيًا:** طريقة لبناء جملة `SET` فقط للحقول التي تم إرسالها.
        *   **Prepared Statement مع بارامترات ديناميكية:** استخدام `mysqli_stmt_bind_param` مع `$types` و `...$params` (argument unpacking) لربط البارامترات.
        *   **التحقق من `mysqli_stmt_affected_rows()`:** مهم لمعرفة إذا تم تحديث أي صف فعلاً.
    *   **"الاختبار (Postman):"**
        *   استخدم `POST` أو `PUT` إلى `api/tasks/update.php`.
        *   أرسل JSON في الـ Body يحتوي على `id` المهمة المراد تحديثها والحقول الجديدة:
            ```json
            {
              "id": 1, // ID of the task to update
              "title": "Updated Task Title",
              "is_completed": true
            }
            ```
        *   تحقق من الاستجابة (200 OK أو 404).
        *   استخدم `read_one.php` أو `read.php` للتأكد من أن المهمة تم تحديثها.

5.  **بناء Endpoint: حذف مهمة (`api/tasks/delete.php`) (10 دقائق)**
    *   **"المهمة:"** إنشاء ملف يستقبل ID مهمة ويحذفها.
    *   **"تحديد الطريقة:"** الفعل HTTP الصحيح للحذف هو `DELETE`. لكن بعض البيئات أو العملاء قد لا يدعمونه بسهولة، لذا قد نستخدم `POST` كبديل أحيانًا. سنستخدم `DELETE` هنا.
    *   **ملف `api/tasks/delete.php`:**
        ```php
        <?php
        // Headers (Allow DELETE)
        header('Access-Control-Allow-Origin: *');
        header('Content-Type: application/json; charset=UTF-8');
        header('Access-Control-Allow-Methods: DELETE');
        header('Access-Control-Allow-Headers: ...');

        require_once '../../config/database.php';
        $db_connection = getDbConnection();
        if (!$db_connection) { /* ... error handling ... */ exit(); }

        // Get ID - Can be from URL (if DELETE request has no body) or from body
        // Let's assume ID comes from JSON body for consistency with PUT/POST
        $data = json_decode(file_get_contents("php://input"));
        $task_id = isset($data->id) ? (int)$data->id : 0;

        if ($task_id <= 0) { /* ... 400 error handling ... */ exit(); }

        // --- Delete Task ---
        // Use Prepared Statement
        $sql = "DELETE FROM tasks WHERE id = ?";
        $stmt = mysqli_prepare($db_connection, $sql);
        if ($stmt === false) { /* ... error handling ... */ exit(); }
        mysqli_stmt_bind_param($stmt, "i", $task_id);

        try {
            if (mysqli_stmt_execute($stmt)) {
                if (mysqli_stmt_affected_rows($stmt) > 0) {
                    http_response_code(200); // OK (or 204 No Content)
                    echo json_encode(['message' => 'Task deleted successfully.']);
                } else {
                    http_response_code(404); // Not Found
                    echo json_encode(['message' => 'Task not found with ID: ' . $task_id]);
                }
            } else {
                 throw new Exception("Statement execution failed: " . mysqli_stmt_error($stmt));
            }
        } catch (Exception $e) {
             /* ... error handling ... */
        } finally {
             mysqli_stmt_close($stmt);
             mysqli_close($db_connection);
        }
        ?>
        ```
    *   **"الاختبار (Postman):"**
        *   استخدم Method: `DELETE`.
        *   أدخل الـ URL: `api/tasks/delete.php`.
        *   أرسل JSON في الـ Body يحتوي على `id` المهمة: `{ "id": 1 }`.
        *   تحقق من الاستجابة (200 أو 404).
        *   استخدم `read.php` للتأكد أن المهمة حُذفت.

---

**الجزء الثاني (1.5 ساعة): اختبار شامل ومناقشة مفتوحة (Open Learning & Problem Solving)**

"الآن أصبح لدينا مجموعة كاملة من Endpoints (CRUD) للـ API. حان الوقت لاختبارها بشكل شامل ومناقشة ما تعلمناه."

1.  **الاختبار الشامل باستخدام Postman (45 دقيقة)**
    *   **"المهمة:"** "كل طالب يستخدم Postman لاختبار السيناريوهات التالية بشكل منهجي:"
        *   **Create:** أضف 3-4 مهام جديدة بعناوين مختلفة (ووصف اختياري لبعضها). تأكد من الحصول على 201 Created لكل منها.
        *   **Read (All):** اطلب قائمة كل المهام. تأكد أن المهام التي أضفتها موجودة وبالترتيب الصحيح (الأحدث أولاً).
        *   **Read (One):** اطلب تفاصيل مهمة موجودة باستخدام ID صحيح. تأكد من الحصول على بياناتها الصحيحة. اطلب مهمة بـ ID غير موجود. تأكد من الحصول على خطأ 404. اطلب بـ ID غير صالح (مثل 0 أو نص). تأكد من الحصول على خطأ 400.
        *   **Update:**
            *   حدث عنوان مهمة موجودة. تحقق من الحصول على 200 OK. استخدم Read (One) للتأكد من التحديث.
            *   حدث حالة مهمة موجودة (من false إلى true). تحقق وتأكد.
            *   حاول تحديث مهمة بـ ID غير موجود. تأكد من الحصول على 404.
            *   حاول تحديث مهمة بدون إرسال أي حقل للتحديث. تأكد من الحصول على 400.
        *   **Delete:**
            *   احذف مهمة موجودة. تحقق من الحصول على 200 OK. استخدم Read (All) للتأكد أنها اختفت.
            *   حاول حذف نفس المهمة مرة أخرى. تأكد من الحصول على 404.
            *   حاول حذف مهمة بـ ID غير صالح. تأكد من الحصول على 400.
    *   **(مساعدة الطلاب في استخدام Postman وتفسير النتائج وحل أي مشاكل في الـ API endpoints)**.

2.  **مناقشة التحسينات والتحديات (30 دقيقة)**
    *   **"نقاش مفتوح:"**
        *   "ما هي نقاط الضعف أو التحسينات الممكنة في الـ API الذي بنيناه؟"
            *   **الأمان:** حاليًا لا يوجد أي مصادقة أو تفويض! أي شخص يمكنه إضافة أو حذف المهام. (الحل: إضافة مصادقة بـ API Key أو الأفضل Token/JWT).
            *   **Prepared Statements:** استخدمناها في بعض الأماكن، لكن يجب التأكد من استخدامها في *كل* مكان يتم فيه إدخال بيانات المستخدم في الـ SQL.
            *   **التحقق من المدخلات (Validation):** التحقق الحالي بسيط جدًا (`empty`). نحتاج لتحقق أكثر تفصيلاً (مثل طول العنوان، صيغة الإيميل لو أضفنا مستخدمين...). يمكن استخدام مكتبات Validation.
            *   **هيكلة الكود (Code Structure):** مع نمو الـ API، وضع كل المنطق في ملف endpoint واحد يصبح غير عملي. يمكن استخدام نمط MVC (Model-View-Controller) أو إنشاء كلاسات للتعامل مع قاعدة البيانات (Models/Repositories) وكلاسات للتحكم بالمنطق (Controllers).
            *   **التعامل مع العلاقات:** لم نطبق علاقات مع مستخدمين أو تصنيفات.
            *   **الاختبار الآلي (Automated Testing):** كتابة اختبارات (Unit tests, Integration tests) للـ API لضمان عمله بشكل صحيح بعد أي تغييرات.
        *   "ما هي التحديات التي واجهتكم أثناء بناء المشروع؟"
        *   "هل فكرة بناء API أصبحت أوضح الآن؟"

3.  **مراجعة سريعة لما تم في المشروع (10 دقائق)**
    *   "خلال هذا الأسبوع، قمنا بتحديد متطلبات API بسيط، صممنا قاعدة بياناته، وأنشأنا نقاط النهاية الرئيسية لعمليات CRUD، واختبرناها باستخدام Postman."
    *   "هذا أعطانا تجربة عملية قيمة جدًا لكيفية بناء الواجهات الخلفية."

4.  **الختام وتوجيهات للأسبوع القادم (5 دقائق)**
    *   "عمل رائع في بناء هذا الـ Mini Project! الأسبوع القادم سيكون مخصصًا للمراجعة الشاملة لكل ما تعلمناه في مرحلة الـ Back-End، وتقييم مرحلي، والتحضير للانتقال للمرحلة التالية (Front-End مع Flutter و Dart). حاولوا مراجعة الأكواد التي كتبتموها وتجربة إضافة بعض التحسينات كتحدي شخصي."
    *   "يعطيكم العافية!"

---

#### **الأسبوع الثامن**

---

#### **الحصة 15 (الاثنين): مراجعة شاملة لما سبق (Back-End)**

**🎯 أهداف الحصة:**

1.  مراجعة المفاهيم الأساسية للغة PHP (المتغيرات، أنواع البيانات، الشروط، الحلقات، الدوال، `$_GET`, `$_POST`, `$_FILES`).
2.  مراجعة أساسيات MySQL وتصميم قواعد البيانات (الجداول، الأعمدة، PK, FK, العلاقات 1:N, N:M).
3.  مراجعة استخدام phpMyAdmin.
4.  مراجعة ربط PHP بـ MySQL (MySQLi، CRUD Operations).
5.  مراجعة بناء API و JSON (Endpoints, `json_encode`, Headers).
6.  مراجعة تأمين API (AuthN vs AuthZ, API Keys).
7.  مراجعة معالجة الأخطاء (Exceptions, `try...catch`, HTTP Status Codes).
8.  مراجعة تحسين الأداء (Indexes, Pagination, JOINs).
9.  الإجابة على أسئلة الطلاب وحل المشكلات العالقة.

**الجزء الأول + الجزء الثاني (3 ساعات): مراجعة تفاعلية وحل تحديات**

"أهلاً بكم في حصة المراجعة الشاملة لمرحلة الـ Back-End! خلال الشهرين الماضيين، قطعنا شوطًا كبيرًا وتعلمنا الكثير من الأساسيات والمفاهيم الهامة في تطوير الواجهات الخلفية باستخدام PHP و MySQL. اليوم، سنقوم بمراجعة سريعة لكل هذه المفاهيم، نجيب على أسئلتكم، ونحل بعض التحديات لنتأكد من استيعابنا للمادة قبل الانتقال إلى Flutter."

**(سيتم تقسيم الوقت (3 ساعات) بين المواضيع التالية بشكل مرن حسب حاجة الطلاب وتفاعلهم. يمكن استخدام السبورة، أمثلة كود سريعة، أسئلة مباشرة، وتحديات قصيرة).**

1.  **أساسيات PHP (تقريبًا 30-40 دقيقة)**
    *   مراجعة سريعة لـ:
        *   المتغيرات (`$`), أنواع البيانات الأساسية, `var_dump()`.
        *   العمليات (حسابية، مقارنة `==` vs `===`, منطقية `&& || !`).
        *   الجمل الشرطية (`if/elseif/else`, `switch`, `break`).
        *   الحلقات (`for`, `while`, أهمية تجنب Infinite Loops).
        *   الدوال البسيطة (تعريف واستدعاء).
        *   المصفوفات العامة: `$_GET`, `$_POST` (متى نستخدم كلًا منهما؟), `$_FILES` (أهم حقولها: `name`, `tmp_name`, `size`, `error`, `type` - ولماذا لا نثق بالكل؟), `$_SERVER` (مثل `REQUEST_METHOD`).
        *   تضمين الملفات (`include`, `require`, `include_once`, `require_once` - ما الفرق؟).
    *   **تحدي سريع:** "اكتب دالة PHP بسيطة تأخذ درجة الطالب كمدخل وترجع التقدير ('A', 'B', 'C', 'D', 'F') باستخدام `if/elseif`."

2.  **قواعد بيانات MySQL و phpMyAdmin (تقريبًا 30-40 دقيقة)**
    *   مراجعة سريعة لـ:
        *   مفهوم قاعدة البيانات العلائقية، الجداول، الصفوف، الأعمدة.
        *   أنواع البيانات الشائعة في MySQL (`INT`, `VARCHAR`, `TEXT`, `BOOLEAN`/`TINYINT`, `TIMESTAMP`/`DATETIME`).
        *   المفاتيح والقيود:
            *   المفتاح الأساسي (PK): ما هو؟ خصائصه؟ (فريد، لا يقبل NULL).
            *   `AUTO_INCREMENT`: ما فائدته؟
            *   المفتاح الفريد (Unique): ما الفرق بينه وبين PK؟
            *   `NOT NULL`: ماذا يعني؟
            *   القيمة الافتراضية (`DEFAULT`).
        *   العلاقات: 1:N (كيف نطبقها؟ FK في جدول 'الكثير') و N:M (كيف نطبقها؟ جدول وسيط Junction Table).
        *   المفتاح الخارجي (FK): ما هو؟ دوره؟ خيارات `ON DELETE`/`ON UPDATE` (`RESTRICT`, `CASCADE`, `SET NULL`).
        *   جولة سريعة في phpMyAdmin (إنشاء DB، إنشاء جدول، Structure, Relation view, SQL tab).
    *   **تحدي سريع:** "صمم على الورق جدولين بسيطين: `Authors` (id, name) و `Books` (id, title, publication_year). كيف تطبق علاقة 1:N (المؤلف الواحد له عدة كتب)؟ وماذا لو أردنا علاقة N:M (الكتاب قد يكون له عدة مؤلفين، والمؤلف له عدة كتب)؟"

3.  **ربط PHP بـ MySQL و CRUD (تقريبًا 20-30 دقيقة)**
    *   مراجعة سريعة لـ:
        *   طرق الاتصال (MySQLi vs PDO - ركزنا على MySQLi).
        *   خطوات الاتصال بـ MySQLi الإجرائي (`mysqli_connect`, `mysqli_set_charset`, `mysqli_close`).
        *   تنفيذ استعلامات CRUD:
            *   `mysqli_query()`: متى ترجع true/false؟ ومتى ترجع كائن نتيجة؟
            *   `INSERT`: الحصول على ID آخر صف مضاف (`mysqli_insert_id`).
            *   `SELECT`: الحصول على عدد الصفوف (`mysqli_num_rows`), جلب صف كمصفوفة ترابطية (`mysqli_fetch_assoc`), أهمية `while` loop.
            *   `UPDATE`/`DELETE`: التحقق من عدد الصفوف المتأثرة (`mysqli_affected_rows`).
        *   **أهمية الأمان:** مخاطر SQL Injection وكيف أن `mysqli_real_escape_string` خطوة أولى ولكن Prepared Statements هي الحل الأفضل.
    *   **سؤال للنقاش:** "ما هو أخطر أمر SQL إذا تم استخدامه بدون `WHERE`؟"

4.  **بناء API و JSON (تقريبًا 20-30 دقيقة)**
    *   مراجعة سريعة لـ:
        *   ما هو API؟ لماذا نستخدمه؟ (فصل الواجهات، إعادة الاستخدام).
        *   ما هو JSON؟ بنيته (Objects `{}`, Arrays `[]`, Key/Value pairs, أنواع البيانات).
        *   خطوات بناء API بـ PHP:
            *   تضمين الاتصال.
            *   تنفيذ الاستعلام.
            *   جمع النتائج في مصفوفة PHP.
            *   **أهم خطوتين:**
                1.  `header('Content-Type: application/json; charset=utf-8');` (تُستدعى أولاً).
                2.  `echo json_encode($array, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE);`
    *   **تحدي سريع:** "حول مصفوفة PHP بسيطة (مثل `$person = ['name' => 'سارة', 'age' => 25];`) إلى نص JSON."

5.  **تأمين API ومعالجة الأخطاء والأداء (تقريبًا 30-40 دقيقة)**
    *   مراجعة سريعة لـ:
        *   `Authentication` (من أنت؟) vs `Authorization` (ماذا تفعل؟).
        *   طرق AuthN (API Key, Token/JWT). تطبيق Static API Key والتحقق من Header (`$_SERVER['HTTP_X_API_KEY']`).
        *   أهمية معالجة الأخطاء.
        *   `try...catch` للاستثناءات. تفعيل Exceptions في MySQLi (`mysqli_report`).
        *   معالجة أخطاء API: تسجيل تفصيلي (`error_log`), إرجاع JSON مناسب + كود حالة HTTP صحيح (`http_response_code`). (أكواد 4xx و 5xx).
        *   تحسين الأداء:
            *   **الفهرسة (Indexing):** أهميتها للأعمدة في `WHERE`, `JOIN`, `ORDER BY`. المفاتيح الخارجية يجب فهرستها.
            *   `SELECT specific_columns` بدل `SELECT *`.
            *   `LIMIT` / `OFFSET` للـ Pagination.
            *   استخدام `JOIN` لتجنب مشكلة N+1.
            *   فكرة Caching و Gzip.
    *   **سؤال للنقاش:** "ما هو كود الحالة HTTP المناسب لإرجاعه إذا طلب المستخدم تفاصيل مهمة بـ ID غير موجود؟ (404). وماذا لو فشل الاتصال بالداتابيز؟ (500)."

6.  **حل تحديات ومشاكل مفتوحة (الوقت المتبقي)**
    *   "الآن، أي مفاهيم لا تزال غير واضحة؟ أي مشاكل واجهتكم في المشروع الصغير أو التمارين ولم نتمكن من حلها؟"
    *   "يمكن طرح أسئلة عامة أو مشاكل كود محددة."
    *   **(يمكن تحضير بعض التحديات الإضافية القصيرة التي تجمع أكثر من مفهوم):**
        *   "اكتب API endpoint بسيط يقبل رقمين عبر GET ويقوم بقسمتهما، مع معالجة خطأ القسمة على صفر باستخدام `try...catch` وإرجاع JSON مناسب."
        *   "صمم جدول `products` (id, name, price) وجدول `categories` (id, name). طبق علاقة 1:N (التصنيف له عدة منتجات). ثم اكتب استعلام JOIN يجلب اسم المنتج واسم تصنيفه."
    *   "الهدف هو التأكد من أن الجميع مرتاح للمفاهيم الأساسية قبل الانتقال للمرحلة التالية."

---

#### **الحصة 16 (الخميس): تقييم مرحلي، مناقشة مفتوحة، وعصف ذهني للمشاريع**

**🎯 أهداف الحصة:**

1.  إجراء تقييم مرحلي سريع لقياس مدى استيعاب الطلاب لمفاهيم المرحلة الأولى.
2.  مناقشة نتائج التقييم وتقديم تغذية راجعة فردية أو جماعية.
3.  فتح باب النقاش للإجابة على أي أسئلة نهائية حول مرحلة الـ Back-End.
4.  عصف ذهني حول أفكار لمشاريع التخرج (التي ستجمع بين Back-End و Front-End).
5.  تقديم لمحة عامة عن المرحلة القادمة (Flutter & Dart).

**الجزء الأول (1.5 ساعة): التقييم المرحلي والمناقشة**

1.  **التقييم المرحلي (45-60 دقيقة)**
    *   "اليوم سنجري تقييمًا بسيطًا وسريعًا لاختبار فهمكم للمواضيع الرئيسية التي غطيناها في PHP و MySQL و APIs. الهدف ليس الدرجات بقدر ما هو قياس مدى استيعابكم وتحديد أي نقاط قد تحتاج لمراجعة إضافية."
    *   **(طبيعة التقييم يمكن أن تكون متنوعة):**
        *   **اختبار قصير (Quiz):** مجموعة أسئلة اختيار من متعدد أو صح/خطأ تغطي المفاهيم النظرية (أنواع البيانات، العلاقات، الفرق بين GET/POST، أكواد HTTP...).
        *   **أسئلة كتابية قصيرة:** اطلب شرح مفهوم معين بكلماتهم (مثل: اشرح الفرق بين `==` و `===`, أو ما هو دور المفتاح الخارجي؟).
        *   **تحدي كود قصير:** أعطِ مشكلة بسيطة واطلب كتابة جزء من الكود لحلها (مثل: كتابة جملة SQL لـ JOIN بسيط، أو كتابة كود PHP لاستقبال بيانات من `$_POST` والتحقق منها).
        *   **مراجعة كود (Code Review):** أعطِ كود PHP/API بسيط يحتوي على أخطاء شائعة (أمنية، منطقية، أدائية) واطلب من الطلاب تحديدها واقتراح تصحيحات.
    *   **(يتم جمع الإجابات أو مناقشتها مباشرة حسب طبيعة التقييم والوقت).**

2.  **مناقشة نتائج التقييم والتغذية الراجعة (15-30 دقيقة)**
    *   "بناءً على إجاباتكم، يبدو أن هناك فهمًا جيدًا لـ [المواضيع القوية]، بينما قد نحتاج لتركيز إضافي على [المواضيع التي تحتاج مراجعة]."
    *   (مناقشة الأخطاء الشائعة التي ظهرت في التقييم).
    *   (تقديم تغذية راجعة بناءة للطلاب).
    *   "هل هناك أي أسئلة أو استفسارات بناءً على التقييم؟"

3.  **مناقشة مفتوحة وأسئلة نهائية (15 دقيقة)**
    *   "هذه فرصتكم الأخيرة لطرح أي أسئلة عالقة لديكم حول PHP, MySQL, APIs, أو أي موضوع غطيناه في الشهرين الماضيين قبل أن ننتقل لعالم Flutter."
    *   "أي مفاهيم لا تزال غير واضحة؟ أي سيناريوهات تودون مناقشتها؟"

---

**الجزء الثاني (1.5 ساعة): عصف ذهني للمشاريع ولمحة عن المرحلة القادمة**

1.  **عصف ذهني لأفكار مشاريع التخرج (45 دقيقة)**
    *   "في نهاية الدورة (الشهر السادس)، سيكون مطلوبًا منكم بناء مشروع متكامل يجمع بين الواجهة الخلفية (API) التي تعلمناها، والواجهة الأمامية (تطبيق موبايل) التي سنتعلمها باستخدام Flutter."
    *   "لنبدأ الآن بالتفكير في بعض الأفكار الممكنة لهذه المشاريع. الهدف هو اختيار فكرة تكونون متحمسين لها وتسمح بتطبيق معظم ما تعلمتموه."
    *   **(توجيه جلسة العصف الذهني):**
        *   "فكروا في مشاكل بسيطة في حياتكم اليومية يمكن لتطبيق موبايل + API حلها."
        *   "ما هي أنواع التطبيقات التي تستخدمونها بكثرة؟ هل يمكن بناء نسخة مبسطة منها؟"
        *   **أمثلة لأفكار يمكن طرحها كنقطة بداية:**
            *   تطبيق قائمة مهام متقدم (مع مستخدمين، تصنيفات، تواريخ استحقاق).
            *   تطبيق تدوين بسيط (مدونة شخصية مع إضافة مقالات وتعليقات).
            *   تطبيق وصفات طعام (عرض وصفات، إضافة وصفات جديدة، بحث).
            *   تطبيق تتبع مصاريف شخصية بسيط.
            *   نظام حجز بسيط (لغرف اجتماعات، مواعيد...).
            *   تطبيق لعرض منتجات متجر صغير (بدون دفع إلكتروني مبدئيًا).
        *   (تشجيع الطلاب على طرح أفكارهم الخاصة، مناقشة جدوى كل فكرة، تحديد الميزات الأساسية لكل فكرة، وكيف ستربط بين الـ API وتطبيق Flutter).
        *   "حاولوا اختيار فكرة ليست بسيطة جدًا ولا معقدة جدًا، بحيث يمكن إنجازها في الوقت المتاح."
        *   (يمكن تقسيم الطلاب لمجموعات صغيرة لمناقشة الأفكار إذا كان العدد كبيرًا).

2.  **لمحة عامة عن المرحلة الثانية: Flutter & Dart (30 دقيقة)**
    *   "ابتداءً من الأسبوع القادم، سننتقل إلى عالم تطوير الواجهات الأمامية لتطبيقات الموبايل باستخدام إطار عمل قوي جدًا من Google وهو **Flutter** ولغة البرمجة الخاصة به **Dart**."
    *   **"ما هو Flutter؟"**
        *   هو UI toolkit (مجموعة أدوات لواجهة المستخدم) مفتوح المصدر من Google.
        *   يسمح ببناء تطبيقات جميلة وعالية الأداء تعمل على **منصات متعددة (Android, iOS, Web, Desktop)** باستخدام **قاعدة كود واحدة (Single Codebase)**. هذا يعني أنك تكتب الكود مرة واحدة ويعمل على أندرويد وآيفون!
    *   **"ما هي لغة Dart؟"**
        *   هي لغة البرمجة التي نستخدمها لكتابة تطبيقات Flutter.
        *   هي لغة حديثة، كائنية التوجه (Object-Oriented)، سهلة التعلم نسبيًا (خاصة إذا كان لديك خلفية في لغات مثل Java, C#, JavaScript).
        *   تتميز بأدائها العالي وقدرتها على الترجمة إلى كود Native فعال.
    *   **"ماذا سنتعلم في المرحلة القادمة؟"**
        *   أساسيات لغة Dart.
        *   مفاهيم Flutter الأساسية (Widgets, Layouts, State Management).
        *   بناء واجهات مستخدم جذابة وتفاعلية.
        *   **ربط تطبيق Flutter بالـ API الذي بنيناه في المرحلة الأولى لجلب البيانات وعرضها وإرسالها.** (هنا تكتمل الصورة!).
        *   استخدام أدوات مثل Figma لتصميم الواجهات.
        *   بناء مشروع Flutter متكامل (سيكون هو مشروع تخرجكم بناءً على الأفكار التي ناقشناها).
    *   **"ماذا تحتاجون للتحضير؟"**
        *   سنحتاج لتثبيت Flutter SDK و Dart SDK وأدوات التطوير (مثل Android Studio أو VS Code مع إضافات Flutter/Dart). سنقوم بذلك معًا في أول حصة.
        *   جهزوا أنفسكم لتعلم طريقة تفكير مختلفة قليلًا عن تطوير الـ Back-End، حيث سنركز أكثر على واجهة المستخدم وتجربة المستخدم والتفاعل.

3.  **ختام المرحلة الأولى وتطلعات للمستقبل (15 دقيقة)**
    *   "بهذا نكون قد أكملنا بنجاح المرحلة الأولى من دورتنا، والتي ركزت على بناء الأساس القوي للواجهات الخلفية باستخدام PHP و MySQL."
    *   "أنا فخور جدًا بالتقدم الذي أحرزتموه جميعًا. لقد تعلمتم الكثير في وقت قصير."
    *   "تذكروا أن ما تعلمناه هنا هو أساس يمكنك البناء عليه لتطوير APIs أكثر تعقيدًا وقوة في المستقبل."
    *   "استعدوا للمرحلة القادمة، ستكون ممتعة ومليئة بالتحديات، وسنرى فيها كيف نحول الـ APIs التي بنيناها إلى تطبيقات موبايل حقيقية يمكن للمستخدمين التفاعل معها."
    *   "أي أسئلة أخيرة قبل أن نختم هذه المرحلة؟"
    *   "يعطيكم ألف عافية، وأراكم الأسبوع القادم لنبدأ رحلتنا مع Flutter!"

---
بالتأكيد! لنبدأ الآن بتفصيل المرحلة الثانية من البرنامج التدريبي، والتي تركز على تطوير الواجهات الأمامية لتطبيقات الموبايل باستخدام Flutter و Dart.

---

# 📅 **المرحلة الثانية: Front-End (Flutter, Dart, Figma)**

---

### **الشهر الرابع: أساسيات Dart و Flutter**

---

#### **الأسبوع الأول (أسبوع 13 في المجمل)**

---

#### **الحصة 1 (الاثنين - حصة 17 في المجمل): مقدمة Dart، تثبيت البيئة، المتغيرات والعمليات**

**🎯 أهداف الحصة:**

1.  التعرف على لغة Dart وأهميتها لتطوير Flutter.
2.  تثبيت الأدوات اللازمة لتطوير Dart (Dart SDK أو Flutter SDK الذي يتضمن Dart).
3.  إعداد بيئة التطوير (VS Code أو Android Studio).
4.  كتابة وتشغيل أول برنامج Dart بسيط.
5.  فهم المتغيرات، أنواع البيانات الأساسية، والعمليات في Dart.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً وسهلاً بكم في المرحلة الثانية من رحلتنا! بعد أن بنينا أساسًا قويًا في عالم الـ Back-End باستخدام PHP و MySQL، حان الوقت الآن لندخل عالم **الواجهات الأمامية (Front-End)** وبناء تطبيقات موبايل جميلة وتفاعلية. الأداة السحرية التي سنستخدمها هي **Flutter**، واللغة التي تشغل هذا السحر هي **Dart**. اليوم، سنبدأ بالتعرف على لغة Dart ونجهز بيئة العمل الخاصة بنا."

1.  **مقدمة عن Dart و Flutter (15 دقيقة)**
    *   **"ما هي لغة Dart؟"**
        *   هي لغة برمجة طورتها Google (نفس الشركة اللي طورت Flutter وأندرويد).
        *   هي لغة حديثة، كائنية التوجه (Object-Oriented)، مصممة لتكون سهلة التعلم نسبيًا، ومُحسَّنة لبناء واجهات المستخدم (UI).
        *   تتميز بالسرعة في التطوير والأداء العالي للتطبيقات النهائية.
        *   هي اللغة **الأساسية والرسمية** لكتابة تطبيقات Flutter. لا يمكن استخدام Flutter بدون Dart.
    *   **"لماذا Flutter؟"** (تذكير سريع)
        *   بناء تطبيقات لمنصات متعددة (Android, iOS, Web, Desktop) من **كود واحد**.
        *   واجهات مستخدم جميلة وتفاعلية مع أداء Native.
        *   مجتمع كبير ومتزايد ودعم قوي من Google.
        *   سرعة التطوير بفضل ميزات مثل Hot Reload.
    *   "إذًا، فهم لغة Dart بشكل جيد هو مفتاحنا لإتقان Flutter."

2.  **تثبيت الأدوات وإعداد البيئة (40 دقيقة)**
    *   "لتطوير تطبيقات Flutter (وبالتالي Dart)، نحتاج لتثبيت حزمة Flutter SDK. هذه الحزمة تحتوي على كل ما نحتاجه، بما في ذلك Dart SDK نفسه."
    *   **"الخطوات الأساسية:"** (سنقوم بها معًا خطوة بخطوة)
        1.  **تحميل Flutter SDK:**
            *   الذهاب للموقع الرسمي لـ Flutter (`flutter.dev`).
            *   اختيار نظام التشغيل (Windows, macOS, Linux).
            *   تحميل الملف المضغوط (zip) الخاص بالـ SDK. **مهم:** لا تقم بفك الضغط في مجلد يتطلب صلاحيات خاصة (مثل `C:\Program Files`). الأفضل إنشاء مجلد مخصص مثل `C:\flutter` أو في مجلد المستخدم.
            *   فك الضغط عن الملف داخل المجلد المختار.
        2.  **إضافة Flutter إلى متغيرات البيئة (PATH):**
            *   **هذه خطوة حاسمة!** يجب أن نخبر نظام التشغيل بمكان وجود أدوات Flutter حتى نتمكن من تشغيل أوامر `flutter` من أي مكان في الـ Command Prompt أو Terminal.
            *   (شرح كيفية إضافة مسار مجلد `flutter\bin` إلى متغير PATH في Windows, macOS, أو Linux).
            *   **التحقق:** افتحوا نافذة Terminal أو Command Prompt جديدة واكتبوا الأمر: `flutter doctor`. هذا الأمر سيقوم بفحص بيئتكم وإخباركم إذا كان كل شيء مثبتًا بشكل صحيح أو إذا كانت هناك أي مشاكل تحتاج لحل (مثل نقص في أدوات Android أو Xcode).
        3.  **تثبيت محرر الأكواد (IDE):**
            *   الخياران الأكثر شيوعًا هما **VS Code** (مجاني وخفيف) و **Android Studio** (أثقل قليلاً ولكنه يوفر أدوات متكاملة خاصة بأندرويد). يمكنكم اختيار ما تفضلون.
            *   **تثبيت الإضافات (Extensions/Plugins):**
                *   في VS Code: ابحثوا عن إضافة "Flutter" وثبتوها (ستقوم بتثبيت إضافة "Dart" تلقائيًا).
                *   في Android Studio: اذهبوا إلى Plugins وابحثوا عن "Flutter" وثبتوها (ستقوم بتثبيت "Dart" أيضًا).
        4.  **(اختياري لكن مهم لتشغيل التطبيقات):** تثبيت أدوات تطوير المنصة المستهدفة:
            *   **لتطوير Android:** تحتاجون لتثبيت **Android Studio** بالكامل (حتى لو كنتم تستخدمون VS Code كمحرر أساسي)، لأنه يأتي مع Android SDK وأدوات البناء والمحاكيات (Emulators). بعد تثبيت Android Studio، شغلوا `flutter doctor` مرة أخرى للتأكد من تعرفه على أدوات أندرويد وقبول التراخيص (`flutter doctor --android-licenses`).
            *   **لتطوير iOS (يتطلب macOS):** تحتاجون لتثبيت **Xcode** من الـ Mac App Store. شغلوا `flutter doctor` للتأكد.
    *   **"الهدف من `flutter doctor`:** يجب أن تظهر علامة صح خضراء بجانب "Flutter", "Android toolchain" (إذا ثبتّم أدوات أندرويد), "Xcode" (إذا كنتم على ماك وثبّتوه), و "Connected device" (عندما نشغل محاكي أو نوصل جهاز حقيقي). لا تقلقوا إذا لم تكن كل العلامات خضراء الآن، سنكمل الإعدادات عند الحاجة."

3.  **كتابة وتشغيل أول برنامج Dart (20 دقيقة)**
    *   "لنجرب لغة Dart بشكل منفصل قبل الدخول في Flutter."
    *   "يمكننا تشغيل كود Dart مباشرة من الـ Terminal أو باستخدام ملفات `.dart`."
    *   **"الطريقة 1: DartPad (أونلاين)"** (إذا كان التثبيت يأخذ وقتًا)
        *   اذهبوا إلى `dartpad.dev`. هذا محرر Dart أونلاين ممتاز للتجارب السريعة.
        *   لاحظوا وجود دالة `main()`. هذه هي نقطة البداية لأي برنامج Dart.
        *   الكود الافتراضي يطبع "Hello, World!". دالة `print()` هي المستخدمة للطباعة في Dart.
        *   اضغطوا `Run` وشوفوا الناتج.
    *   **"الطريقة 2: ملف Dart محلي"**
        *   افتحوا VS Code أو محرركم.
        *   أنشئوا ملفًا جديدًا باسم `hello.dart` (الامتداد مهم).
        *   اكتبوا الكود التالي:
            ```dart
            // The main entry point of the application
            void main() {
              print('Hello, Dart from my computer!'); // Use single quotes for strings (common convention)
            }
            ```
        *   **شرح:**
            *   `void main() { ... }`: تعريف الدالة الرئيسية `main`. `void` تعني أن الدالة لا ترجع أي قيمة.
            *   `print('...');`: طباعة النص المحدد إلى الـ Console. علامات الاقتباس المفردة `'` هي الأكثر شيوعًا للنصوص في Dart، لكن المزدوجة `"` تعمل أيضًا.
            *   **الفاصلة المنقوطة `;` ضرورية** في نهاية كل جملة برمجية في Dart.
        *   **"كيف نشغل الملف؟"**
            *   افتحوا الـ Terminal المدمج في VS Code (أو أي Terminal) وتأكدوا أنكم في نفس المجلد الذي يحتوي على الملف.
            *   اكتبوا الأمر: `dart run hello.dart` (أو فقط `dart hello.dart` في الإصدارات الأقدم).
            *   المفروض تشوفوا الرسالة مطبوعة في الـ Terminal.

4.  **المتغيرات وأنواع البيانات الأساسية في Dart (15 دقائق)**
    *   "مثل PHP، نحتاج لتخزين البيانات في **متغيرات (Variables)**."
    *   **"كيف نعرف متغير؟"**
        *   نستخدم الكلمة المفتاحية `var` إذا أردنا أن Dart تستنتج النوع تلقائيًا (Type Inference).
            ```dart
            var studentName = 'Ahmad'; // Dart infers this is a String
            var studentAge = 21;       // Dart infers this is an int
            ```
        *   أو يمكننا تحديد النوع بشكل صريح (Explicit Typing)، وهذا يُعتبر ممارسة أفضل غالبًا للوضوح وتجنب الأخطاء:
            ```dart
            String courseName = 'Flutter Basics';
            int duration = 6; // months
            double price = 1500.50;
            bool isOnline = true;
            ```
    *   **"أنواع البيانات الأساسية في Dart:"**
        *   **`int`:** أعداد صحيحة (Integers).
        *   **`double`:** أعداد عشرية (Floating-point numbers).
        *   **`String`:** نصوص. يمكن استخدام `'` أو `"`، ويمكن دمج المتغيرات داخل النص باستخدام `$variableName` أو `${expression}` (String Interpolation).
            ```dart
            String greeting = 'Hello, $studentName!'; // Simple variable
            print('Next year, age will be ${studentAge + 1}'); // Expression
            ```
        *   **`bool`:** قيم منطقية (`true` أو `false`).
        *   **`List`:** قائمة مرتبة من العناصر (تشبه المصفوفات Arrays في PHP). `List<String> colors = ['Red', 'Green', 'Blue'];`
        *   **`Map`:** مجموعة من أزواج المفتاح/القيمة (تشبه المصفوفات الترابطية في PHP). `Map<String, dynamic> user = {'name': 'Layla', 'age': 28};`
        *   **`dynamic`:** نوع خاص يعني أن المتغير يمكن أن يحمل أي نوع بيانات، ويتم التحقق منه وقت التشغيل (Runtime). يجب استخدامه بحذر لأنه يفقد ميزة الأمان الخاصة بالأنواع (Type Safety).
    *   **"الثوابت (Constants):"**
        *   `final`: لتعريف متغير لا يمكن تغيير قيمته **بعد إسنادها لأول مرة** (Runtime constant). `final String appVersion = '1.0.2';`
        *   `const`: لتعريف متغير يجب أن تكون قيمته معروفة **وقت الترجمة (Compile-time constant)**. `const double pi = 3.14159;` (تُستخدم للأشياء الثابتة تمامًا).

5.  **العمليات (Operators) في Dart (5 دقائق - مشابهة لـ PHP)**
    *   **الحسابية:** `+`, `-`, `*`, `/` (القسمة ترجع `double`), `~/` (قسمة صحيحة ترجع `int`), `%` (باقي القسمة).
    *   **المقارنة:** `==`, `!=`, `>`, `<`, `>=`, `<=`. (لا يوجد `===` أو `!==` في Dart، فـ `==` تتحقق من القيمة).
    *   **المنطقية:** `&&` (AND), `||` (OR), `!` (NOT).
    *   **الإسناد:** `=`, `+=`, `-=`, `*=`, `/=`, `~/=`, `%=`.
    *   **العمليات الشرطية:** `condition ? expr1 : expr2` (Ternary operator).
    *   **معامل التحقق من Null (`??`):** `String name = maybeNullName ?? 'Guest';` (إذا كان `maybeNullName` يساوي `null`، استخدم `'Guest'`).

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن، لنتأكد من أن بيئة العمل جاهزة عند الجميع ونطبق ما تعلمناه عن Dart."

1.  **التأكد من عمل بيئة Flutter/Dart (30 دقيقة)**
    *   **"مهمة جماعية:"** "شغلوا جميعًا الأمر `flutter doctor -v` (-v يعطي تفاصيل أكثر). لنرى النتائج."
    *   **(مساعدة الطلاب في حل مشاكل `flutter doctor` الشائعة):**
        *   **Flutter SDK not found:** تأكد من إضافة المسار الصحيح لـ `flutter/bin` إلى الـ PATH وإعادة تشغيل الـ Terminal.
        *   **Android toolchain issues:**
            *   Android SDK not found: تأكد من تثبيت Android Studio واختيار تثبيت Android SDK.
            *   CMD tools component missing: افتح Android Studio -> SDK Manager -> SDK Tools -> تأكد من تحديد "Android SDK Command-line Tools" وثبتها.
            *   Android license status unknown: شغل الأمر `flutter doctor --android-licenses` ووافق على كل التراخيص (اكتب `y`).
        *   **Xcode issues (macOS):** تأكد من تثبيت Xcode، وأنه تم تشغيله مرة واحدة على الأقل للموافقة على الشروط، وقد تحتاج لتشغيل `sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer` و `sudo xcodebuild -runFirstLaunch`.
        *   **VS Code/Android Studio issues:** تأكد من تثبيت إضافة Flutter/Dart.
        *   **No connected device:** لا مشكلة الآن، سنتعامل معها لاحقًا عند تشغيل أول تطبيق Flutter.
    *   "الهدف هو الحصول على علامة صح بجانب Flutter و المحرر (VS Code/Android Studio)، وأدوات المنصة التي تودون استهدافها (Android/iOS)."

2.  **تدريب على المتغيرات وأنواع البيانات (25 دقيقة)**
    *   **"مهمة 1:"** "في ملف Dart جديد (مثلاً `dart_basics.dart`)، قوموا بالتالي:"
        *   عرفوا متغيرات لتخزين اسمكم (`String`)، عمركم (`int`)، ومعدلكم الجامعي (`double`).
        *   عرفوا متغير `bool` يحدد إذا كنتم طلابًا حاليين (`true`/`false`).
        *   اطبعوا كل هذه المعلومات باستخدام `print()` وجمل وصفية، استخدموا String Interpolation (`$variable`).
        *   عرفوا متغير `final` لتخزين تاريخ ميلادكم (كنص). حاولوا تغيير قيمته بعد التعريف، ماذا يحدث؟
        *   عرفوا متغير `const` لتخزين اسم الجامعة (إذا كانت ثابتة للجميع).

3.  **تدريب على العمليات (20 دقيقة)**
    *   **"مهمة 2:"** "في نفس الملف:"
        *   عرفوا متغيرين `int`، `num1` و `num2`. اطبعوا ناتج جمعهم، طرحهم، ضربهم، قسمتهم (`/`)، وقسمتهم الصحيحة (`~/`)، وباقي قسمتهم (`%`).
        *   عرفوا متغير للعمر. استخدموا عملية مقارنة للتحقق إذا كان العمر أكبر من أو يساوي 18 واطبعوا النتيجة (`true`/`false`).
        *   عرفوا متغيرين `bool`، `isStudent` و `hasDiscountCoupon`. اكتبوا تعبيرًا منطقيًا باستخدام `||` يطبع `true` إذا كان الشخص طالبًا **أو** لديه كوبون خصم.
        *   استخدموا المعامل الثلاثي (`? :`) لطباعة "Adult" إذا كان العمر >= 18، و "Minor" خلاف ذلك.

4.  **استكشاف DartPad وحل المشكلات (10 دقائق)**
    *   "جربوا استخدام DartPad.dev لكتابة وتنفيذ أكواد Dart بسيطة. إنه مفيد للتجارب السريعة بدون الحاجة للإعداد المحلي."
    *   "ما هي الأخطاء الشائعة التي واجهتكم؟" (نسيان `;`، خطأ في اسم المتغير - Dart حساسة لحالة الأحرف، خطأ في أنواع البيانات عند محاولة إسناد قيمة من نوع خاطئ لمتغير محدد النوع).

5.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1:** اكتب برنامج Dart يحسب مؤشر كتلة الجسم (BMI). عرف متغيرات للطول بالمتر (`double height`) والوزن بالكيلوجرام (`double weight`). المعادلة: BMI = weight / (height * height). اطبع قيمة BMI.
    *   **تمرين 2:** عرف متغيرًا لنص يحتوي على اسمك الأول والأخير. استخدم دوال النصوص في Dart (ابحث عن `string methods dart`) مثل `length` (للطول)، `toUpperCase()` (لتحويله لحروف كبيرة)، `toLowerCase()`، و `contains()` (للتحقق من احتواء النص على كلمة معينة). اطبع نتائج هذه الدوال.
    *   "بداية رائعة في عالم Dart! اليوم جهزنا بيئتنا وعرفنا الأساسيات. في الحصة القادمة، سنتعمق أكثر في Dart ونتعلم عن الدوال، الجمل الشرطية، والحلقات، قبل أن ننتقل إلى Flutter. يعطيكم العافية!"

---

#### **الحصة 2 (الخميس - حصة 18 في المجمل): الدوال Functions، الجمل الشرطية والتكرارات في Dart**

**🎯 أهداف الحصة:**

1.  تعريف واستدعاء الدوال (Functions) في Dart.
2.  فهم أنواع المعاملات (Parameters) في الدوال (Positional, Named, Optional).
3.  استخدام الجمل الشرطية (`if`, `else if`, `else`, `switch`).
4.  استخدام الحلقات التكرارية (`for`, `while`, `do-while`, `for-in`).
5.  تطبيق المفاهيم من خلال تمارين عملية.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم مرة أخرى! في الحصة الماضية، بدأنا رحلتنا مع لغة Dart وتعرفنا على المتغيرات، أنواع البيانات، والعمليات. اليوم، سنبني على هذا الأساس ونتعلم كيف ننظم كودنا بشكل أفضل باستخدام **الدوال (Functions)**، وكيف نتحكم بمسار تنفيذ البرنامج باستخدام **الجمل الشرطية (Conditional Statements)** و**الحلقات التكرارية (Loops)**. هذه المفاهيم مشابهة جدًا لما تعلمناه في PHP، لكن مع بعض الفروقات في بناء الجملة والميزات الإضافية في Dart."

1.  **مراجعة سريعة (5 دقائق)**
    *   "مين يذكرنا بأنواع البيانات الأساسية في Dart؟ وكيف نعرف متغير؟ وكيف نستخدم String Interpolation؟ ممتاز!"

2.  **الدوال (Functions) في Dart (30 دقيقة)**
    *   "ما هي الدالة؟ هي مجموعة من الأوامر (بلوك من الكود) نقوم بتجميعها معًا ونعطيها اسمًا، بحيث يمكننا استدعاؤها (تنفيذها) عدة مرات من أماكن مختلفة في البرنامج، بدلًا من تكرار كتابة نفس الكود."
    *   **"لماذا نستخدم الدوال؟"** تنظيم الكود، إعادة الاستخدام (Reusability)، تسهيل القراءة والصيانة.
    *   **"تعريف دالة بسيطة:"**
        ```dart
        // Function definition
        // returnType functionName(parameterType parameterName, ...) {
        //   // function body
        //   return value; // Optional, if returnType is not void
        // }

        // Example: A function that greets a person
        void greet(String name) { // Takes one String parameter named 'name'
          print('Hello, $name!');
        }

        // Example: A function that adds two numbers and returns the result
        int add(int num1, int num2) { // Takes two int parameters, returns an int
          int sum = num1 + num2;
          return sum;
        }
        ```
    *   **"استدعاء دالة:"** نكتب اسم الدالة متبوعًا بقوسين `()`, ونمرر القيم المطلوبة للمعاملات (Arguments) داخل القوسين.
        ```dart
        void main() {
          greet('Fatima'); // Calling the greet function

          int result = add(5, 3); // Calling add and storing the returned value
          print('The sum is: $result'); // Output: The sum is: 8
        }
        ```
    *   **"أنواع المعاملات (Parameters):"** Dart توفر مرونة كبيرة في تعريف معاملات الدوال:
        *   **1. المعاملات الموضعية الإجبارية (Required Positional Parameters):** مثل `name`, `num1`, `num2` في الأمثلة السابقة. يجب تمرير قيم لها بنفس الترتيب عند استدعاء الدالة.
        *   **2. المعاملات الموضعية الاختيارية (Optional Positional Parameters):** نضعها بين أقواس مربعة `[]`. يجب أن تأتي بعد المعاملات الإجبارية. يمكن إعطاؤها قيمة افتراضية.
            ```dart
            void printAddress(String street, [String? city, String country = 'KSA']) {
               print('Street: $street');
               if (city != null) print('City: $city');
               print('Country: $country');
            }
            // Usage:
            // printAddress('Olaya St'); // city is null, country is 'KSA'
            // printAddress('Olaya St', 'Riyadh'); // country is 'KSA'
            // printAddress('Olaya St', 'Riyadh', 'Saudi Arabia');
            ```
            *   **ملاحظة `String?`:** علامة الاستفهام `?` بعد النوع تعني أن هذا المتغير يمكن أن يحمل القيمة `null` (Null Safety - سنتحدث عنها أكثر لاحقًا).
        *   **3. المعاملات المُسمّاة الاختيارية (Optional Named Parameters):** نضعها بين أقواس معقوفة `{}`. عند استدعاء الدالة، نحدد اسم المعامل متبوعًا بنقطتين `:` ثم القيمة. ترتيبها غير مهم عند الاستدعاء. يمكن جعلها إجبارية باستخدام `required`.
            ```dart
            void setupConnection({String host = 'localhost', int port = 3000, required String user}) {
               print('Connecting to $host:$port as user $user');
            }
            // Usage:
            // setupConnection(user: 'admin'); // host is 'localhost', port is 3000
            // setupConnection(user: 'guest', port: 8080);
            // setupConnection(port: 9000, user: 'test', host: '192.168.1.100');
            ```
        *   **"لماذا المعاملات المسماة مفيدة؟"** تجعل استدعاء الدوال التي لها معاملات كثيرة أوضح وأسهل للقراءة، ولا تحتاج لتذكر الترتيب. وهي شائعة جدًا في Flutter Widgets.
    *   **"دوال السهم (Arrow Functions):"** اختصار لكتابة الدوال التي تحتوي على تعبير واحد فقط يُرجع قيمة.
        ```dart
        // Normal function
        int multiply(int a, int b) {
          return a * b;
        }
        // Arrow function equivalent
        int multiplyArrow(int a, int b) => a * b; // => means 'return'
        ```

3.  **الجمل الشرطية (Conditional Statements) (20 دقيقة - مشابهة لـ PHP/Java/C++)**
    *   "نستخدمها لتنفيذ كود معين فقط إذا تحقق شرط ما."
    *   **`if (condition) { ... }`:**
        ```dart
        int age = 20;
        if (age >= 18) {
          print('Eligible.');
        }
        ```
    *   **`if (condition) { ... } else { ... }`:**
        ```dart
        bool isRaining = true;
        if (isRaining) {
          print('Bring an umbrella!');
        } else {
          print('Enjoy the sunshine!');
        }
        ```
    *   **`if (condition1) { ... } else if (condition2) { ... } else { ... }`:**
        ```dart
        int score = 75;
        if (score >= 90) {
          print('Grade: A');
        } else if (score >= 80) {
          print('Grade: B');
        } else if (score >= 70) {
          print('Grade: C');
        } else {
          print('Grade: F');
        }
        ```
    *   **`switch (variable) { case value: ... break; default: ... }`:**
        *   تُستخدم للمقارنة مع قيم ثابتة (أرقام، نصوص، ثوابت `const`).
        *   **مهم:** في Dart، الـ `case` لا تسقط تلقائيًا (No Fall-through by default). يجب أن ينتهي كل `case` (غير فارغ) بـ `break`, `return`, `throw`, أو `continue` (لقفز لـ label).
        ```dart
        String trafficLight = 'red';
        switch (trafficLight) {
          case 'red':
            print('Stop!');
            break; // Necessary
          case 'yellow':
            print('Prepare to stop.');
            break;
          case 'green':
            print('Go!');
            break;
          default:
            print('Invalid light color.');
        }
        ```

4.  **الحلقات التكرارية (Loops) (30 دقيقة - مشابهة لـ PHP/Java/C++)**
    *   "نستخدمها لتكرار تنفيذ الكود."
    *   **`for` loop:** عندما نعرف عدد التكرارات مسبقًا.
        ```dart
        // Print numbers 0 to 4
        for (int i = 0; i < 5; i++) { // initialization; condition; increment
          print('Current number: $i');
        }
        ```
    *   **`while` loop:** عندما نريد التكرار طالما أن الشرط متحقق (قد لا نعرف عدد المرات).
        ```dart
        int count = 0;
        while (count < 3) {
          print('While count: $count');
          count++; // Don't forget to change the condition variable!
        }
        ```
    *   **`do-while` loop:** مشابه لـ `while`، لكن الكود داخل الحلقة يتنفذ **مرة واحدة على الأقل** قبل التحقق من الشرط.
        ```dart
        int number;
        do {
          number = DateTime.now().second; // Get current second
          print('Checking second: $number');
        } while (number % 10 != 0); // Repeat until second is a multiple of 10
        print('Found a second ending in 0!');
        ```
    *   **`for-in` loop:** طريقة سهلة للمرور على عناصر قائمة (List) أو مجموعة (Set).
        ```dart
        List<String> fruits = ['Apple', 'Banana', 'Orange'];
        print('Available fruits:');
        for (String fruit in fruits) {
          print('- $fruit');
        }

        // Can also use forEach method (more functional style)
        // fruits.forEach((fruit) => print('* $fruit'));
        ```
    *   **`break` و `continue`:**
        *   `break`: للخروج من الحلقة الحالية فورًا (سواء `for`, `while`, `do-while`, `switch`).
        *   `continue`: لتخطي باقي الأوامر في التكرار الحالي والانتقال للتكرار التالي مباشرة.

5.  **مراجعة وختام الجزء الأول (5 دقائق)**
    *   "إذًا، تعلمنا اليوم كيف نعرف ونستخدم الدوال بأنواع معاملاتها المختلفة في Dart، وكيف نستخدم الجمل الشرطية (`if`, `switch`) والحلقات (`for`, `while`, `do-while`, `for-in`) للتحكم بمسار التنفيذ. هذه هي الأدوات الأساسية للتحكم بمنطق البرنامج."
    *   "أي أسئلة على بناء الجملة أو المفاهيم؟"

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن نطبق الدوال والشروط والحلقات في تمارين عملية بلغة Dart."

1.  **تدريب على الدوال (30 دقيقة)**
    *   **"مهمة 1 (دالة بسيطة):"** "اكتبوا دالة اسمها `calculateArea` تأخذ الطول (`int width`) والعرض (`int height`) كمعاملات موضعية إجبارية، وتحسب المساحة (`width * height`) وترجعها (`int`). استدعوا الدالة في `main` واطبعوا النتيجة."
    *   **"مهمة 2 (معاملات اختيارية ومسماة):"** "اكتبوا دالة اسمها `printUserDetails`."
        *   تأخذ الاسم (`String name`) كمعامل موضعي إجباري.
        *   تأخذ العمر (`int? age`) كمعامل مسمى اختياري (قد يكون `null`).
        *   تأخذ المدينة (`String city`) كمعامل مسمى اختياري له قيمة افتراضية `'Unknown'`.
        *   الدالة تطبع الاسم، والعمر (فقط إذا لم يكن `null`)، والمدينة.
        *   استدعوا الدالة في `main` بعدة طرق مختلفة (مرة بالاسم فقط، مرة بالاسم والعمر، مرة بكل المعاملات).
    *   **"مهمة 3 (Arrow Function):"** "حولوا دالة `calculateArea` من المهمة 1 إلى صيغة دالة السهم (`=>`)."

2.  **تدريب على الجمل الشرطية (25 دقيقة)**
    *   **"مهمة 4 (if/else if/else):"** "اكتبوا برنامجًا يأخذ متغيرًا رقميًا يمثل درجة الحرارة (`double temperature`)."
        *   إذا كانت أقل من 10، اطبع "Very Cold".
        *   إذا كانت بين 10 و 20 (شاملة)، اطبع "Cool".
        *   إذا كانت بين 20 و 30 (شاملة)، اطبع "Warm".
        *   إذا كانت أعلى من 30، اطبع "Hot".
    *   **"مهمة 5 (switch):"** "اكتبوا برنامجًا يأخذ متغيرًا نصيًا يمثل يومًا من أيام الأسبوع (`String day`). استخدموا `switch` لطباعة "Weekend" إذا كان اليوم 'Friday' أو 'Saturday'، وطباعة "Weekday" لباقي الأيام (أو `default`)."

3.  **تدريب على الحلقات التكرارية (25 دقيقة)**
    *   **"مهمة 6 (for):"** "استخدموا حلقة `for` لطباعة مربعات الأعداد من 1 إلى 10 (1*1, 2*2, ..., 10*10)."
    *   **"مهمة 7 (while):"** "اكتبوا برنامجًا يبدأ بمتغير `int energyLevel = 5;`. استخدموا حلقة `while` لطباعة مستوى الطاقة الحالي طالما أنه أكبر من 0، وفي كل تكرار أنقصوا مستوى الطاقة بمقدار 1."
    *   **"مهمة 8 (for-in):"** "عرفوا قائمة (`List`) تحتوي على أسماء بعض المدن السعودية. استخدموا حلقة `for-in` لطباعة كل اسم مدينة من القائمة."
    *   **"تحدي (break/continue):"** "عدلوا المهمة 8. داخل الحلقة، إذا كان اسم المدينة هو 'Riyadh'، اطبعوا رسالة خاصة وتوقفوا عن الحلقة (`break`). وإذا كان اسم المدينة هو 'Jeddah'، تخطوا طباعتها وانتقلوا للتكرار التالي (`continue`)."

4.  **حل المشكلات ومناقشة (10 دقائق)**
    *   "هل واجهتكم أخطاء في بناء الدوال أو استدعائها؟ أخطاء في الشروط أو الحلقات؟"
    *   "متى يكون استخدام المعاملات المسماة `{}` أفضل من الموضعية `[]`؟"
    *   "ما الفرق بين `while` و `do-while` عمليًا؟"
    *   "أهمية Null Safety (`?`) عند تعريف المعاملات الاختيارية التي قد لا تُمرر."

5.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1 (دالة Factorial):** اكتب دالة `int factorial(int n)` تحسب مضروب العدد `n` (n! = n * (n-1) * ... * 1). استخدم حلقة `for` أو `while`. تعامل مع حالة `n=0` (مضروب الصفر هو 1).
    *   **تمرين 2 (FizzBuzz in Dart):** اكتب برنامجًا يطبع الأرقام من 1 إلى 30. لكن:
        *   إذا كان الرقم يقبل القسمة على 3، اطبع "Fizz".
        *   إذا كان يقبل القسمة على 5، اطبع "Buzz".
        *   إذا كان يقبل القسمة على 3 و 5 معًا، اطبع "FizzBuzz".
        *   غير ذلك، اطبع الرقم نفسه. (استخدم `%` و `if/else if/else`).
    *   "رائع! اليوم عززنا معرفتنا بلغة Dart وأصبحنا قادرين على كتابة برامج أكثر تعقيدًا ومنطقية. في الحصة القادمة، سندخل في مفهوم هام جدًا وهو البرمجة الكائنية (OOP) في Dart، والتي تعتبر أساسية لفهم كيفية بناء تطبيقات Flutter. يعطيكم العافية!"

---


#### **الحصة 3 (الاثنين - حصة 19 في المجمل): البرمجة الكائنية OOP في Dart**

**🎯 أهداف الحصة:**

1.  فهم المفاهيم الأساسية للبرمجة الكائنية (Class, Object, Encapsulation, Inheritance, Polymorphism).
2.  تعريف واستخدام الكلاسات (Classes) والكائنات (Objects) في Dart.
3.  فهم وإنشاء المُنشِئات (Constructors) بأنواعها المختلفة.
4.  تعريف واستخدام الدوال (Methods) داخل الكلاسات.
5.  فهم مبدأ الوراثة (Inheritance) وتطبيقه باستخدام `extends`.
6.  مقدمة بسيطة للـ Mixins.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم مجددًا! في الحصص السابقة، تعلمنا أساسيات لغة Dart مثل المتغيرات، الدوال، والشروط والحلقات. اليوم، سندخل في مفهوم أساسي ومهم جدًا ليس فقط في Dart ولكن في معظم لغات البرمجة الحديثة، وهو **البرمجة الكائنية التوجه (Object-Oriented Programming - OOP)**. فهم OOP ضروري جدًا لفهم كيفية بناء تطبيقات Flutter، لأن Flutter نفسه مبني بشكل كبير على هذه المفاهيم."

1.  **مراجعة سريعة (5 دقائق)**
    *   "كيف نعرف دالة في Dart؟ وما هي أنواع المعاملات التي تعلمناها؟ وكيف نستخدم `for-in` loop؟ ممتاز!"

2.  **مقدمة للبرمجة الكائنية (OOP) (20 دقيقة)**
    *   **"ما هي OOP؟"** هي طريقة أو أسلوب لتنظيم وكتابة الكود تعتمد على مفهوم "الكائنات" (Objects). بدلًا من التفكير في البرنامج كمجموعة من الدوال والإجراءات المنفصلة، نفكر فيه كمجموعة من الكائنات التي تتفاعل مع بعضها البعض.
    *   **"لماذا OOP؟"**
        *   **محاكاة العالم الحقيقي:** تجعل الكود أقرب لطريقة تفكيرنا في الأشياء من حولنا (سيارة، طالب، حساب بنكي...).
        *   **التنظيم وإعادة الاستخدام:** الكود يصبح أكثر تنظيمًا في وحدات (كلاسات)، مما يسهل إعادة استخدامها في أجزاء مختلفة من المشروع أو في مشاريع أخرى.
        *   **الصيانة والتطوير:** التعديل على جزء (كائن) لا يؤثر بشكل مباشر على الأجزاء الأخرى (طالما الواجهات بينها لم تتغير)، مما يسهل الصيانة والتطوير.
        *   **إخفاء التعقيد (Encapsulation):** يمكن للكائن إخفاء تفاصيله الداخلية المعقدة وعرض واجهة بسيطة للتعامل معه.
    *   **"المفاهيم الأساسية في OOP:"**
        *   **الكلاس (Class):** هو **المخطط (Blueprint)** أو القالب الذي نستخدمه لإنشاء الكائنات. يحدد الخصائص (Properties/Attributes - تشبه المتغيرات) والسلوكيات (Methods - تشبه الدوال) التي ستمتلكها الكائنات من هذا النوع. (مثال: مخطط بناء سيارة).
        *   **الكائن (Object):** هو **نسخة (Instance)** حقيقية وملموسة من الكلاس. لكل كائن حالته الخاصة (قيم خصائصه قد تختلف عن كائنات أخرى من نفس الكلاس). (مثال: سيارتي الفعلية هي كائن من كلاس "سيارة").
        *   **التغليف (Encapsulation):** تجميع البيانات (الخصائص) والعمليات التي تتعامل معها (الدوال) داخل وحدة واحدة (الكلاس)، مع إمكانية إخفاء التفاصيل الداخلية (Implementation Hiding).
        *   **الوراثة (Inheritance):** آلية تسمح لكلاس (الابن/Subclass) بأن يرث الخصائص والسلوكيات من كلاس آخر (الأب/Superclass)، مع إمكانية إضافة خصائص وسلوكيات جديدة أو تعديل الموروثة. (مثال: كلاس "سيارة كهربائية" يرث من كلاس "سيارة").
        *   **تعدد الأشكال (Polymorphism):** قدرة الكائنات من كلاسات مختلفة (مرتبطة بالوراثة) على الاستجابة لنفس الرسالة (استدعاء نفس الدالة) بطرق مختلفة تناسب كل كائن. (مثال: دالة `move()` قد تعمل بشكل مختلف للسيارة والطائرة).

3.  **الكلاسات (Classes) والكائنات (Objects) في Dart (30 دقيقة)**
    *   **"تعريف كلاس بسيط:"** نستخدم الكلمة المفتاحية `class`.
        ```dart
        // Define a blueprint for a Car
        class Car {
          // Properties (Instance Variables)
          String make = 'Unknown'; // Default value
          String model;
          int year;
          bool isEngineOn = false;

          // Constructor (Special method for creating objects)
          // We'll discuss constructors in detail next
          Car(this.model, this.year); // Shorthand constructor

          // Methods (Functions inside the class)
          void startEngine() {
            if (!isEngineOn) {
              isEngineOn = true;
              print('$make $model engine started!');
            } else {
              print('Engine is already running.');
            }
          }

          void stopEngine() {
            if (isEngineOn) {
              isEngineOn = false;
              print('$make $model engine stopped.');
            } else {
              print('Engine is already off.');
            }
          }

          void displayInfo() {
            print('Car Info: $year $make $model, Engine On: $isEngineOn');
          }
        }
        ```
    *   **شرح:**
        *   `class Car { ... }`: تعريف الكلاس.
        *   `String make = 'Unknown';`, `String model;`, `int year;`, `bool isEngineOn = false;`: تعريف الخصائص (المتغيرات) التي سيحملها كل كائن `Car`. `make` و `isEngineOn` لهما قيم افتراضية.
        *   `Car(this.model, this.year);`: هذا **مُنشِئ (Constructor)**. هو دالة خاصة تُستدعى تلقائيًا عند إنشاء كائن جديد من الكلاس. الصيغة `this.propertyName` في المعاملات هي اختصار لـ: "خذ القيمة المُمررة للمُنشِئ وأسندها للخاصية التي تحمل نفس الاسم في هذا الكائن".
        *   `void startEngine() { ... }`, `void stopEngine() { ... }`, `void displayInfo() { ... }`: هذه **الدوال (Methods)**. هي سلوكيات يمكن لكائن `Car` القيام بها. يمكنها الوصول وتعديل خصائص الكائن نفسه (مثل `isEngineOn`).
    *   **"إنشاء كائن (Object Instance):"** نستخدم اسم الكلاس متبوعًا بقوسين `()` ونمرر القيم المطلوبة للمُنشِئ.
        ```dart
        void main() {
          // Create a Car object (instance)
          var myCar = Car('Toyota Camry', 2022); // Calls the constructor

          // Access properties using the dot (.) operator
          myCar.make = 'Toyota'; // We can assign values if they are public

          // Call methods using the dot (.) operator
          myCar.displayInfo(); // Output: Car Info: 2022 Toyota Camry, Engine On: false
          myCar.startEngine();   // Output: Toyota Camry engine started!
          myCar.displayInfo(); // Output: Car Info: 2022 Toyota Camry, Engine On: true
          myCar.stopEngine();    // Output: Toyota Camry engine stopped.

          // Create another Car object
          var friendsCar = Car('Honda Civic', 2021);
          friendsCar.make = 'Honda';
          friendsCar.startEngine();
          friendsCar.displayInfo(); // Output: Car Info: 2021 Honda Civic, Engine On: true
        }
        ```

4.  **المُنـشِئات (Constructors) (20 دقيقة)**
    *   "المُنشئ هو المسؤول عن تهيئة (Initializing) حالة الكائن عند إنشائه."
    *   **المُنشئ الافتراضي (Default Constructor):** إذا لم نكتب أي مُنشئ، Dart توفر مُنشئ افتراضي فارغ لا يأخذ معاملات.
    *   **المُنشئ المُعرّف (Custom Constructor):** مثل `Car(this.model, this.year);` الذي رأيناه.
    *   **المُنشئات المُسمّاة (Named Constructors):** كلاس واحد يمكن أن يكون له عدة مُنشئات مختلفة باستخدام أسماء. مفيدة لتوفير طرق مختلفة لإنشاء الكائن.
        ```dart
        class Point {
          double x = 0;
          double y = 0;

          // Default constructor
          Point(this.x, this.y);

          // Named constructor for origin (0, 0)
          Point.origin() {
            // No need to assign x and y, they default to 0
          }

          // Named constructor from a Map
          Point.fromMap(Map<String, double> map) {
            x = map['x'] ?? 0; // Use ?? for default if key is missing
            y = map['y'] ?? 0;
          }
        }

        void main() {
          var p1 = Point(3, 4);
          var p2 = Point.origin();
          var p3 = Point.fromMap({'x': 5.5, 'y': -1.2});
          print('P2: x=${p2.x}, y=${p2.y}'); // Output: P2: x=0, y=0
        }
        ```
    *   **`this` Keyword:** تشير إلى الكائن الحالي (Instance) داخل الكلاس. نستخدمها للوصول للخصائص والدوال الخاصة بنفس الكائن، أو للتمييز بين معامل الدالة وخاصية الكائن إذا كان لهما نفس الاسم.

5.  **الوراثة (Inheritance) (10 دقيقة)**
    *   "تسمح لكلاس بأن يرث من كلاس آخر."
    *   نستخدم الكلمة المفتاحية `extends`.
    *   الكلاس الابن (Subclass) يرث كل الخصائص والدوال العامة (Public) من الكلاس الأب (Superclass).
    *   يمكن للابن إضافة خصائص ودوال جديدة.
    *   يمكن للابن **تجاوز (Override)** سلوك دالة موروثة باستخدام `@override` annotation (تُعتبر ممارسة جيدة).
    *   نستخدم `super` للوصول إلى أعضاء الكلاس الأب (خاصةً المُنشئ أو الدوال التي تم تجاوزها).
        ```dart
        // Superclass (Parent)
        class Vehicle {
          String color;
          Vehicle(this.color);

          void move() {
            print('Vehicle is moving.');
          }
        }

        // Subclass (Child)
        class Bicycle extends Vehicle {
          int gears;

          // Constructor must call the parent constructor using 'super'
          Bicycle(String color, this.gears) : super(color);

          @override // Good practice to indicate overriding
          void move() {
            super.move(); // Optionally call the parent's method
            print('The $color bicycle with $gears gears is cycling!');
          }
        }

        void main() {
          var bike = Bicycle('Red', 18);
          bike.move();
          print('Bike color: ${bike.color}'); // Inherited property
        }
        ```

6.  **مقدمة للـ Mixins (اختياري - 5 دقائق)**
    *   "Dart لا تدعم الوراثة المتعددة (كلاس يرث من أكثر من كلاس أب). Mixins هي طريقة لإعادة استخدام كود كلاس في عدة كلاسات غير مرتبطة ببعضها بشكل هرمي."
    *   نستخدم الكلمة المفتاحية `mixin` لتعريف mixin، و `with` لتضمينه في كلاس.
        ```dart
        mixin Flyer {
          void fly() {
            print("I'm flying!");
          }
        }

        class Bird with Flyer {
          // ... bird properties and methods
        }
        class Superhero with Flyer {
          // ... superhero properties and methods
        }
        ```
    *   (فقط للإشارة للمفهوم، سنستخدمها لاحقًا عند الحاجة).

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن نطبق مفاهيم OOP بإنشاء كلاساتنا الخاصة."

1.  **تدريب على إنشاء كلاس وكائنات (30 دقيقة)**
    *   **"المهمة 1:"** "أنشئوا كلاسًا جديدًا اسمه `Student` في ملف Dart."
        *   أضيفوا له الخصائص التالية: `String name`, `int id`, `List<double> grades` (قائمة لدرجات الطالب).
        *   أنشئوا مُنشِئًا (Constructor) يأخذ الاسم والـ ID كمعاملات إجبارية، ويهيئ قائمة الدرجات `grades` لتكون قائمة فارغة (`grades = [];`).
        *   أضيفوا دالة (method) اسمها `addGrade(double grade)` تقوم بإضافة درجة جديدة لقائمة `grades`.
        *   أضيفوا دالة اسمها `displayInfo()` تطبع اسم الطالب ورقمه.
    *   **"في دالة `main`:"**
        *   أنشئوا كائنًا واحدًا على الأقل من كلاس `Student`.
        *   استخدموا دالة `addGrade` لإضافة بعض الدرجات لهذا الطالب.
        *   استخدموا دالة `displayInfo` لطباعة معلوماته.
        *   حاولوا الوصول لخاصية `grades` مباشرة وطباعتها (`print(studentObject.grades);`).

2.  **تدريب على حساب المعدل والمنشئات المسماة (30 دقيقة)**
    *   **"المهمة 2:"** "نطور كلاس `Student`:"
        *   أضيفوا دالة جديدة اسمها `calculateAverageGrade()` تقوم بحساب متوسط الدرجات الموجودة في قائمة `grades` وترجعه كـ `double`. (إذا كانت القائمة فارغة، يمكن أن ترجع 0 أو `null`). (تلميح: استخدم حلقة `for-in` لجمع الدرجات واقسمها على عددها `grades.length`).
        *   عدلوا دالة `displayInfo()` لتطبع المعدل أيضًا (بعد استدعاء `calculateAverageGrade`).
        *   أضيفوا مُنشِئًا مسمى (Named Constructor) اسمه `Student.withGrades` يأخذ الاسم والـ ID وقائمة من الدرجات الأولية كمعاملات.
    *   **"في دالة `main`:"**
        *   أنشئوا كائن `Student` جديد باستخدام المُنشئ المسمى `Student.withGrades` مع بعض الدرجات الأولية.
        *   اطبعوا معلوماته ومعدله.

3.  **تدريب على الوراثة (20 دقيقة)**
    *   **"المهمة 3:"** "أنشئوا كلاسًا جديدًا اسمه `GraduateStudent` يرث من كلاس `Student` (`GraduateStudent extends Student`)."
        *   أضيفوا خاصية جديدة لـ `GraduateStudent` اسمها `String thesisTitle`.
        *   أنشئوا مُنشِئًا لـ `GraduateStudent` يأخذ الاسم والـ ID وعنوان الرسالة (`thesisTitle`)، ويتأكد من استدعاء مُنشئ الكلاس الأب (`super(name, id)`).
        *   قوموا بتجاوز (`@override`) دالة `displayInfo()` في `GraduateStudent` بحيث تطبع "Graduate Student:" متبوعًا بالاسم والرقم، ثم تطبع عنوان الرسالة في سطر جديد.
    *   **"في دالة `main`:"**
        *   أنشئوا كائنًا من `GraduateStudent`.
        *   استدعوا دالة `displayInfo()` الخاصة به ولاحظوا كيف تطبع المعلومات الإضافية.
        *   هل يمكنكم استدعاء `addGrade()` على كائن `GraduateStudent`؟ (نعم، لأنها موروثة).

4.  **حل المشكلات ومناقشة (10 دقائق)**
    *   "ما هي الصعوبات التي واجهتكم في تعريف الكلاسات أو المنشئات؟"
    *   "كيف يعمل `this` و `super`؟"
    *   "ما فائدة `@override`؟"
    *   "مفهوم Encapsulation في Dart باستخدام `_` (underscore) قبل اسم الخاصية أو الدالة لجعلها خاصة (Private) للمكتبة (Library) التي تنتمي لها." (شرح سريع).

5.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1:** أنشئ كلاس `BankAccount` بالخصائص `accountNumber` (String), `ownerName` (String), و `_balance` (double - اجعله خاصًا باستخدام `_`). أضف دوال `deposit(double amount)` و `withdraw(double amount)` (تأكد من أن الرصيد كافٍ للسحب). أضف دالة `getBalance()` (Getter) لقراءة الرصيد الخاص. أنشئ كائنًا وجرب العمليات.
    *   **تمرين 2 (بحث):** ابحثوا عن الكلمات المفتاحية `abstract class` و `interface` (التي يتم تمثيلها ضمنيًا بالكلاسات العادية) في Dart. ما هو الفرق بينهما وكيف يُستخدمان؟
    *   "ممتاز! اليوم غطينا المفاهيم الأساسية للبرمجة الكائنية في Dart، وهي حجر الزاوية في بناء تطبيقات Flutter. في الحصة القادمة، سنبدأ أخيرًا بالتعامل مع Flutter نفسه، ننشئ أول تطبيق لنا، ونفهم هيكل المشروع. استعدوا للدخول في عالم الـ Widgets! يعطيكم العافية!"

---

#### **الحصة 4 (الخميس - حصة 20 في المجمل): مقدمة Flutter، تثبيت البيئة، أول تطبيق**

**🎯 أهداف الحصة:**

1.  إعادة التأكيد على مفاهيم Flutter الأساسية (UI Toolkit, Cross-platform, Widgets).
2.  إنشاء أول مشروع Flutter باستخدام الأمر `flutter create`.
3.  فهم هيكل مجلدات وملفات مشروع Flutter الأساسي (`lib`, `pubspec.yaml`, `android`, `ios`).
4.  تشغيل التطبيق الافتراضي (Counter App) على محاكي (Emulator) أو جهاز حقيقي.
5.  فهم وشرح الكود الأساسي في `main.dart` ( `runApp`, `MaterialApp`, `Scaffold`, `StatelessWidget`, `StatefulWidget`).
6.  التعرف على مفهوم Hot Reload و Hot Restart وتجربتهما.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً وسهلاً بكم في لحظة حاسمة! اليوم، سننتقل من تعلم لغة Dart إلى استخدامها لبناء تطبيقات موبايل حقيقية باستخدام **Flutter**. سنقوم بإنشاء أول مشروع Flutter لنا، نفهم مكوناته الأساسية، ونشغله على الشاشة أمامنا. استعدوا لرؤية الكود يتحول إلى واجهة مستخدم!"

1.  **مراجعة سريعة (5 دقائق)**
    *   "ما هي المفاهيم الأساسية للـ OOP التي تعلمناها؟ (Class, Object, Inheritance...). وكيف نعرف كلاس ومنشئ في Dart؟ ممتاز، سنستخدم هذا كثيرًا في Flutter."

2.  **إعادة تعريف Flutter ومفاهيمه الأساسية (10 دقائق)**
    *   **Flutter هو UI Toolkit:** مجموعة أدوات لبناء واجهات المستخدم. لا يوفر كل شيء (مثل الوصول المباشر للكاميرا أو GPS)، لكنه يركز على بناء الواجهة ويتكامل مع إمكانيات المنصة الأصلية.
    *   **Cross-Platform:** كود واحد يعمل على Android, iOS, Web, Desktop.
    *   **Everything is a Widget:** هذه هي الفلسفة الأساسية. الواجهة بأكملها، وكل جزء فيها (زر، نص، صورة، قائمة، حتى التخطيط والمساحات الفارغة) هو عبارة عن Widget.
    *   **Widget Tree:** الواجهة تُبنى كهرم أو شجرة من الـ Widgets المتداخلة.
    *   **Declarative UI:** أنت تصف كيف يجب أن تبدو الواجهة بناءً على الحالة (State) الحالية، و Flutter يتكفل بتحديث الواجهة عند تغير الحالة. (عكس Imperative UI حيث تخبر الواجهة خطوة بخطوة كيف تتغير).

3.  **إنشاء أول مشروع Flutter (15 دقيقة)**
    *   "لنقم بإنشاء مشروع جديد باستخدام أمر Flutter CLI."
    *   **الخطوات:**
        1.  افتحوا الـ Terminal أو Command Prompt.
        2.  انتقلوا إلى المجلد الذي تريدون إنشاء المشروع بداخله (مثلاً مجلد `Desktop` أو مجلد خاص بمشاريعكم). استخدموا أمر `cd` للتنقل.
        3.  اكتبوا الأمر التالي:
            ```bash
            flutter create my_first_app
            ```
            (استبدلوا `my_first_app` بأي اسم مناسب للمشروع، يفضل استخدام حروف صغيرة وشرطة سفلية `_`).
        4.  انتظروا قليلاً حتى يقوم Flutter بإنشاء كل الملفات والمجلدات اللازمة وإعداد المشروع. ستظهر رسالة "All done!".
        5.  **فتح المشروع في المحرر:**
            *   **VS Code:** افتحوا VS Code، ثم File -> Open Folder... واختاروا المجلد `my_first_app` الذي تم إنشاؤه.
            *   **Android Studio:** افتحوا Android Studio، اختاروا Open an Existing Project واختاروا مجلد `my_first_app`.

4.  **فهم هيكل مشروع Flutter (20 دقيقة)**
    *   "عند فتح المشروع، ستجدون العديد من الملفات والمجلدات. لنتعرف على أهمها:"
    *   **`lib/`:** ***هذا هو المجلد الأهم*** الذي سنقضي معظم وقتنا فيه. يحتوي على كود Dart الخاص بتطبيقنا.
        *   **`main.dart`:** نقطة البداية الرئيسية لتطبيق Flutter. يحتوي على دالة `main()` التي تشغل التطبيق.
    *   **`pubspec.yaml`:** ***ملف مهم جدًا!***
        *   هو ملف إعدادات المشروع. يشبه `package.json` في Node.js أو `composer.json` في PHP.
        *   يحتوي على: اسم التطبيق، الوصف، الإصدار.
        *   **`dependencies:`:** قائمة بالمكتبات الخارجية (Packages) التي يعتمد عليها تطبيقنا (مثل مكتبة للتعامل مع HTTP، أو لإدارة الحالة).
        *   **`dev_dependencies:`:** مكتبات تُستخدم فقط أثناء التطوير (مثل أدوات الاختبار أو بناء الكود).
        *   **`flutter:`:** إعدادات خاصة بـ Flutter، مثل:
            *   `uses-material-design: true`: تفعيل استخدام عناصر Material Design.
            *   `assets:`: لتحديد مجلدات الصور والملفات الأخرى التي نريد تضمينها في التطبيق.
        *   **مهم:** بعد أي تعديل على `pubspec.yaml` (مثل إضافة dependency جديدة)، يجب تشغيل الأمر `flutter pub get` في الـ Terminal (أو المحرر غالبًا يقوم بذلك تلقائيًا) لتنزيل وتحديث الاعتماديات.
    *   **`android/`:** يحتوي على مشروع Android الأصلي. Flutter يستخدم هذا المجلد لبناء نسخة Android من التطبيق. نادرًا ما نحتاج لتعديله مباشرة إلا لإعدادات خاصة بالمنصة.
    *   **`ios/`:** يحتوي على مشروع Xcode الأصلي لبناء نسخة iOS. أيضًا نادرًا ما نعدله مباشرة.
    *   **`web/`, `windows/`, `linux/`, `macos/`:** مجلدات بناء المنصات الأخرى (إذا كانت مفعلة).
    *   **`test/`:** يحتوي على ملفات اختبارات الكود (Unit tests, Widget tests).

5.  **تشغيل التطبيق الافتراضي (Counter App) (20 دقيقة)**
    *   "الآن، لنشغل التطبيق الذي أنشأه Flutter افتراضيًا."
    *   **"الخطوات:"**
        1.  **اختيار جهاز (Device):** نحتاج لجهاز لتشغيل التطبيق عليه. يمكن أن يكون:
            *   **محاكي Android (Emulator):**
                *   افتحوا Android Studio -> Device Manager (أو AVD Manager).
                *   أنشئوا جهازًا افتراضيًا جديدًا (Virtual Device) إذا لم يكن لديكم واحد (اختاروا نوع جهاز وإصدار نظام Android).
                *   شغلوا المحاكي الذي تم إنشاؤه.
            *   **محاكي iOS (Simulator - يتطلب macOS و Xcode):**
                *   يمكن تشغيله من Xcode أو باستخدام الأمر `open -a Simulator` في الـ Terminal.
            *   **جهاز Android حقيقي:**
                *   فعلوا خيارات المطور (Developer Options) وتصحيح USB (USB Debugging) على هاتفكم (ابحثوا عن الطريقة الخاصة بهاتفكم).
                *   وصلوا الهاتف بالكمبيوتر عبر USB. وافقوا على أي طلبات للثقة بالكمبيوتر.
            *   **جهاز iPhone/iPad حقيقي (يتطلب macOS و Xcode وإعدادات توقيع):** عملية الإعداد أعقد قليلاً.
        2.  **التأكد من رؤية الجهاز:** في VS Code أو Android Studio، يجب أن يظهر اسم المحاكي أو الجهاز المتصل في شريط الحالة السفلي. يمكنكم الضغط عليه لاختيار جهاز مختلف إذا كان لديكم أكثر من واحد متاح.
        3.  **تشغيل التطبيق:**
            *   **VS Code:** اضغط `F5` أو اذهب إلى Run -> Start Debugging.
            *   **Android Studio:** اضغط على زر التشغيل الأخضر ( المثلث).
            *   **Terminal:** يمكن تشغيله من الـ Terminal باستخدام الأمر `flutter run`.
        4.  **انتظار البناء:** أول مرة يتم فيها تشغيل التطبيق، قد يستغرق بعض الوقت لعملية البناء (Build).
        5.  **رؤية التطبيق:** المفروض أن يظهر التطبيق الافتراضي (تطبيق عداد بسيط مع زر زيادة) على المحاكي أو الجهاز.
    *   **(مساعدة الطلاب في إعداد المحاكيات والأجهزة وتشغيل التطبيق الأول).**

6.  **فهم الكود الأساسي (`main.dart`) (15 دقيقة)**
    *   "لنلقي نظرة سريعة على الكود في `lib/main.dart`:"
        ```dart
        import 'package:flutter/material.dart'; // Import package for Material Design widgets

        void main() {
          runApp(const MyApp()); // Entry point: run the root widget
        }

        // Root Widget: Usually StatelessWidget or StatefulWidget
        class MyApp extends StatelessWidget {
          const MyApp({super.key}); // Constructor

          @override
          Widget build(BuildContext context) {
            return MaterialApp( // Provides basic app structure (Material Design)
              title: 'Flutter Demo',
              theme: ThemeData( // App theme configuration
                colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple),
                useMaterial3: true,
              ),
              home: const MyHomePage(title: 'Flutter Demo Home Page'), // The first screen
            );
          }
        }

        // Home Screen Widget: StatefulWidget because its state (counter) changes
        class MyHomePage extends StatefulWidget {
          const MyHomePage({super.key, required this.title});
          final String title;

          @override
          State<MyHomePage> createState() => _MyHomePageState(); // Create state object
        }

        // State object associated with MyHomePage
        class _MyHomePageState extends State<MyHomePage> {
          int _counter = 0; // State variable

          void _incrementCounter() {
            setState(() { // IMPORTANT: Tells Flutter the state has changed, rebuild UI
              _counter++;
            });
          }

          @override
          Widget build(BuildContext context) {
            // This method is rerun by Flutter when setState is called
            return Scaffold( // Basic page structure (AppBar, Body)
              appBar: AppBar(
                backgroundColor: Theme.of(context).colorScheme.inversePrimary,
                title: Text(widget.title), // Access title from the StatefulWidget
              ),
              body: Center( // Center content horizontally and vertically
                child: Column( // Arrange children vertically
                  mainAxisAlignment: MainAxisAlignment.center, // Center vertically
                  children: <Widget>[
                    const Text(
                      'You have pushed the button this many times:',
                    ),
                    Text(
                      '$_counter', // Display the counter state variable
                      style: Theme.of(context).textTheme.headlineMedium,
                    ),
                  ],
                ),
              ),
              floatingActionButton: FloatingActionButton( // The '+' button
                onPressed: _incrementCounter, // Call the method when pressed
                tooltip: 'Increment',
                child: const Icon(Icons.add),
              ),
            );
          }
        }
        ```
    *   **شرح سريع:**
        *   `import`: لجلب مكتبات أو ملفات أخرى. `material.dart` أساسي لتطبيقات Material Design.
        *   `main()` و `runApp()`: نقطة البداية، تشغل الـ Widget الجذر (`MyApp`).
        *   `MyApp (StatelessWidget)`: الـ Widget الجذر، يمثل التطبيق ككل. يبني `MaterialApp`.
        *   `MaterialApp`: يوفر بنية التطبيق الأساسية (Theme, Routing...). يحدد الشاشة الرئيسية (`home`).
        *   `MyHomePage (StatefulWidget)`: الشاشة الرئيسية. هي StatefulWidget لأن محتواها (العداد) يتغير. ترتبط بكائن State (`_MyHomePageState`).
        *   `_MyHomePageState`: يحتوي على **الحالة (State)** المتغيرة (`_counter`) ودالة لتغييرها (`_incrementCounter`).
        *   **`setState(() { ... });`**: ***هذه أهم دالة في StatefulWidget!*** عندما تتغير الحالة (مثل زيادة `_counter`)، يجب أن نستدعي `setState`. هذا يخبر Flutter أن الحالة تغيرت، فيقوم بإعادة استدعاء دالة `build` لتحديث الواجهة بناءً على الحالة الجديدة.
        *   `build()`: تبني واجهة الشاشة باستخدام Widgets أخرى مثل `Scaffold`, `AppBar`, `Center`, `Column`, `Text`.
        *   `Scaffold`: يوفر هيكل الصفحة الشائع (AppBar في الأعلى، Body في المنتصف، FloatingActionButton...).

7.  **Hot Reload و Hot Restart (5 دقائق)**
    *   "Flutter يوفر ميزتين رائعتين لتسريع التطوير:"
    *   **Hot Reload (البرق الأصفر ⚡️):**
        *   يقوم بحقن الكود الجديد في التطبيق **دون إعادة تشغيله بالكامل ودون فقدان حالته الحالية**.
        *   سريع جدًا (أقل من ثانية غالبًا).
        *   مثالي لتجربة التغييرات في الواجهة (تغيير نص، لون، تخطيط...).
        *   **كيفية تفعيله:** اضغط على أيقونة البرق الأصفر في المحرر، أو احفظ الملف (غالبًا يكون الإعداد الافتراضي).
    *   **Hot Restart (السهم الدائري 🔄):**
        *   يعيد تشغيل التطبيق **بالكامل**، لكنه أسرع من إعادة البناء من الصفر.
        *   يفقد حالة التطبيق الحالية (يرجع العداد للصفر مثلاً).
        *   نحتاجه عندما تكون التغييرات أعمق (مثل تغييرات في `main` أو بنية الحالة الجذرية).
    *   **"جربوهما!"** غيروا النص في `Text` widget أو لون `AppBar` واضغطوا Hot Reload. ثم غيروا القيمة الابتدائية لـ `_counter` واضغطوا Hot Restart.

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحل المشكلات (Open Learning & Problem Solving)**

"الآن، لنتأكد أن الجميع قادر على إنشاء وتشغيل التطبيق وتجربة أساسيات Flutter."

1.  **التأكد من القدرة على إنشاء وتشغيل المشروع (45 دقيقة)**
    *   **"مهمة جماعية:"**
        1.  تأكدوا من أن الجميع تمكن من إنشاء مشروع Flutter جديد بنجاح (`flutter create`).
        2.  تأكدوا من أن الجميع تمكن من فتح المشروع في VS Code أو Android Studio.
        3.  تأكدوا من أن الجميع تمكن من تشغيل محاكي أو توصيل جهاز حقيقي بنجاح.
        4.  تأكدوا من أن الجميع تمكن من تشغيل التطبيق الافتراضي (Counter App) ورؤيته على الجهاز/المحاكي.
    *   **(مساعدة الطلاب بشكل فردي لحل أي مشاكل متبقية في الإعداد والتشغيل. هذه الخطوة حاسمة).** مشاكل شائعة: مشاكل PATH، مشاكل Android SDK/Licenses، عدم تعرف المحرر على الجهاز، بطء أول عملية بناء.

2.  **تجربة تعديلات بسيطة و Hot Reload (30 دقيقة)**
    *   **"المهمة 1:"** "في ملف `main.dart` للتطبيق الافتراضي:"
        *   غيروا النص داخل أول `Text` widget (مثلاً إلى "Welcome to my First Flutter App!"). استخدموا Hot Reload لرؤية التغيير.
        *   غيروا عنوان `AppBar` (النص داخل `Text(widget.title)`). استخدموا Hot Reload.
        *   غيروا أيقونة `FloatingActionButton` (ابحثوا عن أسماء أيقونات أخرى في `Icons.[اسم_الأيقونة]`). استخدموا Hot Reload.
        *   غيروا لون `AppBar` الأساسي. في `MaterialApp` -> `theme` -> `colorScheme`, جربوا تغيير `seedColor` إلى لون آخر مثل `Colors.blue` أو `Colors.green`. استخدموا Hot Reload أو Hot Restart إذا لزم الأمر.
    *   **"المهمة 2:"**
        *   غيروا القيمة الابتدائية لـ `_counter` إلى 5. استخدموا Hot Restart لرؤية التغيير.
        *   عدلوا دالة `_incrementCounter` لتزيد العداد بمقدار 2 بدلًا من 1 (`_counter += 2;`). استخدموا Hot Reload وجربوا الضغط على الزر.

3.  **مناقشة الكود والمفاهيم (10 دقائق)**
    *   "هل مفهوم شجرة الـ Widgets بدأ يتضح؟"
    *   "ما هو الفرق الرئيسي بين `StatelessWidget` و `StatefulWidget` بناءً على التطبيق الافتراضي؟ (Stateful لديه حالة تتغير ويستخدم `setState`)."
    *   "لماذا `setState` مهمة؟ ماذا يحدث لو غيرنا `_counter` مباشرة بدون `setState`؟ (الواجهة لن تتحدث)."
    *   "ما فائدة Hot Reload؟"

4.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1:** حاولوا إضافة `Text` widget ثاني تحت العداد يعرض "Counter is even" إذا كان العداد زوجيًا، و "Counter is odd" إذا كان فرديًا. (تلميح: استخدموا `%` و `if` أو المعامل الثلاثي داخل `Column`).
    *   **تمرين 2 (بحث):** ابحثوا عن وثائق Flutter الرسمية (`docs.flutter.dev`). تصفحوا قسم "Widget catalog". اختاروا Widget بسيط لم نستخدمه (مثل `Icon`, `Image`) وحاولوا إضافته في مكان ما في الـ `Column` داخل `body`.
    *   "تهانينا! لقد أنشأتم وشغلتم أول تطبيق Flutter لكم اليوم! هذه خطوة كبيرة. في الأسبوع القادم، سنبدأ بتصميم واجهاتنا الخاصة باستخدام Figma ثم تحويلها إلى كود Flutter باستخدام الـ Widgets الأساسية. يعطيكم العافية!"

---

بالتأكيد، لنواصل تفصيل خطة دروس المرحلة الثانية (Flutter و Dart).

---

### **الشهر الرابع: أساسيات Dart و Flutter (تابع)**

---

#### **الأسبوع الثالث (أسبوع 15 في المجمل)**

---

#### **الحصة 5 (الاثنين - حصة 21 في المجمل): تصميم الواجهات باستخدام Figma**

**🎯 أهداف الحصة:**

1.  فهم أهمية تصميم الواجهات (UI/UX) قبل البدء بالبرمجة.
2.  التعرف على أداة Figma وميزاتها الأساسية كأداة تصميم وتعاون.
3.  إنشاء حساب Figma واستكشاف الواجهة الرئيسية.
4.  تعلم الأدوات الأساسية في Figma (Frames, Shapes, Text, Colors, Layout Grids).
5.  تصميم واجهة بسيطة لتطبيق ToDo List (شاشة عرض المهام وشاشة إضافة مهمة).
6.  فهم كيفية استخلاص المواصفات (الألوان، الأحجام، المسافات) من تصميم Figma لاستخدامها في Flutter.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم مجددًا! في الحصة الماضية، أنشأنا أول تطبيق Flutter لنا وتعرفنا على هيكل المشروع والـ Widgets الأساسية. قبل أن نتعمق في بناء واجهات معقدة بالكود، من المهم جدًا أن نتعلم كيف **نصمم** هذه الواجهات أولاً. التصميم الجيد (UI/UX) هو مفتاح نجاح أي تطبيق. اليوم، سنتعرف على أداة تصميم قوية وشائعة جدًا اسمها **Figma** وسنستخدمها لتصميم واجهات تطبيق ToDo List الذي بدأنا ببناء الـ API الخاص به."

1.  **مراجعة سريعة (5 دقائق)**
    *   "ما هو الأمر لإنشاء مشروع Flutter جديد؟ (`flutter create`). وما هو أهم مجلد في المشروع؟ (`lib/`). وما هي فائدة Hot Reload؟ ممتاز!"

2.  **أهمية تصميم UI/UX (10 دقائق)**
    *   **UI (User Interface - واجهة المستخدم):** هي الشكل والمظهر المرئي للتطبيق (الألوان، الأزرار، الخطوط، التخطيط).
    *   **UX (User Experience - تجربة المستخدم):** هي الشعور العام والانطباع الذي يتركه التطبيق لدى المستخدم أثناء استخدامه (سهولة الاستخدام، الوضوح، الفعالية).
    *   **"لماذا نصمم قبل البرمجة؟"**
        *   **توفير الوقت والجهد:** اكتشاف المشاكل وتعديل التصميم أسهل وأسرع بكثير في أداة تصميم منه في الكود.
        *   **رؤية واضحة:** يعطي فريق التطوير (والمصممين وأصحاب المصلحة) رؤية واضحة للشكل النهائي للتطبيق.
        *   **التجربة والتكرار:** يسمح بتجربة أفكار تصميم مختلفة بسرعة واختيار الأفضل.
        *   **الاتساق:** يساعد في الحفاظ على تصميم متسق عبر جميع شاشات التطبيق.
        *   **التعاون:** أدوات التصميم تسهل التعاون بين المصممين والمطورين.

3.  **مقدمة لـ Figma (15 دقيقة)**
    *   **"ما هي Figma؟"** هي أداة تصميم واجهات مستخدم وتجربة مستخدم (UI/UX Design Tool) تعمل **مباشرة في المتصفح** (Web-based)، مما يجعلها سهلة الوصول وممتازة للتعاون.
    *   **"مميزاتها الرئيسية:"**
        *   **مجانية (للأفراد والمشاريع الصغيرة):** الخطة المجانية كافية جدًا لتعلمنا ولاستخدامنا الشخصي.
        *   **تعاونية (Collaborative):** يمكن لأكثر من شخص العمل على نفس التصميم في نفس الوقت، وترك تعليقات وملاحظات.
        *   **متعددة المنصات:** تعمل على أي نظام تشغيل من خلال المتصفح، ولها تطبيقات Desktop اختيارية.
        *   **Vector-based:** التصاميم تكون Vector (مثل SVG) مما يعني أنها قابلة للتكبير والتصغير بدون فقدان الجودة.
        *   **Prototyping:** تسمح بإنشاء نماذج أولية تفاعلية (Clickable prototypes) لربط الشاشات وتجربة تدفق المستخدم.
        *   **Design Systems:** تدعم إنشاء وإدارة أنظمة التصميم (مكتبات المكونات والألوان والخطوط).
        *   **سهولة التصدير للمطورين:** توفر أدوات سهلة للمطورين لفحص التصميم، معرفة الأبعاد والألوان والمسافات، وتصدير الأصول (Assets) مثل الأيقونات والصور.

4.  **إنشاء حساب واستكشاف واجهة Figma (15 دقيقة)**
    *   "لنقم بإنشاء حساب مجاني ونلقي نظرة سريعة على الواجهة."
    *   **الخطوات:**
        1.  اذهبوا إلى `figma.com`.
        2.  اضغطوا على `Sign up` وأنشئوا حسابًا جديدًا (يمكن استخدام حساب Google).
        3.  بعد تسجيل الدخول، ستظهر لكم لوحة التحكم (Dashboard).
        4.  اضغطوا على زر `+ New design file` لإنشاء ملف تصميم جديد فارغ.
    *   **جولة في الواجهة:**
        *   **الشريط العلوي (Toolbar):** يحتوي على الأدوات الرئيسية (Move, Frame, Shape tools, Text tool, Pen tool, Comment...).
        *   **القائمة اليسرى (Layers Panel):** تعرض قائمة بكل العناصر (الطبقات) الموجودة في تصميمك وهيكلها (Frames, Groups, Shapes, Text...).
        *   **القائمة اليمنى (Properties Panel):** ***مهمة جدًا!*** تعرض خصائص العنصر المحدد حاليًا (مثل الأبعاد، الموضع، اللون، نوع الخط، المحاذاة، التأثيرات...).
        *   **اللوحة الرئيسية (Canvas):** المساحة البيضاء الكبيرة التي تقومون بالتصميم عليها.

5.  **الأدوات الأساسية في Figma (30 دقيقة)**
    *   **(تطبيق عملي مباشر مع الشرح لكل أداة):**
    *   **1. Frame Tool (F):**
        *   الإطار هو الحاوية الرئيسية لتصميم شاشة التطبيق أو أي جزء من الواجهة.
        *   عند اختيار الأداة، تظهر في القائمة اليمنى أحجام شائعة للأجهزة (iPhone, Android Large...). اختاروا واحدًا (مثلاً Android Large).
        *   يمكن تغيير حجم الإطار وتسميته من القائمة اليسرى.
    *   **2. Shape Tools (R للمستطيل, O للدائرة, L للخط):**
        *   لرسم الأشكال الأساسية (مربعات، دوائر، خطوط...).
        *   استخدموا القائمة اليمنى لتغيير:
            *   `Fill`: لون التعبئة.
            *   `Stroke`: لون وسمك الإطار الخارجي.
            *   `Corner Radius`: لجعل الزوايا دائرية.
            *   `Effects`: لإضافة ظلال (Drop shadow)، تمويه (Blur)...
    *   **3. Text Tool (T):**
        *   لإضافة نصوص. اضغط واسحب لإنشاء مربع نص، أو اضغط فقط لكتابة نص يتمدد تلقائيًا.
        *   استخدموا القائمة اليمنى لتغيير:
            *   `Font Family`: نوع الخط (Figma توفر خطوط Google Fonts تلقائيًا).
            *   `Font Weight`: سمك الخط (Regular, Bold...).
            *   `Font Size`: حجم الخط.
            *   `Letter Spacing`, `Line Height`.
            *   `Alignment`: المحاذاة (يسار، وسط، يمين).
            *   `Fill`: لون النص.
    *   **4. Colors:**
        *   يمكن اختيار الألوان باستخدام منتقي الألوان (Color Picker)، إدخال كود HEX (`#RRGGBB`)، أو RGB، أو HSL.
        *   **Color Styles:** يمكن حفظ الألوان المستخدمة بكثرة كـ "Style" ليسهل إعادة استخدامها وتغييرها مركزيًا لاحقًا. (من القائمة اليمنى -> Fill -> أيقونة الأربع نقاط -> +).
    *   **5. Layout Grids:**
        *   مفيدة لمحاذاة العناصر بشكل منظم.
        *   حددوا الـ Frame، ثم من القائمة اليمنى اضغطوا `+` بجانب `Layout Grid`.
        *   يمكن اختيار شبكة (Grid)، أعمدة (Columns)، أو صفوف (Rows). الأعمدة مفيدة لتصميمات الويب والموبايل. حددوا عدد الأعمدة، الهوامش (Margin)، والمسافة بين الأعمدة (Gutter).
        *   يمكن إظهار وإخفاء الشبكة (Ctrl+Shift+4).
    *   **6. Grouping (Ctrl+G) & Frames (Ctrl+Alt+G):**
        *   `Group`: لتجميع عدة عناصر معًا كوحدة واحدة لتحريكها أو تغيير حجمها سويًا.
        *   `Frame`: مشابه للـ Group لكن له خصائص إضافية مثل الـ Layout Constraints والـ Auto Layout (مفاهيم متقدمة).

6.  **تصميم واجهة ToDo App بسيطة (تطبيق مباشر) (15 دقيقة)**
    *   **"الهدف:"** نصمم شكل مبدئي لشاشة عرض المهام وشاشة إضافة مهمة.
    *   **شاشة عرض المهام (Task List Screen):**
        *   أنشئوا Frame جديد (Android Large). سموه "TaskListScreen".
        *   **AppBar:** ارسموا مستطيلًا في الأعلى. أضيفوا نصًا لعنوان الشاشة (مثل "My Tasks"). أضيفوا أيقونة (يمكن استخدام إضافة مثل Iconify أو نسخ SVG) لزر الإضافة (+). (الشرح: سنبني هذه العناصر كـ Widgets في Flutter).
        *   **قائمة المهام:** ارسموا شكلًا يمثل عنصر مهمة واحدة (مستطيل بزوايا دائرية). بداخله:
            *   مربع اختيار (Checkbox) صغير (يمكن رسمه كمربع).
            *   نص يمثل عنوان المهمة.
            *   (اختياري) أيقونة حذف صغيرة.
        *   كرروا هذا العنصر عدة مرات لتمثيل القائمة.
    *   **شاشة إضافة مهمة (Add Task Screen):**
        *   أنشئوا Frame ثاني. سموه "AddTaskScreen".
        *   **AppBar:** مستطيل، عنوان "Add New Task", زر "Save" أو أيقونة صح.
        *   **حقل الإدخال:** ارسموا مستطيلًا يمثل حقل إدخال عنوان المهمة. أضيفوا نصًا مؤقتًا (Placeholder) بداخله ("Enter task title").
        *   (اختياري) حقل إدخال ثاني للوصف.
    *   **(التركيز على استخدام الأشكال والنصوص والألوان الأساسية فقط في هذه المرحلة).**

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي واستخلاص المواصفات (Open Learning & Problem Solving)**

"الآن، دوركم لتطبيق ما تعلمناه وتصميم الواجهات بأنفسكم."

1.  **التمرين 1: استكشاف Figma وتطبيق الأدوات الأساسية (30 دقيقة)**
    *   **"المهمة:"** "كل طالب يفتح ملف Figma جديد."
        *   أنشئوا Frame بحجم جهاز من اختياركم.
        *   جربوا رسم أشكال مختلفة (مستطيلات، دوائر) وتغيير ألوان التعبئة (`Fill`) والإطار الخارجي (`Stroke`) والزوايا (`Corner Radius`).
        *   جربوا إضافة نصوص مختلفة وتغيير نوع الخط، حجمه، لونه، ووزنه.
        *   جربوا إضافة ظل (`Drop shadow`) لأحد الأشكال.
        *   جربوا تفعيل `Layout Grid` (أعمدة) على الـ Frame وحاولوا محاذاة بعض العناصر مع الأعمدة.
        *   جربوا تحديد عنصرين أو أكثر وتجميعهم (`Ctrl+G`).

2.  **التمرين 2: تصميم واجهة ToDo App (40 دقيقة)**
    *   **"المهمة:"** "بناءً على ما رأيناه، قوموا بتصميم واجهتين بسيطتين لتطبيق ToDo List:"
        *   **شاشة TaskListScreen:** يجب أن تحتوي على AppBar (عنوان، زر إضافة) وقائمة وهمية لعدة مهام (كل مهمة فيها Checkbox ونص).
        *   **شاشة AddTaskScreen:** يجب أن تحتوي على AppBar (عنوان، زر حفظ) وحقل إدخال واحد على الأقل لعنوان المهمة.
    *   "ركزوا على التخطيط الأساسي وترتيب العناصر. لا تقلقوا بشأن التفاصيل الدقيقة جدًا أو الأيقونات المعقدة الآن."
    *   "حاولوا استخدام الألوان والخطوط بشكل متناسق بين الشاشتين."
    *   **(مساعدة الطلاب أثناء التصميم، الإجابة على أسئلة حول استخدام الأدوات).**

3.  **التمرين 3: استخلاص المواصفات للمطور (15 دقيقة)**
    *   **"المهمة:"** "لنفترض أنكم المطورون الآن، وتحتاجون لمعلومات من هذا التصميم لبناء الواجهة في Flutter."
    *   "في تصميمكم، حددوا العناصر التالية وسجلوا قيمها من القائمة اليمنى (Inspect Tab أو Design Tab):"
        *   لون خلفية الـ AppBar. (انسخوا كود HEX).
        *   لون عنوان الـ AppBar وحجم الخط.
        *   لون خلفية عنصر المهمة في القائمة.
        *   مقدار دائرية الزوايا (Corner Radius) لعنصر المهمة.
        *   حجم خط عنوان المهمة ولونه.
        *   المسافة (Padding أو Margin) بين عنصر المهمة وحواف الشاشة، أو بين العناصر داخل المهمة. (يمكن قياس المسافات بالضغط على عنصر وتمرير الفأرة فوق عنصر آخر مع الضغط على Alt/Option).
        *   لون زر الحفظ في شاشة الإضافة.
    *   **"نقاش:"** "لماذا هذه المعلومات مهمة للمطور؟ (لبناء الواجهة بنفس المواصفات البصرية في Flutter)."

4.  **حل المشكلات والختام (5 دقائق)**
    *   "هل واجهتكم صعوبات في استخدام أدوات Figma؟ في محاذاة العناصر؟ في اختيار الألوان؟"
    *   **"تمرين منزلي:"**
        *   حاولوا تحسين تصميمكم لواجهات ToDo App (إضافة تفاصيل أكثر، أيقونات أفضل - يمكن استخدام إضافة مثل Iconify).
        *   (بحث) ابحثوا عن مفهوم "Prototyping" في Figma. حاولوا ربط زر الإضافة (+) في شاشة القائمة بزر الرجوع في شاشة الإضافة لجعل التصميم تفاعليًا.
    *   "رائع! اليوم تعلمنا أساسيات استخدام Figma لتصميم الواجهات. هذه مهارة قيمة جدًا للمطورين أيضًا. في الحصة القادمة، سنبدأ بترجمة هذه التصاميم إلى كود Flutter باستخدام الـ Widgets الأساسية للتخطيط والنصوص. يعطيكم العافية!"

---

#### **الحصة 6 (الخميس - حصة 22 في المجمل): Widgets الأساسية (Text, Container, Row, Column)**

**🎯 أهداف الحصة:**

1.  مراجعة مفهوم "Everything is a Widget" في Flutter.
2.  فهم دور `StatelessWidget` و `StatefulWidget` بشكل أعمق.
3.  التعرف على وبناء واجهات باستخدام `Text` widget وتنسيقه.
4.  استخدام `Container` widget للتحكم بالخلفية، الحجم، الهوامش (Margin)، والحشو الداخلي (Padding).
5.  فهم وبناء تخطيطات أفقية باستخدام `Row` widget.
6.  فهم وبناء تخطيطات رأسية باستخدام `Column` widget.
7.  التعرف على خصائص المحاذاة (`mainAxisAlignment`, `crossAxisAlignment`) في `Row` و `Column`.
8.  البدء في بناء واجهة بسيطة تشبه شاشة قائمة المهام التي تم تصميمها في Figma.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم! بعد أن صممنا واجهاتنا في Figma، حان الوقت لنحول هذه التصاميم إلى واقع ملموس في تطبيق Flutter. كما ذكرنا، كل شيء في Flutter هو Widget. اليوم، سنتعرف على بعض أهم وأبسط الـ Widgets التي تشكل حجر الأساس لبناء أي واجهة: `Text` لعرض النصوص، `Container` لإنشاء حاويات وتطبيق التنسيقات، و `Row` و `Column` لترتيب العناصر أفقيًا ورأسيًا."

1.  **مراجعة سريعة (5 دقائق)**
    *   "ما هي الأداة التي استخدمناها لتصميم الواجهات؟ (Figma). وما هي أهمية التصميم قبل البرمجة؟ ممتاز."
    *   "ما هو المفهوم الأساسي في بناء الواجهات في Flutter؟ (Everything is a Widget)."

2.  **StatelessWidget vs StatefulWidget (إعادة تأكيد) (10 دقائق)**
    *   "تذكروا من تطبيق العداد الافتراضي، كان لدينا نوعين من الـ Widgets:"
    *   **`StatelessWidget`:**
        *   Widget **لا تتغير حالته الداخلية** بعد إنشائه.
        *   يعتمد فقط على البيانات التي يتلقاها في المُنشئ (Constructor) وعلى `BuildContext`.
        *   دالة `build()` الخاصة به تُستدعى مرة واحدة غالبًا (أو عندما تتغير بيانات المُنشئ من الـ Parent Widget).
        *   أمثلة: `Text`, `Icon`, `Container` (إذا كانت خصائصه ثابتة)، `MyApp` في المثال السابق.
    *   **`StatefulWidget`:**
        *   Widget **يمكن أن تتغير حالته الداخلية** أثناء عمر التطبيق (Runtime).
        *   يتكون من جزئين: الكلاس نفسه (الذي يرث من `StatefulWidget`) وكلاس الحالة المرتبط به (الذي يرث من `State<YourWidget>`).
        *   الحالة المتغيرة تُحفظ في كلاس الـ `State`.
        *   عندما تتغير الحالة، نستدعي `setState()`، مما يطلب من Flutter إعادة بناء الواجهة عن طريق استدعاء دالة `build()` الخاصة بكلاس الـ `State`.
        *   أمثلة: `MyHomePage` (العداد)، Checkbox، TextField.
    *   "سنبدأ اليوم بـ `StatelessWidget` ونستخدم `StatefulWidget` عند الحاجة."

3.  **`Text` Widget (15 دقيقة)**
    *   "أبسط Widget، يُستخدم لعرض النصوص."
    *   **الاستخدام الأساسي:**
        ```dart
        Text('Hello, Flutter!')
        ```
    *   **أهم الخصائص (Properties):**
        *   `data` (المعامل الأول الإجباري): النص المراد عرضه (`String`).
        *   `style`: للتحكم بمظهر النص. تأخذ كائنًا من نوع `TextStyle`.
            ```dart
            Text(
              'Styled Text',
              style: TextStyle(
                color: Colors.blue, // لون النص (من كلاس Colors)
                fontSize: 24.0,    // حجم الخط
                fontWeight: FontWeight.bold, // سمك الخط (FontWeight.w100 .. w900, bold)
                fontStyle: FontStyle.italic, // نمط الخط (normal, italic)
                letterSpacing: 1.5, // المسافة بين الحروف
                wordSpacing: 4.0,   // المسافة بين الكلمات
                decoration: TextDecoration.underline, // خط تحت النص (lineThrough, overline)
                decorationColor: Colors.red,
                decorationStyle: TextDecorationStyle.dashed,
              ),
            )
            ```
        *   `textAlign`: لمحاذاة النص داخل المساحة المتاحة له (`TextAlign.left`, `.center`, `.right`, `.justify`).
        *   `maxLines`: لتحديد أقصى عدد من الأسطر.
        *   `overflow`: كيف يتم التعامل مع النص الزائد عن `maxLines` (`TextOverflow.ellipsis` لعرض '...', `TextOverflow.clip` لقص النص).

4.  **`Container` Widget (20 دقيقة)**
    *   "Widget متعدد الاستخدامات جدًا. فكروا فيه كصندوق أو حاوية يمكن تخصيصها بشكل كبير."
    *   "يمكن استخدامه لـ:"
        *   إضافة حشو داخلي (Padding) حول Widget آخر.
        *   إضافة هوامش خارجية (Margin) حول Widget.
        *   تحديد حجم معين (Width, Height).
        *   تطبيق لون خلفية (Color).
        *   تطبيق شكل مزخرف للخلفية (Decoration - مثل زوايا دائرية، إطار، تدرج لوني).
        *   تطبيق تحويلات (Transform - مثل التدوير).
        *   احتواء Widget ابن واحد (`child`).
    *   **مثال:**
        ```dart
        Container(
          padding: const EdgeInsets.all(16.0), // حشو داخلي من كل الجهات
          margin: const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0), // هامش أفقي وعمودي
          width: 200.0, // عرض ثابت
          height: 100.0, // ارتفاع ثابت
          // color: Colors.amber, // لون خلفية (لا يمكن استخدامه مع decoration)
          decoration: BoxDecoration(
            color: Colors.lightBlueAccent, // لون الخلفية داخل decoration
            borderRadius: BorderRadius.circular(12.0), // زوايا دائرية
            border: Border.all( // إطار خارجي
              color: Colors.blueGrey,
              width: 2.0,
            ),
            boxShadow: [ // ظل (يمكن إضافة عدة ظلال)
              BoxShadow(
                color: Colors.grey.withOpacity(0.5),
                spreadRadius: 5,
                blurRadius: 7,
                offset: Offset(0, 3), // changes position of shadow
              ),
            ],
          ),
          alignment: Alignment.center, // محاذاة الـ child في المنتصف (إذا كان حجم الـ Container أكبر)
          child: Text('Inside Container'),
        )
        ```
    *   **`EdgeInsets`:** كلاس لتحديد قيم الحشو والهوامش (`.all()`, `.symmetric()`, `.only()`, `.fromLTRB()`).
    *   **`BoxDecoration`:** كلاس لتطبيق تنسيقات متقدمة على خلفية الـ Container.

5.  **`Row` و `Column` Widgets (التخطيط الأساسي) (30 دقيقة)**
    *   "هذه هي الـ Widgets الأساسية لترتيب عدة Widgets أخرى (الأبناء `children`) إما أفقيًا أو رأسيًا."
    *   **`Row`:** يرتب أبناءه **أفقيًا** (واحد بجانب الآخر).
    *   **`Column`:** يرتب أبناءه **رأسيًا** (واحد فوق الآخر).
    *   **الخاصية الأساسية:** `children`: تأخذ قائمة (`List<Widget>`) بالـ Widgets الأبناء.
        ```dart
        Row(
          children: <Widget>[
            Icon(Icons.star, color: Colors.yellow),
            Text('Rating: 4.5'),
            Icon(Icons.star, color: Colors.yellow),
          ],
        )

        Column(
          children: <Widget>[
            Text('Line 1'),
            Text('Line 2'),
            Container(height: 50, color: Colors.red), // ارتفاع 50 ولون أحمر
          ],
        )
        ```
    *   **خصائص المحاذاة (Alignment):** ***مهمة جدًا!***
        *   **`mainAxisAlignment`:** كيف يتم توزيع الأبناء على **المحور الرئيسي**؟
            *   لـ `Row`: المحور الرئيسي هو الأفقي.
            *   لـ `Column`: المحور الرئيسي هو الرأسي.
            *   القيم الممكنة: `MainAxisAlignment.start` (الافتراضي)، `.end`, `.center`, `.spaceBetween` (مسافات متساوية بين الأبناء)، `.spaceAround` (مسافات متساوية حول الأبناء)، `.spaceEvenly` (مسافات متساوية بين الأبناء وحولهم).
        *   **`crossAxisAlignment`:** كيف يتم محاذاة الأبناء على **المحور المتقاطع**؟
            *   لـ `Row`: المحور المتقاطع هو الرأسي.
            *   لـ `Column`: المحور المتقاطع هو الأفقي.
            *   القيم الممكنة: `CrossAxisAlignment.start` (الافتراضي)، `.end`, `.center`, `.stretch` (تمديد الأبناء ليملأوا المحور المتقاطع)، `.baseline` (محاذاة خط الأساس للنصوص).
    *   **`mainAxisSize`:** كيف يأخذ الـ Row/Column حجمه على المحور الرئيسي؟
        *   `MainAxisSize.max` (الافتراضي): يأخذ كل المساحة المتاحة على المحور الرئيسي.
        *   `MainAxisSize.min`: يأخذ فقط المساحة التي يحتاجها أبناؤه.
    *   **مثال للمحاذاة:**
        ```dart
        Row(
          mainAxisAlignment: MainAxisAlignment.spaceEvenly, // توزيع أفقي متساوي
          crossAxisAlignment: CrossAxisAlignment.center, // محاذاة رأسية للمنتصف
          children: <Widget>[
            Icon(Icons.favorite, size: 50),
            Icon(Icons.audiotrack, size: 30),
            Icon(Icons.beach_access, size: 50),
          ],
        )
        ```

6.  **بناء واجهة بسيطة (ToDo Item) (تطبيق مباشر) (5 دقائق)**
    *   "لنحاول بناء شكل عنصر المهمة الواحدة باستخدام هذه الـ Widgets."
    *   "سنحتاج لـ `Row` لوضع Checkbox والنص بجانب بعض."
    *   "سنضع الـ `Row` داخل `Container` لإضافة Padding وزوايا دائرية وخلفية."
        ```dart
        // Simplified example - Checkbox state is not handled yet
        Container(
          padding: const EdgeInsets.all(12.0),
          margin: const EdgeInsets.symmetric(vertical: 4.0, horizontal: 8.0),
          decoration: BoxDecoration(
            color: Colors.white,
            borderRadius: BorderRadius.circular(8.0),
            boxShadow: [BoxShadow(color: Colors.grey.shade300, blurRadius: 3, offset: Offset(0, 2))],
          ),
          child: Row(
            children: <Widget>[
              Icon(Icons.check_box_outline_blank), // Placeholder for Checkbox
              SizedBox(width: 10.0), // Add space between checkbox and text
              Text(
                'This is a task title',
                style: TextStyle(fontSize: 16.0),
              ),
            ],
          ),
        )
        ```
    *   **`SizedBox`:** Widget بسيط يُستخدم لإضافة مساحة فارغة ذات حجم محدد (width أو height).

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وبناء الواجهات (Open Learning & Problem Solving)**

"حان الوقت لتطبيق هذه الـ Widgets الأساسية وبناء بعض التخطيطات."

1.  **التمرين 1: تجربة `Text` و `TextStyle` (20 دقيقة)**
    *   **"المهمة:"** "في مشروع Flutter (`main.dart`)، قم بتعديل `body` الخاص بـ `Scaffold`."
        *   احذف `Center` و `Column` الموجودين.
        *   ضع `Text` widget مباشرة في `body`.
        *   جرب عرض نص طويل يتكون من عدة أسطر.
        *   طبق `TextStyle` على النص: غير اللون، حجم الخط، اجعله سميكًا (bold)، أضف خطًا تحته (`underline`).
        *   جرب استخدام `textAlign: TextAlign.center` لمحاذاة النص.
        *   جرب تحديد `maxLines: 2` و `overflow: TextOverflow.ellipsis` وشاهد التأثير على النص الطويل.

2.  **التمرين 2: تجربة `Container` (25 دقيقة)**
    *   **"المهمة:"** "استمر في تعديل `body`."
        *   ضع الـ `Text` widget الذي نسقته داخل `Container`.
        *   أعطِ الـ `Container` لون خلفية مختلفًا باستخدام `color` أو `decoration: BoxDecoration(color: ...)`.
        *   أضف `padding` للـ `Container` باستخدام `EdgeInsets.all(20.0)`. لاحظ كيف ابتعد النص عن حواف الـ Container.
        *   أضف `margin` للـ `Container` باستخدام `EdgeInsets.symmetric(horizontal: 30.0)`. لاحظ كيف ابتعد الـ Container عن حواف الشاشة.
        *   أضف زوايا دائرية (`borderRadius`) وإطارًا (`border`) باستخدام `BoxDecoration`.
        *   (اختياري) أعطِ الـ `Container` عرضًا وارتفاعًا ثابتين (`width`, `height`).

3.  **التمرين 3: بناء تخطيط بـ `Row` و `Column` (35 دقيقة)**
    *   **"المهمة:"** "نحاول بناء تخطيط يشبه بطاقة مستخدم بسيطة."
        *   اجعل `body` يحتوي على `Container` واحد كهيكل رئيسي (مع padding بسيط).
        *   داخل هذا الـ `Container`، ضع `Column`.
        *   أبناء الـ `Column` سيكونون:
            *   `Row` علوي يحتوي على:
                *   أيقونة شخص (`Icon(Icons.person)`).
                *   `SizedBox(width: 10)` لإضافة مسافة.
                *   `Text` يحتوي على اسم المستخدم.
            *   `SizedBox(height: 15)` لإضافة مسافة رأسية.
            *   `Text` يحتوي على وصف أو بريد إلكتروني للمستخدم (بنص أصغر ولون أفتح).
            *   `SizedBox(height: 15)`.
            *   `Row` سفلي يحتوي على زرين (يمكن تمثيلهما بـ `Container` صغير ملون أو `Text` مؤقتًا):
                *   `Text('Follow')`
                *   `Text('Message')`
                *   استخدم `mainAxisAlignment: MainAxisAlignment.spaceAround` في هذا الـ `Row` لتوزيع الأزرار.
        *   جرب تغيير `crossAxisAlignment` في الـ `Column` الرئيسي (مثلاً إلى `.center` أو `.start`).
        *   جرب تغيير `mainAxisAlignment` في الـ `Row` العلوي.

4.  **حل المشكلات الشائعة (Overflow, Alignment) (5 دقائق)**
    *   **"Overflow Error (الخطوط الصفراء والسوداء):"** "هذا خطأ شائع جدًا يحدث عندما يكون محتوى Widget (مثل `Text` طويل أو `Row` به عناصر كثيرة) أكبر من المساحة المتاحة له."
        *   **الحلول الممكنة:** استخدام Widgets مثل `Expanded` أو `Flexible` داخل `Row`/`Column`، استخدام `SingleChildScrollView`، تصغير حجم المحتوى، استخدام `overflow` في `Text`. (سنغطي هذه الحلول بتفصيل لاحقًا).
    *   **"صعوبة فهم المحاذاة:"** "أفضل طريقة هي التجربة! غير قيم `mainAxisAlignment` و `crossAxisAlignment` وشاهد التأثير مباشرة باستخدام Hot Reload."

5.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1:** حاول بناء Widget يمثل عنصر المهمة الواحدة (ToDo Item) كما صممناه في Figma وفي المثال السريع اليوم (Container يحتوي على Row، والـ Row يحتوي على أيقونة Checkbox ونص المهمة). اجعل خلفية الـ Container وزواياه مشابهة للتصميم.
    *   **تمرين 2:** أنشئ `Column` يحتوي على 3 `Container` ملونة مختلفة (أحمر، أخضر، أزرق) بأحجام مختلفة. جرب تغيير `mainAxisAlignment` و `crossAxisAlignment` للـ `Column`.
    *   **تمرين 3 (بحث):** ابحث عن `Icon` Widget في Flutter. كيف يمكن تغيير حجم ولون الأيقونة؟ ابحث عن `Image` Widget. كيف يمكن عرض صورة من الإنترنت (`Image.network`) أو من مجلد `assets` المحلي (`Image.asset`)؟ (تلميح: لعرض صورة محلية، ستحتاج لتعديل `pubspec.yaml` وإنشاء مجلد `assets`).
    *   "ممتاز! اليوم بدأنا فعليًا ببناء الواجهات باستخدام أهم الـ Widgets الأساسية: `Text`, `Container`, `Row`, `Column`. في الحصة القادمة، سنتعلم كيف نجعل هذه الواجهات تتفاعل مع المستخدم من خلال إدارة الحالة البسيطة والتعامل مع `StatefulWidget`. يعطيكم العافية!"

---




anass

بالتأكيد، لنواصل تفصيل خطة دروس المرحلة الثانية (Flutter و Dart).

---

### **الشهر الرابع: أساسيات Dart و Flutter (تابع)**

---

#### **الأسبوع الرابع (أسبوع 16 في المجمل)**

---

#### **الحصة 7 (الاثنين - حصة 23 في المجمل): إدارة الحالة الأساسية (setState, StatefulWidget)**

**🎯 أهداف الحصة:**

1.  فهم الحاجة لإدارة الحالة (State Management) في التطبيقات التفاعلية.
2.  مراجعة الفرق بين `StatelessWidget` و `StatefulWidget`.
3.  فهم دورة حياة (Lifecycle) `StatefulWidget` بشكل مبسط.
4.  التركيز على آلية عمل `setState()` وأهميتها في تحديث الواجهة.
5.  بناء `StatefulWidget` بسيط يتفاعل مع المستخدم ويغير حالته.
6.  تطبيق `setState` لتحديث واجهة المستخدم بناءً على تفاعل المستخدم (مثل الضغط على زر، تغيير Checkbox).

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم! في الحصة الماضية، تعلمنا بناء واجهات ثابتة باستخدام `StatelessWidget` وأدوات التخطيط الأساسية مثل `Row`, `Column`, و `Container`. لكن التطبيقات الحقيقية ليست ثابتة! المستخدم يتفاعل معها، البيانات تتغير، والواجهة يجب أن تتجاوب مع هذه التغييرات. اليوم، سنتعلم الآلية الأساسية في Flutter للتعامل مع هذه الواجهات المتغيرة من خلال **إدارة الحالة (State Management)** باستخدام `StatefulWidget` و `setState()`."

1.  **مراجعة سريعة (5 دقائق)**
    *   "ما هي الـ Widgets الأساسية للتخطيط التي تعلمناها؟ (`Row`, `Column`). وما هو الـ Widget الذي نستخدمه لإضافة خلفية وحشو وهوامش؟ (`Container`). ممتاز!"
    *   "ما الفرق المبدئي بين `StatelessWidget` و `StatefulWidget`؟" (Stateless ثابت، Stateful يمكن أن يتغير).

2.  **ما هي الحالة (State) ولماذا نحتاج لإدارتها؟ (15 دقيقة)**
    *   **الحالة (State):** هي ببساطة **البيانات التي يمكن أن تتغير** بمرور الوقت وتؤثر على شكل الواجهة التي يراها المستخدم.
    *   **أمثلة على الحالة:**
        *   قيمة العداد في تطبيق العداد.
        *   هل تم تحديد مربع الاختيار (Checkbox) أم لا.
        *   النص المكتوب حاليًا في حقل الإدخال (TextField).
        *   القائمة الحالية للمهام في تطبيق ToDo.
        *   بيانات المستخدم التي تم جلبها من الـ API.
    *   **لماذا الإدارة مهمة؟**
        *   Flutter يعتمد على نموذج **واجهة المستخدم التصريحية (Declarative UI)**. نحن نصف الواجهة بناءً على الحالة الحالية.
        *   عندما تتغير الحالة (مثلاً المستخدم يضغط زرًا يزيد العداد)، نحتاج لطريقة لإخبار Flutter: "يا Flutter، الحالة تغيرت، أرجوك أعد بناء الواجهة لتعكس هذه الحالة الجديدة!".
        *   إدارة الحالة هي الآلية التي نستخدمها لتخزين الحالة، تعديلها، وإخطار Flutter بإعادة بناء الواجهة عند الحاجة.

3.  **`StatefulWidget` و `State` Object (إعادة تأكيد وتفصيل) (20 دقيقة)**
    *   "عندما نحتاج لـ Widget يمكن أن تتغير حالته، نستخدم `StatefulWidget`."
    *   **هيكلية `StatefulWidget`:** يتكون دائمًا من كلاسين:
        1.  **الكلاس الأول (يرث من `StatefulWidget`):**
            *   هذا الكلاس **ثابت (Immutable)** نفسه، تمامًا مثل `StatelessWidget`. خصائصه يجب أن تكون `final`.
            *   وظيفته الأساسية هي إنشاء كائن الحالة المرتبط به.
            *   يحتوي على دالة `createState()` التي **يجب** أن تقوم بتجاوزها (`@override`) لترجع نسخة (instance) من كلاس الحالة.
            ```dart
            class MyCounterWidget extends StatefulWidget {
              final String initialMessage; // Example property (must be final)
              const MyCounterWidget({super.key, required this.initialMessage});

              @override
              State<MyCounterWidget> createState() => _MyCounterWidgetState(); // Creates the state object
            }
            ```
        2.  **الكلاس الثاني (يرث من `State<YourWidgetClass>`):**
            *   هذا الكلاس هو الذي يحمل **الحالة الفعلية المتغيرة (Mutable State)**.
            *   هو الذي يحتوي على المتغيرات التي ستتغير (مثل `_counter`).
            *   هو الذي يحتوي على دالة `build()` التي تبني واجهة الـ Widget الفعلية بناءً على الحالة الحالية.
            *   هو الذي يحتوي على الدوال التي تقوم بتعديل الحالة (مثل `_incrementCounter`).
            *   هو الذي نستخدم `setState()` بداخله.
            ```dart
            class _MyCounterWidgetState extends State<MyCounterWidget> {
              int _counter = 0; // The actual mutable state

              void _increment() {
                // ... logic to update state ...
              }

              @override
              Widget build(BuildContext context) {
                // Access initialMessage from the widget using 'widget.'
                print(widget.initialMessage);
                // Build the UI based on the current _counter value
                return Column( /* ... UI using _counter ... */ );
              }
            }
            ```
            *   **`widget.`:** داخل كلاس الـ `State`، يمكن الوصول لخصائص الكلاس الأول (`StatefulWidget`) باستخدام `widget.` (مثل `widget.initialMessage`).

4.  **دورة حياة `StatefulWidget` (نظرة مبسطة) (15 دقيقة)**
    *   "كائن الـ `State` له دورة حياة، Flutter يستدعي دوال معينة في مراحل مختلفة:"
        *   `createState()`: (في `StatefulWidget`) تُستدعى مرة واحدة لإنشاء كائن الـ `State`.
        *   `initState()`: (في `State`) تُستدعى **مرة واحدة فقط** بعد إنشاء كائن الـ `State` وقبل أول استدعاء لـ `build`. مكان جيد لعمليات التهيئة الأولية (مثل جلب بيانات أولية، الاشتراك في Streams...).
        *   `build()`: ***تُستدعى كثيرًا!*** تُستدعى لأول مرة بعد `initState`, وتُستدعى **كل مرة يتم فيها استدعاء `setState()`**, أو عندما تتغير إعدادات الـ Widget الأب. وظيفتها بناء شجرة الـ Widgets بناءً على الحالة الحالية. يجب أن تكون سريعة وخالية من العمليات المكلفة.
        *   `setState((){ ... })`: الدالة التي نستدعيها **بأنفسنا** عندما نريد تغيير الحالة وإعادة بناء الواجهة.
        *   `dispose()`: تُستدعى **مرة واحدة فقط** عندما يتم إزالة الـ Widget وكائن الـ `State` الخاص به نهائيًا من شجرة الـ Widgets. مكان جيد لتنظيف الموارد (مثل إلغاء الاشتراكات، إلغاء Timers...).
    *   (سنركز اليوم على `build` و `setState`).

5.  **`setState()`: مفتاح التفاعل (25 دقيقة)**
    *   **"ماذا تفعل `setState` بالضبط؟"**
        1.  تأخذ دالة كمعامل (`void Function()`).
        2.  تقوم بتنفيذ الكود الموجود داخل هذه الدالة (هنا يجب أن نضع الكود الذي يغير قيم متغيرات الحالة).
        3.  بعد تنفيذ الكود، تقوم بإخبار Flutter أن هذا الكائن `State` قد "توسخ" (marked as dirty).
        4.  Flutter، عندما يرى أن الـ `State` توسخ، يقوم بجدولة إعادة بناء لهذا الـ Widget عن طريق استدعاء دالة `build()` الخاصة به مرة أخرى.
    *   **"الصيغة:"**
        ```dart
        setState(() {
          // Update state variables here
          _counter++;
          _isToggled = !_isToggled;
        });
        ```
    *   **"ماذا يحدث لو غيرنا الحالة بدون `setState`؟"**
        ```dart
        // WRONG WAY!
        void _incrementWrong() {
          _counter++; // Variable might change in memory...
          // ... but Flutter doesn't know it needs to rebuild the UI!
          // The UI will NOT update on screen.
        }
        ```
        *   قيمة المتغير قد تتغير في الذاكرة، لكن Flutter لن يعرف بذلك، وبالتالي لن يستدعي دالة `build`، ولن تتحدث الواجهة التي يراها المستخدم!
    *   **"القاعدة الذهبية:" أي تغيير في متغير حالة يتطلب تحديثًا مرئيًا للواجهة يجب أن يحدث داخل دالة `setState()`."**

6.  **مثال عملي: بناء زر Toggle بسيط (تطبيق مباشر) (5 دقائق)**
    *   "لنحول كود بسيط من `Stateless` إلى `Stateful`."
    *   (نبدأ بـ `StatelessWidget` يعرض `Icon(Icons.lightbulb_outline)`. ثم نحوله لـ `StatefulWidget` يحتفظ بحالة `bool _isOn = false`. عند الضغط على الأيقونة، نستدعي `setState` لتغيير `_isOn` وعرض `Icons.lightbulb` أو `Icons.lightbulb_outline` بناءً على الحالة).

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وبناء Widgets تفاعلية (Open Learning & Problem Solving)**

"حان الوقت لنجعل تطبيقاتنا تتفاعل مع المستخدم باستخدام `StatefulWidget`."

1.  **التمرين 1: إعادة بناء تطبيق العداد (25 دقيقة)**
    *   **"المهمة:"** "في مشروع Flutter جديد أو في `main.dart` بعد حذف الكود الافتراضي، قوموا ببناء تطبيق العداد البسيط من الصفر باستخدام `StatefulWidget`."
        1.  أنشئوا `StatelessWidget` لـ `MyApp` يبني `MaterialApp`.
        2.  اجعلوا `home` الخاص بـ `MaterialApp` يشير إلى `StatefulWidget` جديد اسمه `MyCounterPage`.
        3.  أنشئوا كلاس `MyCounterPage` (يرث من `StatefulWidget`).
        4.  أنشئوا كلاس الحالة `_MyCounterPageState` (يرث من `State<MyCounterPage>`).
        5.  داخل `_MyCounterPageState`، عرفوا متغير الحالة `int _counter = 0;`.
        6.  داخل `_MyCounterPageState`، عرفوا دالة `_incrementCounter` تستدعي `setState` وتقوم بزيادة `_counter`.
        7.  في دالة `build` الخاصة بـ `_MyCounterPageState`، ابنوا الواجهة باستخدام `Scaffold`, `AppBar`, `Center`, `Column`, `Text` (لعرض العداد)، و `FloatingActionButton` (الذي يستدعي `_incrementCounter` عند `onPressed`).
    *   "شغلوا التطبيق وتأكدوا أن العداد يعمل عند الضغط على الزر."
    *   **(مساعدة الطلاب في بناء الهيكلية الصحيحة لـ `StatefulWidget` واستخدام `setState`).**

2.  **التمرين 2: بناء زر تبديل (Toggle Button) (30 دقيقة)**
    *   **"المهمة:"** "أنشئوا `StatefulWidget` جديدًا (مثلاً `MyToggleButton`) يعرض زرًا (يمكن استخدام `ElevatedButton` أو `TextButton`)."
        *   عرفوا متغير حالة `bool _isToggled = false;`.
        *   عند الضغط على الزر (`onPressed`), استدعوا `setState` لتبديل قيمة `_isToggled` (من `true` إلى `false` والعكس).
        *   اجعلوا النص المعروض على الزر يتغير بناءً على قيمة `_isToggled` (مثلاً "ON" إذا كانت `true`, و "OFF" إذا كانت `false`).
        *   (اختياري) اجعلوا لون الزر يتغير أيضًا بناءً على الحالة.
    *   "استخدموا هذا الـ Widget داخل `body` في `Scaffold` في `main.dart`."

3.  **التمرين 3: جعل عنصر ToDo تفاعليًا (Checkbox) (25 دقيقة)**
    *   **"المهمة:"** "نرجع للتمرين المنزلي من الحصة الماضية (عنصر ToDo Item). لنحوله إلى `StatefulWidget` ونجعل مربع الاختيار (Checkbox) يعمل."
        1.  حولوا الـ Widget الذي يمثل عنصر المهمة إلى `StatefulWidget` (مثلاً `TodoItemWidget`).
        2.  أضيفوا له خاصية `final String taskTitle;` في الكلاس الأول (لتمرير عنوان المهمة).
        3.  في كلاس الحالة `_TodoItemWidgetState`، عرفوا متغير حالة `bool _isChecked = false;`.
        4.  استخدموا `Checkbox` widget بدلًا من `Icon` الذي كان placeholder.
            *   خاصية `value` للـ `Checkbox` يجب أن تكون مرتبطة بـ `_isChecked`.
            *   خاصية `onChanged` للـ `Checkbox` هي دالة تُستدعى عندما يضغط المستخدم على الـ Checkbox، وتعطيك القيمة الجديدة (`bool? newValue`). داخل هذه الدالة، يجب أن نستدعي `setState` لتحديث `_isChecked` بالقيمة الجديدة.
            ```dart
            Checkbox(
              value: _isChecked,
              onChanged: (bool? newValue) {
                setState(() {
                  _isChecked = newValue ?? false; // Update state with new value
                });
              },
            )
            ```
        5.  (اختياري) يمكن تغيير مظهر النص (مثل إضافة خط يتوسطه `TextDecoration.lineThrough`) عندما تكون `_isChecked` هي `true`.
    *   "في `main.dart`, يمكن إنشاء `Column` يحتوي على عدة نسخ من `TodoItemWidget` (كل واحدة بعنوان مختلف)."

4.  **حل المشكلات ومناقشة (5 دقائق)**
    *   "هل واجهتكم مشاكل في تمرير البيانات للـ `StatefulWidget` (مثل `taskTitle`)؟"
    *   "هل آلية عمل `onChanged` و `setState` في `Checkbox` واضحة؟"
    *   "لماذا نستخدم `widget.` للوصول للخصائص في كلاس الـ State؟"

5.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1:** أنشئ `StatefulWidget` يعرض `Container` ملونًا. أضف زرًا منفصلاً. عند الضغط على الزر، استخدم `setState` لتغيير لون الـ `Container` إلى لون عشوائي. (ابحث عن `import 'dart:math';` و `Color((Random().nextDouble() * 0xFFFFFF).toInt()).withOpacity(1.0);` لتوليد لون عشوائي).
    *   **تمرين 2 (بحث):** اقرأوا أكثر عن دورة حياة `StatefulWidget` في توثيق Flutter الرسمي. حاولوا فهم متى يمكن استخدام `initState` و `dispose` بشكل عملي.
    *   "رائع! اليوم فهمنا كيف نجعل تطبيقات Flutter تتفاعل مع المستخدم باستخدام `StatefulWidget` و `setState`. هذه هي الطريقة الأساسية لإدارة الحالة، وهي كافية للتطبيقات البسيطة. في الحصة القادمة، سنتعلم كيف ننتقل بين شاشات مختلفة داخل تطبيقنا باستخدام Navigation & Routing. يعطيكم العافية!"

---

#### **الحصة 8 (الخميس - حصة 24 في المجمل): التنقل بين الشاشات (Navigation & Routing)**

**🎯 أهداف الحصة:**

1.  فهم الحاجة للتنقل بين الشاشات المختلفة في التطبيق.
2.  التعرف على مفهوم الـ Navigator و Stack (مكدس الشاشات).
3.  استخدام `Navigator.push()` للانتقال إلى شاشة جديدة.
4.  استخدام `Navigator.pop()` للرجوع من الشاشة الحالية.
5.  تمرير البيانات (Passing Data) إلى الشاشة الجديدة عند الانتقال إليها.
6.  استقبال البيانات (Returning Data) من الشاشة عند الرجوع منها.
7.  مقدمة بسيطة للتنقل المُسمّى (Named Routes) كطريقة أكثر تنظيمًا.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم! حتى الآن، تطبيقاتنا تتكون من شاشة واحدة فقط. لكن التطبيقات الحقيقية غالبًا ما تحتوي على عدة شاشات (مثل شاشة تسجيل الدخول، الشاشة الرئيسية، شاشة الإعدادات، شاشة تفاصيل المنتج...). اليوم، سنتعلم كيف نسمح للمستخدم **بالتنقل (Navigate)** بين هذه الشاشات المختلفة في Flutter باستخدام آلية الـ **Navigator**."

1.  **مراجعة سريعة (5 دقائق)**
    *   "ما هو الفرق بين `StatelessWidget` و `StatefulWidget`؟ وكيف نحدث الواجهة عند تغير الحالة في `StatefulWidget`؟ (باستخدام `setState`). ممتاز!"

2.  **مفهوم الـ Navigator و Stack (15 دقيقة)**
    *   "Flutter يدير الشاشات (التي تسمى **Routes** في سياق التنقل) باستخدام Widget اسمه **`Navigator`**."
    *   `Navigator` يعمل مثل **مكدس (Stack)**. تخيلوا كومة من الأوراق أو الكتب:
        *   عندما تنتقل إلى شاشة جديدة، `Navigator` يقوم **بدفع (Push)** هذه الشاشة الجديدة فوق المكدس. الشاشة الجديدة تصبح هي الظاهرة للمستخدم.
        *   عندما ترجع من الشاشة الحالية (مثلاً بالضغط على زر الرجوع)، `Navigator` يقوم **بإزالة (Pop)** الشاشة العلوية من المكدس، فتظهر الشاشة التي كانت تحتها.
    *   الشاشة الأولى التي تظهر عند بدء التطبيق (التي نضعها في `home` في `MaterialApp`) هي أول عنصر في المكدس.
    *   `MaterialApp` يقوم بإنشاء `Navigator` تلقائيًا لنا.

3.  **الانتقال إلى شاشة جديدة (`Navigator.push`) (20 دقيقة)**
    *   "للانتقال من الشاشة الحالية (Screen A) إلى شاشة جديدة (Screen B)، نستخدم الدالة الساكنة (static method) `Navigator.push()`."
    *   **"كيف تعمل؟"**
        *   تأخذ `BuildContext` (للعثور على أقرب `Navigator` في الشجرة).
        *   تأخذ `Route` object. الـ `Route` يصف الشاشة الجديدة وكيفية الانتقال إليها (مثل إضافة Animation).
        *   أبسط وأشهر نوع `Route` هو `MaterialPageRoute`.
    *   **"الصيغة:"**
        ```dart
        // Inside an event handler like onPressed in Screen A:
        Navigator.push(
          context, // The BuildContext of Screen A
          MaterialPageRoute(
            builder: (context) => SecondScreen(), // Function that builds Screen B
          ),
        );
        ```
        *   `builder`: هي دالة تأخذ `context` وترجع الـ Widget الذي يمثل الشاشة الجديدة (مثلاً `SecondScreen()`).
    *   **"مثال: الانتقال من شاشة رئيسية لـ شاشة ثانية عند الضغط على زر"**
        ```dart
        // --- HomeScreen.dart ---
        import 'package:flutter/material.dart';
        import 'second_screen.dart'; // Import the second screen file

        class HomeScreen extends StatelessWidget {
          const HomeScreen({super.key});

          @override
          Widget build(BuildContext context) {
            return Scaffold(
              appBar: AppBar(title: Text('Home Screen')),
              body: Center(
                child: ElevatedButton(
                  child: Text('Go to Second Screen'),
                  onPressed: () {
                    // Navigate to SecondScreen when button is pressed
                    Navigator.push(
                      context,
                      MaterialPageRoute(builder: (context) => const SecondScreen()),
                    );
                  },
                ),
              ),
            );
          }
        }

        // --- second_screen.dart ---
        import 'package:flutter/material.dart';

        class SecondScreen extends StatelessWidget {
          const SecondScreen({super.key});

          @override
          Widget build(BuildContext context) {
            return Scaffold(
              appBar: AppBar(title: Text('Second Screen')), // AppBar automatically shows a back button
              body: Center(
                child: Text('Welcome to the Second Screen!'),
              ),
            );
          }
        }

        // --- main.dart ---
        // ... (MyApp, MaterialApp) ...
        // home: HomeScreen(), // Start with HomeScreen
        ```
        *   **ملاحظة:** `AppBar` في `MaterialPageRoute` يضيف زر الرجوع تلقائيًا للرجوع للشاشة السابقة.

4.  **الرجوع من شاشة (`Navigator.pop`) (10 دقيقة)**
    *   "للرجوع من الشاشة الحالية (Screen B) إلى الشاشة السابقة (Screen A) برمجيًا (بدلًا من استخدام زر الرجوع في AppBar)، نستخدم `Navigator.pop()`."
    *   **"الصيغة:"**
        ```dart
        // Inside an event handler in Screen B:
        Navigator.pop(context); // Simply takes the context
        ```
    *   **"مثال: إضافة زر رجوع مخصص في Screen B"**
        ```dart
        // --- Inside SecondScreen build method ---
        // ...
        body: Center(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              Text('Welcome to the Second Screen!'),
              SizedBox(height: 20),
              ElevatedButton(
                child: Text('Go Back'),
                onPressed: () {
                  Navigator.pop(context); // Go back to HomeScreen
                },
              ),
            ],
          ),
        ),
        // ...
        ```

5.  **تمرير البيانات إلى الشاشة الجديدة (Passing Data) (15 دقيقة)**
    *   "غالبًا، عندما ننتقل لشاشة جديدة، نحتاج لتمرير بعض البيانات إليها (مثل ID المنتج لعرض تفاصيله، أو اسم المستخدم لعرض رسالة ترحيب)."
    *   **"الطريقة الأبسط:"** نضيف معاملات (parameters) إلى مُنشئ (constructor) الشاشة الجديدة، ونمرر البيانات عند إنشائها داخل `MaterialPageRoute`.
    *   **"مثال: تمرير رسالة من HomeScreen إلى SecondScreen"**
        ```dart
        // --- second_screen.dart (Modified) ---
        import 'package:flutter/material.dart';

        class SecondScreen extends StatelessWidget {
          final String messageFromHome; // Add a property to receive data

          // Update constructor to accept the data
          const SecondScreen({super.key, required this.messageFromHome});

          @override
          Widget build(BuildContext context) {
            return Scaffold(
              appBar: AppBar(title: Text('Second Screen')),
              body: Center(
                child: Text(messageFromHome), // Display the received message
              ),
              // ... (Optional Go Back button) ...
            );
          }
        }

        // --- HomeScreen.dart (Modified onPressed) ---
        // ...
        onPressed: () {
          String messageToSend = 'Hello from Home!';
          Navigator.push(
            context,
            MaterialPageRoute(
              builder: (context) => SecondScreen(messageFromHome: messageToSend), // Pass data via constructor
            ),
          );
        },
        // ...
        ```

6.  **استقبال البيانات من الشاشة عند الرجوع (Returning Data) (15 دقيقة)**
    *   "أحيانًا، نحتاج أن الشاشة الثانية (Screen B) ترجع نتيجة أو بيانات للشاشة الأولى (Screen A) عند إغلاقها (Pop)." (مثال: شاشة تطلب من المستخدم اختيار خيار معين، وترجع هذا الخيار للشاشة الأولى).
    *   **"كيف تعمل؟"**
        *   `Navigator.pop()` يمكن أن تأخذ معاملًا اختياريًا ثانيًا وهو القيمة التي نريد إرجاعها.
        *   `Navigator.push()` ترجع `Future`. هذا الـ `Future` سيكتمل (complete) بالقيمة التي تم تمريرها لـ `Navigator.pop()` عندما ترجع الشاشة الثانية. نستخدم `await` لانتظار هذه القيمة.
    *   **"مثال: Screen B ترجع نصًا لـ Screen A"**
        ```dart
        // --- SecondScreen.dart (Modified) ---
        // Add two buttons to return different values
        // ...
        ElevatedButton(
          child: Text('Return "Yes"'),
          onPressed: () {
            Navigator.pop(context, 'Yes!'); // Pass 'Yes!' back
          },
        ),
        ElevatedButton(
          child: Text('Return "No"'),
          onPressed: () {
            Navigator.pop(context, 'No!'); // Pass 'No!' back
          },
        ),
        // ...

        // --- HomeScreen.dart (Modified onPressed - needs async/await) ---
        // Mark the function containing Navigator.push as async
        onPressed: () async { // Add async keyword
          print('Navigating to second screen...');
          // Wait for the result when SecondScreen pops
          final result = await Navigator.push( // Add await
            context,
            MaterialPageRoute(builder: (context) => const SecondScreen()),
          );

          // Use the result after SecondScreen returns
          print('Received result from Second Screen: $result');
          // Optional: Show result in a Snackbar or Dialog
          if (result != null && context.mounted) { // Check context is still valid
             ScaffoldMessenger.of(context)
               ..removeCurrentSnackBar()
               ..showSnackBar(SnackBar(content: Text("Result: $result")));
          }
        },
        // ...
        ```
        *   **`async`/`await`:** نحتاجها لأن `Navigator.push` ترجع `Future`. `async` تُضاف لتعريف الدالة التي تحتوي `await`. `await` توقف التنفيذ مؤقتًا حتى يكتمل الـ `Future` (حتى يتم استدعاء `pop` من الشاشة الثانية).
        *   **`ScaffoldMessenger`:** طريقة لعرض رسائل مؤقتة (Snackbars) في الجزء السفلي من الشاشة.

7.  **مقدمة للتنقل المُسمّى (Named Routes) (5 دقائق)**
    *   "الطريقة التي استخدمناها (`MaterialPageRoute`) تسمى التنقل المجهول (Anonymous Routing). هي تعمل، لكن مع نمو التطبيق، قد يصبح تكرار `MaterialPageRoute` وتمرير البيانات عبر المنشئات مربكًا."
    *   **Named Routes:** هي طريقة لتعريف كل الشاشات (Routes) بأسماء فريدة (مثل `/home`, `/settings`, `/product_details`) مركزيًا في `MaterialApp`، ثم التنقل إليها باستخدام أسمائها.
        ```dart
        // In MaterialApp:
        MaterialApp(
          initialRoute: '/', // Starting route name
          routes: {
            '/': (context) => HomeScreen(), // Route for '/'
            '/second': (context) => SecondScreen(), // Route for '/second'
            // Can define more routes here
          },
        )

        // To navigate:
        Navigator.pushNamed(context, '/second');

        // To pass arguments with named routes requires 'onGenerateRoute' (more advanced)
        ```
    *   **الفائدة:** تجعل كود التنقل أنظف وأكثر قابلية للإدارة والصيانة في التطبيقات الكبيرة. (سنستكشفها أكثر لاحقًا إذا دعت الحاجة).

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وبناء التنقل (Open Learning & Problem Solving)**

"الآن، نطبق ما تعلمناه عن التنقل بين الشاشات."

1.  **التمرين 1: إنشاء شاشتين والتنقل البسيط (30 دقيقة)**
    *   **"المهمة:"**
        1.  في مشروع Flutter، أنشئوا ملفين جديدين داخل مجلد `lib`: `screen_one.dart` و `screen_two.dart`.
        2.  في كل ملف، أنشئوا `StatelessWidget` بسيطًا يمثل شاشة (يحتوي على `Scaffold`, `AppBar` بعنوان مميز، و `Center` به `Text` widget).
        3.  في `main.dart`، اجعلوا `home` في `MaterialApp` يشير إلى `ScreenOne`.
        4.  في `ScreenOne`, أضيفوا زرًا (`ElevatedButton`). عند الضغط عليه (`onPressed`), استخدموا `Navigator.push` للانتقال إلى `ScreenTwo` باستخدام `MaterialPageRoute`.
        5.  في `ScreenTwo`, لاحظوا أن زر الرجوع في `AppBar` يعمل تلقائيًا.
    *   "شغلوا التطبيق وجربوا الانتقال من الشاشة الأولى للثانية والرجوع."

2.  **التمرين 2: تمرير بيانات بسيطة (25 دقيقة)**
    *   **"المهمة:"** "نعدل التمرين السابق لتمرير بيانات."
        1.  عدلوا `ScreenTwo` ليحتوي على خاصية `final String receivedData;` ومُنشئ يقبل هذه القيمة (`required this.receivedData`).
        2.  في `build` الخاص بـ `ScreenTwo`, اعرضوا قيمة `receivedData` في `Text` widget.
        3.  في `ScreenOne`, عند استدعاء `Navigator.push`, مرروا أي نص بسيط كقيمة لمعامل `receivedData` في مُنشئ `ScreenTwo`.
    *   "شغلوا التطبيق وتأكدوا أن النص الذي أرسلتموه من ScreenOne يظهر في ScreenTwo."

3.  **التمرين 3: إرجاع بيانات (30 دقيقة)**
    *   **"المهمة:"** "نجعل ScreenTwo ترجع بيانات لـ ScreenOne."
        1.  في `ScreenTwo`, أضيفوا زرين: "Approve" و "Reject".
        2.  عند الضغط على "Approve"، استخدموا `Navigator.pop(context, 'Approved!');`.
        3.  عند الضغط على "Reject"، استخدموا `Navigator.pop(context, 'Rejected!');`.
        4.  في `ScreenOne`, عدلوا دالة `onPressed` للزر لتكون `async`.
        5.  استخدموا `await Navigator.push(...)` وخزنوا النتيجة في متغير `result`.
        6.  بعد رجوع `push` (عندما يتم استدعاء `pop` في ScreenTwo)، اطبعوا قيمة `result` في الـ Console أو اعرضوها في `SnackBar` باستخدام `ScaffoldMessenger`.
    *   "شغلوا التطبيق، انتقلوا للشاشة الثانية، اضغطوا أحد الزرين، ولاحظوا النتيجة التي تظهر في ScreenOne."

4.  **حل المشكلات ومناقشة (5 دقائق)**
    *   "هل واجهتكم مشاكل في استخدام `async`/`await`؟"
    *   "مشاكل في تمرير البيانات أو استقبالها؟"
    *   "لماذا نحتاج `BuildContext` في دوال `Navigator`؟ (لتحديد موقع الـ Navigator في شجرة الـ Widgets)."

5.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1:** أنشئوا شاشة ثالثة (`ScreenThree`). اجعلوا التنقل كالتالي: ScreenOne -> ScreenTwo -> ScreenThree. تأكدوا أن زر الرجوع في AppBar يعمل بشكل صحيح للعودة خطوة للخلف في كل مرة.
    *   **تمرين 2 (بحث):** ابحثوا عن `Navigator.pushReplacement()` و `Navigator.pushAndRemoveUntil()`. متى يمكن أن تكون هذه الدوال مفيدة؟ (مثال: بعد تسجيل الدخول، قد تريد الانتقال للشاشة الرئيسية واستبدال شاشة تسجيل الدخول بحيث لا يمكن للمستخدم الرجوع إليها بزر الرجوع).
    *   **تمرين 3 (بحث عن Named Routes):** حاولوا إعادة تطبيق التمرين الأول (شاشتين والتنقل البسيط) باستخدام Named Routes. عرفوا المسارات في `MaterialApp` واستخدموا `Navigator.pushNamed()`.
    *   "ممتاز! اليوم تعلمنا كيف نجعل تطبيقاتنا متعددة الشاشات ونسمح للمستخدم بالتنقل بينها وتمرير البيانات. هذه خطوة أساسية لبناء تطبيقات أكثر تعقيدًا. في الأسبوع القادم، سنتعمق أكثر في بناء الواجهات ونتعلم عن Widgets أكثر، وكيفية الاتصال بالـ API الذي بنيناه في المرحلة الأولى. يعطيكم العافية!"

---

بالتأكيد، لنواصل تفصيل خطة دروس المرحلة الثانية (Flutter و Dart) للشهر الخامس.

---

### **الشهر الخامس: بناء تطبيقات Flutter متكاملة**

---

#### **الأسبوع الخامس (أسبوع 17 في المجمل)**

---

#### **الحصة 9 (الاثنين - حصة 25 في المجمل): الاتصال بالـ API (HTTP Package)، التعامل مع JSON**

**🎯 أهداف الحصة:**

1.  فهم الحاجة للاتصال بالـ API من تطبيق Flutter لجلب وعرض البيانات الديناميكية.
2.  التعرف على وإضافة حزمة `http` لمشروع Flutter.
3.  إجراء طلبات GET بسيطة للـ API باستخدام حزمة `http`.
4.  فهم التعامل مع الاستجابات غير المتزامنة (Asynchronous Responses) باستخدام `async`/`await` و `Future`.
5.  تحليل استجابة JSON القادمة من الـ API باستخدام `dart:convert`.
6.  عرض البيانات التي تم جلبها من الـ API في واجهة Flutter.
7.  التعامل مع حالات التحميل (Loading) والخطأ (Error) عند جلب البيانات.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم في الأسبوع الخامس! لقد تعلمنا كيف نبني واجهات Flutter ونتنقل بينها. الآن، حان الوقت لنجعل تطبيقاتنا تتصل بالعالم الخارجي وتجلب بيانات حقيقية من الـ API الذي بنيناه في المرحلة الأولى! اليوم، سنتعلم كيف نستخدم حزمة `http` في Flutter لإجراء طلبات للـ API وكيف نعالج استجابات JSON التي يرجعها."

1.  **مراجعة سريعة (5 دقائق)**
    *   "كيف ننتقل بين الشاشات في Flutter؟ (`Navigator.push`). وكيف نمرر بيانات للشاشة الجديدة؟ (عبر المُنشئ). وكيف نرجع بيانات؟ (`Navigator.pop(context, result)` واستخدام `await`). ممتاز!"

2.  **لماذا نتصل بالـ API؟ (10 دقائق)**
    *   تطبيقاتنا تحتاج لبيانات ديناميكية تتغير باستمرار (قائمة مهام، قائمة منتجات، أخبار، حالة الطقس...).
    *   هذه البيانات غالبًا ما تكون مخزنة في قاعدة بيانات على خادم (Server).
    *   الـ API الذي بنيناه (مثل ToDo API) هو الوسيط الذي يسمح لتطبيق Flutter بالوصول لهذه البيانات وطلبها وتعديلها.
    *   تطبيق Flutter (العميل/Client) يرسل طلبًا (Request) للـ API Endpoint.
    *   الـ API (الخادم/Server) يعالج الطلب، يتفاعل مع قاعدة البيانات، ويرسل استجابة (Response) تحتوي على البيانات المطلوبة (غالبًا بصيغة JSON).

3.  **حزمة `http` في Flutter (15 دقيقة)**
    *   Flutter نفسه لا يأتي مدمجًا به طريقة مباشرة لإجراء طلبات HTTP.
    *   نحتاج لاستخدام مكتبة خارجية (Package). الحزمة الأكثر شيوعًا واستخدامًا لهذا الغرض هي حزمة **`http`**.
    *   **"كيف نضيف الحزمة للمشروع؟"**
        1.  افتحوا ملف `pubspec.yaml`.
        2.  تحت قسم `dependencies:`, أضيفوا السطر التالي (تأكدوا من المحاذاة الصحيحة - مسافتين غالبًا):
            ```yaml
            dependencies:
              flutter:
                sdk: flutter
              http: ^1.2.1 # أو أحدث إصدار متوافق (ابحثوا في pub.dev)
            ```
            (يمكنكم البحث عن أحدث إصدار لحزمة `http` في موقع `pub.dev`).
        3.  احفظوا الملف. المحرر (VS Code/Android Studio) غالبًا سيقوم بتشغيل `flutter pub get` تلقائيًا. إذا لم يحدث، شغلوا الأمر يدويًا في الـ Terminal. هذا يقوم بتنزيل الحزمة وإتاحتها للاستخدام في مشروعكم.
    *   **"كيف نستخدمها في الكود؟"**
        *   نحتاج لعمل `import` للحزمة في ملف Dart الذي سنستخدمها فيه:
            ```dart
            import 'package:http/http.dart' as http; // Use 'as http' for convenience
            ```
            (استخدام `as http` يسمح لنا باستدعاء دوال الحزمة باستخدام `http.get()`, `http.post()` بدلًا من كتابة اسم الدالة مباشرة، لتجنب أي تضارب محتمل في الأسماء).

4.  **إجراء طلب GET بسيط وجلب البيانات (30 دقيقة)**
    *   "طلب GET هو المستخدم عادة لجلب (Fetch) البيانات من الـ API."
    *   **"الدالة الأساسية:"** `http.get()`
        *   تأخذ `Uri` (وليس `String`) كمعامل يمثل رابط الـ API Endpoint.
        *   هي دالة **غير متزامنة (Asynchronous)**، أي أنها لا توقف تنفيذ باقي الكود أثناء انتظارها لرد الخادم.
        *   ترجع `Future<http.Response>`. الـ `Future` يعني أن النتيجة ستكون متاحة في المستقبل. كائن `http.Response` يحتوي على معلومات الاستجابة (مثل status code, headers, body).
    *   **"استخدام `async`/`await`:"** الطريقة الشائعة للتعامل مع `Future` هي باستخدام `async` و `await`.
    *   **"مثال: جلب قائمة المهام من الـ API الذي بنيناه"**
        ```dart
        import 'dart:convert'; // Needed for jsonDecode
        import 'package:http/http.dart' as http;

        // Function to fetch tasks (could be in a separate service class later)
        Future<void> fetchTasks() async { // Mark function as async
          // Replace with your actual API URL
          final String apiUrl = "http://localhost/php_course/todo_api/api/tasks/read.php";
          // Or use your machine's IP if testing on a real device: http://192.168.X.X/...

          print('Attempting to fetch tasks from: $apiUrl');

          try {
            // Send the GET request and wait for the response
            final response = await http.get(Uri.parse(apiUrl));

            print('Response Status Code: ${response.statusCode}');
            // print('Response Body: ${response.body}'); // Print raw JSON string

            // Check if the request was successful (status code 200 OK)
            if (response.statusCode == 200) {
              // Parse the JSON response body
              // response.body is a String containing the JSON
              List<dynamic> tasksJson = jsonDecode(response.body);

              // Now you have a List of Maps (dynamic because values can be int, String, bool)
              print('Successfully parsed ${tasksJson.length} tasks.');

              // You can now process this list (e.g., convert to Task objects, update UI)
              for (var task in tasksJson) {
                print('Task ID: ${task['id']}, Title: ${task['title']}, Completed: ${task['is_completed']}');
              }
              // --- TODO: Update UI with the fetched tasks ---

            } else {
              // If the server did not return a 200 OK response,
              // throw an exception or handle the error appropriately.
              print('Failed to load tasks. Status code: ${response.statusCode}');
              print('Response body: ${response.body}');
              // --- TODO: Show error message to the user ---
            }
          } catch (e) {
            // Catch potential errors during the HTTP request (e.g., network issues, DNS errors)
            print('Error fetching tasks: $e');
            // --- TODO: Show error message to the user ---
          }
        }

        // Example of how to call it (e.g., inside initState or onPressed)
        // void main() {
        //   fetchTasks();
        // }
        ```
    *   **شرح:**
        *   `async` و `await`: لجعل الكود ينتظر رد الخادم قبل المتابعة.
        *   `Uri.parse()`: لتحويل رابط الـ API النصي إلى كائن `Uri` الذي تتطلبه دالة `get`.
        *   `response.statusCode`: كود حالة HTTP (200 يعني نجاح).
        *   `response.body`: **نص (String)** يحتوي على بيانات JSON التي أرجعها الـ API.
        *   `dart:convert`: المكتبة التي تحتوي على دالة `jsonDecode()`.
        *   `jsonDecode(response.body)`: تحول نص JSON إلى بنية بيانات Dart مكافئة (غالبًا `List<dynamic>` إذا كان JSON عبارة عن مصفوفة، أو `Map<String, dynamic>` إذا كان JSON عبارة عن كائن).
        *   **التعامل مع الأخطاء:** استخدام `try...catch` للإمساك بأي أخطاء قد تحدث أثناء الطلب (مثل مشاكل الشبكة). التحقق من `response.statusCode` لمعرفة إذا كان الطلب ناجحًا من جهة الخادم.
        *   **ملاحظة IP:** عند الاختبار على جهاز حقيقي، لا يمكنك استخدام `localhost`. يجب استخدام عنوان IP الخاص بجهاز الكمبيوتر الذي يشغل الخادم (XAMPP) على نفس الشبكة المحلية (مثل `192.168.1.100`).

5.  **عرض البيانات في واجهة Flutter (مقدمة لـ `FutureBuilder`) (20 دقيقة)**
    *   "لدينا الآن دالة `fetchTasks()` التي تجلب البيانات. كيف نعرض هذه البيانات في الواجهة؟ وكيف نعالج حالات التحميل والخطأ؟"
    *   "الطريقة اليدوية تتضمن استخدام `StatefulWidget`, استدعاء `fetchTasks()` في `initState()`, تخزين النتائج في متغير حالة، واستخدام `setState()` لتحديث الواجهة. لكن هذا يتطلب كتابة كود متكرر للتعامل مع حالات التحميل والخطأ."
    *   **`FutureBuilder` Widget:**
        *   Widget مدمج في Flutter مصمم خصيصًا للتعامل مع `Future` وبناء الواجهة بناءً على حالته.
        *   **أهم خصائصه:**
            *   `future`: الـ `Future` الذي ننتظر نتيجته (مثل نتيجة استدعاء `fetchTasks()`).
            *   `builder`: دالة تُستدعى في مراحل مختلفة من حياة الـ `Future`. تأخذ `BuildContext` و `AsyncSnapshot`. `AsyncSnapshot` يحتوي على معلومات عن حالة الـ `Future` (هل اكتمل؟ هل به خطأ؟ وما هي البيانات إذا اكتمل؟).
    *   **"كيف نستخدم `FutureBuilder`؟"**
        *   نعدل دالة `fetchTasks` لترجع `Future<List<dynamic>>` (أو الأفضل `Future<List<Task>>` إذا عرفنا كلاس `Task`).
        *   نستخدم `FutureBuilder` في دالة `build`.
        *   داخل `builder`, نتحقق من حالة `AsyncSnapshot`:
            *   `snapshot.connectionState == ConnectionState.waiting`: إذا كان الـ `Future` لا يزال قيد التنفيذ -> اعرض مؤشر تحميل (مثل `CircularProgressIndicator`).
            *   `snapshot.hasError`: إذا حدث خطأ في الـ `Future` -> اعرض رسالة خطأ (`snapshot.error` يحتوي على الخطأ).
            *   `snapshot.hasData`: إذا اكتمل الـ `Future` بنجاح ولديه بيانات -> اعرض البيانات (`snapshot.data` يحتوي على البيانات).
            *   حالات أخرى (مثل `ConnectionState.none` أو `done` بدون بيانات).
    *   **(الكود سيكون مفصلاً، سنركز على فهم الفكرة الآن ونطبقه في الجزء العملي).**

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وجلب البيانات وعرضها (Open Learning & Problem Solving)**

"حان الوقت لنجعل تطبيق Flutter يتصل بالـ ToDo API الذي بنيناه."

1.  **التمرين 1: إعداد الحزمة واستدعاء API بسيط (30 دقيقة)**
    *   **"المهمة:"**
        1.  تأكدوا من إضافة حزمة `http` إلى `pubspec.yaml` وتشغيل `flutter pub get`.
        2.  في ملف `main.dart` (أو ملف منفصل)، أنشئوا دالة `async` بسيطة اسمها `testApiConnection` تقوم بالتالي:
            *   تحدد رابط API جلب المهام (`read.php`). **(تأكدوا من استخدام IP الصحيح إذا كنتم تختبرون على جهاز حقيقي!).**
            *   تستخدم `http.get()` لإجراء الطلب.
            *   تطبع `statusCode` و `body` للاستجابة في الـ Console باستخدام `print()`.
            *   تستخدم `try...catch` للتعامل مع الأخطاء المحتملة.
        3.  في `main.dart`، أضيفوا زرًا (`ElevatedButton`) في الواجهة. عند الضغط عليه (`onPressed`)، يتم استدعاء دالة `testApiConnection`.
    *   **"الاختبار:"**
        *   تأكدوا أن XAMPP (Apache و MySQL) يعمل والـ ToDo API متاح على الرابط المحدد.
        *   شغلوا تطبيق Flutter. اضغطوا على الزر.
        *   راقبوا الـ Console (Debug Console في VS Code/Android Studio). هل ترون كود الحالة 200 ونص JSON الخاص بالمهام؟
        *   جربوا إيقاف XAMPP أو تغيير الرابط ليكون خاطئًا. هل ترون رسالة خطأ في الـ Console من بلوك `catch`؟
    *   **(مساعدة الطلاب في حل مشاكل الاتصال، مثل أخطاء عنوان IP، أو عدم تشغيل الخادم، أو مشاكل CORS إذا ظهرت - على الرغم من أن `*` في PHP يجب أن تسمح بها مبدئيًا).**

2.  **التمرين 2: تحليل JSON وبناء قائمة بسيطة (40 دقيقة)**
    *   **"المهمة:"** "نطور التمرين السابق لعرض عناوين المهام في قائمة."
        1.  عدلوا دالة `testApiConnection` (أو سموها `fetchAndDisplayTasks`) لتصبح `async Future<List<String>>` (لأننا سنرجع قائمة من العناوين).
        2.  داخل الدالة، بعد التحقق من `statusCode == 200`:
            *   استخدموا `jsonDecode(response.body)` لتحليل الـ JSON إلى `List<dynamic>`.
            *   أنشئوا قائمة فارغة `List<String> titles = [];`.
            *   استخدموا حلقة `for-in` للمرور على كل عنصر `task` في القائمة المحللة.
            *   لكل `task`, أضيفوا قيمة `task['title']` إلى قائمة `titles`.
            *   اجعلوا الدالة ترجع قائمة `titles`.
        3.  إذا حدث خطأ أو لم يكن `statusCode == 200`, يمكن أن ترجع الدالة قائمة فارغة أو ترمي Exception.
        4.  **في الواجهة (UI):**
            *   حولوا الـ Widget الرئيسي إلى `StatefulWidget`.
            *   عرفوا متغير حالة لتخزين قائمة العناوين: `List<String> _taskTitles = [];`.
            *   عرفوا متغير حالة للتحميل: `bool _isLoading = false;`.
            *   عدلوا `onPressed` للزر:
                *   اجعلوه `async`.
                *   قبل استدعاء `fetchAndDisplayTasks`, ضعوا `setState(() { _isLoading = true; });`.
                *   استدعوا الدالة: `final titles = await fetchAndDisplayTasks();`.
                *   بعد الحصول على النتائج، ضعوا `setState(() { _taskTitles = titles; _isLoading = false; });`.
            *   **في دالة `build`:**
                *   اعرضوا `CircularProgressIndicator` إذا كانت `_isLoading` هي `true`.
                *   إذا لم تكن قيد التحميل، اعرضوا `ListView.builder` لعرض قائمة `_taskTitles`.
                    ```dart
                    // Inside build, after checking _isLoading
                    Expanded( // Use Expanded if ListView is inside Column/Row
                      child: ListView.builder(
                        itemCount: _taskTitles.length,
                        itemBuilder: (context, index) {
                          return ListTile( // Simple list item widget
                            title: Text(_taskTitles[index]),
                          );
                        },
                      ),
                    )
                    ```
        5.  (اختياري): أضيفوا معالجة لحالة الخطأ (مثلاً عرض رسالة خطأ إذا فشل الجلب).
    *   **"الاختبار:"** شغلوا التطبيق، اضغطوا الزر. هل يظهر مؤشر التحميل ثم تظهر قائمة بعناوين المهام؟

3.  **التمرين 3: استخدام `FutureBuilder` (15 دقيقة)**
    *   **"المهمة:"** "نعدل التمرين 2 لاستخدام `FutureBuilder` بدلًا من إدارة حالة التحميل يدويًا."
        1.  عدلوا دالة `fetchTasks` لترجع `Future<List<String>>` مباشرة.
        2.  في `StatefulWidget`, عرفوا متغير `Future<List<String>>? _tasksFuture;`.
        3.  في `initState()`, قوموا باستدعاء `fetchTasks()` وإسناد النتيجة لـ `_tasksFuture`.
            ```dart
            @override
            void initState() {
              super.initState();
              _tasksFuture = fetchTasks(); // Call fetch function once
            }
            ```
        4.  في دالة `build`, استبدلوا الكود الذي يعرض `ListView` و `CircularProgressIndicator` بـ `FutureBuilder<List<String>>`:
            ```dart
            FutureBuilder<List<String>>(
              future: _tasksFuture, // The future to observe
              builder: (context, snapshot) {
                if (snapshot.connectionState == ConnectionState.waiting) {
                  return Center(child: CircularProgressIndicator()); // Show loading
                } else if (snapshot.hasError) {
                  return Center(child: Text('Error: ${snapshot.error}')); // Show error
                } else if (snapshot.hasData) {
                  // Data loaded successfully, build the ListView
                  final titles = snapshot.data!; // Use ! assuming data is non-null if hasData is true
                  if (titles.isEmpty) {
                       return Center(child: Text('No tasks found.'));
                  }
                  return ListView.builder(
                    itemCount: titles.length,
                    itemBuilder: (context, index) {
                      return ListTile(title: Text(titles[index]));
                    },
                  );
                } else {
                  return Center(child: Text('No tasks yet.')); // Initial state or no data
                }
              },
            )
            ```
        5.  (يمكن إزالة الزر الآن، لأن `initState` يشغل الجلب تلقائيًا عند بناء الـ Widget).
    *   **"الاختبار:"** شغلوا التطبيق. هل ترون مؤشر التحميل ثم قائمة المهام تظهر تلقائيًا؟

4.  **حل المشكلات ومناقشة (5 دقائق)**
    *   "ما الفرق بين الطريقة اليدوية (`setState`) و `FutureBuilder`؟ متى نستخدم كلًا منهما؟" (`FutureBuilder` أبسط للتعامل مع حالات `Future` الثلاث).
    *   "أهمية تعريف كلاس Model (مثل `Task`) لتحويل بيانات JSON إليه بدلًا من استخدام `List<dynamic>` أو `Map<String, dynamic>` مباشرة (يجعل الكود أكثر أمانًا وقابلية للقراءة)."

5.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1 (إنشاء Model Class):** أنشئ كلاس `Task` في Dart يحتوي على الخصائص `id` (int), `title` (String), `description` (String?), `isCompleted` (bool). أضف مُنشئًا له. أضف دالة factory اسمها `Task.fromJson(Map<String, dynamic> json)` تأخذ Map وتحوله إلى كائن `Task`. عدلوا دالة `fetchTasks` و `FutureBuilder` لاستخدام `List<Task>` بدل `List<String>`. اعرضوا العنوان والحالة في `ListTile`.
    *   **تمرين 2 (بحث):** ابحثوا عن كيفية إرسال Headers مع طلبات `http` (مثل إرسال `X-API-Key` الذي استخدمناه لتأمين الـ API). (تلميح: `http.get(url, headers: {'X-API-Key': 'your_key'})`).
    *   **تمرين 3 (بحث):** كيف نرسل طلب POST باستخدام حزمة `http` لإضافة مهمة جديدة للـ API؟ (تلميح: `http.post(url, headers: {...}, body: jsonEncode({...}))`).
    *   "خطوة هائلة اليوم! لقد ربطنا تطبيق Flutter بالـ API وأصبحنا قادرين على جلب بيانات حقيقية وعرضها. في الحصة القادمة، سنتعلم كيف نخزن بعض البيانات البسيطة محليًا على جهاز المستخدم باستخدام SharedPreferences. يعطيكم العافية!"

---

#### **الحصة 10 (الخميس - حصة 26 في المجمل): التخزين المحلي (SharedPreferences)**

**🎯 أهداف الحصة:**

1.  فهم الحاجة للتخزين المحلي البسيط (Persisting Simple Data).
2.  التعرف على SharedPreferences كآلية لتخزين أزواج المفتاح/القيمة (Key/Value) البسيطة.
3.  إضافة حزمة `shared_preferences` للمشروع.
4.  حفظ أنواع البيانات البسيطة (String, int, double, bool, List<String>) في SharedPreferences.
5.  قراءة البيانات المخزنة من SharedPreferences.
6.  حذف بيانات من SharedPreferences.
7.  مناقشة حالات الاستخدام المناسبة لـ SharedPreferences وحدودها.

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم! في الحصة الماضية، تعلمنا كيف نجلب بيانات من الـ API. لكن ماذا لو أردنا حفظ بعض الإعدادات البسيطة للمستخدم، مثل تفضيلاته للوضع المظلم (Dark Mode)، أو آخر عملية بحث قام بها، أو حتى اسم المستخدم لتجنب طلبه في كل مرة؟ لا نريد بالضرورة تخزين كل هذه الأشياء الصغيرة في قاعدة بيانات على الخادم. هنا يأتي دور **التخزين المحلي البسيط (Simple Local Storage)**، والآلية الأكثر شيوعًا لذلك في Flutter هي **`SharedPreferences`**."

1.  **مراجعة سريعة (5 دقائق)**
    *   "ما هي الحزمة التي استخدمناها للاتصال بالـ API؟ (`http`). وكيف نتعامل مع الاستجابات غير المتزامنة؟ (`async`/`await`). وكيف نحلل JSON؟ (`dart:convert` -> `jsonDecode`). وما هو `FutureBuilder`؟ ممتاز!"

2.  **لماذا نحتاج للتخزين المحلي؟ (10 دقائق)**
    *   **حفظ تفضيلات المستخدم:** مثل لغة التطبيق، الثيم (فاتح/داكن)، إعدادات الإشعارات.
    *   **تذكر معلومات بسيطة:** آخر فلتر استخدمه المستخدم، آخر نتيجة بحث.
    *   **Caching بسيط:** تخزين بيانات لا تتغير كثيرًا (مثل اسم المستخدم) لتجنب طلبها من الـ API في كل مرة.
    *   **حفظ حالة بسيطة:** تذكر إذا كان المستخدم قد شاهد شاشة الترحيب (Onboarding) من قبل أم لا.
    *   **مهم:** SharedPreferences **غير مخصصة** لتخزين كميات كبيرة من البيانات المعقدة أو البيانات الحساسة جدًا (مثل كلمات المرور مباشرة). هي لتخزين أزواج بسيطة من المفتاح/القيمة.

3.  **مقدمة لـ SharedPreferences (15 دقيقة)**
    *   هي آلية توفرها Flutter (عبر حزمة) للتفاعل مع نظام التخزين البسيط الخاص بالمنصة (UserDefaults في iOS و SharedPreferences في Android).
    *   تسمح بتخزين واسترجاع أزواج بسيطة من **مفتاح (String)** و **قيمة (Primitive types)**.
    *   الأنواع المدعومة للقيم: `String`, `int`, `double`, `bool`, و `List<String>`.
    *   العمليات (الحفظ والقراءة) هي **غير متزامنة (Asynchronous)**، لأنها تتضمن عمليات I/O (قراءة/كتابة على القرص). سنحتاج لاستخدام `async`/`await`.
    *   البيانات تُحفظ على الجهاز وتبقى موجودة حتى لو تم إغلاق التطبيق أو إعادة تشغيل الجهاز (تبقى حتى يتم حذف التطبيق أو مسح بياناته).

4.  **إضافة واستخدام حزمة `shared_preferences` (15 دقيقة)**
    *   **1. إضافة الحزمة:**
        *   افتحوا `pubspec.yaml`.
        *   تحت `dependencies:`, أضيفوا:
            ```yaml
            shared_preferences: ^2.2.3 # أو أحدث إصدار متوافق
            ```
        *   احفظوا الملف وشغلوا `flutter pub get`.
    *   **2. الحصول على Instance:**
        *   قبل أن نتمكن من القراءة أو الكتابة، نحتاج للحصول على نسخة (instance) من `SharedPreferences`. هذه العملية غير متزامنة.
        *   نستخدم الدالة الساكنة `SharedPreferences.getInstance()`.
            ```dart
            import 'package:shared_preferences/shared_preferences.dart';

            Future<void> exampleUsage() async {
              // Get instance (must be awaited)
              final SharedPreferences prefs = await SharedPreferences.getInstance();
              // Now we can use 'prefs' to read/write
            }
            ```

5.  **حفظ البيانات (Writing Data) (20 دقيقة)**
    *   نستخدم دوال `set<Type>()` المتوفرة في كائن `SharedPreferences`.
    *   تأخذ مفتاحًا (`String`) وقيمة من النوع المناسب.
    *   ترجع `Future<bool>` يشير إلى نجاح العملية (غالبًا لا نحتاج لانتظارها بـ `await` إلا إذا كان مهمًا جدًا التأكد من اكتمال الحفظ قبل المتابعة).
    *   **الأمثلة:**
        ```dart
        // Inside an async function where 'prefs' is available
        Future<void> saveData(SharedPreferences prefs) async {
          // Save an integer
          await prefs.setInt('counter', 10);
          print('Saved counter');

          // Save a boolean
          await prefs.setBool('darkMode', true);
          print('Saved dark mode preference');

          // Save a double
          await prefs.setDouble('pi', 3.14159);
          print('Saved pi value');

          // Save a String
          await prefs.setString('username', 'flutter_dev');
          print('Saved username');

          // Save a List of Strings
          await prefs.setStringList('items', <String>['Car', 'Bike', 'Plane']);
          print('Saved items list');
        }
        ```

6.  **قراءة البيانات (Reading Data) (15 دقيقة)**
    *   نستخدم دوال `get<Type>()` المتوفرة في كائن `SharedPreferences`.
    *   تأخذ المفتاح (`String`) المطلوب قراءته.
    *   ترجع القيمة المرتبطة بالمفتاح. **مهم:** إذا كان المفتاح غير موجود، ترجع `null`.
    *   لذا، نحتاج للتعامل مع احتمالية `null` باستخدام معامل `??` لتوفير قيمة افتراضية.
    *   **الأمثلة:**
        ```dart
        // Inside a function where 'prefs' is available
        void readData(SharedPreferences prefs) {
          // Read an integer (provide default value 0 if key not found)
          final int counter = prefs.getInt('counter') ?? 0;
          print('Read counter: $counter');

          // Read a boolean (default to false)
          final bool darkMode = prefs.getBool('darkMode') ?? false;
          print('Read dark mode: $darkMode');

          // Read a double (default to 0.0)
          final double pi = prefs.getDouble('pi') ?? 0.0;
          print('Read pi: $pi');

          // Read a String (default to 'Guest')
          final String username = prefs.getString('username') ?? 'Guest';
          print('Read username: $username');

          // Read a List of Strings (default to empty list)
          final List<String> items = prefs.getStringList('items') ?? [];
          print('Read items: $items');

          // Try reading a non-existent key
          final String? nonExistent = prefs.getString('non_existent_key');
          print('Read non-existent key: $nonExistent'); // Output: null
        }
        ```

7.  **حذف البيانات (Removing Data) (5 دقائق)**
    *   نستخدم دالة `remove()`.
    *   تأخذ المفتاح (`String`) المراد حذف قيمته.
    *   ترجع `Future<bool>`.
    *   **مثال:**
        ```dart
        // Inside an async function where 'prefs' is available
        Future<void> removeData(SharedPreferences prefs) async {
          await prefs.remove('counter');
          print('Removed counter');

          // Verify removal
          final int? counter = prefs.getInt('counter');
          print('Counter after removal: $counter'); // Output: null
        }
        ```
    *   يوجد أيضًا دالة `clear()` لحذف **كل** البيانات المخزنة بواسطة التطبيق في SharedPreferences (استخدمها بحذر!).

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وحفظ التفضيلات (Open Learning & Problem Solving)**

"الآن، لنطبق استخدام SharedPreferences لحفظ بعض الإعدادات البسيطة."

1.  **التمرين 1: إعداد الحزمة وحفظ وقراءة قيمة بسيطة (30 دقيقة)**
    *   **"المهمة:"**
        1.  تأكدوا من إضافة حزمة `shared_preferences` إلى `pubspec.yaml` وتشغيل `flutter pub get`.
        2.  في `main.dart`، أنشئوا `StatefulWidget` بسيطًا.
        3.  داخل كلاس الـ `State`, أنشئوا دالتين `async`:
            *   `_saveData()`: تحصل على instance من `SharedPreferences` وتحفظ قيمة نصية بسيطة (مثل اسم المستخدم) باستخدام `setString()`. اطبع رسالة تأكيد في الـ Console.
            *   `_readData()`: تحصل على instance وتقرأ نفس القيمة النصية باستخدام `getString()` (مع قيمة افتراضية) وتطبعها في الـ Console.
        4.  في الواجهة (`build` method), أضيفوا زرين: "Save Data" و "Read Data". كل زر يستدعي الدالة المناسبة عند `onPressed`.
    *   **"الاختبار:"**
        *   شغلوا التطبيق.
        *   اضغطوا زر "Save Data". هل ترون رسالة التأكيد؟
        *   اضغطوا زر "Read Data". هل ترون القيمة التي حفظتموها؟
        *   أغلقوا التطبيق **بالكامل** وأعيدوا تشغيله.
        *   اضغطوا زر "Read Data" مرة أخرى. هل القيمة لا تزال موجودة؟ (نعم، يجب أن تكون).

2.  **التمرين 2: حفظ وقراءة حالة Toggle (مثل Dark Mode) (35 دقيقة)**
    *   **"المهمة:"** "نطور التمرين السابق لحفظ حالة Toggle."
        1.  في كلاس الـ `State`, أضيفوا متغير حالة `bool _isDarkMode = false;`.
        2.  أنشئوا دالة `async _loadDarkModePreference()`:
            *   تحصل على instance من `SharedPreferences`.
            *   تقرأ قيمة 'darkMode' باستخدام `getBool()` (مع قيمة افتراضية `false`).
            *   تستخدم `setState` لتحديث قيمة `_isDarkMode` بالقيمة المقروءة.
        3.  استدعوا `_loadDarkModePreference()` داخل `initState()` لتحميل التفضيل عند بدء تشغيل الـ Widget.
        4.  أنشئوا دالة `async _saveDarkModePreference(bool newValue)`:
            *   تحصل على instance.
            *   تحفظ القيمة الجديدة `newValue` باستخدام `setBool('darkMode', newValue)`.
        5.  في الواجهة (`build`):
            *   اعرضوا `Switch` widget.
            *   اجعلوا `value` للـ `Switch` مرتبطًا بـ `_isDarkMode`.
            *   في `onChanged(bool newValue)`, استدعوا `_saveDarkModePreference(newValue)` ثم استخدموا `setState` لتحديث `_isDarkMode` محليًا لعكس التغيير فورًا في الواجهة.
            ```dart
            Switch(
              value: _isDarkMode,
              onChanged: (bool newValue) {
                _saveDarkModePreference(newValue); // Save preference
                setState(() {
                  _isDarkMode = newValue; // Update UI immediately
                });
              },
            )
            ```
            *   (اختياري) يمكن تغيير لون خلفية `Scaffold` بناءً على `_isDarkMode`.
    *   **"الاختبار:"**
        *   شغلوا التطبيق.
        *   غيروا حالة الـ Switch.
        *   أغلقوا التطبيق وأعيدوا تشغيله. هل يتذكر التطبيق الحالة الأخيرة للـ Switch؟

3.  **التمرين 3: حفظ قائمة نصية بسيطة (مثل سجل البحث) (15 دقيقة)**
    *   **"المهمة:"**
        1.  أضيفوا `TextField` بسيطًا وزر "Add to History".
        2.  عند الضغط على الزر:
            *   اقرأوا النص من `TextField`.
            *   اقرأوا قائمة سجل البحث الحالية من SharedPreferences (`getStringList('searchHistory') ?? []`).
            *   أضيفوا النص الجديد إلى القائمة.
            *   احفظوا القائمة المحدثة مرة أخرى باستخدام `setStringList('searchHistory', updatedList)`.
        3.  (اختياري) اعرضوا القائمة المحفوظة تحت `TextField` باستخدام `ListView.builder`.

4.  **مناقشة حالات الاستخدام والحدود (5 دقائق)**
    *   "متى يكون SharedPreferences خيارًا جيدًا؟" (إعدادات بسيطة، تفضيلات، Flags).
    *   "متى لا يكون مناسبًا؟" (بيانات كبيرة، بيانات معقدة أو مرتبطة ببعضها، بيانات حساسة جدًا تتطلب تشفيرًا قويًا، بيانات تحتاج لاستعلامات معقدة).
    *   "ما هي البدائل للبيانات الأكثر تعقيدًا؟" (قواعد البيانات المحلية مثل SQLite).

5.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1:** أنشئ عدادًا بسيطًا (مثل تطبيق العداد الافتراضي). اجعل قيمة العداد تُحفظ في SharedPreferences في كل مرة يتم فيها الضغط على زر الزيادة، وتُقرأ وتُعرض عند بدء تشغيل التطبيق.
    *   **تمرين 2 (بحث):** هل يمكن تخزين كائن Dart معقد (مثل كائن `Task`) مباشرة في SharedPreferences؟ (الجواب: لا مباشرة. الحلول تتضمن تحويل الكائن إلى JSON String وحفظ النص، ثم قراءة النص وتحويله مرة أخرى لكائن عند الحاجة. ابحث عن "flutter save object to sharedpreferences").
    *   "رائع! اليوم تعلمنا كيف نحافظ على البيانات البسيطة بين جلسات استخدام التطبيق باستخدام SharedPreferences. هذه أداة مفيدة جدًا للإعدادات والتفضيلات. في الحصة القادمة، سنتعامل مع تخزين بيانات أكثر تعقيدًا وتنظيمًا باستخدام قواعد البيانات المحلية SQLite. يعطيكم العافية!"

---
بالتأكيد، لنواصل تفصيل خطة دروس المرحلة الثانية (Flutter و Dart) للأسبوع السادس من الشهر الخامس.

---

### **الشهر الخامس: بناء تطبيقات Flutter متكاملة (تابع)**

---

#### **الأسبوع السادس (أسبوع 18 في المجمل)**

---

#### **الحصة 11 (الاثنين - حصة 27 في المجمل): قواعد البيانات المحلية (SQLite)**

**🎯 أهداف الحصة:**

1.  فهم الحاجة لقاعدة بيانات محلية لتخزين البيانات المنظمة والمعقدة.
2.  التعرف على SQLite كمحرك قاعدة بيانات مدمج وخفيف.
3.  إضافة واستخدام حزمة `sqflite` للتعامل مع SQLite في Flutter.
4.  إنشاء وفتح قاعدة بيانات محلية.
5.  إنشاء جداول باستخدام أوامر SQL.
6.  تنفيذ عمليات CRUD الأساسية (Insert, Query, Update, Delete) على قاعدة البيانات المحلية.
7.  تنظيم كود قاعدة البيانات باستخدام فئة مساعدة (Database Helper Class).

**الجزء الأول (1.5 ساعة): الشرح النظري والتطبيق الموجه (Structured Learning)**

"أهلاً بكم! في الحصة الماضية، تعلمنا كيف نستخدم `SharedPreferences` لتخزين البيانات البسيطة محليًا. لكن ماذا لو أردنا تخزين بيانات أكثر تعقيدًا وتنظيمًا، مثل قائمة المهام الكاملة مع تفاصيلها وحالاتها، ونحتاج للبحث فيها أو ربطها ببيانات أخرى؟ `SharedPreferences` لا تكفي هنا. اليوم، سنتعلم كيف نستخدم **قواعد البيانات العلائقية المحلية** المدمجة في الجهاز باستخدام **SQLite** وحزمة **`sqflite`**."

1.  **مراجعة سريعة (5 دقائق)**
    *   "ما هي الآلية التي استخدمناها لتخزين الإعدادات البسيطة محليًا؟ (`SharedPreferences`). وما هي أنواع البيانات التي تدعمها؟ (Primitives و `List<String>`). وما هي حدودها؟ (غير مناسبة للبيانات الكبيرة أو المعقدة أو المرتبطة). ممتاز!"

2.  **لماذا نحتاج لقاعدة بيانات محلية؟ (10 دقائق)**
    *   **البيانات المنظمة (Structured Data):** عندما تكون بياناتنا لها هيكل واضح وعلاقات بينها (مثل المهام والمستخدمين والتصنيفات).
    *   **كميات كبيرة من البيانات:** لتخزين عدد كبير من السجلات بكفاءة.
    *   **الاستعلامات المعقدة (Complex Queries):** عندما نحتاج للبحث، الفلترة، الترتيب، أو تجميع البيانات بناءً على شروط متعددة.
    *   **الوصول دون اتصال (Offline Access):** لتخزين بيانات تم جلبها من الـ API والسماح للمستخدم بالوصول إليها حتى لو لم يكن متصلاً بالإنترنت.
    *   **الذرة والاتساق (Atomicity & Consistency):** ضمان أن العمليات المتعددة على البيانات تتم كوحدة واحدة (Transactions).

3.  **مقدمة لـ SQLite (15 دقيقة)**
    *   **"ما هي SQLite؟"**
        *   هي مكتبة برمجية توفر **محرك قاعدة بيانات علائقية (Relational Database Engine)**.
        *   **لا تحتاج لخادم (Serverless):** قاعدة البيانات تكون عبارة عن **ملف واحد** يتم تخزينه مباشرة على جهاز المستخدم داخل مساحة تخزين التطبيق.
        *   **مدمجة (Embedded):** يتم تضمينها مباشرة داخل التطبيق.
        *   **خفيفة جدًا وسريعة:** شائعة الاستخدام بشكل هائل في تطبيقات الموبايل والمتصفحات وأنظمة التشغيل.
        *   **تتبع معايير SQL:** يمكننا استخدام أوامر SQL القياسية (CREATE TABLE, SELECT, INSERT, UPDATE, DELETE) للتعامل معها.
        *   **Transactional:** تدعم العمليات الذرية (Transactions).

4.  **حزمة `sqflite` (10 دقيقة)**
    *   هي الحزمة **الرئيسية والموصى بها** من فريق Flutter للتعامل مع قواعد بيانات SQLite في تطبيقات Flutter.
    *   توفر واجهة Dart للتفاعل مع عمليات SQLite الأساسية.
    *   **"إضافة الحزمة:"**
        1.  افتح `pubspec.yaml`.
        2.  تحت `dependencies:`, أضيفوا:
            ```yaml
            sqflite: ^2.3.3 # أو أحدث إصدار متوافق
            path_provider: ^2.1.3 # نحتاج هذه الحزمة لتحديد مسار حفظ ملف الداتابيز
            path: ^1.9.0 # للمساعدة في التعامل مع المسارات
            ```
        3.  احفظوا الملف وشغلوا `flutter pub get`.

5.  **العمليات الأساسية مع `sqflite` (40 دقيقة)**
    *   **1. تحديد المسار وفتح/إنشاء قاعدة البيانات:**
        *   نحتاج لمعرفة مكان آمن لتخزين ملف قاعدة البيانات على جهاز المستخدم. نستخدم `getDatabasesPath()` من `path_provider`.
        *   نستخدم دالة `openDatabase()` من `sqflite`. هذه الدالة تفتح قاعدة البيانات إذا كانت موجودة، أو تنشئها إذا لم تكن موجودة.
        *   `openDatabase()` هي دالة `async` وترجع `Future<Database>`.
        *   **المعاملات الهامة لـ `openDatabase()`:**
            *   `path`: المسار الكامل لملف قاعدة البيانات (نستخدم `join(dbPath, 'database_name.db')` من حزمة `path`).
            *   `version`: رقم إصدار مخطط قاعدة البيانات (مهم لتحديثات المخطط - Migrations).
            *   `onCreate`: دالة `callback` تُستدعى **مرة واحدة فقط** عند إنشاء قاعدة البيانات لأول مرة. هذا هو المكان المثالي لتنفيذ أوامر `CREATE TABLE`.
            *   `onUpgrade`: دالة `callback` تُستدعى إذا قمنا بزيادة رقم `version` عند فتح قاعدة البيانات (تُستخدم لتحديث مخطط الجداول).
            *   `onDowngrade`: تُستدعى عند تقليل رقم `version`.
        ```dart
        import 'package:sqflite/sqflite.dart';
        import 'package:path/path.dart';
        import 'package:path_provider/path_provider.dart'; // Needed for getDatabasesPath

        Future<Database> initializeDB() async {
          String path = await getDatabasesPath(); // Get default database location
          return openDatabase(
            join(path, 'mydatabase.db'), // Specify the database file name
            onCreate: (database, version) async {
              // Run the CREATE TABLE statement when the database is first created.
              await database.execute(
                "CREATE TABLE tasks(id INTEGER PRIMARY KEY AUTOINCREMENT, title TEXT NOT NULL, description TEXT, is_completed INTEGER NOT NULL DEFAULT 0)",
              );
              print("Database and tasks table created!");
              // Can create other tables here too
            },
            version: 1, // Set the version. Increment this for schema changes.
          );
        }
        ```
    *   **2. إنشاء الجداول (`CREATE TABLE`):**
        *   يتم تنفيذه باستخدام `db.execute(sql_statement)` داخل `onCreate` كما في المثال أعلاه.
        *   نستخدم بناء جملة SQL القياسي. لاحظ أنواع بيانات SQLite (INTEGER, TEXT, REAL, BLOB, NULL). `BOOLEAN` يمكن تمثيلها بـ `INTEGER` (0 أو 1). `DATETIME` يمكن تخزينها كـ `TEXT` (ISO8601 format) أو `INTEGER` (Unix timestamp).
    *   **3. إدراج البيانات (`INSERT`):**
        *   **الطريقة المساعدة:** `db.insert('tableName', dataMap)`:
            *   تأخذ اسم الجدول وخريطة (`Map<String, dynamic>`) حيث المفاتيح هي أسماء الأعمدة والقيم هي البيانات المراد إدراجها.
            *   تتعامل تلقائيًا مع تهريب البيانات (prevents SQL injection for values).
            *   ترجع `Future<int>` يمثل `id` الصف الجديد الذي تم إدراجه (أو 0 إذا فشل).
            ```dart
            Future<int> insertTask(Database db, Map<String, dynamic> task) async {
              int result = await db.insert(
                'tasks', // Table name
                task, // Map of column names and values
                conflictAlgorithm: ConflictAlgorithm.replace, // Optional: What to do if PK conflicts
              );
              return result;
            }
            // Usage: await insertTask(db, {'title': 'Buy milk', 'is_completed': 0});
            ```
        *   **الطريقة الخام:** `db.rawInsert(sql_statement, [arguments])`: لتنفيذ جملة INSERT SQL مباشرة مع تمرير المعاملات بشكل آمن.
    *   **4. الاستعلام عن البيانات (`SELECT` / `Query`):**
        *   **الطريقة المساعدة:** `db.query('tableName', columns: [...], where: 'id = ?', whereArgs: [id], orderBy: 'title ASC', limit: 10, offset: 0)`:
            *   توفر طريقة منظمة لبناء استعلام SELECT.
            *   ترجع `Future<List<Map<String, dynamic>>>`. قائمة من الخرائط، كل خريطة تمثل صفًا.
            ```dart
            Future<List<Map<String, dynamic>>> getAllTasks(Database db) async {
              final List<Map<String, dynamic>> maps = await db.query(
                  'tasks',
                  orderBy: 'id DESC' // Get newest first
              );
              return maps;
            }

            Future<Map<String, dynamic>?> getTaskById(Database db, int id) async {
              final List<Map<String, dynamic>> maps = await db.query(
                'tasks',
                where: 'id = ?', // '?' is a placeholder for the argument
                whereArgs: [id], // Pass the argument safely
                limit: 1,
              );
              if (maps.isNotEmpty) {
                return maps.first;
              }
              return null;
            }
            ```
        *   **الطريقة الخام:** `db.rawQuery(sql_statement, [arguments])`.
    *   **5. تحديث البيانات (`UPDATE`):**
        *   **الطريقة المساعدة:** `db.update('tableName', dataMap, where: 'id = ?', whereArgs: [id])`:
            *   تأخذ اسم الجدول، خريطة بالقيم الجديدة، وشرط لتحديد الصفوف المراد تحديثها.
            *   ترجع `Future<int>` يمثل عدد الصفوف التي تم تحديثها.
            ```dart
            Future<int> updateTask(Database db, int id, Map<String, dynamic> taskUpdates) async {
              int result = await db.update(
                'tasks',
                taskUpdates,
                where: 'id = ?',
                whereArgs: [id],
              );
              return result;
            }
            // Usage: await updateTask(db, 1, {'is_completed': 1});
            ```
        *   **الطريقة الخام:** `db.rawUpdate(sql_statement, [arguments])`.
    *   **6. حذف البيانات (`DELETE`):**
        *   **الطريقة المساعدة:** `db.delete('tableName', where: 'id = ?', whereArgs: [id])`:
            *   تأخذ اسم الجدول وشرط لتحديد الصفوف المراد حذفها.
            *   ترجع `Future<int>` يمثل عدد الصفوف التي تم حذفها.
            ```dart
            Future<int> deleteTask(Database db, int id) async {
              int result = await db.delete(
                'tasks',
                where: 'id = ?',
                whereArgs: [id],
              );
              return result;
            }
            // Usage: await deleteTask(db, 1);
            ```
        *   **الطريقة الخام:** `db.rawDelete(sql_statement, [arguments])`.
    *   **7. إغلاق قاعدة البيانات:**
        *   `await db.close();`
        *   مهم إغلاق الاتصال عند الانتهاء من استخدامه (مثلاً في `dispose` للـ Widget الرئيسي أو عند إغلاق التطبيق) لتجنب تسرب الموارد.

6.  **فئة مساعد قاعدة البيانات (Database Helper Class) (10 دقائق)**
    *   "وضع كل كود فتح الداتابيز وعمليات CRUD مباشرة في ملفات الـ Widgets يجعل الكود فوضويًا وصعب الصيانة."
    *   **"الحل:"** إنشاء كلاس منفصل (مثل `DatabaseHelper.dart`) يكون مسؤولاً عن كل ما يتعلق بقاعدة البيانات.
    *   **نمط Singleton:** غالبًا ما يتم تصميم هذا الكلاس كـ Singleton لضمان وجود نسخة (instance) واحدة فقط من قاعدة البيانات مفتوحة طوال فترة تشغيل التطبيق.
        ```dart
        class DatabaseHelper {
          // Private constructor for Singleton
          DatabaseHelper._privateConstructor();
          static final DatabaseHelper instance = DatabaseHelper._privateConstructor();

          static Database? _database; // Static variable to hold the database instance

          // Getter for the database instance (opens DB if not already open)
          Future<Database> get database async {
            if (_database != null) return _database!; // Return existing instance if available
            // If _database is null, initialize it
            _database = await _initDatabase();
            return _database!;
          }

          // Method to initialize the database
          Future<Database> _initDatabase() async {
            String path = join(await getDatabasesPath(), 'tasks_database.db');
            return await openDatabase(
              path,
              version: 1,
              onCreate: _onCreate,
            );
          }

          // Method called when DB is created
          Future<void> _onCreate(Database db, int version) async {
            await db.execute('''
              CREATE TABLE tasks(
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                title TEXT NOT NULL,
                is_completed INTEGER NOT NULL DEFAULT 0
              )
            ''');
          }

          // --- CRUD Methods ---
          Future<int> insertTask(Map<String, dynamic> row) async {
            Database db = await instance.database;
            return await db.insert('tasks', row);
          }

          Future<List<Map<String, dynamic>>> getAllTasks() async {
            Database db = await instance.database;
            return await db.query('tasks', orderBy: 'id DESC');
          }

          // Add updateTask, deleteTask methods here...

          // Close the database (optional, as sqflite handles it, but good practice)
          Future<void> close() async {
            final db = await instance.database;
            db.close();
            _database = null; // Reset the static variable
          }
        }
        ```
    *   **الاستخدام:** `await DatabaseHelper.instance.insertTask(...)`, `await DatabaseHelper.instance.getAllTasks()`.

---

**الجزء الثاني (1.5 ساعة): التطبيق العملي وبناء CRUD محلي (Open Learning & Problem Solving)**

"الآن، لنطبق استخدام `sqflite` لبناء تخزين محلي لتطبيق المهام."

1.  **التمرين 1: إعداد الحزم وإنشاء Helper وفتح الداتابيز (30 دقيقة)**
    *   **"المهمة:"**
        1.  تأكدوا من إضافة حزم `sqflite`, `path_provider`, `path` إلى `pubspec.yaml` وتشغيل `flutter pub get`.
        2.  أنشئوا ملفًا جديدًا `database_helper.dart`.
        3.  انسخوا أو اكتبوا هيكل كلاس `DatabaseHelper` باستخدام نمط Singleton (كما في المثال النظري).
        4.  أكملوا دالة `_initDatabase()` لفتح قاعدة بيانات باسم `tasks.db`.
        5.  أكملوا دالة `_onCreate()` لإنشاء جدول `tasks` بالأعمدة المناسبة (`id` PK AI, `title` TEXT NOT NULL, `is_completed` INTEGER NOT NULL DEFAULT 0).
        6.  في `main.dart` (أو أي Widget رئيسي)، حاولوا الحصول على نسخة من قاعدة البيانات في `initState` (فقط للتأكد من أن عملية الإنشاء والفتح تعمل بدون أخطاء):
            ```dart
            @override
            void initState() {
              super.initState();
              _testDbConnection();
            }

            Future<void> _testDbConnection() async {
              try {
                Database db = await DatabaseHelper.instance.database;
                print("Database connection successful: ${db.path}");
                // You can optionally close it here for testing init only
                // await DatabaseHelper.instance.close();
              } catch (e) {
                print("Error connecting to database: $e");
              }
            }
            ```
    *   **"الاختبار:"** شغلوا التطبيق. هل ترون رسالة نجاح الاتصال بالداتابيز في الـ Console؟ شغلوا التطبيق مرة ثانية، هل يتم الإنشاء مرة أخرى؟ (لا، يجب ألا يتم استدعاء `onCreate` مرة أخرى).

2.  **التمرين 2: إضافة مهمة وقراءتها (40 دقيقة)**
    *   **"المهمة:"** "نجعل التطبيق يضيف مهامًا ويقرأها من SQLite."
        1.  في `DatabaseHelper`, أكملوا دالة `Future<int> insertTask(Map<String, dynamic> row)` باستخدام `db.insert()`.
        2.  في `DatabaseHelper`, أكملوا دالة `Future<List<Map<String, dynamic>>> getAllTasks()` باستخدام `db.query()`.
        3.  **في الواجهة (UI):**
            *   استخدموا `StatefulWidget`.
            *   أضيفوا `TextField` للمستخدم لإدخال عنوان المهمة.
            *   أضيفوا زر "Add Task".
            *   عند الضغط على الزر:
                *   اقرأوا العنوان من `TextField`.
                *   جهزوا `Map` بالبيانات: `{'title': title, 'is_completed': 0}`.
                *   استدعوا `await DatabaseHelper.instance.insertTask(taskMap)`.
                *   (مهم!) بعد الإضافة الناجحة، يجب إعادة تحميل قائمة المهام لعرض المهمة الجديدة. يمكن استدعاء دالة `_loadTasks()` التي سننشئها.
            *   أنشئوا دالة `_loadTasks()` تكون `async`:
                *   تستدعي `await DatabaseHelper.instance.getAllTasks()`.
                *   تستخدم `setState` لتحديث متغير حالة `List<Map<String, dynamic>> _tasks = [];`.
            *   استدعوا `_loadTasks()` في `initState()` لتحميل المهام عند بدء التشغيل.
            *   استخدموا `ListView.builder` لعرض قائمة `_tasks` (اعرضوا `_tasks[index]['title']`).
    *   **"الاختبار:"**
        *   شغلوا التطبيق. هل تظهر قائمة فارغة مبدئيًا؟
        *   أضيفوا مهمة جديدة. هل تظهر في القائمة؟
        *   أغلقوا التطبيق وأعيدوا تشغيله. هل المهام التي أضفتموها لا تزال موجودة؟

3.  **مناقشة التحديث والحذف وتحويل البيانات (15 دقيقة)**
    *   "كيف يمكننا إضافة زر لتغيير حالة `is_completed` للمهمة؟"
        *   (نحتاج دالة `updateTask(int id, Map<String, dynamic> updates)` في Helper تستخدم `db.update()`).
        *   (في الواجهة، زر في `ListTile` يستدعي دالة تأخذ `id` المهمة وقيمتها الحالية، تعكس القيمة، وتستدعي `updateTask` ثم `_loadTasks` لتحديث الواجهة).
    *   "كيف يمكننا إضافة زر لحذف المهمة؟"
        *   (نحتاج دالة `deleteTask(int id)` في Helper تستخدم `db.delete()`).
        *   (زر يستدعي `deleteTask` ثم `_loadTasks`).
    *   "العمل مع `Map<String, dynamic>` ليس مثاليًا. كيف نحسن هذا؟"
        *   (إنشاء كلاس `Task` كما فعلنا سابقًا).
        *   (إضافة دالة `toMap()` في `Task` لتحويل الكائن إلى Map قبل الإرسال لـ `insert` أو `update`).
        *   (تعديل `getAllTasks` لترجع `Future<List<Task>>` عن طريق تحويل كل Map في النتيجة إلى كائن `Task` باستخدام `Task.fromJson` أو مُنشئ مشابه).

4.  **تمارين منزلية + ختام (5 دقائق)**
    *   **تمرين 1:** أكملوا تطبيق ToDo المحلي: أضيفوا وظائف تحديث حالة المهمة (عند الضغط على المهمة نفسها أو على Checkbox) وحذف المهمة (بزر حذف في `ListTile`).
    *   **تمرين 2:** حولوا الكود لاستخدام كلاس `Task` بدلًا من `Map<String, dynamic>` للتعامل مع البيانات بين الواجهة والـ Database Helper.
    *   **تمرين 3 (بحث):** ابحثوا عن كيفية استخدام `onUpgrade` في `sqflite` للتعامل مع تغيير مخطط قاعدة البيانات (مثلاً، إضافة عمود جديد لجدول موجود) عند زيادة رقم إصدار قاعدة البيانات.
    *   "ممتاز! اليوم تعلمنا كيف نستخدم SQLite و `sqflite` لتخزين البيانات المنظمة محليًا في تطبيقات Flutter. هذه مهارة أساسية للتطبيقات التي تحتاج للعمل دون اتصال أو تخزين بيانات معقدة. في الحصة القادمة، سنتحدث عن إدارة الحالة المتقدمة باستخدام Provider. يعطيكم العافية!"

---

